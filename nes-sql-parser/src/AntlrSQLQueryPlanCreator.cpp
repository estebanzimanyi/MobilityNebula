/*
    Licensed under the Apache License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

        https://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.
*/

#include <AntlrSQLParser/AntlrSQLQueryPlanCreator.hpp>

#include <cctype>
#include <cstddef>
#include <cstdint>
#include <memory>
#include <optional>
#include <ranges>
#include <string>
#include <utility>
#include <variant>

#include <AntlrSQLBaseListener.h>
#include <AntlrSQLLexer.h>
#include <AntlrSQLParser.h>
#include <ParserRuleContext.h>
#include <AntlrSQLParser/AntlrSQLHelper.hpp>
#include <DataTypes/DataType.hpp>
#include <DataTypes/DataTypeProvider.hpp>
#include <DataTypes/Schema.hpp>
#include <Functions/ArithmeticalFunctions/AddLogicalFunction.hpp>
#include <Functions/ArithmeticalFunctions/DivLogicalFunction.hpp>
#include <Functions/ArithmeticalFunctions/ModuloLogicalFunction.hpp>
#include <Functions/ArithmeticalFunctions/MulLogicalFunction.hpp>
#include <Functions/ArithmeticalFunctions/SubLogicalFunction.hpp>
#include <Functions/BooleanFunctions/AndLogicalFunction.hpp>
#include <Functions/BooleanFunctions/EqualsLogicalFunction.hpp>
#include <Functions/BooleanFunctions/NegateLogicalFunction.hpp>
#include <Functions/BooleanFunctions/OrLogicalFunction.hpp>
#include <Functions/ComparisonFunctions/GreaterEqualsLogicalFunction.hpp>
#include <Functions/ComparisonFunctions/GreaterLogicalFunction.hpp>
#include <Functions/ComparisonFunctions/LessEqualsLogicalFunction.hpp>
#include <Functions/ComparisonFunctions/LessLogicalFunction.hpp>
#include <Functions/ConcatLogicalFunction.hpp>
#include <Functions/ConstantValueLogicalFunction.hpp>
#include <Functions/FieldAccessLogicalFunction.hpp>
#include <Functions/FieldAssignmentLogicalFunction.hpp>
#include <Functions/LogicalFunction.hpp>
#include <Functions/LogicalFunctionProvider.hpp>
#include <Operators/Windows/Aggregations/ArrayAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/AvgAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/CountAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/MaxAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/MedianAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/VarAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalSequenceAggregationLogicalFunctionV2.hpp>
#include <Operators/Windows/Aggregations/MinAggregationLogicalFunction.hpp>
#include <Functions/Meos/TemporalEContainsGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalIntersectsFunction.hpp>
#include <Operators/Windows/Aggregations/SumAggregationLogicalFunction.hpp>
#include <Operators/Windows/JoinLogicalOperator.hpp>
#include <Operators/Windows/Aggregations/Meos/VarAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalSequenceAggregationLogicalFunction.hpp>
#include <Functions/Meos/TemporalIntersectsGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAIntersectsGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAtStBoxLogicalFunction.hpp>
#include <Functions/Meos/TfloatCeilLogicalFunction.hpp>
#include <Functions/Meos/TfloatExpLogicalFunction.hpp>
#include <Functions/Meos/TfloatFloorLogicalFunction.hpp>
#include <Functions/Meos/TfloatLnLogicalFunction.hpp>
#include <Functions/Meos/TfloatLog10LogicalFunction.hpp>
#include <Functions/Meos/TfloatRadiansLogicalFunction.hpp>
#include <Functions/Meos/TfloatToTintLogicalFunction.hpp>
#include <Functions/Meos/TintToTfloatLogicalFunction.hpp>
#include <Functions/Meos/AddTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/SubTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/MulTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/DivTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AddTintIntLogicalFunction.hpp>
#include <Functions/Meos/SubTintIntLogicalFunction.hpp>
#include <Functions/Meos/MulTintIntLogicalFunction.hpp>
#include <Functions/Meos/DivTintIntLogicalFunction.hpp>
#include <Functions/Meos/AddFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/SubFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/MulFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/DivFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AddIntTintLogicalFunction.hpp>
#include <Functions/Meos/SubIntTintLogicalFunction.hpp>
#include <Functions/Meos/MulIntTintLogicalFunction.hpp>
#include <Functions/Meos/DivIntTintLogicalFunction.hpp>
#include <Functions/Meos/AddTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/SubTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/MulTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/DivTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/AddBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/SubBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/MulBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/DivBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/AddTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/SubTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/MulTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/DivTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/TfloatShiftValueLogicalFunction.hpp>
#include <Functions/Meos/TfloatScaleValueLogicalFunction.hpp>
#include <Functions/Meos/TfloatShiftScaleValueLogicalFunction.hpp>
#include <Functions/Meos/TintShiftValueLogicalFunction.hpp>
#include <Functions/Meos/TintScaleValueLogicalFunction.hpp>
#include <Functions/Meos/TintShiftScaleValueLogicalFunction.hpp>
#include <Functions/Meos/TbigintShiftValueLogicalFunction.hpp>
#include <Functions/Meos/TbigintScaleValueLogicalFunction.hpp>
#include <Functions/Meos/TbigintShiftScaleValueLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTintIntLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/TemporalRoundLogicalFunction.hpp>
#include <Functions/Meos/EverEqTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/EverNeTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/EverLtTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/EverLeTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/EverGtTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/EverGeTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeTbigintBigintLogicalFunction.hpp>
#include <Functions/Meos/EverEqBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/EverNeBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/EverLtBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/EverLeBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/EverGtBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/EverGeBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeBigintTbigintLogicalFunction.hpp>
#include <Functions/Meos/EverEqTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/EverNeTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/EverEqBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/EverNeBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/TbigintToTintLogicalFunction.hpp>
#include <Functions/Meos/TbigintToTfloatLogicalFunction.hpp>
#include <Functions/Meos/TintToTbigintLogicalFunction.hpp>
#include <Functions/Meos/TfloatToTbigintLogicalFunction.hpp>
#include <Functions/Meos/TboolToTintLogicalFunction.hpp>
#include <Functions/Meos/TnotTboolLogicalFunction.hpp>
#include <Functions/Meos/TandTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/TorTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/TeqTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/TneTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/TandBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/TorBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/TeqBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/TneBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/TandTboolTboolLogicalFunction.hpp>
#include <Functions/Meos/TorTboolTboolLogicalFunction.hpp>
#include <Functions/Meos/EcoversTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EdisjointTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/NadTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EcontainsTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AcontainsTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EdisjointTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AdwithinTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AcontainsTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AcoversTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AdisjointTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AintersectsTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AtouchesTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EcontainsTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EcoversTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EdisjointTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EintersectsTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EtouchesTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EverEqTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EverNeTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/NadTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AdwithinTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AcontainsTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AcoversTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AdisjointTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AintersectsTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AtouchesTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EcontainsTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EcoversTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EdisjointTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EintersectsTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EtouchesTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EverEqTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EverNeTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/NadTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AdwithinTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AcontainsTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/AcoversTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/AdisjointTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/AintersectsTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/AtouchesTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/EcontainsTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/EcoversTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/EdisjointTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/EintersectsTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/EtouchesTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/NadTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/AdwithinTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/AcontainsTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AcoversTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AdisjointTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AintersectsTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AtouchesTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EcontainsTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EcoversTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EdisjointTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EintersectsTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EtouchesTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EverEqTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EverNeTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/NadTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AdwithinTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/MindistanceTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/H3indexEqLogicalFunction.hpp>
#include <Functions/Meos/H3indexNeLogicalFunction.hpp>
#include <Functions/Meos/H3indexLtLogicalFunction.hpp>
#include <Functions/Meos/H3indexLeLogicalFunction.hpp>
#include <Functions/Meos/H3indexGtLogicalFunction.hpp>
#include <Functions/Meos/H3indexGeLogicalFunction.hpp>
#include <Functions/Meos/H3indexCmpLogicalFunction.hpp>
#include <Functions/Meos/H3indexOutLogicalFunction.hpp>
#include <Functions/Meos/Th3indexGetResolutionLogicalFunction.hpp>
#include <Functions/Meos/Th3indexGetBaseCellNumberLogicalFunction.hpp>
#include <Functions/Meos/Th3indexIsValidCellLogicalFunction.hpp>
#include <Functions/Meos/Th3indexIsPentagonLogicalFunction.hpp>
#include <Functions/Meos/Th3indexCellToParentLogicalFunction.hpp>
#include <Functions/Meos/Th3indexCellToParentNextLogicalFunction.hpp>
#include <Functions/Meos/Th3indexCellToCenterChildLogicalFunction.hpp>
#include <Functions/Meos/Th3indexCellToCenterChildNextLogicalFunction.hpp>
#include <Functions/Meos/Th3indexCellToChildPosLogicalFunction.hpp>
#include <Functions/Meos/Th3indexAreNeighborCellsLogicalFunction.hpp>
#include <Functions/Meos/Th3indexGridDistanceLogicalFunction.hpp>
#include <Functions/Meos/EverEqTh3indexH3indexLogicalFunction.hpp>
#include <Functions/Meos/EverNeTh3indexH3indexLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTh3indexH3indexLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTh3indexH3indexLogicalFunction.hpp>
#include <Functions/Meos/QuadbinEqLogicalFunction.hpp>
#include <Functions/Meos/QuadbinNeLogicalFunction.hpp>
#include <Functions/Meos/QuadbinLtLogicalFunction.hpp>
#include <Functions/Meos/QuadbinLeLogicalFunction.hpp>
#include <Functions/Meos/QuadbinGtLogicalFunction.hpp>
#include <Functions/Meos/QuadbinGeLogicalFunction.hpp>
#include <Functions/Meos/QuadbinCmpLogicalFunction.hpp>
#include <Functions/Meos/QuadbinGetResolutionLogicalFunction.hpp>
#include <Functions/Meos/QuadbinIsValidCellLogicalFunction.hpp>
#include <Functions/Meos/QuadbinCellAreaLogicalFunction.hpp>
#include <Functions/Meos/QuadbinCellToParentLogicalFunction.hpp>
#include <Functions/Meos/QuadbinCellToQuadkeyLogicalFunction.hpp>
#include <Functions/Meos/QuadbinPointToCellLogicalFunction.hpp>
#include <Functions/Meos/QuadbinTileToCellLogicalFunction.hpp>
#include <Functions/Meos/EverEqTquadbinQuadbinLogicalFunction.hpp>
#include <Functions/Meos/EverNeTquadbinQuadbinLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTquadbinQuadbinLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTquadbinQuadbinLogicalFunction.hpp>
#include <Functions/Meos/EverEqTtextTextLogicalFunction.hpp>
#include <Functions/Meos/EverEqTextTtextLogicalFunction.hpp>
#include <Functions/Meos/EverNeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/EverNeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/EverGeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/EverGeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/EverGtTtextTextLogicalFunction.hpp>
#include <Functions/Meos/EverGtTextTtextLogicalFunction.hpp>
#include <Functions/Meos/EverLeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/EverLeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/EverLtTtextTextLogicalFunction.hpp>
#include <Functions/Meos/EverLtTextTtextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTtextTextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTextTtextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtTtextTextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtTextTtextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtTtextTextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TEqTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TEqTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TNeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TNeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TtextUpperLogicalFunction.hpp>
#include <Functions/Meos/TtextLowerLogicalFunction.hpp>
#include <Functions/Meos/TtextInitcapLogicalFunction.hpp>
#include <Functions/Meos/TextUpperLogicalFunction.hpp>
#include <Functions/Meos/TextLowerLogicalFunction.hpp>
#include <Functions/Meos/TextInitcapLogicalFunction.hpp>
#include <Functions/Meos/TextcatTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TextcatTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TextcatTtextTtextLogicalFunction.hpp>
#include <Functions/Meos/TGeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TGeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TGtTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TGtTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TLeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TLeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TLtTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TLtTextTtextLogicalFunction.hpp>
#include <Functions/Meos/JsonbEqLogicalFunction.hpp>
#include <Functions/Meos/JsonbNeLogicalFunction.hpp>
#include <Functions/Meos/JsonbLtLogicalFunction.hpp>
#include <Functions/Meos/JsonbLeLogicalFunction.hpp>
#include <Functions/Meos/JsonbGtLogicalFunction.hpp>
#include <Functions/Meos/JsonbGeLogicalFunction.hpp>
#include <Functions/Meos/JsonbCmpLogicalFunction.hpp>
#include <Functions/Meos/JsonbContainedLogicalFunction.hpp>
#include <Functions/Meos/JsonbContainsLogicalFunction.hpp>
#include <Functions/Meos/JsonbExistsLogicalFunction.hpp>
#include <Functions/Meos/JsonbArrayLengthLogicalFunction.hpp>
#include <Functions/Meos/JsonbToCstringLogicalFunction.hpp>
#include <Functions/Meos/JsonbPrettyLogicalFunction.hpp>
#include <Functions/Meos/JsonbObjectFieldTextLogicalFunction.hpp>
#include <Functions/Meos/JsonbArrayElementTextLogicalFunction.hpp>
#include <Functions/Meos/EverEqTjsonbJsonbLogicalFunction.hpp>
#include <Functions/Meos/EverNeTjsonbJsonbLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTjsonbJsonbLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTjsonbJsonbLogicalFunction.hpp>
#include <Functions/Meos/EverEqTjsonbTjsonbLogicalFunction.hpp>
#include <Functions/Meos/EverNeTjsonbTjsonbLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTjsonbTjsonbLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTjsonbTjsonbLogicalFunction.hpp>
#include <Functions/Meos/NadTnpointGeoLogicalFunction.hpp>
#include <Functions/Meos/NadTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/NadTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/EverEqNpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/EverEqTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/EverEqTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/EverNeNpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/EverNeTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/EverNeTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqNpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeNpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/NadTposePoseLogicalFunction.hpp>
#include <Functions/Meos/NadTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/NadTposeGeoLogicalFunction.hpp>
#include <Functions/Meos/EverEqPoseTposeLogicalFunction.hpp>
#include <Functions/Meos/EverEqTposePoseLogicalFunction.hpp>
#include <Functions/Meos/EverEqTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/EverNePoseTposeLogicalFunction.hpp>
#include <Functions/Meos/EverNeTposePoseLogicalFunction.hpp>
#include <Functions/Meos/EverNeTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqPoseTposeLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTposePoseLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNePoseTposeLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTposePoseLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/GeomBoundaryLogicalFunction.hpp>
#include <Functions/Meos/GeomCentroidLogicalFunction.hpp>
#include <Functions/Meos/GeomConvexHullLogicalFunction.hpp>
#include <Functions/Meos/GeoReverseLogicalFunction.hpp>
#include <Functions/Meos/GeoPointsLogicalFunction.hpp>
#include <Functions/Meos/GeomUnaryUnionLogicalFunction.hpp>
#include <Functions/Meos/GeoSetSridLogicalFunction.hpp>
#include <Functions/Meos/GeoRoundLogicalFunction.hpp>
#include <Functions/Meos/GeoTransformLogicalFunction.hpp>
#include <Functions/Meos/GeoSridLogicalFunction.hpp>
#include <Functions/Meos/GeoNumGeosLogicalFunction.hpp>
#include <Functions/Meos/GeoNumPointsLogicalFunction.hpp>
#include <Functions/Meos/GeomLengthLogicalFunction.hpp>
#include <Functions/Meos/GeomPerimeterLogicalFunction.hpp>
#include <Functions/Meos/GeomIsEmptyLogicalFunction.hpp>
#include <Functions/Meos/GeomAzimuthLogicalFunction.hpp>
#include <Functions/Meos/GeoIsUnitaryLogicalFunction.hpp>
#include <Functions/Meos/GeoEqualsLogicalFunction.hpp>
#include <Functions/Meos/GeoSameLogicalFunction.hpp>
#include <Functions/Meos/GeomIntersectsLogicalFunction.hpp>
#include <Functions/Meos/GeomIntersects2dLogicalFunction.hpp>
#include <Functions/Meos/GeomIntersects3dLogicalFunction.hpp>
#include <Functions/Meos/GeomContainsLogicalFunction.hpp>
#include <Functions/Meos/GeomCoversLogicalFunction.hpp>
#include <Functions/Meos/GeomDisjoint2dLogicalFunction.hpp>
#include <Functions/Meos/GeomTouchesLogicalFunction.hpp>
#include <Functions/Meos/GeogIntersectsLogicalFunction.hpp>
#include <Functions/Meos/GeogDistanceLogicalFunction.hpp>
#include <Functions/Meos/LineLocatePointLogicalFunction.hpp>
#include <Functions/Meos/GeomDistance2dLogicalFunction.hpp>
#include <Functions/Meos/GeomDistance3dLogicalFunction.hpp>
#include <Functions/Meos/GeomDwithin2dLogicalFunction.hpp>
#include <Functions/Meos/GeomDwithin3dLogicalFunction.hpp>
#include <Functions/Meos/GeogDwithinLogicalFunction.hpp>
#include <Functions/Meos/GeomDwithinLogicalFunction.hpp>
#include <Functions/Meos/GeomIntersection2dLogicalFunction.hpp>
#include <Functions/Meos/GeomIntersection2dCollLogicalFunction.hpp>
#include <Functions/Meos/GeomDifference2dLogicalFunction.hpp>
#include <Functions/Meos/GeomShortestline2dLogicalFunction.hpp>
#include <Functions/Meos/GeomShortestline3dLogicalFunction.hpp>
#include <Functions/Meos/LineInterpolatePointLogicalFunction.hpp>
#include <Functions/Meos/LineSubstringLogicalFunction.hpp>
#include <Functions/Meos/LineNumpointsLogicalFunction.hpp>
#include <Functions/Meos/LinePointNLogicalFunction.hpp>
#include <Functions/Meos/GeoGeoNLogicalFunction.hpp>
#include <Functions/Meos/GeoAsEwktLogicalFunction.hpp>
#include <Functions/Meos/GeoAsGeojsonLogicalFunction.hpp>
#include <Functions/Meos/GeomPointMake2dLogicalFunction.hpp>
#include <Functions/Meos/GeomPointMake3dzLogicalFunction.hpp>
#include <Functions/Meos/GeogPointMake2dLogicalFunction.hpp>
#include <Functions/Meos/GeogPointMake3dzLogicalFunction.hpp>
#include <Functions/Meos/GeogAreaLogicalFunction.hpp>
#include <Functions/Meos/GeogLengthLogicalFunction.hpp>
#include <Functions/Meos/GeogPerimeterLogicalFunction.hpp>
#include <Functions/Meos/GeogToGeomLogicalFunction.hpp>
#include <Functions/Meos/GeomToGeogLogicalFunction.hpp>
#include <Functions/Meos/GeogCentroidLogicalFunction.hpp>
#include <Functions/Meos/IntspanLowerLogicalFunction.hpp>
#include <Functions/Meos/IntspanUpperLogicalFunction.hpp>
#include <Functions/Meos/IntspanWidthLogicalFunction.hpp>
#include <Functions/Meos/IntspanLowerIncLogicalFunction.hpp>
#include <Functions/Meos/IntspanUpperIncLogicalFunction.hpp>
#include <Functions/Meos/FloatspanLowerLogicalFunction.hpp>
#include <Functions/Meos/FloatspanUpperLogicalFunction.hpp>
#include <Functions/Meos/FloatspanWidthLogicalFunction.hpp>
#include <Functions/Meos/FloatspanLowerIncLogicalFunction.hpp>
#include <Functions/Meos/FloatspanUpperIncLogicalFunction.hpp>
#include <Functions/Meos/ContainedIntSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainedFloatSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainedSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainedFloatspanSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpanIntLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpanFloatLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainsFloatspanSpanLogicalFunction.hpp>
#include <Functions/Meos/AcontainsGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/AcoversGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/AcoversTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/AdisjointTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/AdisjointTrgeometryTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/AdwithinTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/AdwithinTrgeometryTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/AintersectsTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/AintersectsTrgeometryTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTrgeometryTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTrgeometryTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/AtouchesTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/EcontainsGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/EcoversGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/EcoversTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/EdisjointTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/EdisjointTrgeometryTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTrgeometryTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/EintersectsTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/EintersectsTrgeometryTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/EtouchesTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/EverEqGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/EverEqTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/EverEqTrgeometryTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/EverNeGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/EverNeTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/EverNeTrgeometryTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/NadTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/NadTrgeometryTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEIntersectsTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAIntersectsTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalECoversTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDisjointTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalADisjointTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalETouchesTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalATouchesTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEContainsTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAContainsTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEIntersectsTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalAIntersectsTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalECoversTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDisjointTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalADisjointTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalETouchesTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalATouchesTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalEContainsTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalAContainsTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalEIntersectsTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAIntersectsTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalECoversTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDisjointTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalADisjointTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalETouchesTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalATouchesTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEContainsTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAContainsTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEIntersectsTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/TemporalAIntersectsTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/TemporalECoversTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDisjointTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/TemporalADisjointTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/TemporalETouchesTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/TemporalATouchesTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/TemporalEContainsTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/TemporalAContainsTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/AboveStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/AdjacentStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/AfterStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/BackStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/BeforeStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/BelowStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/ContainedStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/ContainsStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/FrontStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/LeftStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OveraboveStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverafterStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverbackStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverbelowStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverfrontStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverlapsStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverleftStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverrightStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/RightStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/SameStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/StboxEqLogicalFunction.hpp>
#include <Functions/Meos/StboxGeLogicalFunction.hpp>
#include <Functions/Meos/StboxGtLogicalFunction.hpp>
#include <Functions/Meos/StboxLeLogicalFunction.hpp>
#include <Functions/Meos/StboxLtLogicalFunction.hpp>
#include <Functions/Meos/StboxNeLogicalFunction.hpp>
#include <Functions/Meos/NadStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/StboxCmpLogicalFunction.hpp>
#include <Functions/Meos/AboveStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/AdjacentStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/AfterStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/BackStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/BeforeStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/BelowStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/ContainedStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/ContainsStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/FrontStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/LeftStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OveraboveStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverafterStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverbackStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverbelowStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverfrontStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverlapsStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverleftStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverrightStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/RightStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/SameStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/AboveTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/AfterTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/BackTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/BeforeTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/BelowTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/ContainedTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/ContainsTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/FrontTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/LeftTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OveraboveTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverafterTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverbackTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverbelowTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverfrontTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverleftTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverrightTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/RightTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/SameTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/NadTgeoStboxLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/AfterTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/BeforeTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/ContainedTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/ContainsTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/LeftTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverafterTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverleftTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverrightTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/RightTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/SameTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/AfterTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/BeforeTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/ContainedTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/ContainsTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/LeftTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/OverafterTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/OverleftTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/OverrightTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/RightTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/SameTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/NadTfloatTboxLogicalFunction.hpp>
#include <Functions/Meos/NadTintTboxLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/AfterTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/BeforeTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/ContainedTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/ContainsTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/LeftTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverafterTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverleftTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverrightTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/RightTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/SameTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/TboolEndValueLogicalFunction.hpp>
#include <Functions/Meos/TboolStartValueLogicalFunction.hpp>
#include <Functions/Meos/AboveTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/AfterTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/BackTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/BeforeTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/BelowTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/ContainedTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/ContainsTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/FrontTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/LeftTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OveraboveTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverafterTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverbackTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverbelowTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverfrontTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverleftTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverrightTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/RightTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/SameTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AfterTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/BeforeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/ContainedTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/ContainsTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/OverafterTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/SameTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/TemporalCmpLogicalFunction.hpp>
#include <Functions/Meos/TemporalEqLogicalFunction.hpp>
#include <Functions/Meos/TemporalGeLogicalFunction.hpp>
#include <Functions/Meos/TemporalGtLogicalFunction.hpp>
#include <Functions/Meos/TemporalLeLogicalFunction.hpp>
#include <Functions/Meos/TemporalLtLogicalFunction.hpp>
#include <Functions/Meos/TemporalNeLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/EverEqTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/EverGeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/EverGtTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/EverLeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/EverLtTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/EverNeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/TemporalFrechetDistanceLogicalFunction.hpp>
#include <Functions/Meos/TemporalHausdorffDistanceLogicalFunction.hpp>
#include <Functions/Meos/TemporalDyntimewarpDistanceLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADTGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADTFloatLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADTIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/EverEqTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/EverGeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/EverGtTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/EverLeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/EverLtTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/EverNeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTintIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeTintIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtTintIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeTintIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtTintIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTintIntLogicalFunction.hpp>
#include <Functions/Meos/EverEqTintIntLogicalFunction.hpp>
#include <Functions/Meos/EverGeTintIntLogicalFunction.hpp>
#include <Functions/Meos/EverGtTintIntLogicalFunction.hpp>
#include <Functions/Meos/EverLeTintIntLogicalFunction.hpp>
#include <Functions/Meos/EverLtTintIntLogicalFunction.hpp>
#include <Functions/Meos/EverNeTintIntLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADFloatScalarLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADIntScalarLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/EverEqFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/EverGeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/EverGtFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/EverLeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/EverLtFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/EverNeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqIntTintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeIntTintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtIntTintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeIntTintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtIntTintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeIntTintLogicalFunction.hpp>
#include <Functions/Meos/EverEqIntTintLogicalFunction.hpp>
#include <Functions/Meos/EverGeIntTintLogicalFunction.hpp>
#include <Functions/Meos/EverGtIntTintLogicalFunction.hpp>
#include <Functions/Meos/EverLeIntTintLogicalFunction.hpp>
#include <Functions/Meos/EverLtIntTintLogicalFunction.hpp>
#include <Functions/Meos/EverNeIntTintLogicalFunction.hpp>
#include <Functions/Meos/MultTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/TcontainsTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TcoversTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TcontainsTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TcoversTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/TpointLengthWkbLogicalFunction.hpp>
#include <Functions/Meos/TnpointLengthLogicalFunction.hpp>
#include <Functions/Meos/TcbufferToTfloatLogicalFunction.hpp>
#include <Functions/Meos/NadTcbufferStboxLogicalFunction.hpp>
#include <Functions/Meos/NadTnpointStboxLogicalFunction.hpp>
#include <Functions/Meos/NadTposeStboxLogicalFunction.hpp>
#include <Functions/Meos/AtouchesTpointGeoLogicalFunction.hpp>
#include <Functions/Meos/EtouchesTpointGeoLogicalFunction.hpp>
#include <Functions/Meos/TemporalAContainsGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalADisjointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalATouchesGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalECoversGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDisjointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEIntersectsGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalETouchesGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAtGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalMinusGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinTGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinTGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAContainsTGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalADisjointTGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAIntersectsTGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalATouchesTGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEContainsTGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalECoversTGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDisjointTGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEIntersectsTGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalETouchesTGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAContainsTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalACoversTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalADisjointTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalAIntersectsTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalATouchesTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalEContainsTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalECoversTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDisjointTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalEIntersectsTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalETouchesTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinTCbufferGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinTCbufferGeometryLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/EverEqTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/EverNeTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalAContainsTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalACoversTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalADisjointTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalAIntersectsTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalATouchesTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalEContainsTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalECoversTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDisjointTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalEIntersectsTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalETouchesTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinTCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalADisjointTCbufferTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalAIntersectsTCbufferTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalATouchesTCbufferTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalECoversTCbufferTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADTCbufferTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinTCbufferTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinTCbufferTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalEIntersectsTCbufferTCbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalETouchesTCbufferTCbufferLogicalFunction.hpp>
#include <Plans/LogicalPlan.hpp>
#include <Plans/LogicalPlanBuilder.hpp>
#include <Util/Overloaded.hpp>
#include <Util/Strings.hpp>
#include <WindowTypes/Measures/TimeCharacteristic.hpp>
#include <WindowTypes/Measures/TimeMeasure.hpp>
#include <WindowTypes/Types/SlidingWindow.hpp>
#include <WindowTypes/Types/TumblingWindow.hpp>
#include <fmt/format.h>
#include <fmt/ranges.h>
#include <CommonParserFunctions.hpp>
#include <ErrorHandling.hpp>
#include <ParserUtil.hpp>

namespace NES::Parsers
{
LogicalPlan AntlrSQLQueryPlanCreator::getQueryPlan() const
{
    if (sinks.empty())
    {
        throw InvalidQuerySyntax("Query does not contain sink");
    }
    if (queryPlans.empty())
    {
        throw InvalidQuerySyntax("Query could not be parsed");
    }
    /// Todo #421: support multiple sinks
    INVARIANT(!sinks.empty(), "Need at least one sink!");
    return std::visit(
        Overloaded{
            [&](const std::string& sinkName) { return LogicalPlanBuilder::addSink(sinkName, queryPlans.top()); },
            [&](const std::pair<std::string, ConfigMap>& inlineSink)
            {
                const auto& [type, configOptions] = inlineSink;
                const auto sinkConfig = getSinkConfig(configOptions);
                const auto schemaOpt = getSinkSchema(configOptions);
                const Schema schema = (schemaOpt.has_value() ? schemaOpt.value() : Schema{});
                return LogicalPlanBuilder::addInlineSink(type, schema, sinkConfig, queryPlans.top());
            }},
        sinks.front());
}

Windowing::TimeMeasure buildTimeMeasure(const int size, const uint64_t timebase)
{
    switch (timebase) /// TODO #619: improve this switch case
    {
        case AntlrSQLLexer::MS:
            return API::Milliseconds(size);
        case AntlrSQLLexer::SEC:
            return API::Seconds(size);
        case AntlrSQLLexer::MINUTE:
            return API::Minutes(size);
        case AntlrSQLLexer::HOUR:
            return API::Hours(size);
        case AntlrSQLLexer::DAY:
            return API::Days(size);
        default:
            const AntlrSQLLexer lexer(nullptr);
            const std::string tokenName = std::string(lexer.getVocabulary().getSymbolicName(timebase));
            throw InvalidQuerySyntax("Unknown time unit: {}", tokenName);
    }
}

static LogicalFunction createFunctionFromOpBoolean(LogicalFunction leftFunction, LogicalFunction rightFunction, const uint64_t tokenType)
{
    switch (tokenType) /// TODO #619: improve this switch case
    {
        case AntlrSQLLexer::EQ:
            return EqualsLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        case AntlrSQLLexer::NEQJ:
            return NegateLogicalFunction(EqualsLogicalFunction(std::move(leftFunction), std::move(rightFunction)));
        case AntlrSQLLexer::LT:
            return LessLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        case AntlrSQLLexer::GT:
            return GreaterLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        case AntlrSQLLexer::GTE:
            return GreaterEqualsLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        case AntlrSQLLexer::LTE:
            return LessEqualsLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        default:
            auto lexer = AntlrSQLLexer(nullptr);
            throw InvalidQuerySyntax(
                "Unknown Comparison Operator: {} of type: {}", lexer.getVocabulary().getSymbolicName(tokenType), tokenType);
    }
}

static LogicalFunction createLogicalBinaryFunction(LogicalFunction leftFunction, LogicalFunction rightFunction, const uint64_t tokenType)
{
    switch (tokenType) /// TODO #619: improve this switch case
    {
        case AntlrSQLLexer::AND:
            return AndLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        case AntlrSQLLexer::OR:
            return OrLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        default:
            auto lexer = AntlrSQLLexer(nullptr);
            throw InvalidQuerySyntax(
                "Unknown binary function in SQL query for op {} with type: {} and left {} and right {}",
                lexer.getVocabulary().getSymbolicName(tokenType),
                tokenType,
                std::move(leftFunction),
                std::move(rightFunction));
    }
}

void AntlrSQLQueryPlanCreator::enterSelectClause(AntlrSQLParser::SelectClauseContext* context)
{
    helpers.top().isSelect = true;
    AntlrSQLBaseListener::enterSelectClause(context);
}

void AntlrSQLQueryPlanCreator::enterFromClause(AntlrSQLParser::FromClauseContext* context)
{
    helpers.top().isFrom = true;
    AntlrSQLBaseListener::enterFromClause(context);
}

void AntlrSQLQueryPlanCreator::enterSinkClause(AntlrSQLParser::SinkClauseContext* context)
{
    if (context->sink().empty())
        throw InvalidQuerySyntax("INTO must be followed by at least one sink-identifier.");
    /// Store all specified sinks.
    for (const auto& sink : context->sink())
    {
        if (sink->identifier() != nullptr)
        {
            sinks.emplace_back(bindIdentifier(sink->identifier()));
        }
        else if (sink->inlineSink() != nullptr)
        {
            const auto& sinkInlineSink = sink->inlineSink();

            const auto type = bindIdentifier(sinkInlineSink->type);
            const auto configOptions = bindConfigOptions(sinkInlineSink->parameters->namedConfigExpression());

            sinks.emplace_back(std::make_pair(type, configOptions));
        }
    }
}

void AntlrSQLQueryPlanCreator::exitLogicalBinary(AntlrSQLParser::LogicalBinaryContext* context)
{
    /// If we are exiting a logical binary operator in a join relation, we need to build the binary function for the joinKey and
    /// not for the general function
    if (helpers.top().isJoinRelation)
    {
        if (helpers.top().joinKeyRelationHelper.size() < 2)
        {
            throw InvalidQuerySyntax(
                "Expected two operands for binary op, got {}: {}", helpers.top().joinKeyRelationHelper.size(), context->getText());
        }
        const auto rightFunction = helpers.top().joinKeyRelationHelper.back();
        helpers.top().joinKeyRelationHelper.pop_back();
        const auto leftFunction = helpers.top().joinKeyRelationHelper.back();
        helpers.top().joinKeyRelationHelper.pop_back();

        const auto opTokenType = context->op->getType();
        const auto function = createLogicalBinaryFunction(leftFunction, rightFunction, opTokenType);
        helpers.top().joinKeyRelationHelper.push_back(function);
    }
    else
    {
        if (helpers.top().functionBuilder.size() < 2)
        {
            throw InvalidQuerySyntax(
                "Expected two operands for binary op, got {}: {}", helpers.top().joinKeyRelationHelper.size(), context->getText());
        }
        const auto rightFunction = helpers.top().functionBuilder.back();
        helpers.top().functionBuilder.pop_back();
        const auto leftFunction = helpers.top().functionBuilder.back();
        helpers.top().functionBuilder.pop_back();

        const auto opTokenType = context->op->getType();
        const auto function = createLogicalBinaryFunction(leftFunction, rightFunction, opTokenType);
        helpers.top().functionBuilder.push_back(function);
    }
}

void AntlrSQLQueryPlanCreator::exitSelectClause(AntlrSQLParser::SelectClauseContext* context)
{
    helpers.top().functionBuilder.clear();
    helpers.top().isSelect = false;
    AntlrSQLBaseListener::exitSelectClause(context);
}

void AntlrSQLQueryPlanCreator::exitFromClause(AntlrSQLParser::FromClauseContext* context)
{
    helpers.top().isFrom = false;
    AntlrSQLBaseListener::exitFromClause(context);
}

void AntlrSQLQueryPlanCreator::enterWhereClause(AntlrSQLParser::WhereClauseContext* context)
{
    helpers.top().isWhereOrHaving = true;
    AntlrSQLBaseListener::enterWhereClause(context);
}

void AntlrSQLQueryPlanCreator::exitWhereClause(AntlrSQLParser::WhereClauseContext* context)
{
    helpers.top().isWhereOrHaving = false;
    if (helpers.top().functionBuilder.size() != 1)
    {
        throw InvalidQuerySyntax("There were more than 1 functions in the functionBuilder in exitWhereClause.");
    }
    helpers.top().addWhereClause(helpers.top().functionBuilder.back());
    helpers.top().functionBuilder.clear();
    AntlrSQLBaseListener::exitWhereClause(context);
}

void AntlrSQLQueryPlanCreator::enterComparisonOperator(AntlrSQLParser::ComparisonOperatorContext* context)
{
    auto opTokenType = context->getStart()->getType();
    helpers.top().opBoolean = opTokenType;
    AntlrSQLBaseListener::enterComparisonOperator(context);
}

void AntlrSQLQueryPlanCreator::exitArithmeticBinary(AntlrSQLParser::ArithmeticBinaryContext* context)
{
    if (helpers.empty())
    {
        throw InvalidQuerySyntax("Parser is confused at {}", context->getText());
    }
    LogicalFunction function;

    if (helpers.top().functionBuilder.size() < 2)
    {
        if (helpers.top().functionBuilder.size() + helpers.top().constantBuilder.size() == 2)
        {
            throw InvalidQuerySyntax(
                "Attempted to use a raw constant in a binary expression. {} in `{}`.",
                fmt::join(helpers.top().constantBuilder, ", "),
                context->getText());
        }
        throw InvalidQuerySyntax(
            "There were less than 2 functions in the functionBuilder in exitArithmeticBinary. `{}`.", context->getText());
    }
    const auto rightFunction = helpers.top().functionBuilder.back();
    helpers.top().functionBuilder.pop_back();
    const auto leftFunction = helpers.top().functionBuilder.back();
    helpers.top().functionBuilder.pop_back();
    auto opTokenType = context->op->getType();
    switch (opTokenType) /// TODO #619: improve this switch case
    {
        case AntlrSQLLexer::ASTERISK:
            function = MulLogicalFunction(leftFunction, rightFunction);
            break;
        case AntlrSQLLexer::SLASH:
            function = DivLogicalFunction(leftFunction, rightFunction);
            break;
        case AntlrSQLLexer::PLUS:
            function = AddLogicalFunction(leftFunction, rightFunction);
            break;
        case AntlrSQLLexer::MINUS:
            function = SubLogicalFunction(leftFunction, rightFunction);
            break;
        case AntlrSQLLexer::PERCENT:
            function = ModuloLogicalFunction(leftFunction, rightFunction);
            break;
        default:
            throw InvalidQuerySyntax("Unknown Arithmetic Binary Operator: {} of type: {}", context->op->getText(), opTokenType);
    }
    helpers.top().functionBuilder.push_back(function);
}

void AntlrSQLQueryPlanCreator::exitArithmeticUnary(AntlrSQLParser::ArithmeticUnaryContext* context)
{
    if (helpers.empty())
    {
        throw InvalidQuerySyntax("Parser is confused at {}", context->getText());
    }
    LogicalFunction function;

    if (helpers.top().functionBuilder.empty())
    {
        throw InvalidQuerySyntax("Expected unary operator, got nothing: {}", context->getText());
    }
    const auto innerFunction = helpers.top().functionBuilder.back();
    helpers.top().functionBuilder.pop_back();
    auto opTokenType = context->op->getType();
    switch (opTokenType) /// TODO #619: improve this switch case
    {
        case AntlrSQLLexer::PLUS:
            function = innerFunction;
            break;
        case AntlrSQLLexer::MINUS:
            function = MulLogicalFunction(
                ConstantValueLogicalFunction(DataTypeProvider::provideDataType(DataType::Type::UINT64), "-1"), innerFunction);
            break;
        default:
            throw InvalidQuerySyntax("Unknown Arithmetic Binary Operator: {} of type: {}", context->op->getText(), opTokenType);
    }
    helpers.top().functionBuilder.push_back(function);
}

void AntlrSQLQueryPlanCreator::enterUnquotedIdentifier(AntlrSQLParser::UnquotedIdentifierContext* context)
{
    /// Get Index of Parent Rule to check type of parent rule in conditions
    const auto parentContext = dynamic_cast<antlr4::ParserRuleContext*>(context->parent);
    const bool isParentRuleTableAlias = (parentContext != nullptr) && parentContext->getRuleIndex() == AntlrSQLParser::RuleTableAlias;
    if (helpers.top().isFrom && !helpers.top().isJoinRelation)
    {
        helpers.top().newSourceName = context->getText();
    }
    else if (helpers.top().isJoinRelation && isParentRuleTableAlias)
    {
        helpers.top().joinSourceRenames.emplace_back(context->getText());
    }
    AntlrSQLBaseListener::enterUnquotedIdentifier(context);
}

void AntlrSQLQueryPlanCreator::enterIdentifier(AntlrSQLParser::IdentifierContext* context)
{
    /// Get Index of Parent Rule to check type of parent rule in conditions
    std::optional<size_t> parentRuleIndex;
    if (const auto* const parentContext = dynamic_cast<antlr4::ParserRuleContext*>(context->parent); parentContext != nullptr)
    {
        parentRuleIndex = parentContext->getRuleIndex();
    }
    if (helpers.top().isGroupBy)
    {
        helpers.top().groupByFields.emplace_back(bindIdentifier(context));
    }
    else if (
        (helpers.top().isWhereOrHaving || helpers.top().isSelect || helpers.top().isWindow)
        && AntlrSQLParser::RulePrimaryExpression == parentRuleIndex)
    {
        helpers.top().functionBuilder.emplace_back(FieldAccessLogicalFunction(bindIdentifier(context)));
    }
    else if (helpers.top().isFrom and not helpers.top().isJoinRelation and AntlrSQLParser::RuleErrorCapturingIdentifier == parentRuleIndex)
    {
        /// get main source name
        helpers.top().setSource(bindIdentifier(context));
    }
    else if (
        AntlrSQLParser::RuleNamedExpression == parentRuleIndex and helpers.top().isInFunctionCall() and not helpers.top().isJoinRelation
        and not helpers.top().isInAggFunction())
    {
        /// handle renames of identifiers
        if (helpers.top().isArithmeticBinary)
        {
            throw InvalidQuerySyntax("There must not be a binary arithmetic token at this point: {}", context->getText());
        }
        if ((helpers.top().isWhereOrHaving || helpers.top().isSelect))
        {
            /// The user specified named expression (field access or function) with 'AS THE_NAME'
            /// (we handle cases where the user did not specify a name via 'AS' in 'exitNamedExpression')
            const auto attribute = std::move(helpers.top().functionBuilder.back());
            helpers.top().functionBuilder.pop_back();
            helpers.top().addProjection(FieldIdentifier(bindIdentifier(context)), attribute);
        }
    }
    else if (helpers.top().isInAggFunction() and AntlrSQLParser::RuleNamedExpression == parentRuleIndex)
    {
        auto aggFunc = helpers.top().windowAggs.back();
        helpers.top().windowAggs.pop_back();
        aggFunc->asField = (FieldAccessLogicalFunction(bindIdentifier(context)));
        helpers.top().windowAggs.push_back(aggFunc);
        INVARIANT(
            std::nullopt != helpers.top().functionBuilder.back().tryGet<FieldAccessLogicalFunction>(),
            "The functionBuilder should hold the AccessFunction of the name of the field the aggregation is executed on.");
        helpers.top().functionBuilder.pop_back();
        helpers.top().addProjection(std::nullopt, aggFunc->asField);
        helpers.top().hasUnnamedAggregation = false;
    }
    else if (helpers.top().isJoinRelation and AntlrSQLParser::RulePrimaryExpression == parentRuleIndex)
    {
        helpers.top().joinKeyRelationHelper.emplace_back(FieldAccessLogicalFunction(bindIdentifier(context)));
    }
    else if (helpers.top().isJoinRelation and AntlrSQLParser::RuleErrorCapturingIdentifier == parentRuleIndex)
    {
        helpers.top().joinSources.push_back(bindIdentifier(context));
    }
    else if (helpers.top().isJoinRelation and AntlrSQLParser::RuleTableAlias == parentRuleIndex)
    {
        helpers.top().joinSourceRenames.push_back(bindIdentifier(context));
    }
}

void AntlrSQLQueryPlanCreator::enterPrimaryQuery(AntlrSQLParser::PrimaryQueryContext* context)
{
    if (not helpers.empty() and not helpers.top().isFrom and not helpers.top().isSetOperation)
    {
        throw InvalidQuerySyntax("Subqueries are only supported in FROM clauses, but got {}", context->getText());
    }

    const AntlrSQLHelper helper;
    helpers.push(helper);
    AntlrSQLBaseListener::enterPrimaryQuery(context);
}

void AntlrSQLQueryPlanCreator::exitPrimaryQuery(AntlrSQLParser::PrimaryQueryContext* context)
{
    LogicalPlan queryPlan;

    if (not helpers.top().queryPlans.empty())
    {
        queryPlan = std::move(helpers.top().queryPlans[0]);
    }
    else
    {
        if (helpers.top().getSource().empty())
        {
            const auto [type, configOptions] = helpers.top().getInlineSourceConfig();
            const auto parserConfig = getParserConfig(configOptions);
            const auto sourceConfig = getSourceConfig(configOptions);
            const auto schema = getSourceSchema(configOptions);
            if (!schema.has_value())
            {
                throw InvalidConfigParameter("Inline Source is missing schema definition");
            }

            queryPlan = LogicalPlanBuilder::createLogicalPlan(type, schema.value(), sourceConfig, parserConfig);
        }
        else
        {
            queryPlan = LogicalPlanBuilder::createLogicalPlan(helpers.top().getSource());
        }
    }

    for (auto whereExpr = helpers.top().getWhereClauses().rbegin(); whereExpr != helpers.top().getWhereClauses().rend(); ++whereExpr)
    {
        queryPlan = LogicalPlanBuilder::addSelection(std::move(*whereExpr), queryPlan);
    }

    if (helpers.top().isInAggFunction())
    {
        queryPlan = LogicalPlanBuilder::addWindowAggregation(
            queryPlan, helpers.top().windowType, helpers.top().windowAggs, helpers.top().groupByFields);
    }

    queryPlan = LogicalPlanBuilder::addProjection(helpers.top().getProjections(), helpers.top().asterisk, queryPlan);

    if (helpers.top().windowType != nullptr)
    {
        for (auto havingExpr = helpers.top().getHavingClauses().rbegin(); havingExpr != helpers.top().getHavingClauses().rend();
             ++havingExpr)
        {
            queryPlan = LogicalPlanBuilder::addSelection(*havingExpr, queryPlan);
        }
    }
    helpers.pop();
    if (helpers.empty())
    {
        queryPlans.push(queryPlan);
    }
    else
    {
        auto& subQueryHelper = helpers.top();
        subQueryHelper.queryPlans.push_back(queryPlan);
    }
    AntlrSQLBaseListener::exitPrimaryQuery(context);
}

void AntlrSQLQueryPlanCreator::enterWindowClause(AntlrSQLParser::WindowClauseContext* context)
{
    helpers.top().isWindow = true;
    AntlrSQLBaseListener::enterWindowClause(context);
}

void AntlrSQLQueryPlanCreator::exitWindowClause(AntlrSQLParser::WindowClauseContext* context)
{
    helpers.top().isWindow = false;
    AntlrSQLBaseListener::exitWindowClause(context);
}

void AntlrSQLQueryPlanCreator::enterTimeUnit(AntlrSQLParser::TimeUnitContext* context)
{
    /// Get Index of Parent Rule to check type of parent rule in conditions
    std::optional<size_t> parentRuleIndex;
    if (const auto parentContext = dynamic_cast<antlr4::ParserRuleContext*>(context->parent); parentContext != nullptr)
    {
        parentRuleIndex = parentContext->getRuleIndex();
    }

    auto* token = context->getStop();
    auto timeunit = token->getType();
    if (parentRuleIndex == AntlrSQLParser::RuleAdvancebyParameter)
    {
        helpers.top().timeUnitAdvanceBy = timeunit;
    }
    else
    {
        helpers.top().timeUnit = timeunit;
    }
}

void AntlrSQLQueryPlanCreator::exitSizeParameter(AntlrSQLParser::SizeParameterContext* context)
{
    if (context->children.size() < 3)
    {
        throw InvalidQuerySyntax("SizeParameter must have 'size', a number, and a time unit.");
    }
    helpers.top().size = std::stoi(context->children.at(1)->getText());
    AntlrSQLBaseListener::exitSizeParameter(context);
}

void AntlrSQLQueryPlanCreator::exitAdvancebyParameter(AntlrSQLParser::AdvancebyParameterContext* context)
{
    if (context->children.size() < 3)
    {
        throw InvalidQuerySyntax("AdvancebyParameter must have 'ADVANCE BY', a number, and a time unit.");
    }
    helpers.top().advanceBy = std::stoi(context->children.at(2)->getText());
    AntlrSQLBaseListener::exitAdvancebyParameter(context);
}

void AntlrSQLQueryPlanCreator::exitTimestampParameter(AntlrSQLParser::TimestampParameterContext* context)
{
    helpers.top().timestamp = bindIdentifier(context->name);
}

/// WINDOWS
void AntlrSQLQueryPlanCreator::exitTumblingWindow(AntlrSQLParser::TumblingWindowContext* context)
{
    const auto timeMeasure = buildTimeMeasure(helpers.top().size, helpers.top().timeUnit);
    /// We use the ingestion time if the query does not have a timestamp fieldname specified
    if (helpers.top().timestamp.empty())
    {
        helpers.top().windowType = std::make_shared<Windowing::TumblingWindow>(API::IngestionTime(), timeMeasure);
    }
    else
    {
        helpers.top().windowType = std::make_shared<Windowing::TumblingWindow>(
            Windowing::TimeCharacteristic::createEventTime(FieldAccessLogicalFunction(helpers.top().timestamp)), timeMeasure);
    }
    AntlrSQLBaseListener::exitTumblingWindow(context);
}

void AntlrSQLQueryPlanCreator::exitSlidingWindow(AntlrSQLParser::SlidingWindowContext* context)
{
    const auto timeMeasure = buildTimeMeasure(helpers.top().size, helpers.top().timeUnit);
    const auto slidingLength = buildTimeMeasure(helpers.top().advanceBy, helpers.top().timeUnitAdvanceBy);
    /// We use the ingestion time if the query does not have a timestamp fieldname specified
    if (helpers.top().timestamp.empty())
    {
        helpers.top().windowType = Windowing::SlidingWindow::of(API::IngestionTime(), timeMeasure, slidingLength);
    }
    else
    {
        helpers.top().windowType = Windowing::SlidingWindow::of(
            Windowing::TimeCharacteristic::createEventTime(FieldAccessLogicalFunction(helpers.top().timestamp)),
            timeMeasure,
            slidingLength);
    }
    AntlrSQLBaseListener::exitSlidingWindow(context);
}

void AntlrSQLQueryPlanCreator::exitNamedExpression(AntlrSQLParser::NamedExpressionContext* context)
{
    AntlrSQLHelper& helper = helpers.top();
    if (context->name == nullptr and helper.functionBuilder.size() == 1
        and helper.functionBuilder.back().tryGet<FieldAccessLogicalFunction>() and not helpers.top().hasUnnamedAggregation)
    {
        /// Project onto the specified field and remove the field access from the active functions.
        helpers.top().addProjection(std::nullopt, std::move(helpers.top().functionBuilder.back()));
        helpers.top().functionBuilder.pop_back();
    }
    else if (helper.isSelect && context->getText() == "*" && helper.functionBuilder.empty())
    {
        helper.asterisk = true;
    }
    /// The user did not specify a new name (... AS THE_NAME) for the aggregation function and we need to generate one.
    else if (context->name == nullptr and not helpers.top().functionBuilder.empty() and helpers.top().hasUnnamedAggregation)
    {
        const auto accessFunction = helpers.top().functionBuilder.back();
        helpers.top().functionBuilder.pop_back();
        const auto fieldAccessNode = accessFunction.get<FieldAccessLogicalFunction>();
        const auto lastAggregation = helpers.top().windowAggs.back();
        const auto newName = fmt::format("{}_{}", fieldAccessNode.getFieldName(), Util::toUpperCase(lastAggregation->getName()));
        const auto asField = FieldAccessLogicalFunction(newName);
        lastAggregation->asField = asField;
        helpers.top().windowAggs.pop_back();
        helpers.top().windowAggs.push_back(lastAggregation);
        helpers.top().addProjection(std::nullopt, asField);
        helpers.top().hasUnnamedAggregation = false;
    }
    AntlrSQLBaseListener::exitNamedExpression(context);
}

void AntlrSQLQueryPlanCreator::enterFunctionCall(AntlrSQLParser::FunctionCallContext* context)
{
    AntlrSQLBaseListener::enterFunctionCall(context);
}

void AntlrSQLQueryPlanCreator::enterHavingClause(AntlrSQLParser::HavingClauseContext* context)
{
    helpers.top().isWhereOrHaving = true;
    AntlrSQLBaseListener::enterHavingClause(context);
}

void AntlrSQLQueryPlanCreator::exitHavingClause(AntlrSQLParser::HavingClauseContext* context)
{
    helpers.top().isWhereOrHaving = false;
    if (helpers.top().functionBuilder.size() != 1)
    {
        throw InvalidQuerySyntax("There was more than one function in the functionBuilder in exitHavingClause.");
    }
    helpers.top().addHavingClause(helpers.top().functionBuilder.back());
    helpers.top().functionBuilder.clear();
    AntlrSQLBaseListener::exitHavingClause(context);
}

void AntlrSQLQueryPlanCreator::exitComparison(AntlrSQLParser::ComparisonContext* context)
{
    if (helpers.top().isJoinRelation)
    {
        if (helpers.top().joinKeyRelationHelper.size() < 2)
        {
            throw InvalidQuerySyntax(
                "Requires two functions but got {} at {}", helpers.top().joinKeyRelationHelper.size(), context->getText());
        }
        const auto rightFunction = helpers.top().joinKeyRelationHelper.back();
        helpers.top().joinKeyRelationHelper.pop_back();
        const auto leftFunction = helpers.top().joinKeyRelationHelper.back();
        helpers.top().joinKeyRelationHelper.pop_back();
        const auto function = createFunctionFromOpBoolean(leftFunction, rightFunction, helpers.top().opBoolean);
        helpers.top().joinKeyRelationHelper.push_back(function);
    }
    else
    {
        if (helpers.top().functionBuilder.size() < 2)
        {
            throw InvalidQuerySyntax("Comparison requires two parameters, got {}", context->getText());
        }
        const auto rightFunction = helpers.top().functionBuilder.back();
        helpers.top().functionBuilder.pop_back();
        const auto leftFunction = helpers.top().functionBuilder.back();
        helpers.top().functionBuilder.pop_back();

        const auto function = createFunctionFromOpBoolean(leftFunction, rightFunction, helpers.top().opBoolean);
        helpers.top().functionBuilder.push_back(function);
    }
    AntlrSQLBaseListener::exitComparison(context);
}

void AntlrSQLQueryPlanCreator::enterJoinRelation(AntlrSQLParser::JoinRelationContext* context)
{
    helpers.top().joinKeyRelationHelper.clear();
    helpers.top().isJoinRelation = true;
    AntlrSQLBaseListener::enterJoinRelation(context);
}

void AntlrSQLQueryPlanCreator::enterJoinCriteria(AntlrSQLParser::JoinCriteriaContext* context)
{
    INVARIANT(helpers.top().isJoinRelation, "Join criteria must be inside a join relation.");
    AntlrSQLBaseListener::enterJoinCriteria(context);
}

void AntlrSQLQueryPlanCreator::enterJoinType(AntlrSQLParser::JoinTypeContext* context)
{
    if (not helpers.top().isJoinRelation)
    {
        throw InvalidQuerySyntax("Join type must be inside a join relation.");
    }
    AntlrSQLBaseListener::enterJoinType(context);
}

void AntlrSQLQueryPlanCreator::exitJoinType(AntlrSQLParser::JoinTypeContext* context)
{
    const auto joinType = context->getText();
    auto tokenType = context->getStop()->getType();

    if (joinType.empty() || tokenType == AntlrSQLLexer::INNER)
    {
        helpers.top().joinType = JoinLogicalOperator::JoinType::INNER_JOIN;
    }
    else
    {
        throw InvalidQuerySyntax("Unknown join type: {}, resolved to token type: {}", joinType, tokenType);
    }
    AntlrSQLBaseListener::exitJoinType(context);
}

void AntlrSQLQueryPlanCreator::exitJoinRelation(AntlrSQLParser::JoinRelationContext* context)
{
    helpers.top().isJoinRelation = false;
    if (helpers.top().joinSources.size() == helpers.top().joinSourceRenames.size() + 1)
    {
        helpers.top().joinSourceRenames.emplace_back("");
    }

    /// we assume that the left query plan is the first element in the queryPlans vector and the right query plan is the second element
    if (helpers.top().queryPlans.size() != 2)
    {
        throw InvalidQuerySyntax(
            "Join relation requires two subqueries, but got {} at {}", helpers.top().queryPlans.size(), context->getText());
    }
    const auto leftQueryPlan = helpers.top().queryPlans[0];
    const auto rightQueryPlan = helpers.top().queryPlans[1];
    helpers.top().queryPlans.clear();

    if (helpers.top().joinKeyRelationHelper.size() != 1)
    {
        throw InvalidQuerySyntax("joinFunction is required but empty at {}", context->getText());
    }
    if (!helpers.top().windowType)
    {
        throw InvalidQuerySyntax("windowType is required but empty at {}", context->getText());
    }
    const auto queryPlan = LogicalPlanBuilder::addJoin(
        leftQueryPlan, rightQueryPlan, helpers.top().joinKeyRelationHelper.at(0), helpers.top().windowType, helpers.top().joinType);
    if (not helpers.empty())
    {
        /// we are in a subquery
        helpers.top().queryPlans.push_back(queryPlan);
    }
    else
    {
        /// for now, we will never enter this branch, because we always have a subquery
        /// as we require the join relations to always be a sub-query
        queryPlans.push(queryPlan);
    }
    AntlrSQLBaseListener::exitJoinRelation(context);
}

void AntlrSQLQueryPlanCreator::exitLogicalNot(AntlrSQLParser::LogicalNotContext* context)
{
    if (helpers.empty())
    {
        throw InvalidQuerySyntax("Parser is confused at {}", context->getText());
    }

    if (helpers.top().isJoinRelation)
    {
        if (helpers.top().joinKeyRelationHelper.empty())
        {
            throw InvalidQuerySyntax("Negate requires child op at {}", context->getText());
        }
        const auto innerFunction = helpers.top().joinKeyRelationHelper.back();
        helpers.top().joinKeyRelationHelper.pop_back();
        auto negatedFunction = NegateLogicalFunction(innerFunction);
        helpers.top().joinKeyRelationHelper.emplace_back(negatedFunction);
    }
    else
    {
        if (helpers.top().functionBuilder.empty())
        {
            throw InvalidQuerySyntax("Negate requires child op at {}", context->getText());
        }
        const auto innerFunction = helpers.top().functionBuilder.back();
        helpers.top().functionBuilder.pop_back();
        helpers.top().functionBuilder.emplace_back(NegateLogicalFunction(innerFunction));
    }
    AntlrSQLBaseListener::exitLogicalNot(context);
}

void AntlrSQLQueryPlanCreator::exitConstantDefault(AntlrSQLParser::ConstantDefaultContext* context)
{
    if (context->children.size() != 1)
    {
        throw InvalidQuerySyntax("When exiting a constant, there must be exactly one children in the context {}", context->getText());
    }
    if (const auto stringLiteralContext = dynamic_cast<AntlrSQLParser::StringLiteralContext*>(context->children.at(0)))
    {
        if (!(stringLiteralContext->getText().size() > 2))
        {
            throw InvalidQuerySyntax(
                "A constant string literal must contain at least two quotes and must not be empty at {}", context->getText());
        }
        helpers.top().constantBuilder.push_back(context->getText().substr(1, stringLiteralContext->getText().size() - 2));
    }
    else
    {
        helpers.top().constantBuilder.push_back(context->getText());
    }
}

void AntlrSQLQueryPlanCreator::exitFunctionCall(AntlrSQLParser::FunctionCallContext* context)
{
    const auto funcName = Util::toUpperCase(context->children[0]->getText());
    const auto tokenType = context->getStart()->getType();

    helpers.top().hasUnnamedAggregation = true;
    switch (tokenType) /// TODO #619: improve this switch case
    {
        case AntlrSQLLexer::COUNT:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                std::make_shared<CountAggregationLogicalFunction>(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::AVG:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                std::make_shared<AvgAggregationLogicalFunction>(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::MAX:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                std::make_shared<MaxAggregationLogicalFunction>(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::MIN:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                std::make_shared<MinAggregationLogicalFunction>(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::SUM:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                std::make_shared<SumAggregationLogicalFunction>(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::MEDIAN:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                std::make_shared<MedianAggregationLogicalFunction>(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
                case AntlrSQLLexer::ARRAY_AGG:
            helpers.top().windowAggs.push_back(
                ArrayAggregationLogicalFunction::create(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::VAR:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                VarAggregationLogicalFunction::create(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::TEMPORAL_SEQUENCE:
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TEMPORAL_SEQUENCE requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                
                // Verify all arguments are field access functions
                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_SEQUENCE arguments must be field references");
                }
                
                helpers.top().windowAggs.push_back(
                    TemporalSequenceAggregationLogicalFunctionV2::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                        latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                        timestampFunction.get<FieldAccessLogicalFunction>()));
                // Push back one field access function to satisfy parser expectations
                // This prevents the functionBuilder from being empty when processing the identifier
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        case AntlrSQLLexer::TEMPORAL_EINTERSECTS_GEOMETRY:
            {
                // Convert constants from constantBuilder to ConstantValueLogicalFunction objects
                while (!helpers.top().constantBuilder.empty()) {
                    auto constantValue = std::move(helpers.top().constantBuilder.back());
                    helpers.top().constantBuilder.pop_back();
                    // Assume string constants are VARSIZED (WKT strings)
                    auto dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                    auto constFunction = ConstantValueLogicalFunction(dataType, std::move(constantValue));
                    helpers.top().functionBuilder.push_back(constFunction);
                }

                const auto argCount = helpers.top().functionBuilder.size();
                if (argCount != 4 && argCount != 6) {
                    throw InvalidQuerySyntax("TEMPORAL_EINTERSECTS_GEOMETRY requires either 4 arguments (lon1, lat1, timestamp1, static_geometry) or 6 arguments (lon1, lat1, timestamp1, lon2, lat2, timestamp2), but got {}", argCount);
                }

                if (argCount == 4) {
                    // 4-parameter case: temporal-static intersection (lon1, lat1, timestamp1, static_geometry)
                    const auto staticGeometryFunction = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto timestamp1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lat1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lon1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    
                    const auto function = TemporalIntersectsGeometryLogicalFunction(lon1Function, lat1Function, timestamp1Function, staticGeometryFunction);
                    helpers.top().functionBuilder.push_back(function);
                } else {
                    // 6-parameter case: temporal-temporal intersection (lon1, lat1, timestamp1, lon2, lat2, timestamp2)
                    const auto timestamp2Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lat2Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lon2Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto timestamp1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lat1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lon1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    
                    const auto function = TemporalIntersectsGeometryLogicalFunction(lon1Function, lat1Function, timestamp1Function, lon2Function, lat2Function, timestamp2Function);
                    helpers.top().functionBuilder.push_back(function);
                }
            }
            break;
        case AntlrSQLLexer::TEMPORAL_AINTERSECTS_GEOMETRY:
            {
                // Convert constants from constantBuilder to ConstantValueLogicalFunction objects
                while (!helpers.top().constantBuilder.empty()) {
                    auto constantValue = std::move(helpers.top().constantBuilder.back());
                    helpers.top().constantBuilder.pop_back();
                    // Assume string constants are VARSIZED (WKT strings)
                    auto dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                    auto constFunction = ConstantValueLogicalFunction(dataType, std::move(constantValue));
                    helpers.top().functionBuilder.push_back(constFunction);
                }
                const auto argCount = helpers.top().functionBuilder.size();
                if (argCount != 4 && argCount != 6) {
                    throw InvalidQuerySyntax("TEMPORAL_AINTERSECTS_GEOMETRY requires either 4 arguments (lon1, lat1, timestamp1, static_geometry) or 6 arguments (lon1, lat1, timestamp1, lon2, lat2, timestamp2), but got {}", argCount);
                }
                if (argCount == 4) {
                    // 4-parameter case: temporal-static intersection (lon1, lat1, timestamp1, static_geometry)
                    const auto staticGeometryFunction = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto timestamp1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lat1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lon1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    
                    const auto function = TemporalAIntersectsGeometryLogicalFunction(lon1Function, lat1Function, timestamp1Function, staticGeometryFunction);
                    helpers.top().functionBuilder.push_back(function);
                } else {
                    // 6-parameter case: temporal-temporal intersection (lon1, lat1, timestamp1, lon2, lat2, timestamp2)
                    const auto timestamp2Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lat2Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lon2Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto timestamp1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lat1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lon1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    
                    const auto function = TemporalAIntersectsGeometryLogicalFunction(lon1Function, lat1Function, timestamp1Function, lon2Function, lat2Function, timestamp2Function);
                    helpers.top().functionBuilder.push_back(function);
                }
            }
            break;
        case AntlrSQLLexer::TEMPORAL_ECONTAINS_GEOMETRY: 
        {
            // move any literal WKT that’s still on constantBuilder into functionBuilder
            while(!helpers.top().constantBuilder.empty()){
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            const auto n = helpers.top().functionBuilder.size();
            if(n==6){
                auto ts2 = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto lat2= helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto lon2= helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto ts1 = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto lat1= helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto lon1= helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    TemporalEContainsGeometryLogicalFunction(lon1,lat1,ts1,lon2,lat2,ts2));
            } else if(n==4){
                /* decide order by data-type of first arg */
                auto last = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto third= helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto second= helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto first = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if(first.getDataType().isType(DataType::Type::VARSIZED)) // static,tgeo
                    helpers.top().functionBuilder.emplace_back(
                        TemporalEContainsGeometryLogicalFunction(first,second,third,last));
                else                                                      // tgeo,static
                    helpers.top().functionBuilder.emplace_back(
                        TemporalEContainsGeometryLogicalFunction(first,second,third,last));
            } else {
                throw InvalidQuerySyntax("TEMPORAL_ECONTAINS_GEOMETRY expects 4 or 6 arguments");
            }
        }
        break;
        case AntlrSQLLexer::EDWITHIN_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
            {
                throw InvalidQuerySyntax("EDWITHIN_TGEO_GEO requires exactly five arguments (lon, lat, timestamp, geometry, distance), but got {}", argCount);
            }

            // Move pending constants into the function builder (WKT last)
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                const auto upperValue = Util::toUpperCase(constantValue);
                if (upperValue == "TRUE" || upperValue == "FALSE")
                {
                    dataType = DataTypeProvider::provideDataType(DataType::Type::BOOLEAN);
                }
                else
                {
                    char* endPtr = nullptr;
                    std::strtod(constantValue.c_str(), &endPtr);
                    if (endPtr != nullptr && *endPtr == '\0')
                    {
                        dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                    }
                    else
                    {
                        dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                    }
                }
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            const auto total = helpers.top().functionBuilder.size();
            PRECONDITION(total >= 5, "EDWITHIN_TGEO_GEO requires (lon, lat, timestamp, geometry, distance), but got {}", total);

            // Order after move: [lon, lat, ts, distance, geometry]
            auto geometryFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distanceFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestampFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDWithinGeometryLogicalFunction(lonFunction, latFunction, timestampFunction, geometryFunction, distanceFunction));
        }
        break;
        case AntlrSQLLexer::TGEO_AT_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4 && argCount != 5)
            {
                throw InvalidQuerySyntax("TGEO_AT_STBOX requires four arguments (lon, lat, timestamp, stbox) with an optional fifth border_inc flag, but got {}", argCount);
            }

            // Move pending constants into the function builder (border first if present, STBOX last)
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                const auto upperValue = Util::toUpperCase(constantValue);
                if (upperValue == "TRUE" || upperValue == "FALSE")
                {
                    dataType = DataTypeProvider::provideDataType(DataType::Type::BOOLEAN);
                }
                else
                {
                    char* endPtr = nullptr;
                    std::strtod(constantValue.c_str(), &endPtr);
                    if (endPtr != nullptr && *endPtr == '\0')
                    {
                        dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                    }
                    else
                    {
                        dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                    }
                }
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            const auto total = helpers.top().functionBuilder.size();
            PRECONDITION(total >= 4, "TGEO_AT_STBOX requires (lon, lat, timestamp, stbox) with optional border flag, but got {}", total);

            auto stboxFunction = helpers.top().functionBuilder.back();
            helpers.top().functionBuilder.pop_back();

            LogicalFunction borderFlag = ConstantValueLogicalFunction(
                DataTypeProvider::provideDataType(DataType::Type::BOOLEAN), "TRUE");
            if (!helpers.top().functionBuilder.empty() && helpers.top().functionBuilder.back().getDataType().isType(DataType::Type::BOOLEAN))
            {
                borderFlag = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
            }

            auto timestampFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAtStBoxLogicalFunction(lonFunction, latFunction, timestampFunction, stboxFunction, borderFlag));
        }
        break;

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_CEIL */
        case AntlrSQLLexer::TFLOAT_CEIL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_CEIL requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatCeilLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_CEIL */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_EXP */
        case AntlrSQLLexer::TFLOAT_EXP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_EXP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatExpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_EXP */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_FLOOR */
        case AntlrSQLLexer::TFLOAT_FLOOR:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_FLOOR requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatFloorLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_FLOOR */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_LN */
        case AntlrSQLLexer::TFLOAT_LN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_LN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatLnLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_LN */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_LOG10 */
        case AntlrSQLLexer::TFLOAT_LOG10:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_LOG10 requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatLog10LogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_LOG10 */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_RADIANS */
        case AntlrSQLLexer::TFLOAT_RADIANS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_RADIANS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatRadiansLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_RADIANS */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_TO_TINT */
        case AntlrSQLLexer::TFLOAT_TO_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_TO_TINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatToTintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_TO_TINT */

        /* BEGIN CODEGEN PARSER GLUE: TINT_TO_TFLOAT */
        case AntlrSQLLexer::TINT_TO_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TINT_TO_TFLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintToTfloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_TO_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ADD_TFLOAT_FLOAT */
        case AntlrSQLLexer::ADD_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADD_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: SUB_TFLOAT_FLOAT */
        case AntlrSQLLexer::SUB_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SUB_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SubTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SUB_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: MUL_TFLOAT_FLOAT */
        case AntlrSQLLexer::MUL_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("MUL_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MulTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: MUL_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: DIV_TFLOAT_FLOAT */
        case AntlrSQLLexer::DIV_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DIV_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DivTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DIV_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ADD_TINT_INT */
        case AntlrSQLLexer::ADD_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADD_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: SUB_TINT_INT */
        case AntlrSQLLexer::SUB_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SUB_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SubTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SUB_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: MUL_TINT_INT */
        case AntlrSQLLexer::MUL_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("MUL_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MulTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: MUL_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: DIV_TINT_INT */
        case AntlrSQLLexer::DIV_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DIV_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DivTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DIV_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: ADD_FLOAT_TFLOAT */
        case AntlrSQLLexer::ADD_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADD_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: SUB_FLOAT_TFLOAT */
        case AntlrSQLLexer::SUB_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SUB_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SubFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SUB_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: MUL_FLOAT_TFLOAT */
        case AntlrSQLLexer::MUL_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("MUL_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MulFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: MUL_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: DIV_FLOAT_TFLOAT */
        case AntlrSQLLexer::DIV_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DIV_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DivFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DIV_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ADD_INT_TINT */
        case AntlrSQLLexer::ADD_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADD_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: SUB_INT_TINT */
        case AntlrSQLLexer::SUB_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SUB_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SubIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SUB_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: MUL_INT_TINT */
        case AntlrSQLLexer::MUL_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("MUL_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MulIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: MUL_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: DIV_INT_TINT */
        case AntlrSQLLexer::DIV_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DIV_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DivIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DIV_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: ADD_TBIGINT_BIGINT */
        case AntlrSQLLexer::ADD_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADD_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: SUB_TBIGINT_BIGINT */
        case AntlrSQLLexer::SUB_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SUB_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SubTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SUB_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: MUL_TBIGINT_BIGINT */
        case AntlrSQLLexer::MUL_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("MUL_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MulTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: MUL_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: DIV_TBIGINT_BIGINT */
        case AntlrSQLLexer::DIV_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DIV_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DivTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DIV_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ADD_BIGINT_TBIGINT */
        case AntlrSQLLexer::ADD_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADD_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: SUB_BIGINT_TBIGINT */
        case AntlrSQLLexer::SUB_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SUB_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SubBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SUB_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: MUL_BIGINT_TBIGINT */
        case AntlrSQLLexer::MUL_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("MUL_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MulBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: MUL_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: DIV_BIGINT_TBIGINT */
        case AntlrSQLLexer::DIV_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DIV_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DivBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DIV_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ADD_TNUMBER_TNUMBER */
        case AntlrSQLLexer::ADD_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADD_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: SUB_TNUMBER_TNUMBER */
        case AntlrSQLLexer::SUB_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SUB_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SubTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SUB_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: MUL_TNUMBER_TNUMBER */
        case AntlrSQLLexer::MUL_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MUL_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MulTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MUL_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: DIV_TNUMBER_TNUMBER */
        case AntlrSQLLexer::DIV_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DIV_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DivTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DIV_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_SHIFT_VALUE */
        case AntlrSQLLexer::TFLOAT_SHIFT_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TFLOAT_SHIFT_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatShiftValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_SHIFT_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_SCALE_VALUE */
        case AntlrSQLLexer::TFLOAT_SCALE_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TFLOAT_SCALE_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatScaleValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_SCALE_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_SHIFT_SCALE_VALUE */
        case AntlrSQLLexer::TFLOAT_SHIFT_SCALE_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TFLOAT_SHIFT_SCALE_VALUE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatShiftScaleValueLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_SHIFT_SCALE_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TINT_SHIFT_VALUE */
        case AntlrSQLLexer::TINT_SHIFT_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TINT_SHIFT_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintShiftValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_SHIFT_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TINT_SCALE_VALUE */
        case AntlrSQLLexer::TINT_SCALE_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TINT_SCALE_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintScaleValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_SCALE_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TINT_SHIFT_SCALE_VALUE */
        case AntlrSQLLexer::TINT_SHIFT_SCALE_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TINT_SHIFT_SCALE_VALUE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintShiftScaleValueLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_SHIFT_SCALE_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TBIGINT_SHIFT_VALUE */
        case AntlrSQLLexer::TBIGINT_SHIFT_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TBIGINT_SHIFT_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TbigintShiftValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBIGINT_SHIFT_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TBIGINT_SCALE_VALUE */
        case AntlrSQLLexer::TBIGINT_SCALE_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TBIGINT_SCALE_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TbigintScaleValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBIGINT_SCALE_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TBIGINT_SHIFT_SCALE_VALUE */
        case AntlrSQLLexer::TBIGINT_SHIFT_SCALE_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TBIGINT_SHIFT_SCALE_VALUE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TbigintShiftScaleValueLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBIGINT_SHIFT_SCALE_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TFLOAT_FLOAT */
        case AntlrSQLLexer::TDISTANCE_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TDISTANCE_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TINT_INT */
        case AntlrSQLLexer::TDISTANCE_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TDISTANCE_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TNUMBER_TNUMBER */
        case AntlrSQLLexer::TDISTANCE_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TDISTANCE_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ROUND */
        case AntlrSQLLexer::TEMPORAL_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_ROUND requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalRoundLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ROUND */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TBIGINT_BIGINT */
        case AntlrSQLLexer::EVER_EQ_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_EQ_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TBIGINT_BIGINT */
        case AntlrSQLLexer::EVER_NE_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_NE_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LT_TBIGINT_BIGINT */
        case AntlrSQLLexer::EVER_LT_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LT_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverLtTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LT_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LE_TBIGINT_BIGINT */
        case AntlrSQLLexer::EVER_LE_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LE_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverLeTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LE_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GT_TBIGINT_BIGINT */
        case AntlrSQLLexer::EVER_GT_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GT_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverGtTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GT_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GE_TBIGINT_BIGINT */
        case AntlrSQLLexer::EVER_GE_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GE_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverGeTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GE_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TBIGINT_BIGINT */
        case AntlrSQLLexer::ALWAYS_EQ_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_EQ_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TBIGINT_BIGINT */
        case AntlrSQLLexer::ALWAYS_NE_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_NE_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LT_TBIGINT_BIGINT */
        case AntlrSQLLexer::ALWAYS_LT_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LT_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysLtTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LT_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LE_TBIGINT_BIGINT */
        case AntlrSQLLexer::ALWAYS_LE_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LE_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysLeTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LE_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GT_TBIGINT_BIGINT */
        case AntlrSQLLexer::ALWAYS_GT_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GT_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysGtTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GT_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GE_TBIGINT_BIGINT */
        case AntlrSQLLexer::ALWAYS_GE_TBIGINT_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GE_TBIGINT_BIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysGeTbigintBigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GE_TBIGINT_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_BIGINT_TBIGINT */
        case AntlrSQLLexer::EVER_EQ_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_EQ_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_BIGINT_TBIGINT */
        case AntlrSQLLexer::EVER_NE_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_NE_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LT_BIGINT_TBIGINT */
        case AntlrSQLLexer::EVER_LT_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LT_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverLtBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LT_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LE_BIGINT_TBIGINT */
        case AntlrSQLLexer::EVER_LE_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LE_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverLeBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LE_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GT_BIGINT_TBIGINT */
        case AntlrSQLLexer::EVER_GT_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GT_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverGtBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GT_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GE_BIGINT_TBIGINT */
        case AntlrSQLLexer::EVER_GE_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GE_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverGeBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GE_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_BIGINT_TBIGINT */
        case AntlrSQLLexer::ALWAYS_EQ_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_EQ_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_BIGINT_TBIGINT */
        case AntlrSQLLexer::ALWAYS_NE_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_NE_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LT_BIGINT_TBIGINT */
        case AntlrSQLLexer::ALWAYS_LT_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LT_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysLtBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LT_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LE_BIGINT_TBIGINT */
        case AntlrSQLLexer::ALWAYS_LE_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LE_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysLeBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LE_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GT_BIGINT_TBIGINT */
        case AntlrSQLLexer::ALWAYS_GT_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GT_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysGtBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GT_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GE_BIGINT_TBIGINT */
        case AntlrSQLLexer::ALWAYS_GE_BIGINT_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GE_BIGINT_TBIGINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysGeBigintTbigintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GE_BIGINT_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TBOOL_BOOL */
        case AntlrSQLLexer::EVER_EQ_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_EQ_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TBOOL_BOOL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TBOOL_BOOL */
        case AntlrSQLLexer::EVER_NE_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_NE_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TBOOL_BOOL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TBOOL_BOOL */
        case AntlrSQLLexer::ALWAYS_EQ_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_EQ_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TBOOL_BOOL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TBOOL_BOOL */
        case AntlrSQLLexer::ALWAYS_NE_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_NE_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TBOOL_BOOL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_BOOL_TBOOL */
        case AntlrSQLLexer::EVER_EQ_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_EQ_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_BOOL_TBOOL */
        case AntlrSQLLexer::EVER_NE_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_NE_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_BOOL_TBOOL */
        case AntlrSQLLexer::ALWAYS_EQ_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_EQ_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_BOOL_TBOOL */
        case AntlrSQLLexer::ALWAYS_NE_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_NE_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: TBIGINT_TO_TINT */
        case AntlrSQLLexer::TBIGINT_TO_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBIGINT_TO_TINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TbigintToTintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBIGINT_TO_TINT */

        /* BEGIN CODEGEN PARSER GLUE: TBIGINT_TO_TFLOAT */
        case AntlrSQLLexer::TBIGINT_TO_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBIGINT_TO_TFLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TbigintToTfloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBIGINT_TO_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TINT_TO_TBIGINT */
        case AntlrSQLLexer::TINT_TO_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TINT_TO_TBIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintToTbigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_TO_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_TO_TBIGINT */
        case AntlrSQLLexer::TFLOAT_TO_TBIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_TO_TBIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatToTbigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_TO_TBIGINT */

        /* BEGIN CODEGEN PARSER GLUE: TBOOL_TO_TINT */
        case AntlrSQLLexer::TBOOL_TO_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOOL_TO_TINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboolToTintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOOL_TO_TINT */

        /* BEGIN CODEGEN PARSER GLUE: TNOT_TBOOL */
        case AntlrSQLLexer::TNOT_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TNOT_TBOOL requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnotTboolLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNOT_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: TAND_TBOOL_BOOL */
        case AntlrSQLLexer::TAND_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TAND_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TandTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TAND_TBOOL_BOOL */

        /* BEGIN CODEGEN PARSER GLUE: TOR_TBOOL_BOOL */
        case AntlrSQLLexer::TOR_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TOR_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TorTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TOR_TBOOL_BOOL */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TBOOL_BOOL */
        case AntlrSQLLexer::TEQ_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEQ_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TBOOL_BOOL */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TBOOL_BOOL */
        case AntlrSQLLexer::TNE_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNE_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TBOOL_BOOL */

        /* BEGIN CODEGEN PARSER GLUE: TAND_BOOL_TBOOL */
        case AntlrSQLLexer::TAND_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TAND_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TandBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TAND_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: TOR_BOOL_TBOOL */
        case AntlrSQLLexer::TOR_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TOR_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TorBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TOR_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_BOOL_TBOOL */
        case AntlrSQLLexer::TEQ_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEQ_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: TNE_BOOL_TBOOL */
        case AntlrSQLLexer::TNE_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNE_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: TAND_TBOOL_TBOOL */
        case AntlrSQLLexer::TAND_TBOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TAND_TBOOL_TBOOL requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TandTboolTboolLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TAND_TBOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: TOR_TBOOL_TBOOL */
        case AntlrSQLLexer::TOR_TBOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TOR_TBOOL_TBOOL requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TorTboolTboolLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TOR_TBOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: ECOVERS_TGEO_GEO */
        case AntlrSQLLexer::ECOVERS_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ECOVERS_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcoversTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECOVERS_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EDISJOINT_TGEO_GEO */
        case AntlrSQLLexer::EDISJOINT_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EDISJOINT_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdisjointTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDISJOINT_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TGEO_TGEO */
        case AntlrSQLLexer::NAD_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("NAD_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ECONTAINS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ECONTAINS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ECONTAINS_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcontainsTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECONTAINS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ACONTAINS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ACONTAINS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ACONTAINS_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcontainsTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACONTAINS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EDISJOINT_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::EDISJOINT_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("EDISJOINT_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdisjointTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDISJOINT_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EDWITHIN_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::EDWITHIN_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EDWITHIN_TCBUFFER_CBUFFER requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdwithinTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDWITHIN_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ADWITHIN_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::ADWITHIN_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ADWITHIN_TCBUFFER_CBUFFER requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdwithinTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADWITHIN_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ACONTAINS_TGEO_GEO */
        case AntlrSQLLexer::ACONTAINS_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ACONTAINS_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcontainsTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACONTAINS_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ACOVERS_TGEO_GEO */
        case AntlrSQLLexer::ACOVERS_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ACOVERS_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcoversTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACOVERS_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ADISJOINT_TGEO_GEO */
        case AntlrSQLLexer::ADISJOINT_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ADISJOINT_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdisjointTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADISJOINT_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: AINTERSECTS_TGEO_GEO */
        case AntlrSQLLexer::AINTERSECTS_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("AINTERSECTS_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AintersectsTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: AINTERSECTS_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ATOUCHES_TGEO_GEO */
        case AntlrSQLLexer::ATOUCHES_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ATOUCHES_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AtouchesTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ATOUCHES_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ECONTAINS_TGEO_GEO */
        case AntlrSQLLexer::ECONTAINS_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ECONTAINS_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcontainsTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECONTAINS_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ECOVERS_TGEO_GEO */
        case AntlrSQLLexer::ECOVERS_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ECOVERS_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcoversTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECOVERS_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EDISJOINT_TGEO_GEO */
        case AntlrSQLLexer::EDISJOINT_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EDISJOINT_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdisjointTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDISJOINT_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EINTERSECTS_TGEO_GEO */
        case AntlrSQLLexer::EINTERSECTS_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EINTERSECTS_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EintersectsTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EINTERSECTS_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ETOUCHES_TGEO_GEO */
        case AntlrSQLLexer::ETOUCHES_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ETOUCHES_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EtouchesTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ETOUCHES_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TGEO_GEO */
        case AntlrSQLLexer::EVER_EQ_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_EQ_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TGEO_GEO */
        case AntlrSQLLexer::EVER_NE_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_NE_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TGEO_GEO */
        case AntlrSQLLexer::ALWAYS_EQ_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_EQ_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TGEO_GEO */
        case AntlrSQLLexer::ALWAYS_NE_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_NE_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TGEO_GEO */
        case AntlrSQLLexer::NAD_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAD_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ADWITHIN_TGEO_GEO */
        case AntlrSQLLexer::ADWITHIN_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ADWITHIN_TGEO_GEO requires exactly 5 arguments (lon, lat, timestamp, geometry, distance), but got {}", argCount);

            /* Lift constants (geometry + distance) — same shape as EDWITHIN_TGEO_GEO */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                const auto upperValue = Util::toUpperCase(constantValue);
                if (upperValue == "TRUE" || upperValue == "FALSE")
                {
                    dataType = DataTypeProvider::provideDataType(DataType::Type::BOOLEAN);
                }
                else
                {
                    char* endPtr = nullptr;
                    std::strtod(constantValue.c_str(), &endPtr);
                    if (endPtr != nullptr && *endPtr == '\0')
                        dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                    else
                        dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                }
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            /* After lift: [lon, lat, ts, distance, geometry] (geometry pushed last because lifted last in LIFO) */
            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto dist      = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdwithinTgeoGeoLogicalFunction(lon, lat, timestamp, geometry, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADWITHIN_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ACONTAINS_TGEO_TGEO */
        case AntlrSQLLexer::ACONTAINS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ACONTAINS_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcontainsTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACONTAINS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ACOVERS_TGEO_TGEO */
        case AntlrSQLLexer::ACOVERS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ACOVERS_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcoversTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACOVERS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ADISJOINT_TGEO_TGEO */
        case AntlrSQLLexer::ADISJOINT_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ADISJOINT_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdisjointTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADISJOINT_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: AINTERSECTS_TGEO_TGEO */
        case AntlrSQLLexer::AINTERSECTS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("AINTERSECTS_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AintersectsTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: AINTERSECTS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ATOUCHES_TGEO_TGEO */
        case AntlrSQLLexer::ATOUCHES_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ATOUCHES_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AtouchesTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ATOUCHES_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ECONTAINS_TGEO_TGEO */
        case AntlrSQLLexer::ECONTAINS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ECONTAINS_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcontainsTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECONTAINS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ECOVERS_TGEO_TGEO */
        case AntlrSQLLexer::ECOVERS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ECOVERS_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcoversTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECOVERS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: EDISJOINT_TGEO_TGEO */
        case AntlrSQLLexer::EDISJOINT_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EDISJOINT_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdisjointTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDISJOINT_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: EINTERSECTS_TGEO_TGEO */
        case AntlrSQLLexer::EINTERSECTS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EINTERSECTS_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EintersectsTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EINTERSECTS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ETOUCHES_TGEO_TGEO */
        case AntlrSQLLexer::ETOUCHES_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ETOUCHES_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EtouchesTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ETOUCHES_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TGEO_TGEO */
        case AntlrSQLLexer::EVER_EQ_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EVER_EQ_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TGEO_TGEO */
        case AntlrSQLLexer::EVER_NE_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EVER_NE_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TGEO_TGEO */
        case AntlrSQLLexer::ALWAYS_EQ_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ALWAYS_EQ_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TGEO_TGEO */
        case AntlrSQLLexer::ALWAYS_NE_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ALWAYS_NE_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TGEO_TGEO */
        case AntlrSQLLexer::NAD_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("NAD_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: EDWITHIN_TGEO_TGEO */
        case AntlrSQLLexer::EDWITHIN_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("EDWITHIN_TGEO_TGEO requires exactly 7 arguments (lonA, latA, tsA, lonB, latB, tsB, distance), but got {}", argCount);

            /* Lift the distance constant */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdwithinTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDWITHIN_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ADWITHIN_TGEO_TGEO */
        case AntlrSQLLexer::ADWITHIN_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("ADWITHIN_TGEO_TGEO requires exactly 7 arguments (lonA, latA, tsA, lonB, latB, tsB, distance), but got {}", argCount);

            /* Lift the distance constant */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdwithinTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADWITHIN_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ACONTAINS_TCBUFFER_GEO */
        case AntlrSQLLexer::ACONTAINS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ACONTAINS_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcontainsTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACONTAINS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ACOVERS_TCBUFFER_GEO */
        case AntlrSQLLexer::ACOVERS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ACOVERS_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcoversTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACOVERS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ADISJOINT_TCBUFFER_GEO */
        case AntlrSQLLexer::ADISJOINT_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ADISJOINT_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdisjointTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADISJOINT_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: AINTERSECTS_TCBUFFER_GEO */
        case AntlrSQLLexer::AINTERSECTS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("AINTERSECTS_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AintersectsTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: AINTERSECTS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ATOUCHES_TCBUFFER_GEO */
        case AntlrSQLLexer::ATOUCHES_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ATOUCHES_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AtouchesTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ATOUCHES_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ECONTAINS_TCBUFFER_GEO */
        case AntlrSQLLexer::ECONTAINS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ECONTAINS_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcontainsTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECONTAINS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ECOVERS_TCBUFFER_GEO */
        case AntlrSQLLexer::ECOVERS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ECOVERS_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcoversTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECOVERS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EDISJOINT_TCBUFFER_GEO */
        case AntlrSQLLexer::EDISJOINT_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EDISJOINT_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdisjointTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDISJOINT_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EINTERSECTS_TCBUFFER_GEO */
        case AntlrSQLLexer::EINTERSECTS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EINTERSECTS_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EintersectsTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EINTERSECTS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ETOUCHES_TCBUFFER_GEO */
        case AntlrSQLLexer::ETOUCHES_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ETOUCHES_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EtouchesTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ETOUCHES_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TCBUFFER_GEO */
        case AntlrSQLLexer::NAD_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAD_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EDWITHIN_TCBUFFER_GEO */
        case AntlrSQLLexer::EDWITHIN_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EDWITHIN_TCBUFFER_GEO requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdwithinTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDWITHIN_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ADWITHIN_TCBUFFER_GEO */
        case AntlrSQLLexer::ADWITHIN_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ADWITHIN_TCBUFFER_GEO requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdwithinTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADWITHIN_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ACONTAINS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ACONTAINS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ACONTAINS_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcontainsTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACONTAINS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ACOVERS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ACOVERS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ACOVERS_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcoversTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACOVERS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ADISJOINT_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ADISJOINT_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ADISJOINT_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdisjointTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADISJOINT_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: AINTERSECTS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::AINTERSECTS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("AINTERSECTS_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AintersectsTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: AINTERSECTS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ATOUCHES_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ATOUCHES_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ATOUCHES_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AtouchesTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ATOUCHES_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ECONTAINS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ECONTAINS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ECONTAINS_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcontainsTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECONTAINS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ECOVERS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ECOVERS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ECOVERS_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcoversTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECOVERS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EDISJOINT_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::EDISJOINT_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("EDISJOINT_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdisjointTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDISJOINT_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EINTERSECTS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::EINTERSECTS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("EINTERSECTS_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EintersectsTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EINTERSECTS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ETOUCHES_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ETOUCHES_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ETOUCHES_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EtouchesTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ETOUCHES_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::EVER_EQ_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("EVER_EQ_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::EVER_NE_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("EVER_NE_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ALWAYS_EQ_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ALWAYS_EQ_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ALWAYS_NE_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ALWAYS_NE_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::NAD_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("NAD_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EDWITHIN_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::EDWITHIN_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 9)
                throw InvalidQuerySyntax("EDWITHIN_TCBUFFER_TCBUFFER requires exactly 9 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdwithinTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDWITHIN_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ADWITHIN_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ADWITHIN_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 9)
                throw InvalidQuerySyntax("ADWITHIN_TCBUFFER_TCBUFFER requires exactly 9 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdwithinTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADWITHIN_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: MINDISTANCE_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::MINDISTANCE_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 9)
                throw InvalidQuerySyntax("MINDISTANCE_TCBUFFER_TCBUFFER requires exactly 9 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                MindistanceTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINDISTANCE_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: H3INDEX_EQ */
        case AntlrSQLLexer::H3INDEX_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("H3INDEX_EQ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(H3indexEqLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: H3INDEX_EQ */

        /* BEGIN CODEGEN PARSER GLUE: H3INDEX_NE */
        case AntlrSQLLexer::H3INDEX_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("H3INDEX_NE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(H3indexNeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: H3INDEX_NE */

        /* BEGIN CODEGEN PARSER GLUE: H3INDEX_LT */
        case AntlrSQLLexer::H3INDEX_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("H3INDEX_LT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(H3indexLtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: H3INDEX_LT */

        /* BEGIN CODEGEN PARSER GLUE: H3INDEX_LE */
        case AntlrSQLLexer::H3INDEX_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("H3INDEX_LE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(H3indexLeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: H3INDEX_LE */

        /* BEGIN CODEGEN PARSER GLUE: H3INDEX_GT */
        case AntlrSQLLexer::H3INDEX_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("H3INDEX_GT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(H3indexGtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: H3INDEX_GT */

        /* BEGIN CODEGEN PARSER GLUE: H3INDEX_GE */
        case AntlrSQLLexer::H3INDEX_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("H3INDEX_GE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(H3indexGeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: H3INDEX_GE */

        /* BEGIN CODEGEN PARSER GLUE: H3INDEX_CMP */
        case AntlrSQLLexer::H3INDEX_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("H3INDEX_CMP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(H3indexCmpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: H3INDEX_CMP */

        /* BEGIN CODEGEN PARSER GLUE: H3INDEX_OUT */
        case AntlrSQLLexer::H3INDEX_OUT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("H3INDEX_OUT requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(H3indexOutLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: H3INDEX_OUT */

        /* BEGIN CODEGEN PARSER GLUE: TH3INDEX_GET_RESOLUTION */
        case AntlrSQLLexer::TH3INDEX_GET_RESOLUTION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TH3INDEX_GET_RESOLUTION requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(Th3indexGetResolutionLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TH3INDEX_GET_RESOLUTION */

        /* BEGIN CODEGEN PARSER GLUE: TH3INDEX_GET_BASE_CELL_NUMBER */
        case AntlrSQLLexer::TH3INDEX_GET_BASE_CELL_NUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TH3INDEX_GET_BASE_CELL_NUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(Th3indexGetBaseCellNumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TH3INDEX_GET_BASE_CELL_NUMBER */

        /* BEGIN CODEGEN PARSER GLUE: TH3INDEX_IS_VALID_CELL */
        case AntlrSQLLexer::TH3INDEX_IS_VALID_CELL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TH3INDEX_IS_VALID_CELL requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(Th3indexIsValidCellLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TH3INDEX_IS_VALID_CELL */

        /* BEGIN CODEGEN PARSER GLUE: TH3INDEX_IS_PENTAGON */
        case AntlrSQLLexer::TH3INDEX_IS_PENTAGON:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TH3INDEX_IS_PENTAGON requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(Th3indexIsPentagonLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TH3INDEX_IS_PENTAGON */

        /* BEGIN CODEGEN PARSER GLUE: TH3INDEX_CELL_TO_PARENT */
        case AntlrSQLLexer::TH3INDEX_CELL_TO_PARENT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TH3INDEX_CELL_TO_PARENT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(Th3indexCellToParentLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TH3INDEX_CELL_TO_PARENT */

        /* BEGIN CODEGEN PARSER GLUE: TH3INDEX_CELL_TO_PARENT_NEXT */
        case AntlrSQLLexer::TH3INDEX_CELL_TO_PARENT_NEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TH3INDEX_CELL_TO_PARENT_NEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(Th3indexCellToParentNextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TH3INDEX_CELL_TO_PARENT_NEXT */

        /* BEGIN CODEGEN PARSER GLUE: TH3INDEX_CELL_TO_CENTER_CHILD */
        case AntlrSQLLexer::TH3INDEX_CELL_TO_CENTER_CHILD:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TH3INDEX_CELL_TO_CENTER_CHILD requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(Th3indexCellToCenterChildLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TH3INDEX_CELL_TO_CENTER_CHILD */

        /* BEGIN CODEGEN PARSER GLUE: TH3INDEX_CELL_TO_CENTER_CHILD_NEXT */
        case AntlrSQLLexer::TH3INDEX_CELL_TO_CENTER_CHILD_NEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TH3INDEX_CELL_TO_CENTER_CHILD_NEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(Th3indexCellToCenterChildNextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TH3INDEX_CELL_TO_CENTER_CHILD_NEXT */

        /* BEGIN CODEGEN PARSER GLUE: TH3INDEX_CELL_TO_CHILD_POS */
        case AntlrSQLLexer::TH3INDEX_CELL_TO_CHILD_POS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TH3INDEX_CELL_TO_CHILD_POS requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(Th3indexCellToChildPosLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TH3INDEX_CELL_TO_CHILD_POS */

        /* BEGIN CODEGEN PARSER GLUE: TH3INDEX_ARE_NEIGHBOR_CELLS */
        case AntlrSQLLexer::TH3INDEX_ARE_NEIGHBOR_CELLS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TH3INDEX_ARE_NEIGHBOR_CELLS requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(Th3indexAreNeighborCellsLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TH3INDEX_ARE_NEIGHBOR_CELLS */

        /* BEGIN CODEGEN PARSER GLUE: TH3INDEX_GRID_DISTANCE */
        case AntlrSQLLexer::TH3INDEX_GRID_DISTANCE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TH3INDEX_GRID_DISTANCE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(Th3indexGridDistanceLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TH3INDEX_GRID_DISTANCE */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTH3INDEXH3INDEX */
        case AntlrSQLLexer::EVEREQTH3INDEXH3INDEX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVEREQTH3INDEXH3INDEX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTh3indexH3indexLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTH3INDEXH3INDEX */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETH3INDEXH3INDEX */
        case AntlrSQLLexer::EVERNETH3INDEXH3INDEX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERNETH3INDEXH3INDEX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTh3indexH3indexLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETH3INDEXH3INDEX */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTH3INDEXH3INDEX */
        case AntlrSQLLexer::ALWAYSEQTH3INDEXH3INDEX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSEQTH3INDEXH3INDEX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTh3indexH3indexLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTH3INDEXH3INDEX */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETH3INDEXH3INDEX */
        case AntlrSQLLexer::ALWAYSNETH3INDEXH3INDEX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSNETH3INDEXH3INDEX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTh3indexH3indexLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETH3INDEXH3INDEX */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_EQ */
        case AntlrSQLLexer::QUADBIN_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("QUADBIN_EQ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinEqLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_EQ */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_NE */
        case AntlrSQLLexer::QUADBIN_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("QUADBIN_NE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinNeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_NE */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_LT */
        case AntlrSQLLexer::QUADBIN_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("QUADBIN_LT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinLtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_LT */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_LE */
        case AntlrSQLLexer::QUADBIN_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("QUADBIN_LE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinLeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_LE */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_GT */
        case AntlrSQLLexer::QUADBIN_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("QUADBIN_GT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinGtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_GT */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_GE */
        case AntlrSQLLexer::QUADBIN_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("QUADBIN_GE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinGeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_GE */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_CMP */
        case AntlrSQLLexer::QUADBIN_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("QUADBIN_CMP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinCmpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_CMP */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_GET_RESOLUTION */
        case AntlrSQLLexer::QUADBIN_GET_RESOLUTION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("QUADBIN_GET_RESOLUTION requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinGetResolutionLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_GET_RESOLUTION */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_IS_VALID_CELL */
        case AntlrSQLLexer::QUADBIN_IS_VALID_CELL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("QUADBIN_IS_VALID_CELL requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinIsValidCellLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_IS_VALID_CELL */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_CELL_AREA */
        case AntlrSQLLexer::QUADBIN_CELL_AREA:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("QUADBIN_CELL_AREA requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinCellAreaLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_CELL_AREA */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_CELL_TO_PARENT */
        case AntlrSQLLexer::QUADBIN_CELL_TO_PARENT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("QUADBIN_CELL_TO_PARENT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinCellToParentLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_CELL_TO_PARENT */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_CELL_TO_QUADKEY */
        case AntlrSQLLexer::QUADBIN_CELL_TO_QUADKEY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("QUADBIN_CELL_TO_QUADKEY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinCellToQuadkeyLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_CELL_TO_QUADKEY */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_POINT_TO_CELL */
        case AntlrSQLLexer::QUADBIN_POINT_TO_CELL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("QUADBIN_POINT_TO_CELL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinPointToCellLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_POINT_TO_CELL */

        /* BEGIN CODEGEN PARSER GLUE: QUADBIN_TILE_TO_CELL */
        case AntlrSQLLexer::QUADBIN_TILE_TO_CELL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("QUADBIN_TILE_TO_CELL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(QuadbinTileToCellLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: QUADBIN_TILE_TO_CELL */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTQUADBINQUADBIN */
        case AntlrSQLLexer::EVEREQTQUADBINQUADBIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVEREQTQUADBINQUADBIN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTquadbinQuadbinLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTQUADBINQUADBIN */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETQUADBINQUADBIN */
        case AntlrSQLLexer::EVERNETQUADBINQUADBIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERNETQUADBINQUADBIN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTquadbinQuadbinLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETQUADBINQUADBIN */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTQUADBINQUADBIN */
        case AntlrSQLLexer::ALWAYSEQTQUADBINQUADBIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSEQTQUADBINQUADBIN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTquadbinQuadbinLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTQUADBINQUADBIN */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETQUADBINQUADBIN */
        case AntlrSQLLexer::ALWAYSNETQUADBINQUADBIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSNETQUADBINQUADBIN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTquadbinQuadbinLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETQUADBINQUADBIN */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTTEXTTEXT */
        case AntlrSQLLexer::EVEREQTTEXTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVEREQTTEXTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTTEXTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTEXTTTEXT */
        case AntlrSQLLexer::EVEREQTEXTTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVEREQTEXTTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTEXTTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETTEXTTEXT */
        case AntlrSQLLexer::EVERNETTEXTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERNETTEXTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETTEXTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETEXTTTEXT */
        case AntlrSQLLexer::EVERNETEXTTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERNETEXTTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETEXTTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVERGETTEXTTEXT */
        case AntlrSQLLexer::EVERGETTEXTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERGETTEXTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverGeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGETTEXTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVERGETEXTTTEXT */
        case AntlrSQLLexer::EVERGETEXTTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERGETEXTTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverGeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGETEXTTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVERGTTTEXTTEXT */
        case AntlrSQLLexer::EVERGTTTEXTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERGTTTEXTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverGtTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGTTTEXTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVERGTTEXTTTEXT */
        case AntlrSQLLexer::EVERGTTEXTTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERGTTEXTTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverGtTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGTTEXTTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVERLETTEXTTEXT */
        case AntlrSQLLexer::EVERLETTEXTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERLETTEXTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverLeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLETTEXTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVERLETEXTTTEXT */
        case AntlrSQLLexer::EVERLETEXTTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERLETEXTTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverLeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLETEXTTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVERLTTTEXTTEXT */
        case AntlrSQLLexer::EVERLTTTEXTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERLTTTEXTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverLtTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLTTTEXTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVERLTTEXTTTEXT */
        case AntlrSQLLexer::EVERLTTEXTTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERLTTEXTTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverLtTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLTTEXTTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTTEXTTEXT */
        case AntlrSQLLexer::ALWAYSEQTTEXTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSEQTTEXTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTTEXTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTEXTTTEXT */
        case AntlrSQLLexer::ALWAYSEQTEXTTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSEQTEXTTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTEXTTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETTEXTTEXT */
        case AntlrSQLLexer::ALWAYSNETTEXTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSNETTEXTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETTEXTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETEXTTTEXT */
        case AntlrSQLLexer::ALWAYSNETEXTTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSNETEXTTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETEXTTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGETTEXTTEXT */
        case AntlrSQLLexer::ALWAYSGETTEXTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSGETTEXTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysGeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGETTEXTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGETEXTTTEXT */
        case AntlrSQLLexer::ALWAYSGETEXTTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSGETEXTTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysGeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGETEXTTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGTTTEXTTEXT */
        case AntlrSQLLexer::ALWAYSGTTTEXTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSGTTTEXTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysGtTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGTTTEXTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGTTEXTTTEXT */
        case AntlrSQLLexer::ALWAYSGTTEXTTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSGTTEXTTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysGtTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGTTEXTTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLETTEXTTEXT */
        case AntlrSQLLexer::ALWAYSLETTEXTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSLETTEXTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysLeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLETTEXTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLETEXTTTEXT */
        case AntlrSQLLexer::ALWAYSLETEXTTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSLETEXTTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysLeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLETEXTTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLTTTEXTTEXT */
        case AntlrSQLLexer::ALWAYSLTTTEXTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSLTTTEXTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysLtTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLTTTEXTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLTTEXTTTEXT */
        case AntlrSQLLexer::ALWAYSLTTEXTTTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSLTTEXTTTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysLtTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLTTEXTTTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TTEXT_TEXT */
        case AntlrSQLLexer::TEQ_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEQ_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TEqTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TEXT_TTEXT */
        case AntlrSQLLexer::TEQ_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEQ_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TEqTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TTEXT_TEXT */
        case AntlrSQLLexer::TNE_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNE_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TNeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TEXT_TTEXT */
        case AntlrSQLLexer::TNE_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNE_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TNeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_UPPER */
        case AntlrSQLLexer::TTEXT_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TTEXT_UPPER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextUpperLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_LOWER */
        case AntlrSQLLexer::TTEXT_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TTEXT_LOWER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextLowerLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_INITCAP */
        case AntlrSQLLexer::TTEXT_INITCAP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TTEXT_INITCAP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextInitcapLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_INITCAP */

        /* BEGIN CODEGEN PARSER GLUE: TEXT_UPPER */
        case AntlrSQLLexer::TEXT_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TEXT_UPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXT_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: TEXT_LOWER */
        case AntlrSQLLexer::TEXT_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TEXT_LOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXT_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: TEXT_INITCAP */
        case AntlrSQLLexer::TEXT_INITCAP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TEXT_INITCAP requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextInitcapLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXT_INITCAP */

        /* BEGIN CODEGEN PARSER GLUE: TEXTCAT_TTEXT_TEXT */
        case AntlrSQLLexer::TEXTCAT_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEXTCAT_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextcatTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTCAT_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TEXTCAT_TEXT_TTEXT */
        case AntlrSQLLexer::TEXTCAT_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEXTCAT_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextcatTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTCAT_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TEXTCAT_TTEXT_TTEXT */
        case AntlrSQLLexer::TEXTCAT_TTEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEXTCAT_TTEXT_TTEXT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextcatTtextTtextLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTCAT_TTEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TGE_TTEXT_TEXT */
        case AntlrSQLLexer::TGE_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGE_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TGeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGE_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TGE_TEXT_TTEXT */
        case AntlrSQLLexer::TGE_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGE_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TGeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGE_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TGT_TTEXT_TEXT */
        case AntlrSQLLexer::TGT_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGT_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TGtTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGT_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TGT_TEXT_TTEXT */
        case AntlrSQLLexer::TGT_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGT_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TGtTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGT_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TLE_TTEXT_TEXT */
        case AntlrSQLLexer::TLE_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLE_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TLeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLE_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TLE_TEXT_TTEXT */
        case AntlrSQLLexer::TLE_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLE_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TLeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLE_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TLT_TTEXT_TEXT */
        case AntlrSQLLexer::TLT_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLT_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TLtTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLT_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TLT_TEXT_TTEXT */
        case AntlrSQLLexer::TLT_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLT_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TLtTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLT_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_EQ */
        case AntlrSQLLexer::JSONB_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("JSONB_EQ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbEqLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_EQ */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_NE */
        case AntlrSQLLexer::JSONB_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("JSONB_NE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbNeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_NE */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_LT */
        case AntlrSQLLexer::JSONB_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("JSONB_LT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbLtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_LT */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_LE */
        case AntlrSQLLexer::JSONB_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("JSONB_LE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbLeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_LE */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_GT */
        case AntlrSQLLexer::JSONB_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("JSONB_GT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbGtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_GT */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_GE */
        case AntlrSQLLexer::JSONB_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("JSONB_GE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbGeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_GE */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_CMP */
        case AntlrSQLLexer::JSONB_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("JSONB_CMP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbCmpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_CMP */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_CONTAINED */
        case AntlrSQLLexer::JSONB_CONTAINED:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("JSONB_CONTAINED requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbContainedLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_CONTAINED */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_CONTAINS */
        case AntlrSQLLexer::JSONB_CONTAINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("JSONB_CONTAINS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbContainsLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_CONTAINS */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_EXISTS */
        case AntlrSQLLexer::JSONB_EXISTS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("JSONB_EXISTS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbExistsLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_EXISTS */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_ARRAY_LENGTH */
        case AntlrSQLLexer::JSONB_ARRAY_LENGTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("JSONB_ARRAY_LENGTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbArrayLengthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_ARRAY_LENGTH */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_TO_CSTRING */
        case AntlrSQLLexer::JSONB_TO_CSTRING:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("JSONB_TO_CSTRING requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbToCstringLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_TO_CSTRING */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_PRETTY */
        case AntlrSQLLexer::JSONB_PRETTY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("JSONB_PRETTY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbPrettyLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_PRETTY */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_OBJECT_FIELD_TEXT */
        case AntlrSQLLexer::JSONB_OBJECT_FIELD_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("JSONB_OBJECT_FIELD_TEXT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbObjectFieldTextLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_OBJECT_FIELD_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: JSONB_ARRAY_ELEMENT_TEXT */
        case AntlrSQLLexer::JSONB_ARRAY_ELEMENT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("JSONB_ARRAY_ELEMENT_TEXT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(JsonbArrayElementTextLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: JSONB_ARRAY_ELEMENT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTJSONBJSONB */
        case AntlrSQLLexer::EVEREQTJSONBJSONB:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVEREQTJSONBJSONB requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTjsonbJsonbLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTJSONBJSONB */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETJSONBJSONB */
        case AntlrSQLLexer::EVERNETJSONBJSONB:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERNETJSONBJSONB requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTjsonbJsonbLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETJSONBJSONB */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTJSONBJSONB */
        case AntlrSQLLexer::ALWAYSEQTJSONBJSONB:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSEQTJSONBJSONB requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTjsonbJsonbLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTJSONBJSONB */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETJSONBJSONB */
        case AntlrSQLLexer::ALWAYSNETJSONBJSONB:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSNETJSONBJSONB requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTjsonbJsonbLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETJSONBJSONB */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTJSONBTJSONB */
        case AntlrSQLLexer::EVEREQTJSONBTJSONB:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVEREQTJSONBTJSONB requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTjsonbTjsonbLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTJSONBTJSONB */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETJSONBTJSONB */
        case AntlrSQLLexer::EVERNETJSONBTJSONB:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVERNETJSONBTJSONB requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTjsonbTjsonbLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETJSONBTJSONB */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTJSONBTJSONB */
        case AntlrSQLLexer::ALWAYSEQTJSONBTJSONB:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYSEQTJSONBTJSONB requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTjsonbTjsonbLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTJSONBTJSONB */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETJSONBTJSONB */
        case AntlrSQLLexer::ALWAYSNETJSONBTJSONB:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYSNETJSONBTJSONB requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTjsonbTjsonbLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETJSONBTJSONB */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TNPOINT_GEO */
        case AntlrSQLLexer::NAD_TNPOINT_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAD_TNPOINT_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTnpointGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TNPOINT_GEO */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TNPOINT_NPOINT */
        case AntlrSQLLexer::NAD_TNPOINT_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAD_TNPOINT_NPOINT requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTnpointNpointLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TNPOINT_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TNPOINT_TNPOINT */
        case AntlrSQLLexer::NAD_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("NAD_TNPOINT_TNPOINT requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTnpointTnpointLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQNPOINTTNPOINT */
        case AntlrSQLLexer::EVEREQNPOINTTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVEREQNPOINTTNPOINT requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqNpointTnpointLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQNPOINTTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTNPOINTNPOINT */
        case AntlrSQLLexer::EVEREQTNPOINTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVEREQTNPOINTNPOINT requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTnpointNpointLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTNPOINTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTNPOINTTNPOINT */
        case AntlrSQLLexer::EVEREQTNPOINTTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EVEREQTNPOINTTNPOINT requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTnpointTnpointLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTNPOINTTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERNENPOINTTNPOINT */
        case AntlrSQLLexer::EVERNENPOINTTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVERNENPOINTTNPOINT requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeNpointTnpointLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNENPOINTTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETNPOINTNPOINT */
        case AntlrSQLLexer::EVERNETNPOINTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVERNETNPOINTNPOINT requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTnpointNpointLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETNPOINTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETNPOINTTNPOINT */
        case AntlrSQLLexer::EVERNETNPOINTTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EVERNETNPOINTTNPOINT requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTnpointTnpointLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETNPOINTTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQNPOINTTNPOINT */
        case AntlrSQLLexer::ALWAYSEQNPOINTTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYSEQNPOINTTNPOINT requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqNpointTnpointLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQNPOINTTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTNPOINTNPOINT */
        case AntlrSQLLexer::ALWAYSEQTNPOINTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYSEQTNPOINTNPOINT requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTnpointNpointLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTNPOINTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTNPOINTTNPOINT */
        case AntlrSQLLexer::ALWAYSEQTNPOINTTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ALWAYSEQTNPOINTTNPOINT requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTnpointTnpointLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTNPOINTTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNENPOINTTNPOINT */
        case AntlrSQLLexer::ALWAYSNENPOINTTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYSNENPOINTTNPOINT requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeNpointTnpointLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNENPOINTTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETNPOINTNPOINT */
        case AntlrSQLLexer::ALWAYSNETNPOINTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYSNETNPOINTNPOINT requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTnpointNpointLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETNPOINTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETNPOINTTNPOINT */
        case AntlrSQLLexer::ALWAYSNETNPOINTTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ALWAYSNETNPOINTTNPOINT requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTnpointTnpointLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETNPOINTTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TPOSE_POSE */
        case AntlrSQLLexer::NAD_TPOSE_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("NAD_TPOSE_POSE requires exactly 7 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTposePoseLogicalFunction(a0, a1, a2, a3, a4, a5, a6));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TPOSE_POSE */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TPOSE_TPOSE */
        case AntlrSQLLexer::NAD_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("NAD_TPOSE_TPOSE requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTposeTposeLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TPOSE_GEO */
        case AntlrSQLLexer::NAD_TPOSE_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAD_TPOSE_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTposeGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TPOSE_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQPOSETPOSE */
        case AntlrSQLLexer::EVEREQPOSETPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("EVEREQPOSETPOSE requires exactly 7 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqPoseTposeLogicalFunction(a0, a1, a2, a3, a4, a5, a6));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQPOSETPOSE */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTPOSEPOSE */
        case AntlrSQLLexer::EVEREQTPOSEPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("EVEREQTPOSEPOSE requires exactly 7 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTposePoseLogicalFunction(a0, a1, a2, a3, a4, a5, a6));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTPOSEPOSE */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTPOSETPOSE */
        case AntlrSQLLexer::EVEREQTPOSETPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("EVEREQTPOSETPOSE requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTposeTposeLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTPOSETPOSE */

        /* BEGIN CODEGEN PARSER GLUE: EVERNEPOSETPOSE */
        case AntlrSQLLexer::EVERNEPOSETPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("EVERNEPOSETPOSE requires exactly 7 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNePoseTposeLogicalFunction(a0, a1, a2, a3, a4, a5, a6));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNEPOSETPOSE */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETPOSEPOSE */
        case AntlrSQLLexer::EVERNETPOSEPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("EVERNETPOSEPOSE requires exactly 7 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTposePoseLogicalFunction(a0, a1, a2, a3, a4, a5, a6));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETPOSEPOSE */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETPOSETPOSE */
        case AntlrSQLLexer::EVERNETPOSETPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("EVERNETPOSETPOSE requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTposeTposeLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETPOSETPOSE */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQPOSETPOSE */
        case AntlrSQLLexer::ALWAYSEQPOSETPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("ALWAYSEQPOSETPOSE requires exactly 7 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqPoseTposeLogicalFunction(a0, a1, a2, a3, a4, a5, a6));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQPOSETPOSE */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTPOSEPOSE */
        case AntlrSQLLexer::ALWAYSEQTPOSEPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("ALWAYSEQTPOSEPOSE requires exactly 7 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTposePoseLogicalFunction(a0, a1, a2, a3, a4, a5, a6));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTPOSEPOSE */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTPOSETPOSE */
        case AntlrSQLLexer::ALWAYSEQTPOSETPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ALWAYSEQTPOSETPOSE requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTposeTposeLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTPOSETPOSE */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNEPOSETPOSE */
        case AntlrSQLLexer::ALWAYSNEPOSETPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("ALWAYSNEPOSETPOSE requires exactly 7 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNePoseTposeLogicalFunction(a0, a1, a2, a3, a4, a5, a6));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNEPOSETPOSE */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETPOSEPOSE */
        case AntlrSQLLexer::ALWAYSNETPOSEPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("ALWAYSNETPOSEPOSE requires exactly 7 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTposePoseLogicalFunction(a0, a1, a2, a3, a4, a5, a6));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETPOSEPOSE */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETPOSETPOSE */
        case AntlrSQLLexer::ALWAYSNETPOSETPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ALWAYSNETPOSETPOSE requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTposeTposeLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETPOSETPOSE */

        /* BEGIN CODEGEN PARSER GLUE: GEOMBOUNDARY */
        case AntlrSQLLexer::GEOMBOUNDARY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOMBOUNDARY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomBoundaryLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMBOUNDARY */

        /* BEGIN CODEGEN PARSER GLUE: GEOMCENTROID */
        case AntlrSQLLexer::GEOMCENTROID:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOMCENTROID requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomCentroidLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMCENTROID */

        /* BEGIN CODEGEN PARSER GLUE: GEOMCONVEXHULL */
        case AntlrSQLLexer::GEOMCONVEXHULL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOMCONVEXHULL requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomConvexHullLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMCONVEXHULL */

        /* BEGIN CODEGEN PARSER GLUE: GEOREVERSE */
        case AntlrSQLLexer::GEOREVERSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOREVERSE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoReverseLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOREVERSE */

        /* BEGIN CODEGEN PARSER GLUE: GEOPOINTS */
        case AntlrSQLLexer::GEOPOINTS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOPOINTS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoPointsLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOPOINTS */

        /* BEGIN CODEGEN PARSER GLUE: GEOMUNARYUNION */
        case AntlrSQLLexer::GEOMUNARYUNION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOMUNARYUNION requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomUnaryUnionLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMUNARYUNION */

        /* BEGIN CODEGEN PARSER GLUE: GEO_SET_SRID */
        case AntlrSQLLexer::GEO_SET_SRID:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEO_SET_SRID requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoSetSridLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_SET_SRID */

        /* BEGIN CODEGEN PARSER GLUE: GEO_ROUND */
        case AntlrSQLLexer::GEO_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEO_ROUND requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoRoundLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_ROUND */

        /* BEGIN CODEGEN PARSER GLUE: GEO_TRANSFORM */
        case AntlrSQLLexer::GEO_TRANSFORM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEO_TRANSFORM requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoTransformLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_TRANSFORM */

        /* BEGIN CODEGEN PARSER GLUE: GEOSRID */
        case AntlrSQLLexer::GEOSRID:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOSRID requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoSridLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOSRID */

        /* BEGIN CODEGEN PARSER GLUE: GEONUMGEOS */
        case AntlrSQLLexer::GEONUMGEOS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEONUMGEOS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoNumGeosLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEONUMGEOS */

        /* BEGIN CODEGEN PARSER GLUE: GEONUMPOINTS */
        case AntlrSQLLexer::GEONUMPOINTS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEONUMPOINTS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoNumPointsLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEONUMPOINTS */

        /* BEGIN CODEGEN PARSER GLUE: GEOMLENGTH */
        case AntlrSQLLexer::GEOMLENGTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOMLENGTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomLengthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMLENGTH */

        /* BEGIN CODEGEN PARSER GLUE: GEOMPERIMETER */
        case AntlrSQLLexer::GEOMPERIMETER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOMPERIMETER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomPerimeterLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMPERIMETER */

        /* BEGIN CODEGEN PARSER GLUE: GEOMISEMPTY */
        case AntlrSQLLexer::GEOMISEMPTY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOMISEMPTY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomIsEmptyLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMISEMPTY */

        /* BEGIN CODEGEN PARSER GLUE: GEOMAZIMUTH */
        case AntlrSQLLexer::GEOMAZIMUTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOMAZIMUTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomAzimuthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMAZIMUTH */

        /* BEGIN CODEGEN PARSER GLUE: GEOISUNITARY */
        case AntlrSQLLexer::GEOISUNITARY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOISUNITARY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoIsUnitaryLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOISUNITARY */

        /* BEGIN CODEGEN PARSER GLUE: GEOEQUALS */
        case AntlrSQLLexer::GEOEQUALS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOEQUALS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoEqualsLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOEQUALS */

        /* BEGIN CODEGEN PARSER GLUE: GEOSAME */
        case AntlrSQLLexer::GEOSAME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOSAME requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoSameLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOSAME */

        /* BEGIN CODEGEN PARSER GLUE: GEOMINTERSECTS */
        case AntlrSQLLexer::GEOMINTERSECTS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMINTERSECTS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomIntersectsLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMINTERSECTS */

        /* BEGIN CODEGEN PARSER GLUE: GEOMINTERSECTS2D */
        case AntlrSQLLexer::GEOMINTERSECTS2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMINTERSECTS2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomIntersects2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMINTERSECTS2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOMINTERSECTS3D */
        case AntlrSQLLexer::GEOMINTERSECTS3D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMINTERSECTS3D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomIntersects3dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMINTERSECTS3D */

        /* BEGIN CODEGEN PARSER GLUE: GEOMCONTAINS */
        case AntlrSQLLexer::GEOMCONTAINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMCONTAINS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomContainsLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMCONTAINS */

        /* BEGIN CODEGEN PARSER GLUE: GEOMCOVERS */
        case AntlrSQLLexer::GEOMCOVERS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMCOVERS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomCoversLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMCOVERS */

        /* BEGIN CODEGEN PARSER GLUE: GEOMDISJOINT2D */
        case AntlrSQLLexer::GEOMDISJOINT2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMDISJOINT2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomDisjoint2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMDISJOINT2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOMTOUCHES */
        case AntlrSQLLexer::GEOMTOUCHES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMTOUCHES requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomTouchesLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMTOUCHES */

        /* BEGIN CODEGEN PARSER GLUE: GEOGINTERSECTS */
        case AntlrSQLLexer::GEOGINTERSECTS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOGINTERSECTS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeogIntersectsLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOGINTERSECTS */

        /* BEGIN CODEGEN PARSER GLUE: GEOGDISTANCE */
        case AntlrSQLLexer::GEOGDISTANCE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOGDISTANCE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeogDistanceLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOGDISTANCE */

        /* BEGIN CODEGEN PARSER GLUE: LINELOCATEPOINT */
        case AntlrSQLLexer::LINELOCATEPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LINELOCATEPOINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LineLocatePointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LINELOCATEPOINT */

        /* BEGIN CODEGEN PARSER GLUE: GEOMDISTANCE2D */
        case AntlrSQLLexer::GEOMDISTANCE2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMDISTANCE2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomDistance2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMDISTANCE2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOMDISTANCE3D */
        case AntlrSQLLexer::GEOMDISTANCE3D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMDISTANCE3D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomDistance3dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMDISTANCE3D */

        /* BEGIN CODEGEN PARSER GLUE: GEOMDWITHIN2D */
        case AntlrSQLLexer::GEOMDWITHIN2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("GEOMDWITHIN2D requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomDwithin2dLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMDWITHIN2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOMDWITHIN3D */
        case AntlrSQLLexer::GEOMDWITHIN3D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("GEOMDWITHIN3D requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomDwithin3dLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMDWITHIN3D */

        /* BEGIN CODEGEN PARSER GLUE: GEOGDWITHIN */
        case AntlrSQLLexer::GEOGDWITHIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("GEOGDWITHIN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeogDwithinLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOGDWITHIN */

        /* BEGIN CODEGEN PARSER GLUE: GEOMDWITHIN */
        case AntlrSQLLexer::GEOMDWITHIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("GEOMDWITHIN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomDwithinLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMDWITHIN */

        /* BEGIN CODEGEN PARSER GLUE: GEOMINTERSECTION2D */
        case AntlrSQLLexer::GEOMINTERSECTION2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMINTERSECTION2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomIntersection2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMINTERSECTION2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOMINTERSECTION2DCOLL */
        case AntlrSQLLexer::GEOMINTERSECTION2DCOLL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMINTERSECTION2DCOLL requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomIntersection2dCollLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMINTERSECTION2DCOLL */

        /* BEGIN CODEGEN PARSER GLUE: GEOMDIFFERENCE2D */
        case AntlrSQLLexer::GEOMDIFFERENCE2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMDIFFERENCE2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomDifference2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMDIFFERENCE2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOMSHORTESTLINE2D */
        case AntlrSQLLexer::GEOMSHORTESTLINE2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMSHORTESTLINE2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomShortestline2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMSHORTESTLINE2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOMSHORTESTLINE3D */
        case AntlrSQLLexer::GEOMSHORTESTLINE3D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOMSHORTESTLINE3D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomShortestline3dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMSHORTESTLINE3D */

        /* BEGIN CODEGEN PARSER GLUE: LINE_INTERPOLATE_POINT */
        case AntlrSQLLexer::LINE_INTERPOLATE_POINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LINE_INTERPOLATE_POINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LineInterpolatePointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LINE_INTERPOLATE_POINT */

        /* BEGIN CODEGEN PARSER GLUE: LINE_SUBSTRING */
        case AntlrSQLLexer::LINE_SUBSTRING:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("LINE_SUBSTRING requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LineSubstringLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: LINE_SUBSTRING */

        /* BEGIN CODEGEN PARSER GLUE: LINE_NUMPOINTS */
        case AntlrSQLLexer::LINE_NUMPOINTS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("LINE_NUMPOINTS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LineNumpointsLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: LINE_NUMPOINTS */

        /* BEGIN CODEGEN PARSER GLUE: LINE_POINT_N */
        case AntlrSQLLexer::LINE_POINT_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LINE_POINT_N requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LinePointNLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LINE_POINT_N */

        /* BEGIN CODEGEN PARSER GLUE: GEO_GEO_N */
        case AntlrSQLLexer::GEO_GEO_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEO_GEO_N requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoGeoNLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_GEO_N */

        /* BEGIN CODEGEN PARSER GLUE: GEO_AS_EWKT */
        case AntlrSQLLexer::GEO_AS_EWKT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEO_AS_EWKT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoAsEwktLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_AS_EWKT */

        /* BEGIN CODEGEN PARSER GLUE: GEO_AS_GEOJSON */
        case AntlrSQLLexer::GEO_AS_GEOJSON:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("GEO_AS_GEOJSON requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoAsGeojsonLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_AS_GEOJSON */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_POINT_MAKE2D */
        case AntlrSQLLexer::GEOM_POINT_MAKE2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("GEOM_POINT_MAKE2D requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomPointMake2dLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_POINT_MAKE2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_POINT_MAKE3DZ */
        case AntlrSQLLexer::GEOM_POINT_MAKE3DZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("GEOM_POINT_MAKE3DZ requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomPointMake3dzLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_POINT_MAKE3DZ */

        /* BEGIN CODEGEN PARSER GLUE: GEOG_POINT_MAKE2D */
        case AntlrSQLLexer::GEOG_POINT_MAKE2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("GEOG_POINT_MAKE2D requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeogPointMake2dLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOG_POINT_MAKE2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOG_POINT_MAKE3DZ */
        case AntlrSQLLexer::GEOG_POINT_MAKE3DZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("GEOG_POINT_MAKE3DZ requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeogPointMake3dzLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOG_POINT_MAKE3DZ */

        /* BEGIN CODEGEN PARSER GLUE: GEOGAREA */
        case AntlrSQLLexer::GEOGAREA:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOGAREA requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeogAreaLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOGAREA */

        /* BEGIN CODEGEN PARSER GLUE: GEOGLENGTH */
        case AntlrSQLLexer::GEOGLENGTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOGLENGTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeogLengthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOGLENGTH */

        /* BEGIN CODEGEN PARSER GLUE: GEOGPERIMETER */
        case AntlrSQLLexer::GEOGPERIMETER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOGPERIMETER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeogPerimeterLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOGPERIMETER */

        /* BEGIN CODEGEN PARSER GLUE: GEOGTOGEOM */
        case AntlrSQLLexer::GEOGTOGEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOGTOGEOM requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeogToGeomLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOGTOGEOM */

        /* BEGIN CODEGEN PARSER GLUE: GEOMTOGEOG */
        case AntlrSQLLexer::GEOMTOGEOG:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOMTOGEOG requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomToGeogLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOMTOGEOG */

        /* BEGIN CODEGEN PARSER GLUE: GEOG_CENTROID */
        case AntlrSQLLexer::GEOG_CENTROID:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOG_CENTROID requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeogCentroidLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOG_CENTROID */

        /* BEGIN CODEGEN PARSER GLUE: INTSPANLOWER */
        case AntlrSQLLexer::INTSPANLOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPANLOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspanLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPANLOWER */

        /* BEGIN CODEGEN PARSER GLUE: INTSPANUPPER */
        case AntlrSQLLexer::INTSPANUPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPANUPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspanUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPANUPPER */

        /* BEGIN CODEGEN PARSER GLUE: INTSPANWIDTH */
        case AntlrSQLLexer::INTSPANWIDTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPANWIDTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspanWidthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPANWIDTH */

        /* BEGIN CODEGEN PARSER GLUE: INTSPANLOWERINC */
        case AntlrSQLLexer::INTSPANLOWERINC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPANLOWERINC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspanLowerIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPANLOWERINC */

        /* BEGIN CODEGEN PARSER GLUE: INTSPANUPPERINC */
        case AntlrSQLLexer::INTSPANUPPERINC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPANUPPERINC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspanUpperIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPANUPPERINC */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPANLOWER */
        case AntlrSQLLexer::FLOATSPANLOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPANLOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPANLOWER */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPANUPPER */
        case AntlrSQLLexer::FLOATSPANUPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPANUPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPANUPPER */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPANWIDTH */
        case AntlrSQLLexer::FLOATSPANWIDTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPANWIDTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanWidthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPANWIDTH */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPANLOWERINC */
        case AntlrSQLLexer::FLOATSPANLOWERINC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPANLOWERINC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanLowerIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPANLOWERINC */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPANUPPERINC */
        case AntlrSQLLexer::FLOATSPANUPPERINC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPANUPPERINC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanUpperIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPANUPPERINC */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_INT_SPAN */
        case AntlrSQLLexer::CONTAINED_INT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_INT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedIntSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_INT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_FLOAT_SPAN */
        case AntlrSQLLexer::CONTAINED_FLOAT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_FLOAT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedFloatSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_FLOAT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_SPAN_SPAN */
        case AntlrSQLLexer::CONTAINED_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_SPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_FLOATSPAN_SPAN */
        case AntlrSQLLexer::CONTAINED_FLOATSPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_FLOATSPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedFloatspanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_FLOATSPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPAN_INT */
        case AntlrSQLLexer::CONTAINS_SPAN_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPAN_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpanIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPAN_INT */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPAN_FLOAT */
        case AntlrSQLLexer::CONTAINS_SPAN_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPAN_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpanFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPAN_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPAN_SPAN */
        case AntlrSQLLexer::CONTAINS_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_FLOATSPAN_SPAN */
        case AntlrSQLLexer::CONTAINS_FLOATSPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_FLOATSPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsFloatspanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_FLOATSPAN_SPAN */

        case AntlrSQLParser::ACONTAINS_GEO_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "AcontainsGeoTrgeometry requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(AcontainsGeoTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ACONTAINS_GEO_TRGEOMETRY */
        case AntlrSQLParser::ACOVERS_GEO_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "AcoversGeoTrgeometry requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(AcoversGeoTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ACOVERS_GEO_TRGEOMETRY */
        case AntlrSQLParser::ACOVERS_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "AcoversTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(AcoversTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ACOVERS_TRGEOMETRY_GEO */
        case AntlrSQLParser::ADISJOINT_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "AdisjointTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(AdisjointTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ADISJOINT_TRGEOMETRY_GEO */
        case AntlrSQLParser::ADISJOINT_TRGEOMETRY_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 10,
                         "AdisjointTrgeometryTrgeometry requires 10 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            auto arg7 = visit(ctx->functionParam(7)).as<LogicalFunction>();
            auto arg8 = visit(ctx->functionParam(8)).as<LogicalFunction>();
            auto arg9 = visit(ctx->functionParam(9)).as<LogicalFunction>();
            return LogicalFunction(AdisjointTrgeometryTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6), std::move(arg7), std::move(arg8), std::move(arg9)));
        }
        /* END CODEGEN PARSER GLUE: ADISJOINT_TRGEOMETRY_TRGEOMETRY */
        case AntlrSQLParser::ADWITHIN_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 7,
                         "AdwithinTrgeometryGeo requires 7 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            return LogicalFunction(AdwithinTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6)));
        }
        /* END CODEGEN PARSER GLUE: ADWITHIN_TRGEOMETRY_GEO */
        case AntlrSQLParser::ADWITHIN_TRGEOMETRY_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 11,
                         "AdwithinTrgeometryTrgeometry requires 11 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            auto arg7 = visit(ctx->functionParam(7)).as<LogicalFunction>();
            auto arg8 = visit(ctx->functionParam(8)).as<LogicalFunction>();
            auto arg9 = visit(ctx->functionParam(9)).as<LogicalFunction>();
            auto arg10 = visit(ctx->functionParam(10)).as<LogicalFunction>();
            return LogicalFunction(AdwithinTrgeometryTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6), std::move(arg7), std::move(arg8), std::move(arg9), std::move(arg10)));
        }
        /* END CODEGEN PARSER GLUE: ADWITHIN_TRGEOMETRY_TRGEOMETRY */
        case AntlrSQLParser::AINTERSECTS_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "AintersectsTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(AintersectsTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: AINTERSECTS_TRGEOMETRY_GEO */
        case AntlrSQLParser::AINTERSECTS_TRGEOMETRY_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 10,
                         "AintersectsTrgeometryTrgeometry requires 10 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            auto arg7 = visit(ctx->functionParam(7)).as<LogicalFunction>();
            auto arg8 = visit(ctx->functionParam(8)).as<LogicalFunction>();
            auto arg9 = visit(ctx->functionParam(9)).as<LogicalFunction>();
            return LogicalFunction(AintersectsTrgeometryTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6), std::move(arg7), std::move(arg8), std::move(arg9)));
        }
        /* END CODEGEN PARSER GLUE: AINTERSECTS_TRGEOMETRY_TRGEOMETRY */
        case AntlrSQLParser::ALWAYS_EQ_GEO_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "AlwaysEqGeoTrgeometry requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(AlwaysEqGeoTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_GEO_TRGEOMETRY */
        case AntlrSQLParser::ALWAYS_EQ_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "AlwaysEqTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(AlwaysEqTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TRGEOMETRY_GEO */
        case AntlrSQLParser::ALWAYS_EQ_TRGEOMETRY_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 10,
                         "AlwaysEqTrgeometryTrgeometry requires 10 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            auto arg7 = visit(ctx->functionParam(7)).as<LogicalFunction>();
            auto arg8 = visit(ctx->functionParam(8)).as<LogicalFunction>();
            auto arg9 = visit(ctx->functionParam(9)).as<LogicalFunction>();
            return LogicalFunction(AlwaysEqTrgeometryTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6), std::move(arg7), std::move(arg8), std::move(arg9)));
        }
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TRGEOMETRY_TRGEOMETRY */
        case AntlrSQLParser::ALWAYS_NE_GEO_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "AlwaysNeGeoTrgeometry requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(AlwaysNeGeoTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_GEO_TRGEOMETRY */
        case AntlrSQLParser::ALWAYS_NE_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "AlwaysNeTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(AlwaysNeTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TRGEOMETRY_GEO */
        case AntlrSQLParser::ALWAYS_NE_TRGEOMETRY_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 10,
                         "AlwaysNeTrgeometryTrgeometry requires 10 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            auto arg7 = visit(ctx->functionParam(7)).as<LogicalFunction>();
            auto arg8 = visit(ctx->functionParam(8)).as<LogicalFunction>();
            auto arg9 = visit(ctx->functionParam(9)).as<LogicalFunction>();
            return LogicalFunction(AlwaysNeTrgeometryTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6), std::move(arg7), std::move(arg8), std::move(arg9)));
        }
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TRGEOMETRY_TRGEOMETRY */
        case AntlrSQLParser::ATOUCHES_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "AtouchesTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(AtouchesTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ATOUCHES_TRGEOMETRY_GEO */
        case AntlrSQLParser::ECONTAINS_GEO_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "EcontainsGeoTrgeometry requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(EcontainsGeoTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ECONTAINS_GEO_TRGEOMETRY */
        case AntlrSQLParser::ECOVERS_GEO_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "EcoversGeoTrgeometry requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(EcoversGeoTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ECOVERS_GEO_TRGEOMETRY */
        case AntlrSQLParser::ECOVERS_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "EcoversTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(EcoversTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ECOVERS_TRGEOMETRY_GEO */
        case AntlrSQLParser::EDISJOINT_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "EdisjointTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(EdisjointTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: EDISJOINT_TRGEOMETRY_GEO */
        case AntlrSQLParser::EDISJOINT_TRGEOMETRY_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 10,
                         "EdisjointTrgeometryTrgeometry requires 10 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            auto arg7 = visit(ctx->functionParam(7)).as<LogicalFunction>();
            auto arg8 = visit(ctx->functionParam(8)).as<LogicalFunction>();
            auto arg9 = visit(ctx->functionParam(9)).as<LogicalFunction>();
            return LogicalFunction(EdisjointTrgeometryTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6), std::move(arg7), std::move(arg8), std::move(arg9)));
        }
        /* END CODEGEN PARSER GLUE: EDISJOINT_TRGEOMETRY_TRGEOMETRY */
        case AntlrSQLParser::EDWITHIN_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 7,
                         "EdwithinTrgeometryGeo requires 7 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            return LogicalFunction(EdwithinTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6)));
        }
        /* END CODEGEN PARSER GLUE: EDWITHIN_TRGEOMETRY_GEO */
        case AntlrSQLParser::EDWITHIN_TRGEOMETRY_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 11,
                         "EdwithinTrgeometryTrgeometry requires 11 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            auto arg7 = visit(ctx->functionParam(7)).as<LogicalFunction>();
            auto arg8 = visit(ctx->functionParam(8)).as<LogicalFunction>();
            auto arg9 = visit(ctx->functionParam(9)).as<LogicalFunction>();
            auto arg10 = visit(ctx->functionParam(10)).as<LogicalFunction>();
            return LogicalFunction(EdwithinTrgeometryTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6), std::move(arg7), std::move(arg8), std::move(arg9), std::move(arg10)));
        }
        /* END CODEGEN PARSER GLUE: EDWITHIN_TRGEOMETRY_TRGEOMETRY */
        case AntlrSQLParser::EINTERSECTS_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "EintersectsTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(EintersectsTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: EINTERSECTS_TRGEOMETRY_GEO */
        case AntlrSQLParser::EINTERSECTS_TRGEOMETRY_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 10,
                         "EintersectsTrgeometryTrgeometry requires 10 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            auto arg7 = visit(ctx->functionParam(7)).as<LogicalFunction>();
            auto arg8 = visit(ctx->functionParam(8)).as<LogicalFunction>();
            auto arg9 = visit(ctx->functionParam(9)).as<LogicalFunction>();
            return LogicalFunction(EintersectsTrgeometryTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6), std::move(arg7), std::move(arg8), std::move(arg9)));
        }
        /* END CODEGEN PARSER GLUE: EINTERSECTS_TRGEOMETRY_TRGEOMETRY */
        case AntlrSQLParser::ETOUCHES_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "EtouchesTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(EtouchesTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: ETOUCHES_TRGEOMETRY_GEO */
        case AntlrSQLParser::EVER_EQ_GEO_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "EverEqGeoTrgeometry requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(EverEqGeoTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: EVER_EQ_GEO_TRGEOMETRY */
        case AntlrSQLParser::EVER_EQ_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "EverEqTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(EverEqTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: EVER_EQ_TRGEOMETRY_GEO */
        case AntlrSQLParser::EVER_EQ_TRGEOMETRY_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 10,
                         "EverEqTrgeometryTrgeometry requires 10 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            auto arg7 = visit(ctx->functionParam(7)).as<LogicalFunction>();
            auto arg8 = visit(ctx->functionParam(8)).as<LogicalFunction>();
            auto arg9 = visit(ctx->functionParam(9)).as<LogicalFunction>();
            return LogicalFunction(EverEqTrgeometryTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6), std::move(arg7), std::move(arg8), std::move(arg9)));
        }
        /* END CODEGEN PARSER GLUE: EVER_EQ_TRGEOMETRY_TRGEOMETRY */
        case AntlrSQLParser::EVER_NE_GEO_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "EverNeGeoTrgeometry requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(EverNeGeoTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: EVER_NE_GEO_TRGEOMETRY */
        case AntlrSQLParser::EVER_NE_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "EverNeTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(EverNeTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: EVER_NE_TRGEOMETRY_GEO */
        case AntlrSQLParser::EVER_NE_TRGEOMETRY_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 10,
                         "EverNeTrgeometryTrgeometry requires 10 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            auto arg7 = visit(ctx->functionParam(7)).as<LogicalFunction>();
            auto arg8 = visit(ctx->functionParam(8)).as<LogicalFunction>();
            auto arg9 = visit(ctx->functionParam(9)).as<LogicalFunction>();
            return LogicalFunction(EverNeTrgeometryTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6), std::move(arg7), std::move(arg8), std::move(arg9)));
        }
        /* END CODEGEN PARSER GLUE: EVER_NE_TRGEOMETRY_TRGEOMETRY */
        case AntlrSQLParser::NAD_TRGEOMETRY_GEO: {
            PRECONDITION(ctx->functionParam().size() == 6,
                         "NadTrgeometryGeo requires 6 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            return LogicalFunction(NadTrgeometryGeoLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5)));
        }
        /* END CODEGEN PARSER GLUE: NAD_TRGEOMETRY_GEO */
        case AntlrSQLParser::NAD_TRGEOMETRY_TRGEOMETRY: {
            PRECONDITION(ctx->functionParam().size() == 10,
                         "NadTrgeometryTrgeometry requires 10 args but got {}",
                         ctx->functionParam().size());
            auto arg0 = visit(ctx->functionParam(0)).as<LogicalFunction>();
            auto arg1 = visit(ctx->functionParam(1)).as<LogicalFunction>();
            auto arg2 = visit(ctx->functionParam(2)).as<LogicalFunction>();
            auto arg3 = visit(ctx->functionParam(3)).as<LogicalFunction>();
            auto arg4 = visit(ctx->functionParam(4)).as<LogicalFunction>();
            auto arg5 = visit(ctx->functionParam(5)).as<LogicalFunction>();
            auto arg6 = visit(ctx->functionParam(6)).as<LogicalFunction>();
            auto arg7 = visit(ctx->functionParam(7)).as<LogicalFunction>();
            auto arg8 = visit(ctx->functionParam(8)).as<LogicalFunction>();
            auto arg9 = visit(ctx->functionParam(9)).as<LogicalFunction>();
            return LogicalFunction(NadTrgeometryTrgeometryLogicalFunction(std::move(arg0), std::move(arg1), std::move(arg2), std::move(arg3), std::move(arg4), std::move(arg5), std::move(arg6), std::move(arg7), std::move(arg8), std::move(arg9)));
        }
        /* END CODEGEN PARSER GLUE: NAD_TRGEOMETRY_TRGEOMETRY */
        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EINTERSECTS_TPOSE_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_EINTERSECTS_TPOSE_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_EINTERSECTS_TPOSE_GEOMETRY requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEIntersectsTPoseGeometryLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EINTERSECTS_TPOSE_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_AINTERSECTS_TPOSE_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_AINTERSECTS_TPOSE_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_AINTERSECTS_TPOSE_GEOMETRY requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAIntersectsTPoseGeometryLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_AINTERSECTS_TPOSE_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ECOVERS_TPOSE_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ECOVERS_TPOSE_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_ECOVERS_TPOSE_GEOMETRY requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalECoversTPoseGeometryLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ECOVERS_TPOSE_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EDISJOINT_TPOSE_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_EDISJOINT_TPOSE_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_EDISJOINT_TPOSE_GEOMETRY requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDisjointTPoseGeometryLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EDISJOINT_TPOSE_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ADISJOINT_TPOSE_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ADISJOINT_TPOSE_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_ADISJOINT_TPOSE_GEOMETRY requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADisjointTPoseGeometryLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ADISJOINT_TPOSE_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ETOUCHES_TPOSE_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ETOUCHES_TPOSE_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_ETOUCHES_TPOSE_GEOMETRY requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalETouchesTPoseGeometryLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ETOUCHES_TPOSE_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ATOUCHES_TPOSE_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ATOUCHES_TPOSE_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_ATOUCHES_TPOSE_GEOMETRY requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalATouchesTPoseGeometryLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ATOUCHES_TPOSE_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ECONTAINS_TPOSE_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ECONTAINS_TPOSE_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_ECONTAINS_TPOSE_GEOMETRY requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEContainsTPoseGeometryLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ECONTAINS_TPOSE_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ACONTAINS_TPOSE_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ACONTAINS_TPOSE_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_ACONTAINS_TPOSE_GEOMETRY requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAContainsTPoseGeometryLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ACONTAINS_TPOSE_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EDWITHIN_TPOSE_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_EDWITHIN_TPOSE_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_EDWITHIN_TPOSE_GEOMETRY requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDWithinTPoseGeometryLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EDWITHIN_TPOSE_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ADWITHIN_TPOSE_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ADWITHIN_TPOSE_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_ADWITHIN_TPOSE_GEOMETRY requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADWithinTPoseGeometryLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ADWITHIN_TPOSE_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EINTERSECTS_TPOSE_TPOSE */
        case AntlrSQLLexer::TEMPORAL_EINTERSECTS_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_EINTERSECTS_TPOSE_TPOSE requires exactly 8 arguments (xA, yA, thetaA, tsA, xB, yB, thetaB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEIntersectsTPoseTPoseLogicalFunction(xA, yA, thetaA, tsA, xB, yB, thetaB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EINTERSECTS_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_AINTERSECTS_TPOSE_TPOSE */
        case AntlrSQLLexer::TEMPORAL_AINTERSECTS_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_AINTERSECTS_TPOSE_TPOSE requires exactly 8 arguments (xA, yA, thetaA, tsA, xB, yB, thetaB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAIntersectsTPoseTPoseLogicalFunction(xA, yA, thetaA, tsA, xB, yB, thetaB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_AINTERSECTS_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ECOVERS_TPOSE_TPOSE */
        case AntlrSQLLexer::TEMPORAL_ECOVERS_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_ECOVERS_TPOSE_TPOSE requires exactly 8 arguments (xA, yA, thetaA, tsA, xB, yB, thetaB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalECoversTPoseTPoseLogicalFunction(xA, yA, thetaA, tsA, xB, yB, thetaB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ECOVERS_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EDISJOINT_TPOSE_TPOSE */
        case AntlrSQLLexer::TEMPORAL_EDISJOINT_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_EDISJOINT_TPOSE_TPOSE requires exactly 8 arguments (xA, yA, thetaA, tsA, xB, yB, thetaB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDisjointTPoseTPoseLogicalFunction(xA, yA, thetaA, tsA, xB, yB, thetaB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EDISJOINT_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ADISJOINT_TPOSE_TPOSE */
        case AntlrSQLLexer::TEMPORAL_ADISJOINT_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_ADISJOINT_TPOSE_TPOSE requires exactly 8 arguments (xA, yA, thetaA, tsA, xB, yB, thetaB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADisjointTPoseTPoseLogicalFunction(xA, yA, thetaA, tsA, xB, yB, thetaB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ADISJOINT_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ETOUCHES_TPOSE_TPOSE */
        case AntlrSQLLexer::TEMPORAL_ETOUCHES_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_ETOUCHES_TPOSE_TPOSE requires exactly 8 arguments (xA, yA, thetaA, tsA, xB, yB, thetaB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalETouchesTPoseTPoseLogicalFunction(xA, yA, thetaA, tsA, xB, yB, thetaB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ETOUCHES_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ATOUCHES_TPOSE_TPOSE */
        case AntlrSQLLexer::TEMPORAL_ATOUCHES_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_ATOUCHES_TPOSE_TPOSE requires exactly 8 arguments (xA, yA, thetaA, tsA, xB, yB, thetaB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalATouchesTPoseTPoseLogicalFunction(xA, yA, thetaA, tsA, xB, yB, thetaB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ATOUCHES_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ECONTAINS_TPOSE_TPOSE */
        case AntlrSQLLexer::TEMPORAL_ECONTAINS_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_ECONTAINS_TPOSE_TPOSE requires exactly 8 arguments (xA, yA, thetaA, tsA, xB, yB, thetaB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEContainsTPoseTPoseLogicalFunction(xA, yA, thetaA, tsA, xB, yB, thetaB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ECONTAINS_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ACONTAINS_TPOSE_TPOSE */
        case AntlrSQLLexer::TEMPORAL_ACONTAINS_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_ACONTAINS_TPOSE_TPOSE requires exactly 8 arguments (xA, yA, thetaA, tsA, xB, yB, thetaB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAContainsTPoseTPoseLogicalFunction(xA, yA, thetaA, tsA, xB, yB, thetaB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ACONTAINS_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EDWITHIN_TPOSE_TPOSE */
        case AntlrSQLLexer::TEMPORAL_EDWITHIN_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 9)
                throw InvalidQuerySyntax("TEMPORAL_EDWITHIN_TPOSE_TPOSE requires exactly 9 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDWithinTPoseTPoseLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EDWITHIN_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ADWITHIN_TPOSE_TPOSE */
        case AntlrSQLLexer::TEMPORAL_ADWITHIN_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 9)
                throw InvalidQuerySyntax("TEMPORAL_ADWITHIN_TPOSE_TPOSE requires exactly 9 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADWithinTPoseTPoseLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ADWITHIN_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NAD_TPOSE_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_NAD_TPOSE_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_NAD_TPOSE_GEOMETRY requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADTPoseGeometryLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NAD_TPOSE_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NAD_TPOSE_TPOSE */
        case AntlrSQLLexer::TEMPORAL_NAD_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_NAD_TPOSE_TPOSE requires exactly 8 arguments (xA, yA, thetaA, tsA, xB, yB, thetaB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto thetaA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto yA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto xA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADTPoseTPoseLogicalFunction(xA, yA, thetaA, tsA, xB, yB, thetaB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NAD_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EINTERSECTS_TNPOINT_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_EINTERSECTS_TNPOINT_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_EINTERSECTS_TNPOINT_GEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEIntersectsTNpointGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EINTERSECTS_TNPOINT_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_AINTERSECTS_TNPOINT_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_AINTERSECTS_TNPOINT_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_AINTERSECTS_TNPOINT_GEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAIntersectsTNpointGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_AINTERSECTS_TNPOINT_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ECOVERS_TNPOINT_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ECOVERS_TNPOINT_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_ECOVERS_TNPOINT_GEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalECoversTNpointGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ECOVERS_TNPOINT_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EDISJOINT_TNPOINT_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_EDISJOINT_TNPOINT_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_EDISJOINT_TNPOINT_GEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDisjointTNpointGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EDISJOINT_TNPOINT_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ADISJOINT_TNPOINT_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ADISJOINT_TNPOINT_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_ADISJOINT_TNPOINT_GEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADisjointTNpointGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ADISJOINT_TNPOINT_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ETOUCHES_TNPOINT_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ETOUCHES_TNPOINT_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_ETOUCHES_TNPOINT_GEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalETouchesTNpointGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ETOUCHES_TNPOINT_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ATOUCHES_TNPOINT_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ATOUCHES_TNPOINT_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_ATOUCHES_TNPOINT_GEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalATouchesTNpointGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ATOUCHES_TNPOINT_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ECONTAINS_TNPOINT_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ECONTAINS_TNPOINT_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_ECONTAINS_TNPOINT_GEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEContainsTNpointGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ECONTAINS_TNPOINT_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ACONTAINS_TNPOINT_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ACONTAINS_TNPOINT_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_ACONTAINS_TNPOINT_GEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAContainsTNpointGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ACONTAINS_TNPOINT_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EDWITHIN_TNPOINT_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_EDWITHIN_TNPOINT_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_EDWITHIN_TNPOINT_GEOMETRY requires exactly 5 arguments (lon, lat, timestamp, geometry, distance), but got {}", argCount);

            /* Lift constants (geometry + distance) — same shape as EDWITHIN_TGEO_GEO */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                const auto upperValue = Util::toUpperCase(constantValue);
                if (upperValue == "TRUE" || upperValue == "FALSE")
                {
                    dataType = DataTypeProvider::provideDataType(DataType::Type::BOOLEAN);
                }
                else
                {
                    char* endPtr = nullptr;
                    std::strtod(constantValue.c_str(), &endPtr);
                    if (endPtr != nullptr && *endPtr == '\0')
                        dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                    else
                        dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                }
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            /* After lift: [lon, lat, ts, distance, geometry] (geometry pushed last because lifted last in LIFO) */
            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto dist      = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDWithinTNpointGeometryLogicalFunction(lon, lat, timestamp, geometry, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EDWITHIN_TNPOINT_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ADWITHIN_TNPOINT_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_ADWITHIN_TNPOINT_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_ADWITHIN_TNPOINT_GEOMETRY requires exactly 5 arguments (lon, lat, timestamp, geometry, distance), but got {}", argCount);

            /* Lift constants (geometry + distance) — same shape as EDWITHIN_TGEO_GEO */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                const auto upperValue = Util::toUpperCase(constantValue);
                if (upperValue == "TRUE" || upperValue == "FALSE")
                {
                    dataType = DataTypeProvider::provideDataType(DataType::Type::BOOLEAN);
                }
                else
                {
                    char* endPtr = nullptr;
                    std::strtod(constantValue.c_str(), &endPtr);
                    if (endPtr != nullptr && *endPtr == '\0')
                        dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                    else
                        dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                }
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            /* After lift: [lon, lat, ts, distance, geometry] (geometry pushed last because lifted last in LIFO) */
            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto dist      = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADWithinTNpointGeometryLogicalFunction(lon, lat, timestamp, geometry, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ADWITHIN_TNPOINT_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EINTERSECTS_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TEMPORAL_EINTERSECTS_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_EINTERSECTS_TNPOINT_TNPOINT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEIntersectsTNpointTNpointLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EINTERSECTS_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_AINTERSECTS_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TEMPORAL_AINTERSECTS_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_AINTERSECTS_TNPOINT_TNPOINT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAIntersectsTNpointTNpointLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_AINTERSECTS_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ECOVERS_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TEMPORAL_ECOVERS_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_ECOVERS_TNPOINT_TNPOINT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalECoversTNpointTNpointLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ECOVERS_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EDISJOINT_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TEMPORAL_EDISJOINT_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_EDISJOINT_TNPOINT_TNPOINT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDisjointTNpointTNpointLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EDISJOINT_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ADISJOINT_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TEMPORAL_ADISJOINT_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_ADISJOINT_TNPOINT_TNPOINT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADisjointTNpointTNpointLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ADISJOINT_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ETOUCHES_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TEMPORAL_ETOUCHES_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_ETOUCHES_TNPOINT_TNPOINT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalETouchesTNpointTNpointLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ETOUCHES_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ATOUCHES_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TEMPORAL_ATOUCHES_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_ATOUCHES_TNPOINT_TNPOINT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalATouchesTNpointTNpointLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ATOUCHES_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ECONTAINS_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TEMPORAL_ECONTAINS_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_ECONTAINS_TNPOINT_TNPOINT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEContainsTNpointTNpointLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ECONTAINS_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ACONTAINS_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TEMPORAL_ACONTAINS_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_ACONTAINS_TNPOINT_TNPOINT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAContainsTNpointTNpointLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ACONTAINS_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EDWITHIN_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TEMPORAL_EDWITHIN_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("TEMPORAL_EDWITHIN_TNPOINT_TNPOINT requires exactly 7 arguments (lonA, latA, tsA, lonB, latB, tsB, distance), but got {}", argCount);

            /* Lift the distance constant */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDWithinTNpointTNpointLogicalFunction(lonA, latA, tsA, lonB, latB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EDWITHIN_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ADWITHIN_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TEMPORAL_ADWITHIN_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("TEMPORAL_ADWITHIN_TNPOINT_TNPOINT requires exactly 7 arguments (lonA, latA, tsA, lonB, latB, tsB, distance), but got {}", argCount);

            /* Lift the distance constant */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADWithinTNpointTNpointLogicalFunction(lonA, latA, tsA, lonB, latB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ADWITHIN_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NAD_TNPOINT_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_NAD_TNPOINT_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_NAD_TNPOINT_GEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADTNpointGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NAD_TNPOINT_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NAD_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TEMPORAL_NAD_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_NAD_TNPOINT_TNPOINT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADTNpointTNpointLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NAD_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ABOVESTBOXSTBOX */
        case AntlrSQLLexer::ABOVESTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ABOVESTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AboveStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ABOVESTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENTSTBOXSTBOX */
        case AntlrSQLLexer::ADJACENTSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENTSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdjacentStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENTSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: AFTERSTBOXSTBOX */
        case AntlrSQLLexer::AFTERSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTERSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AfterStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTERSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: BACKSTBOXSTBOX */
        case AntlrSQLLexer::BACKSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BACKSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BackStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BACKSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: BEFORESTBOXSTBOX */
        case AntlrSQLLexer::BEFORESTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORESTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BeforeStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORESTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: BELOWSTBOXSTBOX */
        case AntlrSQLLexer::BELOWSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BELOWSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BelowStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BELOWSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINEDSTBOXSTBOX */
        case AntlrSQLLexer::CONTAINEDSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINEDSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainedStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINEDSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINSSTBOXSTBOX */
        case AntlrSQLLexer::CONTAINSSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINSSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainsStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINSSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: FRONTSTBOXSTBOX */
        case AntlrSQLLexer::FRONTSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("FRONTSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                FrontStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: FRONTSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: LEFTSTBOXSTBOX */
        case AntlrSQLLexer::LEFTSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFTSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                LeftStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFTSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERABOVESTBOXSTBOX */
        case AntlrSQLLexer::OVERABOVESTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERABOVESTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OveraboveStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERABOVESTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTERSTBOXSTBOX */
        case AntlrSQLLexer::OVERAFTERSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTERSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverafterStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTERSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBACKSTBOXSTBOX */
        case AntlrSQLLexer::OVERBACKSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBACKSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbackStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBACKSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORESTBOXSTBOX */
        case AntlrSQLLexer::OVERBEFORESTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORESTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbeforeStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORESTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBELOWSTBOXSTBOX */
        case AntlrSQLLexer::OVERBELOWSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBELOWSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbelowStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBELOWSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERFRONTSTBOXSTBOX */
        case AntlrSQLLexer::OVERFRONTSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERFRONTSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverfrontStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERFRONTSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPSSTBOXSTBOX */
        case AntlrSQLLexer::OVERLAPSSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLAPSSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverlapsStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPSSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFTSTBOXSTBOX */
        case AntlrSQLLexer::OVERLEFTSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFTSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverleftStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFTSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHTSTBOXSTBOX */
        case AntlrSQLLexer::OVERRIGHTSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHTSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverrightStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHTSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: RIGHTSTBOXSTBOX */
        case AntlrSQLLexer::RIGHTSTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHTSTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                RightStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHTSTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: SAMESTBOXSTBOX */
        case AntlrSQLLexer::SAMESTBOXSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SAMESTBOXSTBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                SameStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAMESTBOXSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: STBOXEQ */
        case AntlrSQLLexer::STBOXEQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOXEQ requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                StboxEqLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOXEQ */

        /* BEGIN CODEGEN PARSER GLUE: STBOXGE */
        case AntlrSQLLexer::STBOXGE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOXGE requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                StboxGeLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOXGE */

        /* BEGIN CODEGEN PARSER GLUE: STBOXGT */
        case AntlrSQLLexer::STBOXGT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOXGT requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                StboxGtLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOXGT */

        /* BEGIN CODEGEN PARSER GLUE: STBOXLE */
        case AntlrSQLLexer::STBOXLE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOXLE requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                StboxLeLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOXLE */

        /* BEGIN CODEGEN PARSER GLUE: STBOXLT */
        case AntlrSQLLexer::STBOXLT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOXLT requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                StboxLtLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOXLT */

        /* BEGIN CODEGEN PARSER GLUE: STBOXNE */
        case AntlrSQLLexer::STBOXNE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOXNE requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                StboxNeLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOXNE */

        /* BEGIN CODEGEN PARSER GLUE: NAD_STBOX_STBOX */
        case AntlrSQLLexer::NAD_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NAD_STBOX_STBOX requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadStboxStboxLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_CMP */
        case AntlrSQLLexer::STBOX_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOX_CMP requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                StboxCmpLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_CMP */

        /* BEGIN CODEGEN PARSER GLUE: ABOVESTBOXTSPATIAL */
        case AntlrSQLLexer::ABOVESTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ABOVESTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AboveStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ABOVESTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENTSTBOXTSPATIAL */
        case AntlrSQLLexer::ADJACENTSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ADJACENTSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdjacentStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENTSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: AFTERSTBOXTSPATIAL */
        case AntlrSQLLexer::AFTERSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("AFTERSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AfterStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTERSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: BACKSTBOXTSPATIAL */
        case AntlrSQLLexer::BACKSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("BACKSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BackStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: BACKSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: BEFORESTBOXTSPATIAL */
        case AntlrSQLLexer::BEFORESTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("BEFORESTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BeforeStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORESTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: BELOWSTBOXTSPATIAL */
        case AntlrSQLLexer::BELOWSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("BELOWSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BelowStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: BELOWSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINEDSTBOXTSPATIAL */
        case AntlrSQLLexer::CONTAINEDSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("CONTAINEDSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainedStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINEDSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINSSTBOXTSPATIAL */
        case AntlrSQLLexer::CONTAINSSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("CONTAINSSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainsStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINSSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: FRONTSTBOXTSPATIAL */
        case AntlrSQLLexer::FRONTSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("FRONTSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                FrontStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: FRONTSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: LEFTSTBOXTSPATIAL */
        case AntlrSQLLexer::LEFTSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("LEFTSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                LeftStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFTSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERABOVESTBOXTSPATIAL */
        case AntlrSQLLexer::OVERABOVESTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERABOVESTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OveraboveStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERABOVESTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTERSTBOXTSPATIAL */
        case AntlrSQLLexer::OVERAFTERSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERAFTERSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverafterStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTERSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBACKSTBOXTSPATIAL */
        case AntlrSQLLexer::OVERBACKSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERBACKSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbackStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBACKSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORESTBOXTSPATIAL */
        case AntlrSQLLexer::OVERBEFORESTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERBEFORESTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbeforeStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORESTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBELOWSTBOXTSPATIAL */
        case AntlrSQLLexer::OVERBELOWSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERBELOWSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbelowStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBELOWSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERFRONTSTBOXTSPATIAL */
        case AntlrSQLLexer::OVERFRONTSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERFRONTSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverfrontStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERFRONTSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPSSTBOXTSPATIAL */
        case AntlrSQLLexer::OVERLAPSSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERLAPSSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverlapsStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPSSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFTSTBOXTSPATIAL */
        case AntlrSQLLexer::OVERLEFTSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERLEFTSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverleftStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFTSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHTSTBOXTSPATIAL */
        case AntlrSQLLexer::OVERRIGHTSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERRIGHTSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverrightStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHTSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: RIGHTSTBOXTSPATIAL */
        case AntlrSQLLexer::RIGHTSTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("RIGHTSTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                RightStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHTSTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: SAMESTBOXTSPATIAL */
        case AntlrSQLLexer::SAMESTBOXTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("SAMESTBOXTSPATIAL requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                SameStboxTspatialLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAMESTBOXTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: ABOVETSPATIALSTBOX */
        case AntlrSQLLexer::ABOVETSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ABOVETSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AboveTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ABOVETSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENTTSPATIALSTBOX */
        case AntlrSQLLexer::ADJACENTTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ADJACENTTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdjacentTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENTTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: AFTERTSPATIALSTBOX */
        case AntlrSQLLexer::AFTERTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("AFTERTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AfterTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTERTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: BACKTSPATIALSTBOX */
        case AntlrSQLLexer::BACKTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("BACKTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BackTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: BACKTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: BEFORETSPATIALSTBOX */
        case AntlrSQLLexer::BEFORETSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("BEFORETSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BeforeTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORETSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: BELOWTSPATIALSTBOX */
        case AntlrSQLLexer::BELOWTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("BELOWTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BelowTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: BELOWTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINEDTSPATIALSTBOX */
        case AntlrSQLLexer::CONTAINEDTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("CONTAINEDTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainedTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINEDTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINSTSPATIALSTBOX */
        case AntlrSQLLexer::CONTAINSTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("CONTAINSTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainsTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINSTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: FRONTTSPATIALSTBOX */
        case AntlrSQLLexer::FRONTTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("FRONTTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                FrontTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: FRONTTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: LEFTTSPATIALSTBOX */
        case AntlrSQLLexer::LEFTTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("LEFTTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                LeftTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFTTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERABOVETSPATIALSTBOX */
        case AntlrSQLLexer::OVERABOVETSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERABOVETSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OveraboveTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERABOVETSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTERTSPATIALSTBOX */
        case AntlrSQLLexer::OVERAFTERTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERAFTERTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverafterTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTERTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBACKTSPATIALSTBOX */
        case AntlrSQLLexer::OVERBACKTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERBACKTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbackTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBACKTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORETSPATIALSTBOX */
        case AntlrSQLLexer::OVERBEFORETSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERBEFORETSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbeforeTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORETSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBELOWTSPATIALSTBOX */
        case AntlrSQLLexer::OVERBELOWTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERBELOWTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbelowTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBELOWTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERFRONTTSPATIALSTBOX */
        case AntlrSQLLexer::OVERFRONTTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERFRONTTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverfrontTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERFRONTTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPSTSPATIALSTBOX */
        case AntlrSQLLexer::OVERLAPSTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERLAPSTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverlapsTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPSTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFTTSPATIALSTBOX */
        case AntlrSQLLexer::OVERLEFTTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERLEFTTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverleftTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFTTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHTTSPATIALSTBOX */
        case AntlrSQLLexer::OVERRIGHTTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERRIGHTTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverrightTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHTTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: RIGHTTSPATIALSTBOX */
        case AntlrSQLLexer::RIGHTTSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("RIGHTTSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                RightTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHTTSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: SAMETSPATIALSTBOX */
        case AntlrSQLLexer::SAMETSPATIALSTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("SAMETSPATIALSTBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                SameTspatialStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAMETSPATIALSTBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TGEO_STBOX */
        case AntlrSQLLexer::NAD_TGEO_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAD_TGEO_STBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTgeoStboxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TGEO_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENTTBOXTNUMBER */
        case AntlrSQLLexer::ADJACENTTBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADJACENTTBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdjacentTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENTTBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: AFTERTBOXTNUMBER */
        case AntlrSQLLexer::AFTERTBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("AFTERTBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AfterTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTERTBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: BEFORETBOXTNUMBER */
        case AntlrSQLLexer::BEFORETBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("BEFORETBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BeforeTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORETBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINEDTBOXTNUMBER */
        case AntlrSQLLexer::CONTAINEDTBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINEDTBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainedTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINEDTBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINSTBOXTNUMBER */
        case AntlrSQLLexer::CONTAINSTBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINSTBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainsTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINSTBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: LEFTTBOXTNUMBER */
        case AntlrSQLLexer::LEFTTBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("LEFTTBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                LeftTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFTTBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTERTBOXTNUMBER */
        case AntlrSQLLexer::OVERAFTERTBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERAFTERTBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverafterTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTERTBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORETBOXTNUMBER */
        case AntlrSQLLexer::OVERBEFORETBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERBEFORETBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbeforeTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORETBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPSTBOXTNUMBER */
        case AntlrSQLLexer::OVERLAPSTBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLAPSTBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverlapsTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPSTBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFTTBOXTNUMBER */
        case AntlrSQLLexer::OVERLEFTTBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLEFTTBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverleftTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFTTBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHTTBOXTNUMBER */
        case AntlrSQLLexer::OVERRIGHTTBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERRIGHTTBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverrightTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHTTBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: RIGHTTBOXTNUMBER */
        case AntlrSQLLexer::RIGHTTBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("RIGHTTBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                RightTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHTTBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: SAMETBOXTNUMBER */
        case AntlrSQLLexer::SAMETBOXTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SAMETBOXTNUMBER requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                SameTboxTnumberLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAMETBOXTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENTTNUMBERTBOX */
        case AntlrSQLLexer::ADJACENTTNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADJACENTTNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdjacentTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENTTNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: AFTERTNUMBERTBOX */
        case AntlrSQLLexer::AFTERTNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("AFTERTNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AfterTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTERTNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: BEFORETNUMBERTBOX */
        case AntlrSQLLexer::BEFORETNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("BEFORETNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BeforeTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORETNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINEDTNUMBERTBOX */
        case AntlrSQLLexer::CONTAINEDTNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINEDTNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainedTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINEDTNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINSTNUMBERTBOX */
        case AntlrSQLLexer::CONTAINSTNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINSTNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainsTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINSTNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: LEFTTNUMBERTBOX */
        case AntlrSQLLexer::LEFTTNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("LEFTTNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                LeftTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFTTNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTERTNUMBERTBOX */
        case AntlrSQLLexer::OVERAFTERTNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERAFTERTNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverafterTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTERTNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORETNUMBERTBOX */
        case AntlrSQLLexer::OVERBEFORETNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERBEFORETNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbeforeTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORETNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPSTNUMBERTBOX */
        case AntlrSQLLexer::OVERLAPSTNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLAPSTNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverlapsTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPSTNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFTTNUMBERTBOX */
        case AntlrSQLLexer::OVERLEFTTNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLEFTTNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverleftTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFTTNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHTTNUMBERTBOX */
        case AntlrSQLLexer::OVERRIGHTTNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERRIGHTTNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverrightTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHTTNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: RIGHTTNUMBERTBOX */
        case AntlrSQLLexer::RIGHTTNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("RIGHTTNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                RightTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHTTNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: SAMETNUMBERTBOX */
        case AntlrSQLLexer::SAMETNUMBERTBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SAMETNUMBERTBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                SameTnumberTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAMETNUMBERTBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TFLOAT_TBOX */
        case AntlrSQLLexer::NAD_TFLOAT_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("NAD_TFLOAT_TBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTfloatTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TFLOAT_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TINT_TBOX */
        case AntlrSQLLexer::NAD_TINT_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("NAD_TINT_TBOX requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTintTboxLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TINT_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENTTNUMBERTNUMBER */
        case AntlrSQLLexer::ADJACENTTNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENTTNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdjacentTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENTTNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: AFTERTNUMBERTNUMBER */
        case AntlrSQLLexer::AFTERTNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTERTNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AfterTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTERTNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: BEFORETNUMBERTNUMBER */
        case AntlrSQLLexer::BEFORETNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORETNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BeforeTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORETNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINEDTNUMBERTNUMBER */
        case AntlrSQLLexer::CONTAINEDTNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINEDTNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainedTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINEDTNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINSTNUMBERTNUMBER */
        case AntlrSQLLexer::CONTAINSTNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINSTNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainsTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINSTNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: LEFTTNUMBERTNUMBER */
        case AntlrSQLLexer::LEFTTNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFTTNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                LeftTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFTTNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTERTNUMBERTNUMBER */
        case AntlrSQLLexer::OVERAFTERTNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTERTNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverafterTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTERTNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORETNUMBERTNUMBER */
        case AntlrSQLLexer::OVERBEFORETNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORETNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbeforeTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORETNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPSTNUMBERTNUMBER */
        case AntlrSQLLexer::OVERLAPSTNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLAPSTNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverlapsTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPSTNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFTTNUMBERTNUMBER */
        case AntlrSQLLexer::OVERLEFTTNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFTTNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverleftTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFTTNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHTTNUMBERTNUMBER */
        case AntlrSQLLexer::OVERRIGHTTNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHTTNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverrightTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHTTNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: RIGHTTNUMBERTNUMBER */
        case AntlrSQLLexer::RIGHTTNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHTTNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                RightTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHTTNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: SAMETNUMBERTNUMBER */
        case AntlrSQLLexer::SAMETNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SAMETNUMBERTNUMBER requires exactly 2 arguments (arg0, arg1), but got {}", argCount);

            /* Lift constant blobs into function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto arg1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto arg0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                SameTnumberTnumberLogicalFunction(arg0, arg1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAMETNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: TBOOL_END_VALUE */
        case AntlrSQLLexer::TBOOL_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOOL_END_VALUE requires exactly 2 arguments (value, timestamp), but got {}", argCount);

            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TboolEndValueLogicalFunction(value, timestamp));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOOL_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TBOOL_START_VALUE */
        case AntlrSQLLexer::TBOOL_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOOL_START_VALUE requires exactly 2 arguments (value, timestamp), but got {}", argCount);

            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TboolStartValueLogicalFunction(value, timestamp));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOOL_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: ABOVETSPATIALTSPATIAL */
        case AntlrSQLLexer::ABOVETSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ABOVETSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AboveTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ABOVETSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENTTSPATIALTSPATIAL */
        case AntlrSQLLexer::ADJACENTTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ADJACENTTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdjacentTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENTTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: AFTERTSPATIALTSPATIAL */
        case AntlrSQLLexer::AFTERTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("AFTERTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AfterTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTERTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: BACKTSPATIALTSPATIAL */
        case AntlrSQLLexer::BACKTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("BACKTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BackTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: BACKTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: BEFORETSPATIALTSPATIAL */
        case AntlrSQLLexer::BEFORETSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("BEFORETSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BeforeTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORETSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: BELOWTSPATIALTSPATIAL */
        case AntlrSQLLexer::BELOWTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("BELOWTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BelowTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: BELOWTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINEDTSPATIALTSPATIAL */
        case AntlrSQLLexer::CONTAINEDTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("CONTAINEDTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainedTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINEDTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINSTSPATIALTSPATIAL */
        case AntlrSQLLexer::CONTAINSTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("CONTAINSTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainsTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINSTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: FRONTTSPATIALTSPATIAL */
        case AntlrSQLLexer::FRONTTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("FRONTTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                FrontTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: FRONTTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: LEFTTSPATIALTSPATIAL */
        case AntlrSQLLexer::LEFTTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("LEFTTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                LeftTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFTTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERABOVETSPATIALTSPATIAL */
        case AntlrSQLLexer::OVERABOVETSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERABOVETSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OveraboveTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERABOVETSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTERTSPATIALTSPATIAL */
        case AntlrSQLLexer::OVERAFTERTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERAFTERTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverafterTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTERTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBACKTSPATIALTSPATIAL */
        case AntlrSQLLexer::OVERBACKTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERBACKTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbackTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBACKTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORETSPATIALTSPATIAL */
        case AntlrSQLLexer::OVERBEFORETSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERBEFORETSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbeforeTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORETSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBELOWTSPATIALTSPATIAL */
        case AntlrSQLLexer::OVERBELOWTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERBELOWTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbelowTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBELOWTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERFRONTTSPATIALTSPATIAL */
        case AntlrSQLLexer::OVERFRONTTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERFRONTTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverfrontTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERFRONTTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPSTSPATIALTSPATIAL */
        case AntlrSQLLexer::OVERLAPSTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERLAPSTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverlapsTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPSTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFTTSPATIALTSPATIAL */
        case AntlrSQLLexer::OVERLEFTTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERLEFTTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverleftTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFTTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHTTSPATIALTSPATIAL */
        case AntlrSQLLexer::OVERRIGHTTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERRIGHTTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverrightTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHTTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: RIGHTTSPATIALTSPATIAL */
        case AntlrSQLLexer::RIGHTTSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("RIGHTTSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                RightTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHTTSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: SAMETSPATIALTSPATIAL */
        case AntlrSQLLexer::SAMETSPATIALTSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("SAMETSPATIALTSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                SameTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAMETSPATIALTSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENTTEMPORALTEMPORAL */
        case AntlrSQLLexer::ADJACENTTEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ADJACENTTEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdjacentTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENTTEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: AFTERTEMPORALTEMPORAL */
        case AntlrSQLLexer::AFTERTEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("AFTERTEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AfterTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTERTEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: BEFORETEMPORALTEMPORAL */
        case AntlrSQLLexer::BEFORETEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("BEFORETEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BeforeTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORETEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINEDTEMPORALTEMPORAL */
        case AntlrSQLLexer::CONTAINEDTEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("CONTAINEDTEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainedTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINEDTEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINSTEMPORALTEMPORAL */
        case AntlrSQLLexer::CONTAINSTEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("CONTAINSTEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainsTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINSTEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTERTEMPORALTEMPORAL */
        case AntlrSQLLexer::OVERAFTERTEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERAFTERTEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverafterTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTERTEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORETEMPORALTEMPORAL */
        case AntlrSQLLexer::OVERBEFORETEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERBEFORETEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbeforeTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORETEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPSTEMPORALTEMPORAL */
        case AntlrSQLLexer::OVERLAPSTEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERLAPSTEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverlapsTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPSTEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: SAMETEMPORALTEMPORAL */
        case AntlrSQLLexer::SAMETEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("SAMETEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                SameTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAMETEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALCMP */
        case AntlrSQLLexer::TEMPORALCMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALCMP requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalCmpLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALCMP */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEQ */
        case AntlrSQLLexer::TEMPORALEQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALEQ requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEqLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEQ */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALGE */
        case AntlrSQLLexer::TEMPORALGE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALGE requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalGeLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALGE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALGT */
        case AntlrSQLLexer::TEMPORALGT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALGT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalGtLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALGT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALLE */
        case AntlrSQLLexer::TEMPORALLE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALLE requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalLeLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALLE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALLT */
        case AntlrSQLLexer::TEMPORALLT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALLT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalLtLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALLT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALNE */
        case AntlrSQLLexer::TEMPORALNE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALNE requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNeLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALNE */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTEMPORALTEMPORAL */
        case AntlrSQLLexer::ALWAYSEQTEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ALWAYSEQTEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGETEMPORALTEMPORAL */
        case AntlrSQLLexer::ALWAYSGETEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ALWAYSGETEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGeTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGETEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGTTEMPORALTEMPORAL */
        case AntlrSQLLexer::ALWAYSGTTEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ALWAYSGTTEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGtTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGTTEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLETEMPORALTEMPORAL */
        case AntlrSQLLexer::ALWAYSLETEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ALWAYSLETEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLeTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLETEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLTTEMPORALTEMPORAL */
        case AntlrSQLLexer::ALWAYSLTTEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ALWAYSLTTEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLtTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLTTEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETEMPORALTEMPORAL */
        case AntlrSQLLexer::ALWAYSNETEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ALWAYSNETEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTEMPORALTEMPORAL */
        case AntlrSQLLexer::EVEREQTEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EVEREQTEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: EVERGETEMPORALTEMPORAL */
        case AntlrSQLLexer::EVERGETEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EVERGETEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGeTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGETEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: EVERGTTEMPORALTEMPORAL */
        case AntlrSQLLexer::EVERGTTEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EVERGTTEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGtTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGTTEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: EVERLETEMPORALTEMPORAL */
        case AntlrSQLLexer::EVERLETEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EVERLETEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLeTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLETEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: EVERLTTEMPORALTEMPORAL */
        case AntlrSQLLexer::EVERLTTEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EVERLTTEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLtTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLTTEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETEMPORALTEMPORAL */
        case AntlrSQLLexer::EVERNETEMPORALTEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EVERNETEMPORALTEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETEMPORALTEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALFRECHETDISTANCE */
        case AntlrSQLLexer::TEMPORALFRECHETDISTANCE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALFRECHETDISTANCE requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalFrechetDistanceLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALFRECHETDISTANCE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALHAUSDORFFDISTANCE */
        case AntlrSQLLexer::TEMPORALHAUSDORFFDISTANCE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALHAUSDORFFDISTANCE requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalHausdorffDistanceLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALHAUSDORFFDISTANCE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALDYNTIMEWARPDISTANCE */
        case AntlrSQLLexer::TEMPORALDYNTIMEWARPDISTANCE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALDYNTIMEWARPDISTANCE requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalDyntimewarpDistanceLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALDYNTIMEWARPDISTANCE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALNADTGEOMETRY */
        case AntlrSQLLexer::TEMPORALNADTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALNADTGEOMETRY requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADTGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALNADTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NAD_TFLOAT */
        case AntlrSQLLexer::TEMPORAL_NAD_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_NAD_TFLOAT requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADTFloatLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NAD_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NAD_TINT */
        case AntlrSQLLexer::TEMPORAL_NAD_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_NAD_TINT requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADTIntLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NAD_TINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTFLOATFLOAT */
        case AntlrSQLLexer::ALWAYSEQTFLOATFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSEQTFLOATFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTFLOATFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGETFLOATFLOAT */
        case AntlrSQLLexer::ALWAYSGETFLOATFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSGETFLOATFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGeTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGETFLOATFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGTTFLOATFLOAT */
        case AntlrSQLLexer::ALWAYSGTTFLOATFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSGTTFLOATFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGtTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGTTFLOATFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLETFLOATFLOAT */
        case AntlrSQLLexer::ALWAYSLETFLOATFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSLETFLOATFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLeTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLETFLOATFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLTTFLOATFLOAT */
        case AntlrSQLLexer::ALWAYSLTTFLOATFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSLTTFLOATFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLtTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLTTFLOATFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETFLOATFLOAT */
        case AntlrSQLLexer::ALWAYSNETFLOATFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSNETFLOATFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETFLOATFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTFLOATFLOAT */
        case AntlrSQLLexer::EVEREQTFLOATFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVEREQTFLOATFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTFLOATFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVERGETFLOATFLOAT */
        case AntlrSQLLexer::EVERGETFLOATFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERGETFLOATFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGeTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGETFLOATFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVERGTTFLOATFLOAT */
        case AntlrSQLLexer::EVERGTTFLOATFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERGTTFLOATFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGtTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGTTFLOATFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVERLETFLOATFLOAT */
        case AntlrSQLLexer::EVERLETFLOATFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERLETFLOATFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLeTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLETFLOATFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVERLTTFLOATFLOAT */
        case AntlrSQLLexer::EVERLTTFLOATFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERLTTFLOATFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLtTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLTTFLOATFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETFLOATFLOAT */
        case AntlrSQLLexer::EVERNETFLOATFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERNETFLOATFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETFLOATFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTINTINT */
        case AntlrSQLLexer::ALWAYSEQTINTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSEQTINTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTINTINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGETINTINT */
        case AntlrSQLLexer::ALWAYSGETINTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSGETINTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGeTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGETINTINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGTTINTINT */
        case AntlrSQLLexer::ALWAYSGTTINTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSGTTINTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGtTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGTTINTINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLETINTINT */
        case AntlrSQLLexer::ALWAYSLETINTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSLETINTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLeTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLETINTINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLTTINTINT */
        case AntlrSQLLexer::ALWAYSLTTINTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSLTTINTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLtTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLTTINTINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETINTINT */
        case AntlrSQLLexer::ALWAYSNETINTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSNETINTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETINTINT */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTINTINT */
        case AntlrSQLLexer::EVEREQTINTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVEREQTINTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTINTINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERGETINTINT */
        case AntlrSQLLexer::EVERGETINTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERGETINTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGeTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGETINTINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERGTTINTINT */
        case AntlrSQLLexer::EVERGTTINTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERGTTINTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGtTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGTTINTINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERLETINTINT */
        case AntlrSQLLexer::EVERLETINTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERLETINTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLeTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLETINTINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERLTTINTINT */
        case AntlrSQLLexer::EVERLTTINTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERLTTINTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLtTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLTTINTINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETINTINT */
        case AntlrSQLLexer::EVERNETINTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERNETINTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETINTINT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NAD_FLOAT_SCALAR */
        case AntlrSQLLexer::TEMPORAL_NAD_FLOAT_SCALAR:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_NAD_FLOAT_SCALAR requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADFloatScalarLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NAD_FLOAT_SCALAR */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NAD_INT_SCALAR */
        case AntlrSQLLexer::TEMPORAL_NAD_INT_SCALAR:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_NAD_INT_SCALAR requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADIntScalarLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NAD_INT_SCALAR */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQFLOATTFLOAT */
        case AntlrSQLLexer::ALWAYSEQFLOATTFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSEQFLOATTFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQFLOATTFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGEFLOATTFLOAT */
        case AntlrSQLLexer::ALWAYSGEFLOATTFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSGEFLOATTFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGeFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGEFLOATTFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGTFLOATTFLOAT */
        case AntlrSQLLexer::ALWAYSGTFLOATTFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSGTFLOATTFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGtFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGTFLOATTFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLEFLOATTFLOAT */
        case AntlrSQLLexer::ALWAYSLEFLOATTFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSLEFLOATTFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLeFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLEFLOATTFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLTFLOATTFLOAT */
        case AntlrSQLLexer::ALWAYSLTFLOATTFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSLTFLOATTFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLtFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLTFLOATTFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNEFLOATTFLOAT */
        case AntlrSQLLexer::ALWAYSNEFLOATTFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSNEFLOATTFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNEFLOATTFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQFLOATTFLOAT */
        case AntlrSQLLexer::EVEREQFLOATTFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVEREQFLOATTFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQFLOATTFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVERGEFLOATTFLOAT */
        case AntlrSQLLexer::EVERGEFLOATTFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERGEFLOATTFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGeFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGEFLOATTFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVERGTFLOATTFLOAT */
        case AntlrSQLLexer::EVERGTFLOATTFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERGTFLOATTFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGtFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGTFLOATTFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVERLEFLOATTFLOAT */
        case AntlrSQLLexer::EVERLEFLOATTFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERLEFLOATTFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLeFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLEFLOATTFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVERLTFLOATTFLOAT */
        case AntlrSQLLexer::EVERLTFLOATTFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERLTFLOATTFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLtFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLTFLOATTFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVERNEFLOATTFLOAT */
        case AntlrSQLLexer::EVERNEFLOATTFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERNEFLOATTFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNEFLOATTFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQINTTINT */
        case AntlrSQLLexer::ALWAYSEQINTTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSEQINTTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQINTTINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGEINTTINT */
        case AntlrSQLLexer::ALWAYSGEINTTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSGEINTTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGeIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGEINTTINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSGTINTTINT */
        case AntlrSQLLexer::ALWAYSGTINTTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSGTINTTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGtIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSGTINTTINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLEINTTINT */
        case AntlrSQLLexer::ALWAYSLEINTTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSLEINTTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLeIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLEINTTINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSLTINTTINT */
        case AntlrSQLLexer::ALWAYSLTINTTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSLTINTTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLtIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSLTINTTINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNEINTTINT */
        case AntlrSQLLexer::ALWAYSNEINTTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYSNEINTTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNEINTTINT */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQINTTINT */
        case AntlrSQLLexer::EVEREQINTTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVEREQINTTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQINTTINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERGEINTTINT */
        case AntlrSQLLexer::EVERGEINTTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERGEINTTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGeIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGEINTTINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERGTINTTINT */
        case AntlrSQLLexer::EVERGTINTTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERGTINTTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGtIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERGTINTTINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERLEINTTINT */
        case AntlrSQLLexer::EVERLEINTTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERLEINTTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLeIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLEINTTINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERLTINTTINT */
        case AntlrSQLLexer::EVERLTINTTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERLTINTTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLtIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERLTINTTINT */

        /* BEGIN CODEGEN PARSER GLUE: EVERNEINTTINT */
        case AntlrSQLLexer::EVERNEINTTINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVERNEINTTINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNEINTTINT */

        /* BEGIN CODEGEN PARSER GLUE: MULTTNUMBERTNUMBER */
        case AntlrSQLLexer::MULTTNUMBERTNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MULTTNUMBERTNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MultTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MULTTNUMBERTNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: TCONTAINSTGEOTGEO */
        case AntlrSQLLexer::TCONTAINSTGEOTGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TCONTAINSTGEOTGEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcontainsTgeoTgeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCONTAINSTGEOTGEO */

        /* BEGIN CODEGEN PARSER GLUE: TCOVERSTGEOTGEO */
        case AntlrSQLLexer::TCOVERSTGEOTGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TCOVERSTGEOTGEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcoversTgeoTgeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCOVERSTGEOTGEO */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCETGEOTGEO */
        case AntlrSQLLexer::TDISTANCETGEOTGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TDISTANCETGEOTGEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTgeoTgeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCETGEOTGEO */

        /* BEGIN CODEGEN PARSER GLUE: TCONTAINSTCBUFFERTCBUFFER */
        case AntlrSQLLexer::TCONTAINSTCBUFFERTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TCONTAINSTCBUFFERTCBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcontainsTcbufferTcbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCONTAINSTCBUFFERTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TCOVERSTCBUFFERTCBUFFER */
        case AntlrSQLLexer::TCOVERSTCBUFFERTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TCOVERSTCBUFFERTCBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcoversTcbufferTcbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCOVERSTCBUFFERTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCETCBUFFERTCBUFFER */
        case AntlrSQLLexer::TDISTANCETCBUFFERTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TDISTANCETCBUFFERTCBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTcbufferTcbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCETCBUFFERTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCETNPOINTTNPOINT */
        case AntlrSQLLexer::TDISTANCETNPOINTTNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TDISTANCETNPOINTTNPOINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTnpointTnpointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCETNPOINTTNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCETPOSETPOSE */
        case AntlrSQLLexer::TDISTANCETPOSETPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TDISTANCETPOSETPOSE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTposeTposeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCETPOSETPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TPOINT_LENGTH_WKB */
        case AntlrSQLLexer::TPOINT_LENGTH_WKB:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TPOINT_LENGTH_WKB requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TpointLengthWkbLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOINT_LENGTH_WKB */

        /* BEGIN CODEGEN PARSER GLUE: TNPOINT_LENGTH */
        case AntlrSQLLexer::TNPOINT_LENGTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNPOINT_LENGTH requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnpointLengthLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNPOINT_LENGTH */

        /* BEGIN CODEGEN PARSER GLUE: TCBUFFER_TO_TFLOAT */
        case AntlrSQLLexer::TCBUFFER_TO_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TCBUFFER_TO_TFLOAT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcbufferToTfloatLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCBUFFER_TO_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TCBUFFER_STBOX */
        case AntlrSQLLexer::NAD_TCBUFFER_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAD_TCBUFFER_STBOX requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTcbufferStboxLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TCBUFFER_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TNPOINT_STBOX */
        case AntlrSQLLexer::NAD_TNPOINT_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAD_TNPOINT_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTnpointStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TNPOINT_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TPOSE_STBOX */
        case AntlrSQLLexer::NAD_TPOSE_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAD_TPOSE_STBOX requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTposeStboxLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TPOSE_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: ATOUCHESTPOINTGEO */
        case AntlrSQLLexer::ATOUCHESTPOINTGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ATOUCHESTPOINTGEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AtouchesTpointGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ATOUCHESTPOINTGEO */

        /* BEGIN CODEGEN PARSER GLUE: ETOUCHESTPOINTGEO */
        case AntlrSQLLexer::ETOUCHESTPOINTGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ETOUCHESTPOINTGEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EtouchesTpointGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ETOUCHESTPOINTGEO */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALACONTAINSGEOMETRY */
        case AntlrSQLLexer::TEMPORALACONTAINSGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORALACONTAINSGEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAContainsGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALACONTAINSGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALADISJOINTGEOMETRY */
        case AntlrSQLLexer::TEMPORALADISJOINTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORALADISJOINTGEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADisjointGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALADISJOINTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALAINTERSECTSGEOMETRY */
        case AntlrSQLLexer::TEMPORALAINTERSECTSGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORALAINTERSECTSGEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAIntersectsGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALAINTERSECTSGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALATOUCHESGEOMETRY */
        case AntlrSQLLexer::TEMPORALATOUCHESGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORALATOUCHESGEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalATouchesGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALATOUCHESGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALECONTAINSGEOMETRY */
        case AntlrSQLLexer::TEMPORALECONTAINSGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORALECONTAINSGEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEContainsGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALECONTAINSGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALECOVERSGEOMETRY */
        case AntlrSQLLexer::TEMPORALECOVERSGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORALECOVERSGEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalECoversGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALECOVERSGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEDISJOINTGEOMETRY */
        case AntlrSQLLexer::TEMPORALEDISJOINTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORALEDISJOINTGEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDisjointGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEDISJOINTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEINTERSECTSGEOMETRY */
        case AntlrSQLLexer::TEMPORALEINTERSECTSGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORALEINTERSECTSGEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEIntersectsGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEINTERSECTSGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALETOUCHESGEOMETRY */
        case AntlrSQLLexer::TEMPORALETOUCHESGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORALETOUCHESGEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalETouchesGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALETOUCHESGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALINTERSECTSGEOMETRY */
        case AntlrSQLLexer::TEMPORALINTERSECTSGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORALINTERSECTSGEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalIntersectsGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALINTERSECTSGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALATGEOMETRY */
        case AntlrSQLLexer::TEMPORALATGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORALATGEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAtGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALATGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALMINUSGEOMETRY */
        case AntlrSQLLexer::TEMPORALMINUSGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORALMINUSGEOMETRY requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalMinusGeometryLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALMINUSGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALADWITHINGEOMETRY */
        case AntlrSQLLexer::TEMPORALADWITHINGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALADWITHINGEOMETRY requires exactly 5 arguments (lon, lat, timestamp, geometry, distance), but got {}", argCount);

            /* Lift constants (geometry + distance) — same shape as EDWITHIN_TGEO_GEO */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                const auto upperValue = Util::toUpperCase(constantValue);
                if (upperValue == "TRUE" || upperValue == "FALSE")
                {
                    dataType = DataTypeProvider::provideDataType(DataType::Type::BOOLEAN);
                }
                else
                {
                    char* endPtr = nullptr;
                    std::strtod(constantValue.c_str(), &endPtr);
                    if (endPtr != nullptr && *endPtr == '\0')
                        dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                    else
                        dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                }
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            /* After lift: [lon, lat, ts, distance, geometry] (geometry pushed last because lifted last in LIFO) */
            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto dist      = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADWithinGeometryLogicalFunction(lon, lat, timestamp, geometry, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALADWITHINGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEDWITHINGEOMETRY */
        case AntlrSQLLexer::TEMPORALEDWITHINGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALEDWITHINGEOMETRY requires exactly 5 arguments (lon, lat, timestamp, geometry, distance), but got {}", argCount);

            /* Lift constants (geometry + distance) — same shape as EDWITHIN_TGEO_GEO */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                const auto upperValue = Util::toUpperCase(constantValue);
                if (upperValue == "TRUE" || upperValue == "FALSE")
                {
                    dataType = DataTypeProvider::provideDataType(DataType::Type::BOOLEAN);
                }
                else
                {
                    char* endPtr = nullptr;
                    std::strtod(constantValue.c_str(), &endPtr);
                    if (endPtr != nullptr && *endPtr == '\0')
                        dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                    else
                        dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                }
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            /* After lift: [lon, lat, ts, distance, geometry] (geometry pushed last because lifted last in LIFO) */
            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto dist      = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDWithinGeometryLogicalFunction(lon, lat, timestamp, geometry, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEDWITHINGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALADWITHINTGEOMETRY */
        case AntlrSQLLexer::TEMPORALADWITHINTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("TEMPORALADWITHINTGEOMETRY requires exactly 7 arguments (lonA, latA, tsA, lonB, latB, tsB, distance), but got {}", argCount);

            /* Lift the distance constant */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADWithinTGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALADWITHINTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEDWITHINTGEOMETRY */
        case AntlrSQLLexer::TEMPORALEDWITHINTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("TEMPORALEDWITHINTGEOMETRY requires exactly 7 arguments (lonA, latA, tsA, lonB, latB, tsB, distance), but got {}", argCount);

            /* Lift the distance constant */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDWithinTGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEDWITHINTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALACONTAINSTGEOMETRY */
        case AntlrSQLLexer::TEMPORALACONTAINSTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALACONTAINSTGEOMETRY requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAContainsTGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALACONTAINSTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALADISJOINTTGEOMETRY */
        case AntlrSQLLexer::TEMPORALADISJOINTTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALADISJOINTTGEOMETRY requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADisjointTGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALADISJOINTTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALAINTERSECTSTGEOMETRY */
        case AntlrSQLLexer::TEMPORALAINTERSECTSTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALAINTERSECTSTGEOMETRY requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAIntersectsTGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALAINTERSECTSTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALATOUCHESTGEOMETRY */
        case AntlrSQLLexer::TEMPORALATOUCHESTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALATOUCHESTGEOMETRY requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalATouchesTGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALATOUCHESTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALECONTAINSTGEOMETRY */
        case AntlrSQLLexer::TEMPORALECONTAINSTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALECONTAINSTGEOMETRY requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEContainsTGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALECONTAINSTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALECOVERSTGEOMETRY */
        case AntlrSQLLexer::TEMPORALECOVERSTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALECOVERSTGEOMETRY requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalECoversTGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALECOVERSTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEDISJOINTTGEOMETRY */
        case AntlrSQLLexer::TEMPORALEDISJOINTTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALEDISJOINTTGEOMETRY requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDisjointTGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEDISJOINTTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEINTERSECTSTGEOMETRY */
        case AntlrSQLLexer::TEMPORALEINTERSECTSTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALEINTERSECTSTGEOMETRY requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEIntersectsTGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEINTERSECTSTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALETOUCHESTGEOMETRY */
        case AntlrSQLLexer::TEMPORALETOUCHESTGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALETOUCHESTGEOMETRY requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalETouchesTGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALETOUCHESTGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NAD_GEOMETRY */
        case AntlrSQLLexer::TEMPORAL_NAD_GEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_NAD_GEOMETRY requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADGeometryLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NAD_GEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALACONTAINSTCBUFFER */
        case AntlrSQLLexer::TEMPORALACONTAINSTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALACONTAINSTCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAContainsTCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALACONTAINSTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALACOVERSTCBUFFER */
        case AntlrSQLLexer::TEMPORALACOVERSTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALACOVERSTCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalACoversTCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALACOVERSTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALADISJOINTTCBUFFER */
        case AntlrSQLLexer::TEMPORALADISJOINTTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALADISJOINTTCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADisjointTCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALADISJOINTTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALAINTERSECTSTCBUFFER */
        case AntlrSQLLexer::TEMPORALAINTERSECTSTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALAINTERSECTSTCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAIntersectsTCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALAINTERSECTSTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALATOUCHESTCBUFFER */
        case AntlrSQLLexer::TEMPORALATOUCHESTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALATOUCHESTCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalATouchesTCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALATOUCHESTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALECONTAINSTCBUFFER */
        case AntlrSQLLexer::TEMPORALECONTAINSTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALECONTAINSTCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEContainsTCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALECONTAINSTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALECOVERSTCBUFFER */
        case AntlrSQLLexer::TEMPORALECOVERSTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALECOVERSTCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalECoversTCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALECOVERSTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEDISJOINTTCBUFFER */
        case AntlrSQLLexer::TEMPORALEDISJOINTTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALEDISJOINTTCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDisjointTCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEDISJOINTTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEINTERSECTSTCBUFFER */
        case AntlrSQLLexer::TEMPORALEINTERSECTSTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALEINTERSECTSTCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEIntersectsTCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEINTERSECTSTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALETOUCHESTCBUFFER */
        case AntlrSQLLexer::TEMPORALETOUCHESTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALETOUCHESTCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalETouchesTCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALETOUCHESTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NAD_TCBUFFER */
        case AntlrSQLLexer::TEMPORAL_NAD_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_NAD_TCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADTCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NAD_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALADWITHINTCBUFFERGEOMETRY */
        case AntlrSQLLexer::TEMPORALADWITHINTCBUFFERGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALADWITHINTCBUFFERGEOMETRY requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADWithinTCbufferGeometryLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALADWITHINTCBUFFERGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEDWITHINTCBUFFERGEOMETRY */
        case AntlrSQLLexer::TEMPORALEDWITHINTCBUFFERGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALEDWITHINTCBUFFERGEOMETRY requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDWithinTCbufferGeometryLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEDWITHINTCBUFFERGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSEQTCBUFFERCBUFFER */
        case AntlrSQLLexer::ALWAYSEQTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYSEQTCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSEQTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYSNETCBUFFERCBUFFER */
        case AntlrSQLLexer::ALWAYSNETCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYSNETCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYSNETCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EVEREQTCBUFFERCBUFFER */
        case AntlrSQLLexer::EVEREQTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVEREQTCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVEREQTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EVERNETCBUFFERCBUFFER */
        case AntlrSQLLexer::EVERNETCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVERNETCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVERNETCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALACONTAINSTCBUFFERCBUFFER */
        case AntlrSQLLexer::TEMPORALACONTAINSTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALACONTAINSTCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAContainsTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALACONTAINSTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALACOVERSTCBUFFERCBUFFER */
        case AntlrSQLLexer::TEMPORALACOVERSTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALACOVERSTCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalACoversTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALACOVERSTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALADISJOINTTCBUFFERCBUFFER */
        case AntlrSQLLexer::TEMPORALADISJOINTTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALADISJOINTTCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADisjointTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALADISJOINTTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALAINTERSECTSTCBUFFERCBUFFER */
        case AntlrSQLLexer::TEMPORALAINTERSECTSTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALAINTERSECTSTCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAIntersectsTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALAINTERSECTSTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALATOUCHESTCBUFFERCBUFFER */
        case AntlrSQLLexer::TEMPORALATOUCHESTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALATOUCHESTCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalATouchesTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALATOUCHESTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALECONTAINSTCBUFFERCBUFFER */
        case AntlrSQLLexer::TEMPORALECONTAINSTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALECONTAINSTCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEContainsTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALECONTAINSTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALECOVERSTCBUFFERCBUFFER */
        case AntlrSQLLexer::TEMPORALECOVERSTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALECOVERSTCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalECoversTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALECOVERSTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEDISJOINTTCBUFFERCBUFFER */
        case AntlrSQLLexer::TEMPORALEDISJOINTTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALEDISJOINTTCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDisjointTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEDISJOINTTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEINTERSECTSTCBUFFERCBUFFER */
        case AntlrSQLLexer::TEMPORALEINTERSECTSTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALEINTERSECTSTCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEIntersectsTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEINTERSECTSTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALETOUCHESTCBUFFERCBUFFER */
        case AntlrSQLLexer::TEMPORALETOUCHESTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORALETOUCHESTCBUFFERCBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalETouchesTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALETOUCHESTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NAD_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::TEMPORAL_NAD_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEMPORAL_NAD_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NAD_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALADWITHINTCBUFFERCBUFFER */
        case AntlrSQLLexer::TEMPORALADWITHINTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALADWITHINTCBUFFERCBUFFER requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADWithinTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALADWITHINTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEDWITHINTCBUFFERCBUFFER */
        case AntlrSQLLexer::TEMPORALEDWITHINTCBUFFERCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORALEDWITHINTCBUFFERCBUFFER requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDWithinTCbufferCbufferLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEDWITHINTCBUFFERCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALADISJOINTTCBUFFERTCBUFFER */
        case AntlrSQLLexer::TEMPORALADISJOINTTCBUFFERTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORALADISJOINTTCBUFFERTCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADisjointTCbufferTCbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALADISJOINTTCBUFFERTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALAINTERSECTSTCBUFFERTCBUFFER */
        case AntlrSQLLexer::TEMPORALAINTERSECTSTCBUFFERTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORALAINTERSECTSTCBUFFERTCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAIntersectsTCbufferTCbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALAINTERSECTSTCBUFFERTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALATOUCHESTCBUFFERTCBUFFER */
        case AntlrSQLLexer::TEMPORALATOUCHESTCBUFFERTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORALATOUCHESTCBUFFERTCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalATouchesTCbufferTCbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALATOUCHESTCBUFFERTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALECOVERSTCBUFFERTCBUFFER */
        case AntlrSQLLexer::TEMPORALECOVERSTCBUFFERTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORALECOVERSTCBUFFERTCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalECoversTCbufferTCbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALECOVERSTCBUFFERTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NAD_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::TEMPORAL_NAD_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_NAD_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNADTCbufferTCbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NAD_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALADWITHINTCBUFFERTCBUFFER */
        case AntlrSQLLexer::TEMPORALADWITHINTCBUFFERTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 9)
                throw InvalidQuerySyntax("TEMPORALADWITHINTCBUFFERTCBUFFER requires exactly 9 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalADWithinTCbufferTCbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALADWITHINTCBUFFERTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORALEDWITHINTCBUFFERTCBUFFER */
        case AntlrSQLLexer::TEMPORALEDWITHINTCBUFFERTCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 9)
                throw InvalidQuerySyntax("TEMPORALEDWITHINTCBUFFERTCBUFFER requires exactly 9 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEDWithinTCbufferTCbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORALEDWITHINTCBUFFERTCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_AT_STBOX */
        case AntlrSQLLexer::TEMPORAL_AT_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_AT_STBOX requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalAtStBoxLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_AT_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EINTERSECTS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::TEMPORAL_EINTERSECTS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_EINTERSECTS_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEIntersectsTCbufferTCbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EINTERSECTS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ETOUCHES_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::TEMPORAL_ETOUCHES_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TEMPORAL_ETOUCHES_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalETouchesTCbufferTCbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ETOUCHES_TCBUFFER_TCBUFFER */


        default:
            /// Check if the function is a constructor for a datatype
            if (const auto dataType = DataTypeProvider::tryProvideDataType(funcName); dataType.has_value())
            {
                if (helpers.top().constantBuilder.empty())
                {
                    throw InvalidQuerySyntax("Expected constant, got nothing at {}", context->getText());
                }
                helpers.top().hasUnnamedAggregation = false;
                auto value = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                auto constFunctionItem = ConstantValueLogicalFunction(*dataType, std::move(value));
                helpers.top().functionBuilder.emplace_back(constFunctionItem);
            }
            else if (funcName == "VAR")
            {
                if (helpers.top().functionBuilder.empty())
                {
                    throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
                }
                const auto& lastArg = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().windowAggs.push_back(std::make_shared<VarAggregationLogicalFunction>(lastArg));
            }
            else if (funcName == "TEMPORAL_SEQUENCE")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TEMPORAL_SEQUENCE requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalSequenceAggregationLogicalFunctionV2::create(lon, lat, ts));
            }
            else if (auto logicalFunction = LogicalFunctionProvider::tryProvide(funcName, helpers.top().functionBuilder))
            {
                /// Remove exactly the functions used to create the 'logicalFunction' from the back of the function builder
                helpers.top().functionBuilder.resize(helpers.top().functionBuilder.size() - logicalFunction.value().getChildren().size());
                helpers.top().functionBuilder.push_back(*logicalFunction);
            }
            else if (funcName == "TEMPORAL_INTERSECTS")
            {
                if (helpers.top().functionBuilder.size() != 3) {
                    throw InvalidQuerySyntax("TEMPORAL_INTERSECTS requires exactly three arguments (lon, lat, timestamp), but got {}", helpers.top().functionBuilder.size());
                }
                const auto ts = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(TemporalIntersectsFunction(lon, lat, ts));
            }
            else
            {
                throw InvalidQuerySyntax("Unknown (aggregation) function: {}, resolved to token type: {}", funcName, tokenType);
            }
    }
}

void AntlrSQLQueryPlanCreator::exitThresholdMinSizeParameter(AntlrSQLParser::ThresholdMinSizeParameterContext* context)
{
    helpers.top().minimumCount = std::stoi(context->getText());
}

void AntlrSQLQueryPlanCreator::enterInlineSource(AntlrSQLParser::InlineSourceContext* context)
{
    const auto type = bindIdentifier(context->type);

    const auto parameters = bindConfigOptions(context->parameters->namedConfigExpression());

    helpers.top().setInlineSource(type, parameters);
}

void AntlrSQLQueryPlanCreator::enterSetOperation(AntlrSQLParser::SetOperationContext*)
{
    AntlrSQLHelper helper;
    helper.isSetOperation = true;
    helpers.push(helper);
}

void AntlrSQLQueryPlanCreator::exitSetOperation(AntlrSQLParser::SetOperationContext* context)
{
    INVARIANT(!helpers.empty(), "the set operation helper should not disappear before this function call");

    auto& helperPlans = helpers.top().queryPlans;
    if (helperPlans.size() < 2)
    {
        throw InvalidQuerySyntax("Union does not have sufficient amount of QueryPlans for unifying.");
    }

    auto rightQuery = std::move(helperPlans.back());
    helperPlans.pop_back();
    auto leftQuery = std::move(helperPlans.back());
    helperPlans.pop_back();
    helpers.pop();

    auto queryPlan = LogicalPlanBuilder::addUnion(std::move(leftQuery), std::move(rightQuery));
    if (!helpers.empty())
    {
        /// we are in a subquery
        helpers.top().queryPlans.push_back(std::move(queryPlan));
    }
    else
    {
        queryPlans.push(std::move(queryPlan));
    }
    AntlrSQLBaseListener::exitSetOperation(context);
}

void AntlrSQLQueryPlanCreator::enterGroupByClause(AntlrSQLParser::GroupByClauseContext* context)
{
    helpers.top().isGroupBy = true;
    AntlrSQLBaseListener::enterGroupByClause(context);
}

void AntlrSQLQueryPlanCreator::exitGroupByClause(AntlrSQLParser::GroupByClauseContext* context)
{
    helpers.top().isGroupBy = false;
    AntlrSQLBaseListener::exitGroupByClause(context);
}
}
