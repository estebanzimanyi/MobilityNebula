/*
    Licensed under the Apache License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

        https://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.
*/

#include <AntlrSQLParser/AntlrSQLQueryPlanCreator.hpp>

#include <cctype>
#include <cstddef>
#include <cstdint>
#include <memory>
#include <optional>
#include <ranges>
#include <string>
#include <utility>
#include <variant>

#include <AntlrSQLBaseListener.h>
#include <AntlrSQLLexer.h>
#include <AntlrSQLParser.h>
#include <ParserRuleContext.h>
#include <AntlrSQLParser/AntlrSQLHelper.hpp>
#include <DataTypes/DataType.hpp>
#include <DataTypes/DataTypeProvider.hpp>
#include <DataTypes/Schema.hpp>
#include <Functions/ArithmeticalFunctions/AddLogicalFunction.hpp>
#include <Functions/ArithmeticalFunctions/DivLogicalFunction.hpp>
#include <Functions/ArithmeticalFunctions/ModuloLogicalFunction.hpp>
#include <Functions/ArithmeticalFunctions/MulLogicalFunction.hpp>
#include <Functions/ArithmeticalFunctions/SubLogicalFunction.hpp>
#include <Functions/BooleanFunctions/AndLogicalFunction.hpp>
#include <Functions/BooleanFunctions/EqualsLogicalFunction.hpp>
#include <Functions/BooleanFunctions/NegateLogicalFunction.hpp>
#include <Functions/BooleanFunctions/OrLogicalFunction.hpp>
#include <Functions/ComparisonFunctions/GreaterEqualsLogicalFunction.hpp>
#include <Functions/ComparisonFunctions/GreaterLogicalFunction.hpp>
#include <Functions/ComparisonFunctions/LessEqualsLogicalFunction.hpp>
#include <Functions/ComparisonFunctions/LessLogicalFunction.hpp>
#include <Functions/ConcatLogicalFunction.hpp>
#include <Functions/ConstantValueLogicalFunction.hpp>
#include <Functions/FieldAccessLogicalFunction.hpp>
#include <Functions/FieldAssignmentLogicalFunction.hpp>
#include <Functions/LogicalFunction.hpp>
#include <Functions/LogicalFunctionProvider.hpp>
#include <Operators/Windows/Aggregations/ArrayAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/AvgAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/CountAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/MaxAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/MedianAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/VarAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalSequenceAggregationLogicalFunctionV2.hpp>
#include <Operators/Windows/Aggregations/MinAggregationLogicalFunction.hpp>
#include <Functions/Meos/EcontainsTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/TemporalIntersectsFunction.hpp>
#include <Operators/Windows/Aggregations/SumAggregationLogicalFunction.hpp>
#include <Operators/Windows/JoinLogicalOperator.hpp>
#include <Operators/Windows/Aggregations/Meos/VarAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalSequenceAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalLengthAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/PairMeetingAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/CrossDistanceAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalNumInstantsAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalNumSequencesAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalNumTimestampsAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TfloatStartValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TfloatEndValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TfloatMinValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TfloatMaxValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TnumberIntegralAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TintStartValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TintEndValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TintMinValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TintMaxValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TnumberAvgValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TnumberTwavgAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalTIntAvgValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalStartTimestamptzAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalEndTimestamptzAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalLowerIncAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalUpperIncAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TpointIsSimpleAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TspatialExtentTransfnAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TnumberExtentTransfnAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/FloatExtentTransfnAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/IntExtentTransfnAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/BigintExtentTransfnAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TimestamptzExtentTransfnAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/FloatUnionTransfnAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/IntUnionTransfnAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/BigintUnionTransfnAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TimestamptzUnionTransfnAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TpointTrajectoryAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TLengthExpAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TgeoCentroidAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TpointAzimuthAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TpointAngularDifferenceAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TgeompointToTgeometryAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalCopyAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TnumberAbsAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TnumberDeltaValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TnumberAngularDifferenceAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalDerivativeAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalAtMaxAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalAtMinAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalMinusMaxAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TemporalMinusMinAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TnpointCumulativeLengthAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TnpointSpeedAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TnpointToTgeompointAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TpointCumulativeLengthAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TpointSpeedAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TpointGetXAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TpointGetYAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TnumberTrendAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TgeoStartValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TgeoEndValueAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TgeoConvexHullAggregationLogicalFunction.hpp>
#include <Operators/Windows/Aggregations/Meos/TpointTwcentroidAggregationLogicalFunction.hpp>
#include <Functions/Meos/TemporalIntersectsGeometryLogicalFunction.hpp>
#include <Functions/Meos/AintersectsTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/TgeoAtStboxLogicalFunction.hpp>
#include <Functions/Meos/AdisjointTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EintersectsTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EcontainsTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EcoversTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EdisjointTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EintersectsTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EtouchesTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AcontainsTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AdisjointTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AintersectsTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AtouchesTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/NadTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/NadTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AdwithinTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AdwithinTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EdisjointTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AtouchesTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EcoversTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AcontainsTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EtouchesTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/NadTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/NadTintIntLogicalFunction.hpp>
#include <Functions/Meos/NadTfloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/NadTintTintLogicalFunction.hpp>
#include <Functions/Meos/TgeoAtGeomLogicalFunction.hpp>
#include <Functions/Meos/TgeoMinusGeomLogicalFunction.hpp>
#include <Functions/Meos/EcontainsTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/EcoversTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/EdisjointTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/EintersectsTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/EtouchesTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/AcontainsTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/AcoversTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/AdisjointTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/AintersectsTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/AtouchesTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/EcontainsTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/EcoversTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/EdisjointTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/EintersectsTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/EtouchesTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AcontainsTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AcoversTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AdisjointTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AintersectsTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AtouchesTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AdisjointTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AintersectsTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AtouchesTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EcoversTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EintersectsTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EtouchesTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/AdwithinTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AdwithinTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/EdwithinTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AdwithinTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TemporalEContainsTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalECoversTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDisjointTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEIntersectsTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalETouchesTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAContainsTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalADisjointTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAIntersectsTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalATouchesTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEContainsTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalECoversTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDisjointTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEIntersectsTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalETouchesTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAContainsTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalADisjointTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalAIntersectsTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalATouchesTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalNADTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalEDWithinTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinTPoseGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinTPoseTPoseLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinTNpointGeometryLogicalFunction.hpp>
#include <Functions/Meos/TemporalADWithinTNpointTNpointLogicalFunction.hpp>
#include <Functions/Meos/NadTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/NadTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/NadTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTintIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeTintIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtTintIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeTintIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtTintIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTintIntLogicalFunction.hpp>
#include <Functions/Meos/EverEqTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/EverEqTintIntLogicalFunction.hpp>
#include <Functions/Meos/EverGeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/EverGeTintIntLogicalFunction.hpp>
#include <Functions/Meos/EverGtTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/EverGtTintIntLogicalFunction.hpp>
#include <Functions/Meos/EverLeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/EverLeTintIntLogicalFunction.hpp>
#include <Functions/Meos/EverLtTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/EverLtTintIntLogicalFunction.hpp>
#include <Functions/Meos/EverNeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/EverNeTintIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqIntTintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeIntTintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtIntTintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeIntTintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtIntTintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeIntTintLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/EverEqFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/EverEqIntTintLogicalFunction.hpp>
#include <Functions/Meos/EverEqTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/EverGeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/EverGeIntTintLogicalFunction.hpp>
#include <Functions/Meos/EverGeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/EverGtFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/EverGtIntTintLogicalFunction.hpp>
#include <Functions/Meos/EverGtTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/EverLeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/EverLeIntTintLogicalFunction.hpp>
#include <Functions/Meos/EverLeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/EverLtFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/EverLtIntTintLogicalFunction.hpp>
#include <Functions/Meos/EverLtTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/EverNeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/EverNeIntTintLogicalFunction.hpp>
#include <Functions/Meos/EverNeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AtouchesTpointGeoLogicalFunction.hpp>
#include <Functions/Meos/EtouchesTpointGeoLogicalFunction.hpp>
#include <Functions/Meos/EverEqTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/EverEqTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EverEqTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EverEqTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EverNeTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/EverNeTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EverNeTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/EverNeTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AboveTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/AfterTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/AfterTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/BackTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/BeforeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/BeforeTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/BelowTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/ContainedTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/ContainedTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/ContainsTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/ContainsTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/EverEqTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/EverNeTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/FrontTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/LeftTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/NadTnpointGeoLogicalFunction.hpp>
#include <Functions/Meos/NadTposeGeoLogicalFunction.hpp>
#include <Functions/Meos/OveraboveTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverafterTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/OverafterTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverbackTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverbelowTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverfrontTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverleftTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverrightTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/RightTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/SameTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/SameTspatialTspatialLogicalFunction.hpp>
#include <Functions/Meos/TboolEndValueLogicalFunction.hpp>
#include <Functions/Meos/TboolStartValueLogicalFunction.hpp>
#include <Functions/Meos/TemporalCmpLogicalFunction.hpp>
#include <Functions/Meos/TemporalDyntimewarpDistanceLogicalFunction.hpp>
#include <Functions/Meos/TemporalEqLogicalFunction.hpp>
#include <Functions/Meos/TemporalFrechetDistanceLogicalFunction.hpp>
#include <Functions/Meos/TemporalGeLogicalFunction.hpp>
#include <Functions/Meos/TemporalGtLogicalFunction.hpp>
#include <Functions/Meos/TemporalHausdorffDistanceLogicalFunction.hpp>
#include <Functions/Meos/TemporalLeLogicalFunction.hpp>
#include <Functions/Meos/TemporalLtLogicalFunction.hpp>
#include <Functions/Meos/TemporalNeLogicalFunction.hpp>
#include <Functions/Meos/TnpointLengthLogicalFunction.hpp>
#include <Functions/Meos/TboolToTintLogicalFunction.hpp>
#include <Functions/Meos/TcbufferToTfloatLogicalFunction.hpp>
#include <Functions/Meos/TfloatCeilLogicalFunction.hpp>
#include <Functions/Meos/TfloatExpLogicalFunction.hpp>
#include <Functions/Meos/TfloatFloorLogicalFunction.hpp>
#include <Functions/Meos/TfloatLnLogicalFunction.hpp>
#include <Functions/Meos/TfloatLog10LogicalFunction.hpp>
#include <Functions/Meos/TfloatRadiansLogicalFunction.hpp>
#include <Functions/Meos/TfloatToTintLogicalFunction.hpp>
#include <Functions/Meos/TintToTfloatLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/AfterTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/BeforeTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/ContainedTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/ContainsTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/LeftTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/NadTcbufferStboxLogicalFunction.hpp>
#include <Functions/Meos/NadTfloatTboxLogicalFunction.hpp>
#include <Functions/Meos/NadTgeoStboxLogicalFunction.hpp>
#include <Functions/Meos/NadTintTboxLogicalFunction.hpp>
#include <Functions/Meos/NadTnpointStboxLogicalFunction.hpp>
#include <Functions/Meos/NadTposeStboxLogicalFunction.hpp>
#include <Functions/Meos/OverafterTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/OverleftTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/OverrightTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/RightTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/SameTnumberTboxLogicalFunction.hpp>
#include <Functions/Meos/AboveStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/AboveTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/AdjacentStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/AfterStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/AfterTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/AfterTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/BackStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/BackTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/BeforeStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/BeforeTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/BeforeTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/BelowStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/BelowTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/ContainedStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/ContainedTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/ContainedTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/ContainsStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/ContainsTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/ContainsTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/FrontStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/FrontTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/LeftStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/LeftTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/LeftTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OveraboveStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OveraboveTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverafterStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverafterTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverafterTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverbackStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverbackTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverbelowStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverbelowTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverfrontStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverfrontTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverlapsStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverleftStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverleftTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverleftTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/OverrightStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/OverrightTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverrightTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/RightStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/RightTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/RightTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/SameStboxTspatialLogicalFunction.hpp>
#include <Functions/Meos/SameTboxTnumberLogicalFunction.hpp>
#include <Functions/Meos/SameTspatialStboxLogicalFunction.hpp>
#include <Functions/Meos/TpointLengthLogicalFunction.hpp>
#include <Functions/Meos/AboveStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/AdjacentStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/AfterStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/BackStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/BeforeStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/BelowStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/ContainedStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/ContainsStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/FrontStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/LeftStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/NadStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OveraboveStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverafterStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverbackStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverbelowStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverfrontStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverlapsStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverleftStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/OverrightStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/RightStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/SameStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/StboxCmpLogicalFunction.hpp>
#include <Functions/Meos/StboxEqLogicalFunction.hpp>
#include <Functions/Meos/StboxGeLogicalFunction.hpp>
#include <Functions/Meos/StboxGtLogicalFunction.hpp>
#include <Functions/Meos/StboxLeLogicalFunction.hpp>
#include <Functions/Meos/StboxLtLogicalFunction.hpp>
#include <Functions/Meos/StboxNeLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/AfterTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/BeforeTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/ContainedTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/ContainsTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/LeftTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverafterTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverleftTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverrightTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/RightTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/SameTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/EverEqTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/EverEqTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/EverNeTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/EverNeTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/NadTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/NadTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/AddTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/DivTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/MulTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/SubTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/TcontainsTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TcontainsTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TcoversTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TcoversTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTnumberTnumberLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainedSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/LeftSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/RightSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/OverlapsSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/OverleftSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/OverrightSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainsSetSetLogicalFunction.hpp>
#include <Functions/Meos/ContainedSetSetLogicalFunction.hpp>
#include <Functions/Meos/LeftSetSetLogicalFunction.hpp>
#include <Functions/Meos/RightSetSetLogicalFunction.hpp>
#include <Functions/Meos/OverlapsSetSetLogicalFunction.hpp>
#include <Functions/Meos/OverleftSetSetLogicalFunction.hpp>
#include <Functions/Meos/OverrightSetSetLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpansetSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainedSpansetSpanLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpansetSpanLogicalFunction.hpp>
#include <Functions/Meos/LeftSpansetSpanLogicalFunction.hpp>
#include <Functions/Meos/RightSpansetSpanLogicalFunction.hpp>
#include <Functions/Meos/OverlapsSpansetSpanLogicalFunction.hpp>
#include <Functions/Meos/OverleftSpansetSpanLogicalFunction.hpp>
#include <Functions/Meos/OverrightSpansetSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpansetSpansetLogicalFunction.hpp>
#include <Functions/Meos/ContainedSpansetSpansetLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpansetSpansetLogicalFunction.hpp>
#include <Functions/Meos/LeftSpansetSpansetLogicalFunction.hpp>
#include <Functions/Meos/RightSpansetSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverlapsSpansetSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverleftSpansetSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverrightSpansetSpansetLogicalFunction.hpp>
#include <Functions/Meos/UnionSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/MinusSpanSpanLogicalFunction.hpp>
#include <Functions/Meos/UnionSetSetLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSetSetLogicalFunction.hpp>
#include <Functions/Meos/MinusSetSetLogicalFunction.hpp>
#include <Functions/Meos/UnionSpansetSpansetLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpansetSpansetLogicalFunction.hpp>
#include <Functions/Meos/MinusSpansetSpansetLogicalFunction.hpp>
#include <Functions/Meos/UnionSpanSpansetLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpanSpansetLogicalFunction.hpp>
#include <Functions/Meos/MinusSpanSpansetLogicalFunction.hpp>
#include <Functions/Meos/UnionSpansetSpanLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpansetSpanLogicalFunction.hpp>
#include <Functions/Meos/MinusSpansetSpanLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpanFloatLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpanIntLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpansetBigintLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpansetFloatLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpansetIntLogicalFunction.hpp>
#include <Functions/Meos/BeforeTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/ContainedBigintSetLogicalFunction.hpp>
#include <Functions/Meos/ContainedBigintSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainedBigintSpansetLogicalFunction.hpp>
#include <Functions/Meos/ContainedFloatSetLogicalFunction.hpp>
#include <Functions/Meos/ContainedFloatSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainedFloatSpansetLogicalFunction.hpp>
#include <Functions/Meos/ContainedIntSetLogicalFunction.hpp>
#include <Functions/Meos/ContainedIntSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainedIntSpansetLogicalFunction.hpp>
#include <Functions/Meos/ContainedTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/ContainsSetBigintLogicalFunction.hpp>
#include <Functions/Meos/ContainsSetFloatLogicalFunction.hpp>
#include <Functions/Meos/ContainsSetIntLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpanBigintLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpanFloatLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpanIntLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpansetBigintLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpansetFloatLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpansetIntLogicalFunction.hpp>
#include <Functions/Meos/ContainsTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/DistanceSetIntLogicalFunction.hpp>
#include <Functions/Meos/DistanceSpanIntLogicalFunction.hpp>
#include <Functions/Meos/DistanceSpansetIntLogicalFunction.hpp>
#include <Functions/Meos/IntersectionFloatSetLogicalFunction.hpp>
#include <Functions/Meos/IntersectionIntSetLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSetBigintLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSetFloatLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSetIntLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpanBigintLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpanFloatLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpanIntLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpansetBigintLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpansetFloatLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpansetIntLogicalFunction.hpp>
#include <Functions/Meos/IntersectionStboxStboxLogicalFunction.hpp>
#include <Functions/Meos/IntersectionTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/LeftBigintSetLogicalFunction.hpp>
#include <Functions/Meos/LeftBigintSpanLogicalFunction.hpp>
#include <Functions/Meos/LeftBigintSpansetLogicalFunction.hpp>
#include <Functions/Meos/LeftFloatSetLogicalFunction.hpp>
#include <Functions/Meos/LeftFloatSpanLogicalFunction.hpp>
#include <Functions/Meos/LeftFloatSpansetLogicalFunction.hpp>
#include <Functions/Meos/LeftIntSetLogicalFunction.hpp>
#include <Functions/Meos/LeftIntSpanLogicalFunction.hpp>
#include <Functions/Meos/LeftIntSpansetLogicalFunction.hpp>
#include <Functions/Meos/LeftSetBigintLogicalFunction.hpp>
#include <Functions/Meos/LeftSetFloatLogicalFunction.hpp>
#include <Functions/Meos/LeftSetIntLogicalFunction.hpp>
#include <Functions/Meos/LeftSpanBigintLogicalFunction.hpp>
#include <Functions/Meos/LeftSpanFloatLogicalFunction.hpp>
#include <Functions/Meos/LeftSpanIntLogicalFunction.hpp>
#include <Functions/Meos/LeftSpansetBigintLogicalFunction.hpp>
#include <Functions/Meos/LeftSpansetFloatLogicalFunction.hpp>
#include <Functions/Meos/LeftSpansetIntLogicalFunction.hpp>
#include <Functions/Meos/LeftTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/MinusBigintSetLogicalFunction.hpp>
#include <Functions/Meos/MinusBigintSpanLogicalFunction.hpp>
#include <Functions/Meos/MinusBigintSpansetLogicalFunction.hpp>
#include <Functions/Meos/MinusFloatSetLogicalFunction.hpp>
#include <Functions/Meos/MinusFloatSpanLogicalFunction.hpp>
#include <Functions/Meos/MinusFloatSpansetLogicalFunction.hpp>
#include <Functions/Meos/MinusIntSetLogicalFunction.hpp>
#include <Functions/Meos/MinusIntSpanLogicalFunction.hpp>
#include <Functions/Meos/MinusIntSpansetLogicalFunction.hpp>
#include <Functions/Meos/MinusSetBigintLogicalFunction.hpp>
#include <Functions/Meos/MinusSetFloatLogicalFunction.hpp>
#include <Functions/Meos/MinusSetIntLogicalFunction.hpp>
#include <Functions/Meos/MinusSpanBigintLogicalFunction.hpp>
#include <Functions/Meos/MinusSpanFloatLogicalFunction.hpp>
#include <Functions/Meos/MinusSpanIntLogicalFunction.hpp>
#include <Functions/Meos/MinusSpansetBigintLogicalFunction.hpp>
#include <Functions/Meos/MinusSpansetFloatLogicalFunction.hpp>
#include <Functions/Meos/MinusSpansetIntLogicalFunction.hpp>
#include <Functions/Meos/NadTboxintTboxintLogicalFunction.hpp>
#include <Functions/Meos/OverafterTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/OverleftBigintSetLogicalFunction.hpp>
#include <Functions/Meos/OverleftBigintSpanLogicalFunction.hpp>
#include <Functions/Meos/OverleftBigintSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverleftFloatSetLogicalFunction.hpp>
#include <Functions/Meos/OverleftFloatSpanLogicalFunction.hpp>
#include <Functions/Meos/OverleftFloatSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverleftIntSetLogicalFunction.hpp>
#include <Functions/Meos/OverleftIntSpanLogicalFunction.hpp>
#include <Functions/Meos/OverleftIntSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverleftSetBigintLogicalFunction.hpp>
#include <Functions/Meos/OverleftSetFloatLogicalFunction.hpp>
#include <Functions/Meos/OverleftSetIntLogicalFunction.hpp>
#include <Functions/Meos/OverleftSpanBigintLogicalFunction.hpp>
#include <Functions/Meos/OverleftSpanFloatLogicalFunction.hpp>
#include <Functions/Meos/OverleftSpanIntLogicalFunction.hpp>
#include <Functions/Meos/OverleftSpansetBigintLogicalFunction.hpp>
#include <Functions/Meos/OverleftSpansetFloatLogicalFunction.hpp>
#include <Functions/Meos/OverleftSpansetIntLogicalFunction.hpp>
#include <Functions/Meos/OverleftTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/OverrightBigintSetLogicalFunction.hpp>
#include <Functions/Meos/OverrightBigintSpanLogicalFunction.hpp>
#include <Functions/Meos/OverrightBigintSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverrightFloatSetLogicalFunction.hpp>
#include <Functions/Meos/OverrightFloatSpanLogicalFunction.hpp>
#include <Functions/Meos/OverrightFloatSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverrightIntSetLogicalFunction.hpp>
#include <Functions/Meos/OverrightIntSpanLogicalFunction.hpp>
#include <Functions/Meos/OverrightIntSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverrightSetBigintLogicalFunction.hpp>
#include <Functions/Meos/OverrightSetFloatLogicalFunction.hpp>
#include <Functions/Meos/OverrightSetIntLogicalFunction.hpp>
#include <Functions/Meos/OverrightSpanBigintLogicalFunction.hpp>
#include <Functions/Meos/OverrightSpanFloatLogicalFunction.hpp>
#include <Functions/Meos/OverrightSpanIntLogicalFunction.hpp>
#include <Functions/Meos/OverrightSpansetBigintLogicalFunction.hpp>
#include <Functions/Meos/OverrightSpansetFloatLogicalFunction.hpp>
#include <Functions/Meos/OverrightSpansetIntLogicalFunction.hpp>
#include <Functions/Meos/OverrightTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/RightBigintSetLogicalFunction.hpp>
#include <Functions/Meos/RightBigintSpanLogicalFunction.hpp>
#include <Functions/Meos/RightBigintSpansetLogicalFunction.hpp>
#include <Functions/Meos/RightFloatSetLogicalFunction.hpp>
#include <Functions/Meos/RightFloatSpanLogicalFunction.hpp>
#include <Functions/Meos/RightFloatSpansetLogicalFunction.hpp>
#include <Functions/Meos/RightIntSetLogicalFunction.hpp>
#include <Functions/Meos/RightIntSpanLogicalFunction.hpp>
#include <Functions/Meos/RightIntSpansetLogicalFunction.hpp>
#include <Functions/Meos/RightSetBigintLogicalFunction.hpp>
#include <Functions/Meos/RightSetFloatLogicalFunction.hpp>
#include <Functions/Meos/RightSetIntLogicalFunction.hpp>
#include <Functions/Meos/RightSpanBigintLogicalFunction.hpp>
#include <Functions/Meos/RightSpanFloatLogicalFunction.hpp>
#include <Functions/Meos/RightSpanIntLogicalFunction.hpp>
#include <Functions/Meos/RightSpansetBigintLogicalFunction.hpp>
#include <Functions/Meos/RightSpansetFloatLogicalFunction.hpp>
#include <Functions/Meos/RightSpansetIntLogicalFunction.hpp>
#include <Functions/Meos/RightTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/SameTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/TboxEqLogicalFunction.hpp>
#include <Functions/Meos/TboxGeLogicalFunction.hpp>
#include <Functions/Meos/TboxGtLogicalFunction.hpp>
#include <Functions/Meos/TboxLeLogicalFunction.hpp>
#include <Functions/Meos/TboxLtLogicalFunction.hpp>
#include <Functions/Meos/TboxNeLogicalFunction.hpp>
#include <Functions/Meos/UnionBigintSetLogicalFunction.hpp>
#include <Functions/Meos/UnionBigintSpanLogicalFunction.hpp>
#include <Functions/Meos/UnionBigintSpansetLogicalFunction.hpp>
#include <Functions/Meos/UnionFloatSetLogicalFunction.hpp>
#include <Functions/Meos/UnionFloatSpanLogicalFunction.hpp>
#include <Functions/Meos/UnionFloatSpansetLogicalFunction.hpp>
#include <Functions/Meos/UnionIntSetLogicalFunction.hpp>
#include <Functions/Meos/UnionIntSpanLogicalFunction.hpp>
#include <Functions/Meos/UnionIntSpansetLogicalFunction.hpp>
#include <Functions/Meos/UnionSetBigintLogicalFunction.hpp>
#include <Functions/Meos/UnionSetFloatLogicalFunction.hpp>
#include <Functions/Meos/UnionSetIntLogicalFunction.hpp>
#include <Functions/Meos/UnionSpanBigintLogicalFunction.hpp>
#include <Functions/Meos/UnionSpanFloatLogicalFunction.hpp>
#include <Functions/Meos/UnionSpanIntLogicalFunction.hpp>
#include <Functions/Meos/UnionSpansetBigintLogicalFunction.hpp>
#include <Functions/Meos/UnionSpansetFloatLogicalFunction.hpp>
#include <Functions/Meos/UnionSpansetIntLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpanDateLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpanSpansetLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpanTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpansetDateLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpansetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/AfterDateSpanLogicalFunction.hpp>
#include <Functions/Meos/AfterDateSpansetLogicalFunction.hpp>
#include <Functions/Meos/AfterSetDateLogicalFunction.hpp>
#include <Functions/Meos/AfterSetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/AfterSpanDateLogicalFunction.hpp>
#include <Functions/Meos/AfterSpanTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/AfterSpansetDateLogicalFunction.hpp>
#include <Functions/Meos/AfterSpansetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/AfterTimestamptzSetLogicalFunction.hpp>
#include <Functions/Meos/AfterTimestamptzSpanLogicalFunction.hpp>
#include <Functions/Meos/AfterTimestamptzSpansetLogicalFunction.hpp>
#include <Functions/Meos/BeforeDateSetLogicalFunction.hpp>
#include <Functions/Meos/BeforeDateSpanLogicalFunction.hpp>
#include <Functions/Meos/BeforeDateSpansetLogicalFunction.hpp>
#include <Functions/Meos/BeforeSetDateLogicalFunction.hpp>
#include <Functions/Meos/BeforeSetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/BeforeSpanDateLogicalFunction.hpp>
#include <Functions/Meos/BeforeSpanTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/BeforeSpansetDateLogicalFunction.hpp>
#include <Functions/Meos/BeforeSpansetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/BeforeTimestamptzSetLogicalFunction.hpp>
#include <Functions/Meos/BeforeTimestamptzSpanLogicalFunction.hpp>
#include <Functions/Meos/BeforeTimestamptzSpansetLogicalFunction.hpp>
#include <Functions/Meos/ContainedDateSetLogicalFunction.hpp>
#include <Functions/Meos/ContainedDateSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainedDateSpansetLogicalFunction.hpp>
#include <Functions/Meos/ContainedSpanSpansetLogicalFunction.hpp>
#include <Functions/Meos/ContainedTimestamptzSetLogicalFunction.hpp>
#include <Functions/Meos/ContainedTimestamptzSpanLogicalFunction.hpp>
#include <Functions/Meos/ContainedTimestamptzSpansetLogicalFunction.hpp>
#include <Functions/Meos/ContainsSetDateLogicalFunction.hpp>
#include <Functions/Meos/ContainsSetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpanDateLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpanSpansetLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpanTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpansetDateLogicalFunction.hpp>
#include <Functions/Meos/ContainsSpansetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/LeftSpanSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverafterDateSetLogicalFunction.hpp>
#include <Functions/Meos/OverafterDateSpanLogicalFunction.hpp>
#include <Functions/Meos/OverafterDateSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverafterSetDateLogicalFunction.hpp>
#include <Functions/Meos/OverafterSetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/OverafterSpanDateLogicalFunction.hpp>
#include <Functions/Meos/OverafterSpanTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/OverafterSpansetDateLogicalFunction.hpp>
#include <Functions/Meos/OverafterSpansetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/OverafterTimestamptzSetLogicalFunction.hpp>
#include <Functions/Meos/OverafterTimestamptzSpanLogicalFunction.hpp>
#include <Functions/Meos/OverafterTimestamptzSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeDateSetLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeDateSpanLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeDateSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeSetDateLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeSetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeSpanDateLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeSpanTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeSpansetDateLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeSpansetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTimestamptzSetLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTimestamptzSpanLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTimestamptzSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverlapsSpanSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverleftSpanSpansetLogicalFunction.hpp>
#include <Functions/Meos/OverrightSpanSpansetLogicalFunction.hpp>
#include <Functions/Meos/RightSpanSpansetLogicalFunction.hpp>
#include <Functions/Meos/IntspanLowerLogicalFunction.hpp>
#include <Functions/Meos/IntspanUpperLogicalFunction.hpp>
#include <Functions/Meos/IntspanWidthLogicalFunction.hpp>
#include <Functions/Meos/BigintspanLowerLogicalFunction.hpp>
#include <Functions/Meos/BigintspanUpperLogicalFunction.hpp>
#include <Functions/Meos/BigintspanWidthLogicalFunction.hpp>
#include <Functions/Meos/FloatspanLowerLogicalFunction.hpp>
#include <Functions/Meos/FloatspanUpperLogicalFunction.hpp>
#include <Functions/Meos/FloatspanWidthLogicalFunction.hpp>
#include <Functions/Meos/DatespanLowerLogicalFunction.hpp>
#include <Functions/Meos/DatespanUpperLogicalFunction.hpp>
#include <Functions/Meos/TstzspanLowerLogicalFunction.hpp>
#include <Functions/Meos/TstzspanUpperLogicalFunction.hpp>
#include <Functions/Meos/IntspansetLowerLogicalFunction.hpp>
#include <Functions/Meos/IntspansetUpperLogicalFunction.hpp>
#include <Functions/Meos/BigintspansetLowerLogicalFunction.hpp>
#include <Functions/Meos/BigintspansetUpperLogicalFunction.hpp>
#include <Functions/Meos/FloatspansetLowerLogicalFunction.hpp>
#include <Functions/Meos/FloatspansetUpperLogicalFunction.hpp>
#include <Functions/Meos/TstzspansetLowerLogicalFunction.hpp>
#include <Functions/Meos/TstzspansetUpperLogicalFunction.hpp>
#include <Functions/Meos/IntsetStartValueLogicalFunction.hpp>
#include <Functions/Meos/IntsetEndValueLogicalFunction.hpp>
#include <Functions/Meos/BigintsetStartValueLogicalFunction.hpp>
#include <Functions/Meos/BigintsetEndValueLogicalFunction.hpp>
#include <Functions/Meos/FloatsetStartValueLogicalFunction.hpp>
#include <Functions/Meos/FloatsetEndValueLogicalFunction.hpp>
#include <Functions/Meos/DatesetStartValueLogicalFunction.hpp>
#include <Functions/Meos/DatesetEndValueLogicalFunction.hpp>
#include <Functions/Meos/TstzsetStartValueLogicalFunction.hpp>
#include <Functions/Meos/TstzsetEndValueLogicalFunction.hpp>
#include <Functions/Meos/SpanLowerIncLogicalFunction.hpp>
#include <Functions/Meos/SpanUpperIncLogicalFunction.hpp>
#include <Functions/Meos/SpansetLowerIncLogicalFunction.hpp>
#include <Functions/Meos/SpansetUpperIncLogicalFunction.hpp>
#include <Functions/Meos/SetNumValuesLogicalFunction.hpp>
#include <Functions/Meos/SpansetNumSpansLogicalFunction.hpp>
#include <Functions/Meos/CbufferCmpLogicalFunction.hpp>
#include <Functions/Meos/CbufferEqLogicalFunction.hpp>
#include <Functions/Meos/CbufferGeLogicalFunction.hpp>
#include <Functions/Meos/CbufferGtLogicalFunction.hpp>
#include <Functions/Meos/CbufferLeLogicalFunction.hpp>
#include <Functions/Meos/CbufferLtLogicalFunction.hpp>
#include <Functions/Meos/CbufferNeLogicalFunction.hpp>
#include <Functions/Meos/CbufferNsameLogicalFunction.hpp>
#include <Functions/Meos/CbufferSameLogicalFunction.hpp>
#include <Functions/Meos/ContainsCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/CoversCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/DisjointCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/IntersectsCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/NpointCmpLogicalFunction.hpp>
#include <Functions/Meos/NpointEqLogicalFunction.hpp>
#include <Functions/Meos/NpointGeLogicalFunction.hpp>
#include <Functions/Meos/NpointGtLogicalFunction.hpp>
#include <Functions/Meos/NpointLeLogicalFunction.hpp>
#include <Functions/Meos/NpointLtLogicalFunction.hpp>
#include <Functions/Meos/NpointNeLogicalFunction.hpp>
#include <Functions/Meos/NpointSameLogicalFunction.hpp>
#include <Functions/Meos/NsegmentCmpLogicalFunction.hpp>
#include <Functions/Meos/NsegmentEqLogicalFunction.hpp>
#include <Functions/Meos/NsegmentGeLogicalFunction.hpp>
#include <Functions/Meos/NsegmentGtLogicalFunction.hpp>
#include <Functions/Meos/NsegmentLeLogicalFunction.hpp>
#include <Functions/Meos/NsegmentLtLogicalFunction.hpp>
#include <Functions/Meos/NsegmentNeLogicalFunction.hpp>
#include <Functions/Meos/PoseCmpLogicalFunction.hpp>
#include <Functions/Meos/PoseEqLogicalFunction.hpp>
#include <Functions/Meos/PoseGeLogicalFunction.hpp>
#include <Functions/Meos/PoseGtLogicalFunction.hpp>
#include <Functions/Meos/PoseLeLogicalFunction.hpp>
#include <Functions/Meos/PoseLtLogicalFunction.hpp>
#include <Functions/Meos/PoseNeLogicalFunction.hpp>
#include <Functions/Meos/PoseNsameLogicalFunction.hpp>
#include <Functions/Meos/PoseSameLogicalFunction.hpp>
#include <Functions/Meos/TouchesCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/AdjacentSpanBigintLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/AfterDateSetLogicalFunction.hpp>
#include <Functions/Meos/AfterTboxTboxLogicalFunction.hpp>
#include <Functions/Meos/DatespansetEndDateLogicalFunction.hpp>
#include <Functions/Meos/DatespansetNumDatesLogicalFunction.hpp>
#include <Functions/Meos/DatespansetStartDateLogicalFunction.hpp>
#include <Functions/Meos/DistanceBigintsetBigintsetLogicalFunction.hpp>
#include <Functions/Meos/DistanceBigintspanBigintspanLogicalFunction.hpp>
#include <Functions/Meos/DistanceBigintspansetBigintspanLogicalFunction.hpp>
#include <Functions/Meos/DistanceBigintspansetBigintspansetLogicalFunction.hpp>
#include <Functions/Meos/DistanceCbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/DistanceDatesetDatesetLogicalFunction.hpp>
#include <Functions/Meos/DistanceDatespanDatespanLogicalFunction.hpp>
#include <Functions/Meos/DistanceDatespansetDatespanLogicalFunction.hpp>
#include <Functions/Meos/DistanceDatespansetDatespansetLogicalFunction.hpp>
#include <Functions/Meos/DistanceFloatsetFloatsetLogicalFunction.hpp>
#include <Functions/Meos/DistanceFloatspanFloatspanLogicalFunction.hpp>
#include <Functions/Meos/DistanceFloatspansetFloatspanLogicalFunction.hpp>
#include <Functions/Meos/DistanceFloatspansetFloatspansetLogicalFunction.hpp>
#include <Functions/Meos/DistanceIntsetIntsetLogicalFunction.hpp>
#include <Functions/Meos/DistanceIntspanIntspanLogicalFunction.hpp>
#include <Functions/Meos/DistanceIntspansetIntspanLogicalFunction.hpp>
#include <Functions/Meos/DistanceIntspansetIntspansetLogicalFunction.hpp>
#include <Functions/Meos/DistancePosePoseLogicalFunction.hpp>
#include <Functions/Meos/DistanceSetBigintLogicalFunction.hpp>
#include <Functions/Meos/DistanceSetDateLogicalFunction.hpp>
#include <Functions/Meos/DistanceSetFloatLogicalFunction.hpp>
#include <Functions/Meos/DistanceSetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/DistanceSpanBigintLogicalFunction.hpp>
#include <Functions/Meos/DistanceSpanDateLogicalFunction.hpp>
#include <Functions/Meos/DistanceSpanFloatLogicalFunction.hpp>
#include <Functions/Meos/DistanceSpanTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/DistanceSpansetBigintLogicalFunction.hpp>
#include <Functions/Meos/DistanceSpansetDateLogicalFunction.hpp>
#include <Functions/Meos/DistanceSpansetFloatLogicalFunction.hpp>
#include <Functions/Meos/DistanceSpansetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/DistanceTstzsetTstzsetLogicalFunction.hpp>
#include <Functions/Meos/DistanceTstzspanTstzspanLogicalFunction.hpp>
#include <Functions/Meos/DistanceTstzspansetTstzspanLogicalFunction.hpp>
#include <Functions/Meos/DistanceTstzspansetTstzspansetLogicalFunction.hpp>
#include <Functions/Meos/NadTboxfloatTboxfloatLogicalFunction.hpp>
#include <Functions/Meos/SetCmpLogicalFunction.hpp>
#include <Functions/Meos/SetEqLogicalFunction.hpp>
#include <Functions/Meos/SetGeLogicalFunction.hpp>
#include <Functions/Meos/SetGtLogicalFunction.hpp>
#include <Functions/Meos/SetLeLogicalFunction.hpp>
#include <Functions/Meos/SetLtLogicalFunction.hpp>
#include <Functions/Meos/SetNeLogicalFunction.hpp>
#include <Functions/Meos/SpanCmpLogicalFunction.hpp>
#include <Functions/Meos/SpanEqLogicalFunction.hpp>
#include <Functions/Meos/SpanGeLogicalFunction.hpp>
#include <Functions/Meos/SpanGtLogicalFunction.hpp>
#include <Functions/Meos/SpanLeLogicalFunction.hpp>
#include <Functions/Meos/SpanLtLogicalFunction.hpp>
#include <Functions/Meos/SpanNeLogicalFunction.hpp>
#include <Functions/Meos/SpansetCmpLogicalFunction.hpp>
#include <Functions/Meos/SpansetEqLogicalFunction.hpp>
#include <Functions/Meos/SpansetGeLogicalFunction.hpp>
#include <Functions/Meos/SpansetGtLogicalFunction.hpp>
#include <Functions/Meos/SpansetLeLogicalFunction.hpp>
#include <Functions/Meos/SpansetLtLogicalFunction.hpp>
#include <Functions/Meos/SpansetNeLogicalFunction.hpp>
#include <Functions/Meos/TboxCmpLogicalFunction.hpp>
#include <Functions/Meos/TstzspansetEndTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/TstzspansetNumTimestampsLogicalFunction.hpp>
#include <Functions/Meos/TstzspansetStartTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/AddFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/AddIntTintLogicalFunction.hpp>
#include <Functions/Meos/AddTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/AddTintIntLogicalFunction.hpp>
#include <Functions/Meos/DivFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/DivIntTintLogicalFunction.hpp>
#include <Functions/Meos/DivTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/DivTintIntLogicalFunction.hpp>
#include <Functions/Meos/SubFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/SubIntTintLogicalFunction.hpp>
#include <Functions/Meos/SubTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/SubTintIntLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTintIntLogicalFunction.hpp>
#include <Functions/Meos/TeqFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/TeqIntTintLogicalFunction.hpp>
#include <Functions/Meos/TeqTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/TeqTintIntLogicalFunction.hpp>
#include <Functions/Meos/TfloatFromBaseTempLogicalFunction.hpp>
#include <Functions/Meos/TfloatMinusValueLogicalFunction.hpp>
#include <Functions/Meos/TfloatShiftValueLogicalFunction.hpp>
#include <Functions/Meos/TgeFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/TgeIntTintLogicalFunction.hpp>
#include <Functions/Meos/TgeTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/TgeTintIntLogicalFunction.hpp>
#include <Functions/Meos/TgtFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/TgtIntTintLogicalFunction.hpp>
#include <Functions/Meos/TgtTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/TgtTintIntLogicalFunction.hpp>
#include <Functions/Meos/TintFromBaseTempLogicalFunction.hpp>
#include <Functions/Meos/TintMinusValueLogicalFunction.hpp>
#include <Functions/Meos/TintShiftValueLogicalFunction.hpp>
#include <Functions/Meos/TleFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/TleIntTintLogicalFunction.hpp>
#include <Functions/Meos/TleTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/TleTintIntLogicalFunction.hpp>
#include <Functions/Meos/TltFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/TltIntTintLogicalFunction.hpp>
#include <Functions/Meos/TltTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/TltTintIntLogicalFunction.hpp>
#include <Functions/Meos/TneFloatTfloatLogicalFunction.hpp>
#include <Functions/Meos/TneIntTintLogicalFunction.hpp>
#include <Functions/Meos/TneTfloatFloatLogicalFunction.hpp>
#include <Functions/Meos/TneTintIntLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqNpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqPoseTposeLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTposePoseLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeNpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNePoseTposeLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTposePoseLogicalFunction.hpp>
#include <Functions/Meos/EverEqBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/EverEqCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EverEqNpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/EverEqPoseTposeLogicalFunction.hpp>
#include <Functions/Meos/EverEqTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/EverEqTposePoseLogicalFunction.hpp>
#include <Functions/Meos/EverNeBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/EverNeCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EverNeNpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/EverNePoseTposeLogicalFunction.hpp>
#include <Functions/Meos/EverNeTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/EverNeTposePoseLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTextTtextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTtextTextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtTextTtextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysGtTtextTextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtTextTtextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysLtTtextTextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/EverEqTextTtextLogicalFunction.hpp>
#include <Functions/Meos/EverEqTtextTextLogicalFunction.hpp>
#include <Functions/Meos/EverGeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/EverGeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/EverGtTextTtextLogicalFunction.hpp>
#include <Functions/Meos/EverGtTtextTextLogicalFunction.hpp>
#include <Functions/Meos/EverLeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/EverLeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/EverLtTextTtextLogicalFunction.hpp>
#include <Functions/Meos/EverLtTtextTextLogicalFunction.hpp>
#include <Functions/Meos/EverNeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/EverNeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TeqTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TeqTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TextcatTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TextcatTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TgeTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TgeTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TgtTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TgtTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TleTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TleTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TltTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TltTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TneTextTtextLogicalFunction.hpp>
#include <Functions/Meos/TneTtextTextLogicalFunction.hpp>
#include <Functions/Meos/TtextFromBaseTempLogicalFunction.hpp>
#include <Functions/Meos/TtextMinusValueLogicalFunction.hpp>
#include <Functions/Meos/AcontainsGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EcoversGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EverEqGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/EverNeGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TcontainsGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TcontainsTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/TcoversGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TcoversTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/TdisjointGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TdisjointTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/TeqGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TeqTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/TintersectsGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TintersectsTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/TneGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TneTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/TtouchesGeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TtouchesTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/AcontainsCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AcontainsGeoTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AcoversCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/AcoversGeoTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EcontainsCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/EcoversCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TcontainsCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TcontainsGeoTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TcontainsTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TcontainsTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/TcoversCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TcoversGeoTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TcoversTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TcoversTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/TdisjointCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TdisjointGeoTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TdisjointTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TdisjointTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTnpointPointLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTposePointLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTposePoseLogicalFunction.hpp>
#include <Functions/Meos/TeqBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/TeqCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TeqPoseTposeLogicalFunction.hpp>
#include <Functions/Meos/TeqTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/TeqTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TeqTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/TeqTposePoseLogicalFunction.hpp>
#include <Functions/Meos/TintersectsCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TintersectsGeoTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TintersectsTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TintersectsTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/TneBoolTboolLogicalFunction.hpp>
#include <Functions/Meos/TneCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TnePoseTposeLogicalFunction.hpp>
#include <Functions/Meos/TneTboolBoolLogicalFunction.hpp>
#include <Functions/Meos/TneTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TneTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/TneTposePoseLogicalFunction.hpp>
#include <Functions/Meos/TtouchesCbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TtouchesGeoTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TtouchesTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TtouchesTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/TemporalAtTstzsetLogicalFunction.hpp>
#include <Functions/Meos/TemporalAtTstzspanLogicalFunction.hpp>
#include <Functions/Meos/TemporalAtTstzspansetLogicalFunction.hpp>
#include <Functions/Meos/TemporalAtValuesLogicalFunction.hpp>
#include <Functions/Meos/TemporalMinusTstzsetLogicalFunction.hpp>
#include <Functions/Meos/TemporalMinusTstzspanLogicalFunction.hpp>
#include <Functions/Meos/TemporalMinusTstzspansetLogicalFunction.hpp>
#include <Functions/Meos/TemporalMinusValuesLogicalFunction.hpp>
#include <Functions/Meos/TnumberAtSpanLogicalFunction.hpp>
#include <Functions/Meos/TnumberAtSpansetLogicalFunction.hpp>
#include <Functions/Meos/TnumberAtTboxLogicalFunction.hpp>
#include <Functions/Meos/TnumberMinusSpanLogicalFunction.hpp>
#include <Functions/Meos/TnumberMinusSpansetLogicalFunction.hpp>
#include <Functions/Meos/TnumberMinusTboxLogicalFunction.hpp>
#include <Functions/Meos/TeqTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/TgeTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/TgtTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/TleTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/TltTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/TneTemporalTemporalLogicalFunction.hpp>
#include <Functions/Meos/TdisjointTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TdisjointTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TextcatTtextTtextLogicalFunction.hpp>
#include <Functions/Meos/TintersectsTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TintersectsTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/TtouchesTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TtouchesTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTemporalTstzspanLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTstzspanTemporalLogicalFunction.hpp>
#include <Functions/Meos/AfterTemporalTstzspanLogicalFunction.hpp>
#include <Functions/Meos/AfterTstzspanTemporalLogicalFunction.hpp>
#include <Functions/Meos/BeforeTemporalTstzspanLogicalFunction.hpp>
#include <Functions/Meos/BeforeTstzspanTemporalLogicalFunction.hpp>
#include <Functions/Meos/OverafterTemporalTstzspanLogicalFunction.hpp>
#include <Functions/Meos/OverafterTstzspanTemporalLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTemporalTstzspanLogicalFunction.hpp>
#include <Functions/Meos/OverbeforeTstzspanTemporalLogicalFunction.hpp>
#include <Functions/Meos/AdjacentTnumberNumspanLogicalFunction.hpp>
#include <Functions/Meos/AdjacentNumspanTnumberLogicalFunction.hpp>
#include <Functions/Meos/IntspansetWidthLogicalFunction.hpp>
#include <Functions/Meos/BigintspansetWidthLogicalFunction.hpp>
#include <Functions/Meos/FloatspansetWidthLogicalFunction.hpp>
#include <Functions/Meos/IntspanShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/IntsetShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/IntspansetShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/BigintspanShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/BigintsetShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/BigintspansetShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/FloatspanShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/FloatsetShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/FloatspansetShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/IntToSetLogicalFunction.hpp>
#include <Functions/Meos/IntToSpanLogicalFunction.hpp>
#include <Functions/Meos/IntToSpansetLogicalFunction.hpp>
#include <Functions/Meos/BigintToSetLogicalFunction.hpp>
#include <Functions/Meos/BigintToSpanLogicalFunction.hpp>
#include <Functions/Meos/BigintToSpansetLogicalFunction.hpp>
#include <Functions/Meos/FloatToSetLogicalFunction.hpp>
#include <Functions/Meos/FloatToSpanLogicalFunction.hpp>
#include <Functions/Meos/FloatToSpansetLogicalFunction.hpp>
#include <Functions/Meos/DateToSetLogicalFunction.hpp>
#include <Functions/Meos/DateToSpanLogicalFunction.hpp>
#include <Functions/Meos/DateToSpansetLogicalFunction.hpp>
#include <Functions/Meos/TimestamptzToSetLogicalFunction.hpp>
#include <Functions/Meos/TimestamptzToSpanLogicalFunction.hpp>
#include <Functions/Meos/TimestamptzToSpansetLogicalFunction.hpp>
#include <Functions/Meos/IntersectionBigintSetLogicalFunction.hpp>
#include <Functions/Meos/IntersectionDateSetLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSetDateLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSetTextLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpanDateLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpanTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpansetDateLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSpansetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/IntersectionTextSetLogicalFunction.hpp>
#include <Functions/Meos/IntersectionTimestamptzSetLogicalFunction.hpp>
#include <Functions/Meos/MinusDateSetLogicalFunction.hpp>
#include <Functions/Meos/MinusDateSpanLogicalFunction.hpp>
#include <Functions/Meos/MinusDateSpansetLogicalFunction.hpp>
#include <Functions/Meos/MinusSetDateLogicalFunction.hpp>
#include <Functions/Meos/MinusSetTextLogicalFunction.hpp>
#include <Functions/Meos/MinusSetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/MinusSpanDateLogicalFunction.hpp>
#include <Functions/Meos/MinusSpanTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/MinusSpansetDateLogicalFunction.hpp>
#include <Functions/Meos/MinusSpansetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/MinusTextSetLogicalFunction.hpp>
#include <Functions/Meos/MinusTimestamptzSetLogicalFunction.hpp>
#include <Functions/Meos/MinusTimestamptzSpanLogicalFunction.hpp>
#include <Functions/Meos/MinusTimestamptzSpansetLogicalFunction.hpp>
#include <Functions/Meos/UnionDateSetLogicalFunction.hpp>
#include <Functions/Meos/UnionDateSpanLogicalFunction.hpp>
#include <Functions/Meos/UnionDateSpansetLogicalFunction.hpp>
#include <Functions/Meos/UnionSetDateLogicalFunction.hpp>
#include <Functions/Meos/UnionSetTextLogicalFunction.hpp>
#include <Functions/Meos/UnionSetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/UnionSpanDateLogicalFunction.hpp>
#include <Functions/Meos/UnionSpanTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/UnionSpansetDateLogicalFunction.hpp>
#include <Functions/Meos/UnionSpansetTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/UnionTextSetLogicalFunction.hpp>
#include <Functions/Meos/UnionTimestamptzSetLogicalFunction.hpp>
#include <Functions/Meos/UnionTimestamptzSpanLogicalFunction.hpp>
#include <Functions/Meos/UnionTimestamptzSpansetLogicalFunction.hpp>
#include <Functions/Meos/DatesetShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/DatespanShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/DatespansetShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/TstzsetShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/TstzspanShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/TstzspansetShiftScaleLogicalFunction.hpp>
#include <Functions/Meos/TboxHashLogicalFunction.hpp>
#include <Functions/Meos/TboxHastLogicalFunction.hpp>
#include <Functions/Meos/TboxHasxLogicalFunction.hpp>
#include <Functions/Meos/StboxHasxLogicalFunction.hpp>
#include <Functions/Meos/StboxHaszLogicalFunction.hpp>
#include <Functions/Meos/StboxHastLogicalFunction.hpp>
#include <Functions/Meos/StboxIsgeodeticLogicalFunction.hpp>
#include <Functions/Meos/StboxSridLogicalFunction.hpp>
#include <Functions/Meos/StboxAreaLogicalFunction.hpp>
#include <Functions/Meos/StboxPerimeterLogicalFunction.hpp>
#include <Functions/Meos/TboxToIntspanLogicalFunction.hpp>
#include <Functions/Meos/TboxToFloatspanLogicalFunction.hpp>
#include <Functions/Meos/TboxToTstzspanLogicalFunction.hpp>
#include <Functions/Meos/IntToTboxLogicalFunction.hpp>
#include <Functions/Meos/FloatToTboxLogicalFunction.hpp>
#include <Functions/Meos/TimestamptzToTboxLogicalFunction.hpp>
#include <Functions/Meos/SpanToTboxLogicalFunction.hpp>
#include <Functions/Meos/SetToTboxLogicalFunction.hpp>
#include <Functions/Meos/SpansetToTboxLogicalFunction.hpp>
#include <Functions/Meos/TnumberToTboxLogicalFunction.hpp>
#include <Functions/Meos/TboxXminLogicalFunction.hpp>
#include <Functions/Meos/TboxXmaxLogicalFunction.hpp>
#include <Functions/Meos/TboxTminLogicalFunction.hpp>
#include <Functions/Meos/TboxTmaxLogicalFunction.hpp>
#include <Functions/Meos/TboxXminIncLogicalFunction.hpp>
#include <Functions/Meos/TboxXmaxIncLogicalFunction.hpp>
#include <Functions/Meos/TboxTminIncLogicalFunction.hpp>
#include <Functions/Meos/TboxTmaxIncLogicalFunction.hpp>
#include <Functions/Meos/TboxfloatXminLogicalFunction.hpp>
#include <Functions/Meos/TboxfloatXmaxLogicalFunction.hpp>
#include <Functions/Meos/TboxintXminLogicalFunction.hpp>
#include <Functions/Meos/TboxintXmaxLogicalFunction.hpp>
#include <Functions/Meos/StboxTminLogicalFunction.hpp>
#include <Functions/Meos/StboxTmaxLogicalFunction.hpp>
#include <Functions/Meos/StboxTminIncLogicalFunction.hpp>
#include <Functions/Meos/StboxTmaxIncLogicalFunction.hpp>
#include <Functions/Meos/TintValueNLogicalFunction.hpp>
#include <Functions/Meos/TfloatValueNLogicalFunction.hpp>
#include <Functions/Meos/TboolValueNLogicalFunction.hpp>
#include <Functions/Meos/IntsetValueNLogicalFunction.hpp>
#include <Functions/Meos/BigintsetValueNLogicalFunction.hpp>
#include <Functions/Meos/FloatsetValueNLogicalFunction.hpp>
#include <Functions/Meos/DatesetValueNLogicalFunction.hpp>
#include <Functions/Meos/TstzsetValueNLogicalFunction.hpp>
#include <Functions/Meos/SetToSpanLogicalFunction.hpp>
#include <Functions/Meos/SetToSpansetLogicalFunction.hpp>
#include <Functions/Meos/TnumberToSpanLogicalFunction.hpp>
#include <Functions/Meos/SpansetSpanLogicalFunction.hpp>
#include <Functions/Meos/SpansetStartSpanLogicalFunction.hpp>
#include <Functions/Meos/SpansetEndSpanLogicalFunction.hpp>
#include <Functions/Meos/SetCopyLogicalFunction.hpp>
#include <Functions/Meos/SpansetCopyLogicalFunction.hpp>
#include <Functions/Meos/IntTimestamptzToTboxLogicalFunction.hpp>
#include <Functions/Meos/FloatTimestamptzToTboxLogicalFunction.hpp>
#include <Functions/Meos/IntTstzspanToTboxLogicalFunction.hpp>
#include <Functions/Meos/FloatTstzspanToTboxLogicalFunction.hpp>
#include <Functions/Meos/NumspanTimestamptzToTboxLogicalFunction.hpp>
#include <Functions/Meos/NumspanTstzspanToTboxLogicalFunction.hpp>
#include <Functions/Meos/CbufferToStboxLogicalFunction.hpp>
#include <Functions/Meos/PoseToStboxLogicalFunction.hpp>
#include <Functions/Meos/NpointToStboxLogicalFunction.hpp>
#include <Functions/Meos/NsegmentToStboxLogicalFunction.hpp>
#include <Functions/Meos/TtextLowerLogicalFunction.hpp>
#include <Functions/Meos/TtextUpperLogicalFunction.hpp>
#include <Functions/Meos/TtextInitcapLogicalFunction.hpp>
#include <Functions/Meos/TtextStartValueLogicalFunction.hpp>
#include <Functions/Meos/TtextEndValueLogicalFunction.hpp>
#include <Functions/Meos/TtextMinValueLogicalFunction.hpp>
#include <Functions/Meos/TtextMaxValueLogicalFunction.hpp>
#include <Functions/Meos/TtextValueNLogicalFunction.hpp>
#include <Functions/Meos/TextsetValueNLogicalFunction.hpp>
#include <Functions/Meos/TgeoValueNLogicalFunction.hpp>
#include <Functions/Meos/TposeValueNLogicalFunction.hpp>
#include <Functions/Meos/TemporalToTstzspanLogicalFunction.hpp>
#include <Functions/Meos/TemporalTimeLogicalFunction.hpp>
#include <Functions/Meos/TnumberValuespansLogicalFunction.hpp>
#include <Functions/Meos/SpanHashLogicalFunction.hpp>
#include <Functions/Meos/SetHashLogicalFunction.hpp>
#include <Functions/Meos/SpansetHashLogicalFunction.hpp>
#include <Functions/Meos/StboxHashLogicalFunction.hpp>
#include <Functions/Meos/CbufferHashLogicalFunction.hpp>
#include <Functions/Meos/NpointHashLogicalFunction.hpp>
#include <Functions/Meos/PoseHashLogicalFunction.hpp>
#include <Functions/Meos/TemporalHashLogicalFunction.hpp>
#include <Functions/Meos/CbufferSridLogicalFunction.hpp>
#include <Functions/Meos/NpointSridLogicalFunction.hpp>
#include <Functions/Meos/NsegmentSridLogicalFunction.hpp>
#include <Functions/Meos/PoseSridLogicalFunction.hpp>
#include <Functions/Meos/CbufferRoundLogicalFunction.hpp>
#include <Functions/Meos/FloatRoundLogicalFunction.hpp>
#include <Functions/Meos/FloatspanRoundLogicalFunction.hpp>
#include <Functions/Meos/FloatspansetRoundLogicalFunction.hpp>
#include <Functions/Meos/NpointRoundLogicalFunction.hpp>
#include <Functions/Meos/PoseRoundLogicalFunction.hpp>
#include <Functions/Meos/SetRoundLogicalFunction.hpp>
#include <Functions/Meos/StboxRoundLogicalFunction.hpp>
#include <Functions/Meos/TboxRoundLogicalFunction.hpp>
#include <Functions/Meos/TemporalRoundLogicalFunction.hpp>
#include <Functions/Meos/DatespanDurationLogicalFunction.hpp>
#include <Functions/Meos/TstzspanDurationLogicalFunction.hpp>
#include <Functions/Meos/DatespansetDurationLogicalFunction.hpp>
#include <Functions/Meos/TstzspansetDurationLogicalFunction.hpp>
#include <Functions/Meos/TemporalDurationLogicalFunction.hpp>
#include <Functions/Meos/CbufferRadiusLogicalFunction.hpp>
#include <Functions/Meos/DatespanToTstzspanLogicalFunction.hpp>
#include <Functions/Meos/DatespansetToTstzspansetLogicalFunction.hpp>
#include <Functions/Meos/FloatspanToIntspanLogicalFunction.hpp>
#include <Functions/Meos/FloatspansetToIntspansetLogicalFunction.hpp>
#include <Functions/Meos/IntspanToFloatspanLogicalFunction.hpp>
#include <Functions/Meos/IntspansetToFloatspansetLogicalFunction.hpp>
#include <Functions/Meos/NpointPositionLogicalFunction.hpp>
#include <Functions/Meos/NpointRouteLogicalFunction.hpp>
#include <Functions/Meos/NsegmentRouteLogicalFunction.hpp>
#include <Functions/Meos/PoseRotationLogicalFunction.hpp>
#include <Functions/Meos/SpanToSpansetLogicalFunction.hpp>
#include <Functions/Meos/StboxToTstzspanLogicalFunction.hpp>
#include <Functions/Meos/StboxVolumeLogicalFunction.hpp>
#include <Functions/Meos/StboxXmaxLogicalFunction.hpp>
#include <Functions/Meos/StboxXminLogicalFunction.hpp>
#include <Functions/Meos/StboxYmaxLogicalFunction.hpp>
#include <Functions/Meos/StboxYminLogicalFunction.hpp>
#include <Functions/Meos/StboxZmaxLogicalFunction.hpp>
#include <Functions/Meos/StboxZminLogicalFunction.hpp>
#include <Functions/Meos/TimestamptzToStboxLogicalFunction.hpp>
#include <Functions/Meos/TstzsetToStboxLogicalFunction.hpp>
#include <Functions/Meos/TstzspanToDatespanLogicalFunction.hpp>
#include <Functions/Meos/TstzspanToStboxLogicalFunction.hpp>
#include <Functions/Meos/TstzspansetToDatespansetLogicalFunction.hpp>
#include <Functions/Meos/TstzspansetToStboxLogicalFunction.hpp>
#include <Functions/Meos/GeosetStartValueLogicalFunction.hpp>
#include <Functions/Meos/GeosetEndValueLogicalFunction.hpp>
#include <Functions/Meos/GeosetValueNLogicalFunction.hpp>
#include <Functions/Meos/CbuffersetStartValueLogicalFunction.hpp>
#include <Functions/Meos/CbuffersetEndValueLogicalFunction.hpp>
#include <Functions/Meos/CbuffersetValueNLogicalFunction.hpp>
#include <Functions/Meos/NpointsetStartValueLogicalFunction.hpp>
#include <Functions/Meos/NpointsetEndValueLogicalFunction.hpp>
#include <Functions/Meos/NpointsetValueNLogicalFunction.hpp>
#include <Functions/Meos/PosesetStartValueLogicalFunction.hpp>
#include <Functions/Meos/PosesetEndValueLogicalFunction.hpp>
#include <Functions/Meos/PosesetValueNLogicalFunction.hpp>
#include <Functions/Meos/GeoCopyLogicalFunction.hpp>
#include <Functions/Meos/GeoIsEmptyLogicalFunction.hpp>
#include <Functions/Meos/GeoIsUnitaryLogicalFunction.hpp>
#include <Functions/Meos/GeoNumGeosLogicalFunction.hpp>
#include <Functions/Meos/GeoNumPointsLogicalFunction.hpp>
#include <Functions/Meos/GeoPointsLogicalFunction.hpp>
#include <Functions/Meos/GeoReverseLogicalFunction.hpp>
#include <Functions/Meos/GeoRoundLogicalFunction.hpp>
#include <Functions/Meos/GeoSridLogicalFunction.hpp>
#include <Functions/Meos/GeomBoundaryLogicalFunction.hpp>
#include <Functions/Meos/GeomCentroidLogicalFunction.hpp>
#include <Functions/Meos/GeomConvexHullLogicalFunction.hpp>
#include <Functions/Meos/GeomLengthLogicalFunction.hpp>
#include <Functions/Meos/GeomPerimeterLogicalFunction.hpp>
#include <Functions/Meos/LineNumpointsLogicalFunction.hpp>
#include <Functions/Meos/TintAtValueLogicalFunction.hpp>
#include <Functions/Meos/TfloatAtValueLogicalFunction.hpp>
#include <Functions/Meos/TtextAtValueLogicalFunction.hpp>
#include <Functions/Meos/TboolAtValueLogicalFunction.hpp>
#include <Functions/Meos/TboolMinusValueLogicalFunction.hpp>
#include <Functions/Meos/TstzspanExpandLogicalFunction.hpp>
#include <Functions/Meos/TboxExpandTimeLogicalFunction.hpp>
#include <Functions/Meos/StboxExpandTimeLogicalFunction.hpp>
#include <Functions/Meos/AddTimestamptzIntervalLogicalFunction.hpp>
#include <Functions/Meos/MinusTimestamptzIntervalLogicalFunction.hpp>
#include <Functions/Meos/TimestamptzShiftLogicalFunction.hpp>
#include <Functions/Meos/AddIntervalIntervalLogicalFunction.hpp>
#include <Functions/Meos/MulIntervalDoubleLogicalFunction.hpp>
#include <Functions/Meos/ContainedCbufferSetLogicalFunction.hpp>
#include <Functions/Meos/ContainedGeoSetLogicalFunction.hpp>
#include <Functions/Meos/ContainedNpointSetLogicalFunction.hpp>
#include <Functions/Meos/ContainedPoseSetLogicalFunction.hpp>
#include <Functions/Meos/ContainedTextSetLogicalFunction.hpp>
#include <Functions/Meos/ContainsSetCbufferLogicalFunction.hpp>
#include <Functions/Meos/ContainsSetGeoLogicalFunction.hpp>
#include <Functions/Meos/ContainsSetNpointLogicalFunction.hpp>
#include <Functions/Meos/ContainsSetPoseLogicalFunction.hpp>
#include <Functions/Meos/ContainsSetTextLogicalFunction.hpp>
#include <Functions/Meos/IntersectionGeoSetLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSetCbufferLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSetGeoLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSetNpointLogicalFunction.hpp>
#include <Functions/Meos/IntersectionSetPoseLogicalFunction.hpp>
#include <Functions/Meos/LeftSetTextLogicalFunction.hpp>
#include <Functions/Meos/LeftTextSetLogicalFunction.hpp>
#include <Functions/Meos/MinusCbufferSetLogicalFunction.hpp>
#include <Functions/Meos/MinusGeoSetLogicalFunction.hpp>
#include <Functions/Meos/MinusNpointSetLogicalFunction.hpp>
#include <Functions/Meos/MinusPoseSetLogicalFunction.hpp>
#include <Functions/Meos/MinusSetCbufferLogicalFunction.hpp>
#include <Functions/Meos/MinusSetGeoLogicalFunction.hpp>
#include <Functions/Meos/MinusSetNpointLogicalFunction.hpp>
#include <Functions/Meos/MinusSetPoseLogicalFunction.hpp>
#include <Functions/Meos/OverleftSetTextLogicalFunction.hpp>
#include <Functions/Meos/OverleftTextSetLogicalFunction.hpp>
#include <Functions/Meos/OverrightSetTextLogicalFunction.hpp>
#include <Functions/Meos/OverrightTextSetLogicalFunction.hpp>
#include <Functions/Meos/RightSetTextLogicalFunction.hpp>
#include <Functions/Meos/RightTextSetLogicalFunction.hpp>
#include <Functions/Meos/UnionGeoSetLogicalFunction.hpp>
#include <Functions/Meos/UnionSetCbufferLogicalFunction.hpp>
#include <Functions/Meos/UnionSetGeoLogicalFunction.hpp>
#include <Functions/Meos/UnionSetNpointLogicalFunction.hpp>
#include <Functions/Meos/UnionSetPoseLogicalFunction.hpp>
#include <Functions/Meos/ContainedNumspanTnumberLogicalFunction.hpp>
#include <Functions/Meos/ContainedTemporalTstzspanLogicalFunction.hpp>
#include <Functions/Meos/ContainedTnumberNumspanLogicalFunction.hpp>
#include <Functions/Meos/ContainedTstzspanTemporalLogicalFunction.hpp>
#include <Functions/Meos/ContainsNumspanTnumberLogicalFunction.hpp>
#include <Functions/Meos/ContainsTemporalTstzspanLogicalFunction.hpp>
#include <Functions/Meos/ContainsTnumberNumspanLogicalFunction.hpp>
#include <Functions/Meos/ContainsTstzspanTemporalLogicalFunction.hpp>
#include <Functions/Meos/LeftNumspanTnumberLogicalFunction.hpp>
#include <Functions/Meos/LeftTnumberNumspanLogicalFunction.hpp>
#include <Functions/Meos/OverlapsNumspanTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTemporalTstzspanLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTnumberNumspanLogicalFunction.hpp>
#include <Functions/Meos/OverlapsTstzspanTemporalLogicalFunction.hpp>
#include <Functions/Meos/OverleftNumspanTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverleftTnumberNumspanLogicalFunction.hpp>
#include <Functions/Meos/OverrightNumspanTnumberLogicalFunction.hpp>
#include <Functions/Meos/OverrightTnumberNumspanLogicalFunction.hpp>
#include <Functions/Meos/RightNumspanTnumberLogicalFunction.hpp>
#include <Functions/Meos/RightTnumberNumspanLogicalFunction.hpp>
#include <Functions/Meos/SameNumspanTnumberLogicalFunction.hpp>
#include <Functions/Meos/SameTemporalTstzspanLogicalFunction.hpp>
#include <Functions/Meos/SameTnumberNumspanLogicalFunction.hpp>
#include <Functions/Meos/SameTstzspanTemporalLogicalFunction.hpp>
#include <Functions/Meos/NadTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/NadTposePoseLogicalFunction.hpp>
#include <Functions/Meos/NaiTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/NaiTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/NaiTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/NaiTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/NaiTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/NaiTnpointGeoLogicalFunction.hpp>
#include <Functions/Meos/NaiTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/NaiTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/NaiTposeGeoLogicalFunction.hpp>
#include <Functions/Meos/NaiTposePoseLogicalFunction.hpp>
#include <Functions/Meos/NaiTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/ShortestlineTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/ShortestlineTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/ShortestlineTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/ShortestlineTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/ShortestlineTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/ShortestlineTnpointGeoLogicalFunction.hpp>
#include <Functions/Meos/ShortestlineTnpointNpointLogicalFunction.hpp>
#include <Functions/Meos/ShortestlineTnpointTnpointLogicalFunction.hpp>
#include <Functions/Meos/ShortestlineTposeGeoLogicalFunction.hpp>
#include <Functions/Meos/ShortestlineTposePoseLogicalFunction.hpp>
#include <Functions/Meos/ShortestlineTposeTposeLogicalFunction.hpp>
#include <Functions/Meos/TnpointRouteLogicalFunction.hpp>
#include <Functions/Meos/TnpointTrajectoryLogicalFunction.hpp>
#include <Functions/Meos/TnpointTwcentroidLogicalFunction.hpp>
#include <Functions/Meos/TposeStartValueLogicalFunction.hpp>
#include <Functions/Meos/TposeEndValueLogicalFunction.hpp>
#include <Functions/Meos/TposeTrajectoryLogicalFunction.hpp>
#include <Functions/Meos/CbufferCopyLogicalFunction.hpp>
#include <Functions/Meos/CbufferPointLogicalFunction.hpp>
#include <Functions/Meos/CbufferToGeomLogicalFunction.hpp>
#include <Functions/Meos/PoseCopyLogicalFunction.hpp>
#include <Functions/Meos/PoseToPointLogicalFunction.hpp>
#include <Functions/Meos/NpointToGeompointLogicalFunction.hpp>
#include <Functions/Meos/NsegmentToGeomLogicalFunction.hpp>
#include <Functions/Meos/StboxToGeoLogicalFunction.hpp>
#include <Functions/Meos/TextsetStartValueLogicalFunction.hpp>
#include <Functions/Meos/TextsetEndValueLogicalFunction.hpp>
#include <Functions/Meos/TboolFromBaseTempLogicalFunction.hpp>
#include <Functions/Meos/TfloatDegreesLogicalFunction.hpp>
#include <Functions/Meos/TboxMakeLogicalFunction.hpp>
#include <Functions/Meos/TcbufferAtCbufferLogicalFunction.hpp>
#include <Functions/Meos/TcbufferAtGeomLogicalFunction.hpp>
#include <Functions/Meos/TcbufferMinusCbufferLogicalFunction.hpp>
#include <Functions/Meos/TcbufferMinusGeomLogicalFunction.hpp>
#include <Functions/Meos/TgeoAtValueLogicalFunction.hpp>
#include <Functions/Meos/TgeoMinusValueLogicalFunction.hpp>
#include <Functions/Meos/TnpointAtNpointLogicalFunction.hpp>
#include <Functions/Meos/TnpointMinusNpointLogicalFunction.hpp>
#include <Functions/Meos/TpointAtGeomLogicalFunction.hpp>
#include <Functions/Meos/TpointAtValueLogicalFunction.hpp>
#include <Functions/Meos/TpointMinusGeomLogicalFunction.hpp>
#include <Functions/Meos/TpointMinusValueLogicalFunction.hpp>
#include <Functions/Meos/TposeAtGeomLogicalFunction.hpp>
#include <Functions/Meos/TposeAtPoseLogicalFunction.hpp>
#include <Functions/Meos/TposeMinusGeomLogicalFunction.hpp>
#include <Functions/Meos/TposeMinusPoseLogicalFunction.hpp>
#include <Functions/Meos/TcbufferAtStboxLogicalFunction.hpp>
#include <Functions/Meos/TcbufferMinusStboxLogicalFunction.hpp>
#include <Functions/Meos/TgeoMinusStboxLogicalFunction.hpp>
#include <Functions/Meos/TposeAtStboxLogicalFunction.hpp>
#include <Functions/Meos/TposeMinusStboxLogicalFunction.hpp>
#include <Functions/Meos/DistanceCbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/DistanceCbufferStboxLogicalFunction.hpp>
#include <Functions/Meos/DistancePoseGeoLogicalFunction.hpp>
#include <Functions/Meos/DistancePoseStboxLogicalFunction.hpp>
#include <Functions/Meos/NadCbufferStboxLogicalFunction.hpp>
#include <Functions/Meos/NadStboxGeoLogicalFunction.hpp>
#include <Functions/Meos/IntspanMakeLogicalFunction.hpp>
#include <Functions/Meos/BigintspanMakeLogicalFunction.hpp>
#include <Functions/Meos/FloatspanMakeLogicalFunction.hpp>
#include <Functions/Meos/DatespanMakeLogicalFunction.hpp>
#include <Functions/Meos/TstzspanMakeLogicalFunction.hpp>
#include <Functions/Meos/TfloatinstMakeLogicalFunction.hpp>
#include <Functions/Meos/TintinstMakeLogicalFunction.hpp>
#include <Functions/Meos/TpointinstMakeLogicalFunction.hpp>
#include <Functions/Meos/TgeoinstMakeLogicalFunction.hpp>
#include <Functions/Meos/TnpointinstMakeLogicalFunction.hpp>
#include <Functions/Meos/NpointMakeLogicalFunction.hpp>
#include <Functions/Meos/CbufferMakeLogicalFunction.hpp>
#include <Functions/Meos/PoseMake2dLogicalFunction.hpp>
#include <Functions/Meos/PoseMakePoint2dLogicalFunction.hpp>
#include <Functions/Meos/GeoEqualsLogicalFunction.hpp>
#include <Functions/Meos/GeoGeoNLogicalFunction.hpp>
#include <Functions/Meos/GeoSameLogicalFunction.hpp>
#include <Functions/Meos/GeomContainsLogicalFunction.hpp>
#include <Functions/Meos/GeomCoversLogicalFunction.hpp>
#include <Functions/Meos/GeomDifference2dLogicalFunction.hpp>
#include <Functions/Meos/GeomDisjoint2dLogicalFunction.hpp>
#include <Functions/Meos/GeomDistance2dLogicalFunction.hpp>
#include <Functions/Meos/GeomDistance3dLogicalFunction.hpp>
#include <Functions/Meos/GeomIntersection2dLogicalFunction.hpp>
#include <Functions/Meos/GeomIntersection2dCollLogicalFunction.hpp>
#include <Functions/Meos/GeomIntersects2dLogicalFunction.hpp>
#include <Functions/Meos/GeomIntersects3dLogicalFunction.hpp>
#include <Functions/Meos/GeomShortestline2dLogicalFunction.hpp>
#include <Functions/Meos/GeomShortestline3dLogicalFunction.hpp>
#include <Functions/Meos/GeomToCbufferLogicalFunction.hpp>
#include <Functions/Meos/GeomTouchesLogicalFunction.hpp>
#include <Functions/Meos/GeomUnaryUnionLogicalFunction.hpp>
#include <Functions/Meos/TfloatValuesLogicalFunction.hpp>
#include <Functions/Meos/TintValuesLogicalFunction.hpp>
#include <Functions/Meos/TboolValuesLogicalFunction.hpp>
#include <Functions/Meos/TgeoValuesLogicalFunction.hpp>
#include <Functions/Meos/TposeValuesLogicalFunction.hpp>
#include <Functions/Meos/TtextValuesLogicalFunction.hpp>
#include <Functions/Meos/IntsetValuesLogicalFunction.hpp>
#include <Functions/Meos/DatesetValuesLogicalFunction.hpp>
#include <Functions/Meos/GeosetValuesLogicalFunction.hpp>
#include <Functions/Meos/CbuffersetValuesLogicalFunction.hpp>
#include <Functions/Meos/NpointsetValuesLogicalFunction.hpp>
#include <Functions/Meos/PosesetValuesLogicalFunction.hpp>
#include <Functions/Meos/TextsetValuesLogicalFunction.hpp>
#include <Functions/Meos/TemporalTimestampsLogicalFunction.hpp>
#include <Functions/Meos/FloatsetValuesLogicalFunction.hpp>
#include <Functions/Meos/BigintsetValuesLogicalFunction.hpp>
#include <Functions/Meos/TstzsetValuesLogicalFunction.hpp>
#include <Functions/Meos/TemporalSpansLogicalFunction.hpp>
#include <Functions/Meos/TemporalInstantsLogicalFunction.hpp>
#include <Functions/Meos/TdwithinTcbufferCbufferLogicalFunction.hpp>
#include <Functions/Meos/TdwithinTcbufferGeoLogicalFunction.hpp>
#include <Functions/Meos/TdwithinTcbufferTcbufferLogicalFunction.hpp>
#include <Functions/Meos/TdwithinTgeoGeoLogicalFunction.hpp>
#include <Functions/Meos/TdwithinTgeoTgeoLogicalFunction.hpp>
#include <Functions/Meos/IntGetBinLogicalFunction.hpp>
#include <Functions/Meos/BigintGetBinLogicalFunction.hpp>
#include <Functions/Meos/FloatGetBinLogicalFunction.hpp>
#include <Functions/Meos/StboxShiftScaleTimeLogicalFunction.hpp>
#include <Functions/Meos/TboxShiftScaleTimeLogicalFunction.hpp>
#include <Functions/Meos/TimestamptzGetBinLogicalFunction.hpp>
#include <Functions/Meos/DateGetBinLogicalFunction.hpp>
#include <Functions/Meos/TimestamptzTprecisionLogicalFunction.hpp>
#include <Functions/Meos/TstzspanTprecisionLogicalFunction.hpp>
#include <Functions/Meos/TstzsetTprecisionLogicalFunction.hpp>
#include <Functions/Meos/TstzspansetTprecisionLogicalFunction.hpp>
#include <Functions/Meos/TemporalAfterTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/TemporalBeforeTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/TemporalDeleteTimestamptzLogicalFunction.hpp>
#include <Functions/Meos/TemporalDeleteTstzsetLogicalFunction.hpp>
#include <Functions/Meos/TemporalDeleteTstzspanLogicalFunction.hpp>
#include <Functions/Meos/TemporalDeleteTstzspansetLogicalFunction.hpp>
#include <Functions/Meos/SetSplitNSpansLogicalFunction.hpp>
#include <Functions/Meos/SetSplitEachNSpansLogicalFunction.hpp>
#include <Functions/Meos/SpansetSplitNSpansLogicalFunction.hpp>
#include <Functions/Meos/SpansetSplitEachNSpansLogicalFunction.hpp>
#include <Functions/Meos/TgeoSplitNStboxesLogicalFunction.hpp>
#include <Functions/Meos/TgeoSplitEachNStboxesLogicalFunction.hpp>
#include <Functions/Meos/TnumberSplitNTboxesLogicalFunction.hpp>
#include <Functions/Meos/TnumberSplitEachNTboxesLogicalFunction.hpp>
#include <Functions/Meos/GeoSplitNStboxesLogicalFunction.hpp>
#include <Functions/Meos/GeoSplitEachNStboxesLogicalFunction.hpp>
#include <Functions/Meos/IntspanBinsLogicalFunction.hpp>
#include <Functions/Meos/FloatspanBinsLogicalFunction.hpp>
#include <Functions/Meos/IntspansetBinsLogicalFunction.hpp>
#include <Functions/Meos/FloatspansetBinsLogicalFunction.hpp>
#include <Functions/Meos/TfloatValueBinsLogicalFunction.hpp>
#include <Functions/Meos/TintValueBinsLogicalFunction.hpp>
#include <Functions/Meos/TfloatValueBoxesLogicalFunction.hpp>
#include <Functions/Meos/TintValueBoxesLogicalFunction.hpp>
#include <Functions/Meos/TfloatTimeBoxesLogicalFunction.hpp>
#include <Functions/Meos/TintTimeBoxesLogicalFunction.hpp>
#include <Functions/Meos/TemporalTimeBinsLogicalFunction.hpp>
#include <Functions/Meos/DatespanBinsLogicalFunction.hpp>
#include <Functions/Meos/BigintspanBinsLogicalFunction.hpp>
#include <Functions/Meos/BigintspansetBinsLogicalFunction.hpp>
#include <Functions/Meos/StboxSpaceTilesLogicalFunction.hpp>
#include <Functions/Meos/StboxTimeTilesLogicalFunction.hpp>
#include <Functions/Meos/StboxSpaceTimeTilesLogicalFunction.hpp>
#include <Functions/Meos/TgeoSpaceBoxesLogicalFunction.hpp>
#include <Functions/Meos/TemporalSequencesLogicalFunction.hpp>
#include <Functions/Meos/GeomToGeogLogicalFunction.hpp>
#include <Functions/Meos/TgeompointToTgeometryLogicalFunction.hpp>
#include <Functions/Meos/GeogDwithinLogicalFunction.hpp>
#include <Functions/Meos/FloatsetCeilLogicalFunction.hpp>
#include <Functions/Meos/FloatsetFloorLogicalFunction.hpp>
#include <Functions/Meos/FloatsetRadiansLogicalFunction.hpp>
#include <Functions/Meos/TextsetLowerLogicalFunction.hpp>
#include <Functions/Meos/TextsetUpperLogicalFunction.hpp>
#include <Functions/Meos/TextsetInitcapLogicalFunction.hpp>
#include <Functions/Meos/IntsetToFloatsetLogicalFunction.hpp>
#include <Functions/Meos/FloatsetToIntsetLogicalFunction.hpp>
#include <Functions/Meos/DatesetToTstzsetLogicalFunction.hpp>
#include <Functions/Meos/TstzsetToDatesetLogicalFunction.hpp>
#include <Functions/Meos/TstzspansetTimestampsLogicalFunction.hpp>
#include <Functions/Meos/GeogToGeomLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/AlwaysEqTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/AlwaysNeTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/EverEqGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/EverEqTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/EverNeGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/EverNeTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/TdistanceTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/TeqGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/TeqTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/TneGeoTrgeometryLogicalFunction.hpp>
#include <Functions/Meos/TneTrgeometryGeoLogicalFunction.hpp>
#include <Functions/Meos/TrgeometryEndValueLogicalFunction.hpp>
#include <Functions/Meos/TrgeometryGeomLogicalFunction.hpp>
#include <Functions/Meos/TrgeometryStartValueLogicalFunction.hpp>
#include <Plans/LogicalPlan.hpp>
#include <Plans/LogicalPlanBuilder.hpp>
#include <Util/Overloaded.hpp>
#include <Util/Strings.hpp>
#include <WindowTypes/Measures/TimeCharacteristic.hpp>
#include <WindowTypes/Measures/TimeMeasure.hpp>
#include <WindowTypes/Types/SlidingWindow.hpp>
#include <WindowTypes/Types/TumblingWindow.hpp>
#include <fmt/format.h>
#include <fmt/ranges.h>
#include <CommonParserFunctions.hpp>
#include <ErrorHandling.hpp>
#include <ParserUtil.hpp>

namespace NES::Parsers
{
LogicalPlan AntlrSQLQueryPlanCreator::getQueryPlan() const
{
    if (sinks.empty())
    {
        throw InvalidQuerySyntax("Query does not contain sink");
    }
    if (queryPlans.empty())
    {
        throw InvalidQuerySyntax("Query could not be parsed");
    }
    /// Todo #421: support multiple sinks
    INVARIANT(!sinks.empty(), "Need at least one sink!");
    return std::visit(
        Overloaded{
            [&](const std::string& sinkName) { return LogicalPlanBuilder::addSink(sinkName, queryPlans.top()); },
            [&](const std::pair<std::string, ConfigMap>& inlineSink)
            {
                const auto& [type, configOptions] = inlineSink;
                const auto sinkConfig = getSinkConfig(configOptions);
                const auto schemaOpt = getSinkSchema(configOptions);
                const Schema schema = (schemaOpt.has_value() ? schemaOpt.value() : Schema{});
                return LogicalPlanBuilder::addInlineSink(type, schema, sinkConfig, queryPlans.top());
            }},
        sinks.front());
}

Windowing::TimeMeasure buildTimeMeasure(const int size, const uint64_t timebase)
{
    switch (timebase) /// TODO #619: improve this switch case
    {
        case AntlrSQLLexer::MS:
            return API::Milliseconds(size);
        case AntlrSQLLexer::SEC:
            return API::Seconds(size);
        case AntlrSQLLexer::MINUTE:
            return API::Minutes(size);
        case AntlrSQLLexer::HOUR:
            return API::Hours(size);
        case AntlrSQLLexer::DAY:
            return API::Days(size);
        default:
            const AntlrSQLLexer lexer(nullptr);
            const std::string tokenName = std::string(lexer.getVocabulary().getSymbolicName(timebase));
            throw InvalidQuerySyntax("Unknown time unit: {}", tokenName);
    }
}

static LogicalFunction createFunctionFromOpBoolean(LogicalFunction leftFunction, LogicalFunction rightFunction, const uint64_t tokenType)
{
    switch (tokenType) /// TODO #619: improve this switch case
    {
        case AntlrSQLLexer::EQ:
            return EqualsLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        case AntlrSQLLexer::NEQJ:
            return NegateLogicalFunction(EqualsLogicalFunction(std::move(leftFunction), std::move(rightFunction)));
        case AntlrSQLLexer::LT:
            return LessLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        case AntlrSQLLexer::GT:
            return GreaterLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        case AntlrSQLLexer::GTE:
            return GreaterEqualsLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        case AntlrSQLLexer::LTE:
            return LessEqualsLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        default:
            auto lexer = AntlrSQLLexer(nullptr);
            throw InvalidQuerySyntax(
                "Unknown Comparison Operator: {} of type: {}", lexer.getVocabulary().getSymbolicName(tokenType), tokenType);
    }
}

static LogicalFunction createLogicalBinaryFunction(LogicalFunction leftFunction, LogicalFunction rightFunction, const uint64_t tokenType)
{
    switch (tokenType) /// TODO #619: improve this switch case
    {
        case AntlrSQLLexer::AND:
            return AndLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        case AntlrSQLLexer::OR:
            return OrLogicalFunction(std::move(leftFunction), std::move(rightFunction));
        default:
            auto lexer = AntlrSQLLexer(nullptr);
            throw InvalidQuerySyntax(
                "Unknown binary function in SQL query for op {} with type: {} and left {} and right {}",
                lexer.getVocabulary().getSymbolicName(tokenType),
                tokenType,
                std::move(leftFunction),
                std::move(rightFunction));
    }
}

void AntlrSQLQueryPlanCreator::enterSelectClause(AntlrSQLParser::SelectClauseContext* context)
{
    helpers.top().isSelect = true;
    AntlrSQLBaseListener::enterSelectClause(context);
}

void AntlrSQLQueryPlanCreator::enterFromClause(AntlrSQLParser::FromClauseContext* context)
{
    helpers.top().isFrom = true;
    AntlrSQLBaseListener::enterFromClause(context);
}

void AntlrSQLQueryPlanCreator::enterSinkClause(AntlrSQLParser::SinkClauseContext* context)
{
    if (context->sink().empty())
        throw InvalidQuerySyntax("INTO must be followed by at least one sink-identifier.");
    /// Store all specified sinks.
    for (const auto& sink : context->sink())
    {
        if (sink->identifier() != nullptr)
        {
            sinks.emplace_back(bindIdentifier(sink->identifier()));
        }
        else if (sink->inlineSink() != nullptr)
        {
            const auto& sinkInlineSink = sink->inlineSink();

            const auto type = bindIdentifier(sinkInlineSink->type);
            const auto configOptions = bindConfigOptions(sinkInlineSink->parameters->namedConfigExpression());

            sinks.emplace_back(std::make_pair(type, configOptions));
        }
    }
}

void AntlrSQLQueryPlanCreator::exitLogicalBinary(AntlrSQLParser::LogicalBinaryContext* context)
{
    /// If we are exiting a logical binary operator in a join relation, we need to build the binary function for the joinKey and
    /// not for the general function
    if (helpers.top().isJoinRelation)
    {
        if (helpers.top().joinKeyRelationHelper.size() < 2)
        {
            throw InvalidQuerySyntax(
                "Expected two operands for binary op, got {}: {}", helpers.top().joinKeyRelationHelper.size(), context->getText());
        }
        const auto rightFunction = helpers.top().joinKeyRelationHelper.back();
        helpers.top().joinKeyRelationHelper.pop_back();
        const auto leftFunction = helpers.top().joinKeyRelationHelper.back();
        helpers.top().joinKeyRelationHelper.pop_back();

        const auto opTokenType = context->op->getType();
        const auto function = createLogicalBinaryFunction(leftFunction, rightFunction, opTokenType);
        helpers.top().joinKeyRelationHelper.push_back(function);
    }
    else
    {
        if (helpers.top().functionBuilder.size() < 2)
        {
            throw InvalidQuerySyntax(
                "Expected two operands for binary op, got {}: {}", helpers.top().joinKeyRelationHelper.size(), context->getText());
        }
        const auto rightFunction = helpers.top().functionBuilder.back();
        helpers.top().functionBuilder.pop_back();
        const auto leftFunction = helpers.top().functionBuilder.back();
        helpers.top().functionBuilder.pop_back();

        const auto opTokenType = context->op->getType();
        const auto function = createLogicalBinaryFunction(leftFunction, rightFunction, opTokenType);
        helpers.top().functionBuilder.push_back(function);
    }
}

void AntlrSQLQueryPlanCreator::exitSelectClause(AntlrSQLParser::SelectClauseContext* context)
{
    helpers.top().functionBuilder.clear();
    helpers.top().isSelect = false;
    AntlrSQLBaseListener::exitSelectClause(context);
}

void AntlrSQLQueryPlanCreator::exitFromClause(AntlrSQLParser::FromClauseContext* context)
{
    helpers.top().isFrom = false;
    AntlrSQLBaseListener::exitFromClause(context);
}

void AntlrSQLQueryPlanCreator::enterWhereClause(AntlrSQLParser::WhereClauseContext* context)
{
    helpers.top().isWhereOrHaving = true;
    AntlrSQLBaseListener::enterWhereClause(context);
}

void AntlrSQLQueryPlanCreator::exitWhereClause(AntlrSQLParser::WhereClauseContext* context)
{
    helpers.top().isWhereOrHaving = false;
    if (helpers.top().functionBuilder.size() != 1)
    {
        throw InvalidQuerySyntax("There were more than 1 functions in the functionBuilder in exitWhereClause.");
    }
    helpers.top().addWhereClause(helpers.top().functionBuilder.back());
    helpers.top().functionBuilder.clear();
    AntlrSQLBaseListener::exitWhereClause(context);
}

void AntlrSQLQueryPlanCreator::enterComparisonOperator(AntlrSQLParser::ComparisonOperatorContext* context)
{
    auto opTokenType = context->getStart()->getType();
    helpers.top().opBoolean = opTokenType;
    AntlrSQLBaseListener::enterComparisonOperator(context);
}

void AntlrSQLQueryPlanCreator::exitArithmeticBinary(AntlrSQLParser::ArithmeticBinaryContext* context)
{
    if (helpers.empty())
    {
        throw InvalidQuerySyntax("Parser is confused at {}", context->getText());
    }
    LogicalFunction function;

    if (helpers.top().functionBuilder.size() < 2)
    {
        if (helpers.top().functionBuilder.size() + helpers.top().constantBuilder.size() == 2)
        {
            throw InvalidQuerySyntax(
                "Attempted to use a raw constant in a binary expression. {} in `{}`.",
                fmt::join(helpers.top().constantBuilder, ", "),
                context->getText());
        }
        throw InvalidQuerySyntax(
            "There were less than 2 functions in the functionBuilder in exitArithmeticBinary. `{}`.", context->getText());
    }
    const auto rightFunction = helpers.top().functionBuilder.back();
    helpers.top().functionBuilder.pop_back();
    const auto leftFunction = helpers.top().functionBuilder.back();
    helpers.top().functionBuilder.pop_back();
    auto opTokenType = context->op->getType();
    switch (opTokenType) /// TODO #619: improve this switch case
    {
        case AntlrSQLLexer::ASTERISK:
            function = MulLogicalFunction(leftFunction, rightFunction);
            break;
        case AntlrSQLLexer::SLASH:
            function = DivLogicalFunction(leftFunction, rightFunction);
            break;
        case AntlrSQLLexer::PLUS:
            function = AddLogicalFunction(leftFunction, rightFunction);
            break;
        case AntlrSQLLexer::MINUS:
            function = SubLogicalFunction(leftFunction, rightFunction);
            break;
        case AntlrSQLLexer::PERCENT:
            function = ModuloLogicalFunction(leftFunction, rightFunction);
            break;
        default:
            throw InvalidQuerySyntax("Unknown Arithmetic Binary Operator: {} of type: {}", context->op->getText(), opTokenType);
    }
    helpers.top().functionBuilder.push_back(function);
}

void AntlrSQLQueryPlanCreator::exitArithmeticUnary(AntlrSQLParser::ArithmeticUnaryContext* context)
{
    if (helpers.empty())
    {
        throw InvalidQuerySyntax("Parser is confused at {}", context->getText());
    }
    LogicalFunction function;

    if (helpers.top().functionBuilder.empty())
    {
        throw InvalidQuerySyntax("Expected unary operator, got nothing: {}", context->getText());
    }
    const auto innerFunction = helpers.top().functionBuilder.back();
    helpers.top().functionBuilder.pop_back();
    auto opTokenType = context->op->getType();
    switch (opTokenType) /// TODO #619: improve this switch case
    {
        case AntlrSQLLexer::PLUS:
            function = innerFunction;
            break;
        case AntlrSQLLexer::MINUS:
            function = MulLogicalFunction(
                ConstantValueLogicalFunction(DataTypeProvider::provideDataType(DataType::Type::UINT64), "-1"), innerFunction);
            break;
        default:
            throw InvalidQuerySyntax("Unknown Arithmetic Binary Operator: {} of type: {}", context->op->getText(), opTokenType);
    }
    helpers.top().functionBuilder.push_back(function);
}

void AntlrSQLQueryPlanCreator::enterUnquotedIdentifier(AntlrSQLParser::UnquotedIdentifierContext* context)
{
    /// Get Index of Parent Rule to check type of parent rule in conditions
    const auto parentContext = dynamic_cast<antlr4::ParserRuleContext*>(context->parent);
    const bool isParentRuleTableAlias = (parentContext != nullptr) && parentContext->getRuleIndex() == AntlrSQLParser::RuleTableAlias;
    if (helpers.top().isFrom && !helpers.top().isJoinRelation)
    {
        helpers.top().newSourceName = context->getText();
    }
    else if (helpers.top().isJoinRelation && isParentRuleTableAlias)
    {
        helpers.top().joinSourceRenames.emplace_back(context->getText());
    }
    AntlrSQLBaseListener::enterUnquotedIdentifier(context);
}

void AntlrSQLQueryPlanCreator::enterIdentifier(AntlrSQLParser::IdentifierContext* context)
{
    /// Get Index of Parent Rule to check type of parent rule in conditions
    std::optional<size_t> parentRuleIndex;
    if (const auto* const parentContext = dynamic_cast<antlr4::ParserRuleContext*>(context->parent); parentContext != nullptr)
    {
        parentRuleIndex = parentContext->getRuleIndex();
    }
    if (helpers.top().isGroupBy)
    {
        helpers.top().groupByFields.emplace_back(bindIdentifier(context));
    }
    else if (
        (helpers.top().isWhereOrHaving || helpers.top().isSelect || helpers.top().isWindow)
        && AntlrSQLParser::RulePrimaryExpression == parentRuleIndex)
    {
        helpers.top().functionBuilder.emplace_back(FieldAccessLogicalFunction(bindIdentifier(context)));
    }
    else if (helpers.top().isFrom and not helpers.top().isJoinRelation and AntlrSQLParser::RuleErrorCapturingIdentifier == parentRuleIndex)
    {
        /// get main source name
        helpers.top().setSource(bindIdentifier(context));
    }
    else if (
        AntlrSQLParser::RuleNamedExpression == parentRuleIndex and helpers.top().isInFunctionCall() and not helpers.top().isJoinRelation
        and not helpers.top().isInAggFunction())
    {
        /// handle renames of identifiers
        if (helpers.top().isArithmeticBinary)
        {
            throw InvalidQuerySyntax("There must not be a binary arithmetic token at this point: {}", context->getText());
        }
        if ((helpers.top().isWhereOrHaving || helpers.top().isSelect))
        {
            /// The user specified named expression (field access or function) with 'AS THE_NAME'
            /// (we handle cases where the user did not specify a name via 'AS' in 'exitNamedExpression')
            const auto attribute = std::move(helpers.top().functionBuilder.back());
            helpers.top().functionBuilder.pop_back();
            helpers.top().addProjection(FieldIdentifier(bindIdentifier(context)), attribute);
        }
    }
    else if (helpers.top().isInAggFunction() and AntlrSQLParser::RuleNamedExpression == parentRuleIndex)
    {
        auto aggFunc = helpers.top().windowAggs.back();
        helpers.top().windowAggs.pop_back();
        aggFunc->asField = (FieldAccessLogicalFunction(bindIdentifier(context)));
        helpers.top().windowAggs.push_back(aggFunc);
        INVARIANT(
            std::nullopt != helpers.top().functionBuilder.back().tryGet<FieldAccessLogicalFunction>(),
            "The functionBuilder should hold the AccessFunction of the name of the field the aggregation is executed on.");
        helpers.top().functionBuilder.pop_back();
        helpers.top().addProjection(std::nullopt, aggFunc->asField);
        helpers.top().hasUnnamedAggregation = false;
    }
    else if (helpers.top().isJoinRelation and AntlrSQLParser::RulePrimaryExpression == parentRuleIndex)
    {
        helpers.top().joinKeyRelationHelper.emplace_back(FieldAccessLogicalFunction(bindIdentifier(context)));
    }
    else if (helpers.top().isJoinRelation and AntlrSQLParser::RuleErrorCapturingIdentifier == parentRuleIndex)
    {
        helpers.top().joinSources.push_back(bindIdentifier(context));
    }
    else if (helpers.top().isJoinRelation and AntlrSQLParser::RuleTableAlias == parentRuleIndex)
    {
        helpers.top().joinSourceRenames.push_back(bindIdentifier(context));
    }
}

void AntlrSQLQueryPlanCreator::enterPrimaryQuery(AntlrSQLParser::PrimaryQueryContext* context)
{
    if (not helpers.empty() and not helpers.top().isFrom and not helpers.top().isSetOperation)
    {
        throw InvalidQuerySyntax("Subqueries are only supported in FROM clauses, but got {}", context->getText());
    }

    const AntlrSQLHelper helper;
    helpers.push(helper);
    AntlrSQLBaseListener::enterPrimaryQuery(context);
}

void AntlrSQLQueryPlanCreator::exitPrimaryQuery(AntlrSQLParser::PrimaryQueryContext* context)
{
    LogicalPlan queryPlan;

    if (not helpers.top().queryPlans.empty())
    {
        queryPlan = std::move(helpers.top().queryPlans[0]);
    }
    else
    {
        if (helpers.top().getSource().empty())
        {
            const auto [type, configOptions] = helpers.top().getInlineSourceConfig();
            const auto parserConfig = getParserConfig(configOptions);
            const auto sourceConfig = getSourceConfig(configOptions);
            const auto schema = getSourceSchema(configOptions);
            if (!schema.has_value())
            {
                throw InvalidConfigParameter("Inline Source is missing schema definition");
            }

            queryPlan = LogicalPlanBuilder::createLogicalPlan(type, schema.value(), sourceConfig, parserConfig);
        }
        else
        {
            queryPlan = LogicalPlanBuilder::createLogicalPlan(helpers.top().getSource());
        }
    }

    for (auto whereExpr = helpers.top().getWhereClauses().rbegin(); whereExpr != helpers.top().getWhereClauses().rend(); ++whereExpr)
    {
        queryPlan = LogicalPlanBuilder::addSelection(std::move(*whereExpr), queryPlan);
    }

    if (helpers.top().isInAggFunction())
    {
        queryPlan = LogicalPlanBuilder::addWindowAggregation(
            queryPlan, helpers.top().windowType, helpers.top().windowAggs, helpers.top().groupByFields);
    }

    queryPlan = LogicalPlanBuilder::addProjection(helpers.top().getProjections(), helpers.top().asterisk, queryPlan);

    if (helpers.top().windowType != nullptr)
    {
        for (auto havingExpr = helpers.top().getHavingClauses().rbegin(); havingExpr != helpers.top().getHavingClauses().rend();
             ++havingExpr)
        {
            queryPlan = LogicalPlanBuilder::addSelection(*havingExpr, queryPlan);
        }
    }
    helpers.pop();
    if (helpers.empty())
    {
        queryPlans.push(queryPlan);
    }
    else
    {
        auto& subQueryHelper = helpers.top();
        subQueryHelper.queryPlans.push_back(queryPlan);
    }
    AntlrSQLBaseListener::exitPrimaryQuery(context);
}

void AntlrSQLQueryPlanCreator::enterWindowClause(AntlrSQLParser::WindowClauseContext* context)
{
    helpers.top().isWindow = true;
    AntlrSQLBaseListener::enterWindowClause(context);
}

void AntlrSQLQueryPlanCreator::exitWindowClause(AntlrSQLParser::WindowClauseContext* context)
{
    helpers.top().isWindow = false;
    AntlrSQLBaseListener::exitWindowClause(context);
}

void AntlrSQLQueryPlanCreator::enterTimeUnit(AntlrSQLParser::TimeUnitContext* context)
{
    /// Get Index of Parent Rule to check type of parent rule in conditions
    std::optional<size_t> parentRuleIndex;
    if (const auto parentContext = dynamic_cast<antlr4::ParserRuleContext*>(context->parent); parentContext != nullptr)
    {
        parentRuleIndex = parentContext->getRuleIndex();
    }

    auto* token = context->getStop();
    auto timeunit = token->getType();
    if (parentRuleIndex == AntlrSQLParser::RuleAdvancebyParameter)
    {
        helpers.top().timeUnitAdvanceBy = timeunit;
    }
    else
    {
        helpers.top().timeUnit = timeunit;
    }
}

void AntlrSQLQueryPlanCreator::exitSizeParameter(AntlrSQLParser::SizeParameterContext* context)
{
    if (context->children.size() < 3)
    {
        throw InvalidQuerySyntax("SizeParameter must have 'size', a number, and a time unit.");
    }
    helpers.top().size = std::stoi(context->children.at(1)->getText());
    AntlrSQLBaseListener::exitSizeParameter(context);
}

void AntlrSQLQueryPlanCreator::exitAdvancebyParameter(AntlrSQLParser::AdvancebyParameterContext* context)
{
    if (context->children.size() < 3)
    {
        throw InvalidQuerySyntax("AdvancebyParameter must have 'ADVANCE BY', a number, and a time unit.");
    }
    helpers.top().advanceBy = std::stoi(context->children.at(2)->getText());
    AntlrSQLBaseListener::exitAdvancebyParameter(context);
}

void AntlrSQLQueryPlanCreator::exitTimestampParameter(AntlrSQLParser::TimestampParameterContext* context)
{
    helpers.top().timestamp = bindIdentifier(context->name);
}

/// WINDOWS
void AntlrSQLQueryPlanCreator::exitTumblingWindow(AntlrSQLParser::TumblingWindowContext* context)
{
    const auto timeMeasure = buildTimeMeasure(helpers.top().size, helpers.top().timeUnit);
    /// We use the ingestion time if the query does not have a timestamp fieldname specified
    if (helpers.top().timestamp.empty())
    {
        helpers.top().windowType = std::make_shared<Windowing::TumblingWindow>(API::IngestionTime(), timeMeasure);
    }
    else
    {
        helpers.top().windowType = std::make_shared<Windowing::TumblingWindow>(
            Windowing::TimeCharacteristic::createEventTime(FieldAccessLogicalFunction(helpers.top().timestamp)), timeMeasure);
    }
    AntlrSQLBaseListener::exitTumblingWindow(context);
}

void AntlrSQLQueryPlanCreator::exitSlidingWindow(AntlrSQLParser::SlidingWindowContext* context)
{
    const auto timeMeasure = buildTimeMeasure(helpers.top().size, helpers.top().timeUnit);
    const auto slidingLength = buildTimeMeasure(helpers.top().advanceBy, helpers.top().timeUnitAdvanceBy);
    /// We use the ingestion time if the query does not have a timestamp fieldname specified
    if (helpers.top().timestamp.empty())
    {
        helpers.top().windowType = Windowing::SlidingWindow::of(API::IngestionTime(), timeMeasure, slidingLength);
    }
    else
    {
        helpers.top().windowType = Windowing::SlidingWindow::of(
            Windowing::TimeCharacteristic::createEventTime(FieldAccessLogicalFunction(helpers.top().timestamp)),
            timeMeasure,
            slidingLength);
    }
    AntlrSQLBaseListener::exitSlidingWindow(context);
}

void AntlrSQLQueryPlanCreator::exitNamedExpression(AntlrSQLParser::NamedExpressionContext* context)
{
    AntlrSQLHelper& helper = helpers.top();
    if (context->name == nullptr and helper.functionBuilder.size() == 1
        and helper.functionBuilder.back().tryGet<FieldAccessLogicalFunction>() and not helpers.top().hasUnnamedAggregation)
    {
        /// Project onto the specified field and remove the field access from the active functions.
        helpers.top().addProjection(std::nullopt, std::move(helpers.top().functionBuilder.back()));
        helpers.top().functionBuilder.pop_back();
    }
    else if (helper.isSelect && context->getText() == "*" && helper.functionBuilder.empty())
    {
        helper.asterisk = true;
    }
    /// The user did not specify a new name (... AS THE_NAME) for the aggregation function and we need to generate one.
    else if (context->name == nullptr and not helpers.top().functionBuilder.empty() and helpers.top().hasUnnamedAggregation)
    {
        const auto accessFunction = helpers.top().functionBuilder.back();
        helpers.top().functionBuilder.pop_back();
        const auto fieldAccessNode = accessFunction.get<FieldAccessLogicalFunction>();
        const auto lastAggregation = helpers.top().windowAggs.back();
        const auto newName = fmt::format("{}_{}", fieldAccessNode.getFieldName(), Util::toUpperCase(lastAggregation->getName()));
        const auto asField = FieldAccessLogicalFunction(newName);
        lastAggregation->asField = asField;
        helpers.top().windowAggs.pop_back();
        helpers.top().windowAggs.push_back(lastAggregation);
        helpers.top().addProjection(std::nullopt, asField);
        helpers.top().hasUnnamedAggregation = false;
    }
    AntlrSQLBaseListener::exitNamedExpression(context);
}

void AntlrSQLQueryPlanCreator::enterFunctionCall(AntlrSQLParser::FunctionCallContext* context)
{
    AntlrSQLBaseListener::enterFunctionCall(context);
}

void AntlrSQLQueryPlanCreator::enterHavingClause(AntlrSQLParser::HavingClauseContext* context)
{
    helpers.top().isWhereOrHaving = true;
    AntlrSQLBaseListener::enterHavingClause(context);
}

void AntlrSQLQueryPlanCreator::exitHavingClause(AntlrSQLParser::HavingClauseContext* context)
{
    helpers.top().isWhereOrHaving = false;
    if (helpers.top().functionBuilder.size() != 1)
    {
        throw InvalidQuerySyntax("There was more than one function in the functionBuilder in exitHavingClause.");
    }
    helpers.top().addHavingClause(helpers.top().functionBuilder.back());
    helpers.top().functionBuilder.clear();
    AntlrSQLBaseListener::exitHavingClause(context);
}

void AntlrSQLQueryPlanCreator::exitComparison(AntlrSQLParser::ComparisonContext* context)
{
    if (helpers.top().isJoinRelation)
    {
        if (helpers.top().joinKeyRelationHelper.size() < 2)
        {
            throw InvalidQuerySyntax(
                "Requires two functions but got {} at {}", helpers.top().joinKeyRelationHelper.size(), context->getText());
        }
        const auto rightFunction = helpers.top().joinKeyRelationHelper.back();
        helpers.top().joinKeyRelationHelper.pop_back();
        const auto leftFunction = helpers.top().joinKeyRelationHelper.back();
        helpers.top().joinKeyRelationHelper.pop_back();
        const auto function = createFunctionFromOpBoolean(leftFunction, rightFunction, helpers.top().opBoolean);
        helpers.top().joinKeyRelationHelper.push_back(function);
    }
    else
    {
        if (helpers.top().functionBuilder.size() < 2)
        {
            throw InvalidQuerySyntax("Comparison requires two parameters, got {}", context->getText());
        }
        const auto rightFunction = helpers.top().functionBuilder.back();
        helpers.top().functionBuilder.pop_back();
        const auto leftFunction = helpers.top().functionBuilder.back();
        helpers.top().functionBuilder.pop_back();

        const auto function = createFunctionFromOpBoolean(leftFunction, rightFunction, helpers.top().opBoolean);
        helpers.top().functionBuilder.push_back(function);
    }
    AntlrSQLBaseListener::exitComparison(context);
}

void AntlrSQLQueryPlanCreator::enterJoinRelation(AntlrSQLParser::JoinRelationContext* context)
{
    helpers.top().joinKeyRelationHelper.clear();
    helpers.top().isJoinRelation = true;
    AntlrSQLBaseListener::enterJoinRelation(context);
}

void AntlrSQLQueryPlanCreator::enterJoinCriteria(AntlrSQLParser::JoinCriteriaContext* context)
{
    INVARIANT(helpers.top().isJoinRelation, "Join criteria must be inside a join relation.");
    AntlrSQLBaseListener::enterJoinCriteria(context);
}

void AntlrSQLQueryPlanCreator::enterJoinType(AntlrSQLParser::JoinTypeContext* context)
{
    if (not helpers.top().isJoinRelation)
    {
        throw InvalidQuerySyntax("Join type must be inside a join relation.");
    }
    AntlrSQLBaseListener::enterJoinType(context);
}

void AntlrSQLQueryPlanCreator::exitJoinType(AntlrSQLParser::JoinTypeContext* context)
{
    const auto joinType = context->getText();
    auto tokenType = context->getStop()->getType();

    if (joinType.empty() || tokenType == AntlrSQLLexer::INNER)
    {
        helpers.top().joinType = JoinLogicalOperator::JoinType::INNER_JOIN;
    }
    else
    {
        throw InvalidQuerySyntax("Unknown join type: {}, resolved to token type: {}", joinType, tokenType);
    }
    AntlrSQLBaseListener::exitJoinType(context);
}

void AntlrSQLQueryPlanCreator::exitJoinRelation(AntlrSQLParser::JoinRelationContext* context)
{
    helpers.top().isJoinRelation = false;
    if (helpers.top().joinSources.size() == helpers.top().joinSourceRenames.size() + 1)
    {
        helpers.top().joinSourceRenames.emplace_back("");
    }

    /// we assume that the left query plan is the first element in the queryPlans vector and the right query plan is the second element
    if (helpers.top().queryPlans.size() != 2)
    {
        throw InvalidQuerySyntax(
            "Join relation requires two subqueries, but got {} at {}", helpers.top().queryPlans.size(), context->getText());
    }
    const auto leftQueryPlan = helpers.top().queryPlans[0];
    const auto rightQueryPlan = helpers.top().queryPlans[1];
    helpers.top().queryPlans.clear();

    if (helpers.top().joinKeyRelationHelper.size() != 1)
    {
        throw InvalidQuerySyntax("joinFunction is required but empty at {}", context->getText());
    }
    if (!helpers.top().windowType)
    {
        throw InvalidQuerySyntax("windowType is required but empty at {}", context->getText());
    }
    const auto queryPlan = LogicalPlanBuilder::addJoin(
        leftQueryPlan, rightQueryPlan, helpers.top().joinKeyRelationHelper.at(0), helpers.top().windowType, helpers.top().joinType);
    if (not helpers.empty())
    {
        /// we are in a subquery
        helpers.top().queryPlans.push_back(queryPlan);
    }
    else
    {
        /// for now, we will never enter this branch, because we always have a subquery
        /// as we require the join relations to always be a sub-query
        queryPlans.push(queryPlan);
    }
    AntlrSQLBaseListener::exitJoinRelation(context);
}

void AntlrSQLQueryPlanCreator::exitLogicalNot(AntlrSQLParser::LogicalNotContext* context)
{
    if (helpers.empty())
    {
        throw InvalidQuerySyntax("Parser is confused at {}", context->getText());
    }

    if (helpers.top().isJoinRelation)
    {
        if (helpers.top().joinKeyRelationHelper.empty())
        {
            throw InvalidQuerySyntax("Negate requires child op at {}", context->getText());
        }
        const auto innerFunction = helpers.top().joinKeyRelationHelper.back();
        helpers.top().joinKeyRelationHelper.pop_back();
        auto negatedFunction = NegateLogicalFunction(innerFunction);
        helpers.top().joinKeyRelationHelper.emplace_back(negatedFunction);
    }
    else
    {
        if (helpers.top().functionBuilder.empty())
        {
            throw InvalidQuerySyntax("Negate requires child op at {}", context->getText());
        }
        const auto innerFunction = helpers.top().functionBuilder.back();
        helpers.top().functionBuilder.pop_back();
        helpers.top().functionBuilder.emplace_back(NegateLogicalFunction(innerFunction));
    }
    AntlrSQLBaseListener::exitLogicalNot(context);
}

void AntlrSQLQueryPlanCreator::exitConstantDefault(AntlrSQLParser::ConstantDefaultContext* context)
{
    if (context->children.size() != 1)
    {
        throw InvalidQuerySyntax("When exiting a constant, there must be exactly one children in the context {}", context->getText());
    }
    if (const auto stringLiteralContext = dynamic_cast<AntlrSQLParser::StringLiteralContext*>(context->children.at(0)))
    {
        if (!(stringLiteralContext->getText().size() > 2))
        {
            throw InvalidQuerySyntax(
                "A constant string literal must contain at least two quotes and must not be empty at {}", context->getText());
        }
        helpers.top().constantBuilder.push_back(context->getText().substr(1, stringLiteralContext->getText().size() - 2));
    }
    else
    {
        helpers.top().constantBuilder.push_back(context->getText());
    }
}

void AntlrSQLQueryPlanCreator::exitFunctionCall(AntlrSQLParser::FunctionCallContext* context)
{
    const auto funcName = Util::toUpperCase(context->children[0]->getText());
    const auto tokenType = context->getStart()->getType();

    helpers.top().hasUnnamedAggregation = true;
    switch (tokenType) /// TODO #619: improve this switch case
    {
        case AntlrSQLLexer::COUNT:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                std::make_shared<CountAggregationLogicalFunction>(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::AVG:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                std::make_shared<AvgAggregationLogicalFunction>(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::MAX:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                std::make_shared<MaxAggregationLogicalFunction>(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::MIN:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                std::make_shared<MinAggregationLogicalFunction>(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::SUM:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                std::make_shared<SumAggregationLogicalFunction>(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::MEDIAN:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                std::make_shared<MedianAggregationLogicalFunction>(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
                case AntlrSQLLexer::ARRAY_AGG:
            helpers.top().windowAggs.push_back(
                ArrayAggregationLogicalFunction::create(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::VAR:
            if (helpers.top().functionBuilder.empty())
            {
                throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
            }
            helpers.top().windowAggs.push_back(
                VarAggregationLogicalFunction::create(helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>()));
            break;
        case AntlrSQLLexer::TEMPORAL_SEQUENCE:
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TEMPORAL_SEQUENCE requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                // Verify all arguments are field access functions
                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_SEQUENCE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalSequenceAggregationLogicalFunctionV2::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                        latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                        timestampFunction.get<FieldAccessLogicalFunction>()));
                // Push back one field access function to satisfy parser expectations
                // This prevents the functionBuilder from being empty when processing the identifier
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        case AntlrSQLLexer::TEMPORAL_LENGTH:
            // Same three-input shape as TEMPORAL_SEQUENCE; differs only in the
            // result type (FLOAT64 instead of VARSIZED). Closes BerlinMOD-Q6 to a
            // full streaming-form cell.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TEMPORAL_LENGTH requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_LENGTH arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalLengthAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                     latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                     timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        case AntlrSQLLexer::PAIR_MEETING:
            // Five-arg aggregation: lon, lat, ts, vehicle_id (FieldAccess) + dMeet
            // (numeric constant — meeting-distance threshold in metres). The first four
            // are pulled from functionBuilder; the fifth is pulled from constantBuilder
            // (the parser parks numeric/string literals there). Closes Q5 × 3 cells to
            // full; this branch makes the dMeet configurable per-query.
            {
                if (helpers.top().constantBuilder.empty()) {
                    throw InvalidQuerySyntax(
                        "PAIR_MEETING requires a numeric constant fifth argument (dMeet metres), "
                        "e.g. PAIR_MEETING(lon, lat, timestamp, vehicle_id, 200.0)");
                }
                auto dMeetString = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                double dMeetMetres;
                try {
                    dMeetMetres = std::stod(dMeetString);
                } catch (const std::exception&) {
                    throw InvalidQuerySyntax(
                        "PAIR_MEETING fifth argument must be a numeric constant (dMeet metres), got `{}`",
                        dMeetString);
                }

                if (helpers.top().functionBuilder.size() != 4) {
                    throw InvalidQuerySyntax(
                        "PAIR_MEETING requires exactly five arguments (lon, lat, timestamp, vehicle_id, dMeet), "
                        "got {} field args + 1 constant",
                        helpers.top().functionBuilder.size());
                }

                const auto vidFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !vidFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("PAIR_MEETING field arguments (lon, lat, timestamp, vehicle_id) must be field references");
                }

                helpers.top().windowAggs.push_back(
                    PairMeetingAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                  latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                  timestampFunction.get<FieldAccessLogicalFunction>(),
                                                                  vidFunction.get<FieldAccessLogicalFunction>(),
                                                                  dMeetMetres));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        case AntlrSQLLexer::CROSS_DISTANCE:
            // Six-arg aggregation: lon, lat, ts, vehicle_id (FieldAccess) + vidA, vidB
            // (numeric constants — target vehicle IDs). The first four are pulled from
            // functionBuilder; the fifth and sixth are pulled from constantBuilder.
            // Closes Q9 × 3 cells to full; this branch makes the target vehicle pair
            // configurable per-query. Mirrors PAIR_MEETING's 5-arg constant-parameterization
            // pattern (PR #19).
            {
                // Pull the two vid constants from constantBuilder. Note: the constants
                // are pushed in source order, so the LAST one pushed (vidB in the SQL
                // call) is on top of the stack — pop in reverse order.
                if (helpers.top().constantBuilder.size() < 2) {
                    throw InvalidQuerySyntax(
                        "CROSS_DISTANCE requires two numeric constant arguments (vidA, vidB), "
                        "e.g. CROSS_DISTANCE(lon, lat, timestamp, vehicle_id, 100, 200)");
                }
                auto vidBString = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                auto vidAString = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                uint64_t vidA, vidB;
                try {
                    vidA = std::stoull(vidAString);
                    vidB = std::stoull(vidBString);
                } catch (const std::exception&) {
                    throw InvalidQuerySyntax(
                        "CROSS_DISTANCE constant arguments must be unsigned integers (vidA, vidB), got `{}` and `{}`",
                        vidAString, vidBString);
                }

                if (helpers.top().functionBuilder.size() != 4) {
                    throw InvalidQuerySyntax(
                        "CROSS_DISTANCE requires exactly six arguments (lon, lat, timestamp, vehicle_id, vidA, vidB), "
                        "got {} field args + 2 constants",
                        helpers.top().functionBuilder.size());
                }

                const auto vidFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !vidFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("CROSS_DISTANCE field arguments (lon, lat, timestamp, vehicle_id) must be field references");
                }

                helpers.top().windowAggs.push_back(
                    CrossDistanceAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>(),
                                                                    vidFunction.get<FieldAccessLogicalFunction>(),
                                                                    vidA, vidB));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        case AntlrSQLLexer::EINTERSECTS_TGEO_GEO:
            {
                // Convert constants from constantBuilder to ConstantValueLogicalFunction objects
                while (!helpers.top().constantBuilder.empty()) {
                    auto constantValue = std::move(helpers.top().constantBuilder.back());
                    helpers.top().constantBuilder.pop_back();
                    // Assume string constants are VARSIZED (WKT strings)
                    auto dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                    auto constFunction = ConstantValueLogicalFunction(dataType, std::move(constantValue));
                    helpers.top().functionBuilder.push_back(constFunction);
                }

                const auto argCount = helpers.top().functionBuilder.size();
                if (argCount != 4 && argCount != 6) {
                    throw InvalidQuerySyntax("EINTERSECTS_TGEO_GEO requires either 4 arguments (lon1, lat1, timestamp1, static_geometry) or 6 arguments (lon1, lat1, timestamp1, lon2, lat2, timestamp2), but got {}", argCount);
                }

                if (argCount == 4) {
                    // 4-parameter case: temporal-static intersection (lon1, lat1, timestamp1, static_geometry)
                    const auto staticGeometryFunction = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto timestamp1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lat1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lon1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    
                    const auto function = TemporalIntersectsGeometryLogicalFunction(lon1Function, lat1Function, timestamp1Function, staticGeometryFunction);
                    helpers.top().functionBuilder.push_back(function);
                } else {
                    // 6-parameter case: temporal-temporal intersection (lon1, lat1, timestamp1, lon2, lat2, timestamp2)
                    const auto timestamp2Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lat2Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lon2Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto timestamp1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lat1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lon1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    
                    const auto function = TemporalIntersectsGeometryLogicalFunction(lon1Function, lat1Function, timestamp1Function, lon2Function, lat2Function, timestamp2Function);
                    helpers.top().functionBuilder.push_back(function);
                }
            }
            break;
        case AntlrSQLLexer::AINTERSECTS_TGEO_GEO:
            {
                // Convert constants from constantBuilder to ConstantValueLogicalFunction objects
                while (!helpers.top().constantBuilder.empty()) {
                    auto constantValue = std::move(helpers.top().constantBuilder.back());
                    helpers.top().constantBuilder.pop_back();
                    // Assume string constants are VARSIZED (WKT strings)
                    auto dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                    auto constFunction = ConstantValueLogicalFunction(dataType, std::move(constantValue));
                    helpers.top().functionBuilder.push_back(constFunction);
                }
                const auto argCount = helpers.top().functionBuilder.size();
                if (argCount != 4 && argCount != 6) {
                    throw InvalidQuerySyntax("AINTERSECTS_TGEO_GEO requires either 4 arguments (lon1, lat1, timestamp1, static_geometry) or 6 arguments (lon1, lat1, timestamp1, lon2, lat2, timestamp2), but got {}", argCount);
                }
                if (argCount == 4) {
                    // 4-parameter case: temporal-static intersection (lon1, lat1, timestamp1, static_geometry)
                    const auto staticGeometryFunction = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto timestamp1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lat1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lon1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    
                    const auto function = AintersectsTgeoGeoLogicalFunction(lon1Function, lat1Function, timestamp1Function, staticGeometryFunction);
                    helpers.top().functionBuilder.push_back(function);
                } else {
                    // 6-parameter case: temporal-temporal intersection (lon1, lat1, timestamp1, lon2, lat2, timestamp2)
                    const auto timestamp2Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lat2Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lon2Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto timestamp1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lat1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    const auto lon1Function = helpers.top().functionBuilder.back();
                    helpers.top().functionBuilder.pop_back();
                    
                    const auto function = AintersectsTgeoGeoLogicalFunction(lon1Function, lat1Function, timestamp1Function, lon2Function, lat2Function, timestamp2Function);
                    helpers.top().functionBuilder.push_back(function);
                }
            }
            break;
        case AntlrSQLLexer::ECONTAINS_TGEO_GEO: 
        {
            // move any literal WKT that’s still on constantBuilder into functionBuilder
            while(!helpers.top().constantBuilder.empty()){
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            const auto n = helpers.top().functionBuilder.size();
            if(n==6){
                auto ts2 = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto lat2= helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto lon2= helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto ts1 = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto lat1= helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto lon1= helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    EcontainsTgeoGeoLogicalFunction(lon1,lat1,ts1,lon2,lat2,ts2));
            } else if(n==4){
                /* decide order by data-type of first arg */
                auto last = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto third= helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto second= helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                auto first = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if(first.getDataType().isType(DataType::Type::VARSIZED)) // static,tgeo
                    helpers.top().functionBuilder.emplace_back(
                        EcontainsTgeoGeoLogicalFunction(first,second,third,last));
                else                                                      // tgeo,static
                    helpers.top().functionBuilder.emplace_back(
                        EcontainsTgeoGeoLogicalFunction(first,second,third,last));
            } else {
                throw InvalidQuerySyntax("ECONTAINS_TGEO_GEO expects 4 or 6 arguments");
            }
        }
        break;
        case AntlrSQLLexer::EDWITHIN_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
            {
                throw InvalidQuerySyntax("EDWITHIN_TGEO_GEO requires exactly five arguments (lon, lat, timestamp, geometry, distance), but got {}", argCount);
            }

            // Move pending constants into the function builder (WKT last)
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                const auto upperValue = Util::toUpperCase(constantValue);
                if (upperValue == "TRUE" || upperValue == "FALSE")
                {
                    dataType = DataTypeProvider::provideDataType(DataType::Type::BOOLEAN);
                }
                else
                {
                    char* endPtr = nullptr;
                    std::strtod(constantValue.c_str(), &endPtr);
                    if (endPtr != nullptr && *endPtr == '\0')
                    {
                        dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                    }
                    else
                    {
                        dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                    }
                }
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            const auto total = helpers.top().functionBuilder.size();
            PRECONDITION(total >= 5, "EDWITHIN_TGEO_GEO requires (lon, lat, timestamp, geometry, distance), but got {}", total);

            // Order after move: [lon, lat, ts, distance, geometry]
            auto geometryFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distanceFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestampFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdwithinTgeoGeoLogicalFunction(lonFunction, latFunction, timestampFunction, geometryFunction, distanceFunction));
        }
        break;
        case AntlrSQLLexer::TGEO_AT_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4 && argCount != 5)
            {
                throw InvalidQuerySyntax("TGEO_AT_STBOX requires four arguments (lon, lat, timestamp, stbox) with an optional fifth border_inc flag, but got {}", argCount);
            }

            // Move pending constants into the function builder (border first if present, STBOX last)
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                const auto upperValue = Util::toUpperCase(constantValue);
                if (upperValue == "TRUE" || upperValue == "FALSE")
                {
                    dataType = DataTypeProvider::provideDataType(DataType::Type::BOOLEAN);
                }
                else
                {
                    char* endPtr = nullptr;
                    std::strtod(constantValue.c_str(), &endPtr);
                    if (endPtr != nullptr && *endPtr == '\0')
                    {
                        dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                    }
                    else
                    {
                        dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                    }
                }
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            const auto total = helpers.top().functionBuilder.size();
            PRECONDITION(total >= 4, "TGEO_AT_STBOX requires (lon, lat, timestamp, stbox) with optional border flag, but got {}", total);

            auto stboxFunction = helpers.top().functionBuilder.back();
            helpers.top().functionBuilder.pop_back();

            LogicalFunction borderFlag = ConstantValueLogicalFunction(
                DataTypeProvider::provideDataType(DataType::Type::BOOLEAN), "TRUE");
            if (!helpers.top().functionBuilder.empty() && helpers.top().functionBuilder.back().getDataType().isType(DataType::Type::BOOLEAN))
            {
                borderFlag = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
            }

            auto timestampFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonFunction = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TgeoAtStboxLogicalFunction(lonFunction, latFunction, timestampFunction, stboxFunction, borderFlag));
        }
        break;

        /* BEGIN CODEGEN PARSER GLUE: ADISJOINT_TGEO_GEO */
        case AntlrSQLLexer::ADISJOINT_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ADISJOINT_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdisjointTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADISJOINT_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ECONTAINS_TGEO_TGEO */
        case AntlrSQLLexer::ECONTAINS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ECONTAINS_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcontainsTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECONTAINS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ECOVERS_TGEO_TGEO */
        case AntlrSQLLexer::ECOVERS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ECOVERS_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcoversTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECOVERS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: EDISJOINT_TGEO_TGEO */
        case AntlrSQLLexer::EDISJOINT_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EDISJOINT_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdisjointTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDISJOINT_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: EINTERSECTS_TGEO_TGEO */
        case AntlrSQLLexer::EINTERSECTS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EINTERSECTS_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EintersectsTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EINTERSECTS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ETOUCHES_TGEO_TGEO */
        case AntlrSQLLexer::ETOUCHES_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ETOUCHES_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EtouchesTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ETOUCHES_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ACONTAINS_TGEO_TGEO */
        case AntlrSQLLexer::ACONTAINS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ACONTAINS_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcontainsTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACONTAINS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ADISJOINT_TGEO_TGEO */
        case AntlrSQLLexer::ADISJOINT_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ADISJOINT_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdisjointTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADISJOINT_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: AINTERSECTS_TGEO_TGEO */
        case AntlrSQLLexer::AINTERSECTS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("AINTERSECTS_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AintersectsTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: AINTERSECTS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ATOUCHES_TGEO_TGEO */
        case AntlrSQLLexer::ATOUCHES_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ATOUCHES_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AtouchesTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ATOUCHES_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TGEO_GEO */
        case AntlrSQLLexer::NAD_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAD_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TGEO_TGEO */
        case AntlrSQLLexer::NAD_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("NAD_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: EDWITHIN_TGEO_TGEO */
        case AntlrSQLLexer::EDWITHIN_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("EDWITHIN_TGEO_TGEO requires exactly 7 arguments (lonA, latA, tsA, lonB, latB, tsB, distance), but got {}", argCount);

            /* Lift the distance constant */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdwithinTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDWITHIN_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ADWITHIN_TGEO_GEO */
        case AntlrSQLLexer::ADWITHIN_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ADWITHIN_TGEO_GEO requires exactly 5 arguments (lon, lat, timestamp, geometry, distance), but got {}", argCount);

            /* Lift constants (geometry + distance) — same shape as EDWITHIN_TGEO_GEO */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                const auto upperValue = Util::toUpperCase(constantValue);
                if (upperValue == "TRUE" || upperValue == "FALSE")
                {
                    dataType = DataTypeProvider::provideDataType(DataType::Type::BOOLEAN);
                }
                else
                {
                    char* endPtr = nullptr;
                    std::strtod(constantValue.c_str(), &endPtr);
                    if (endPtr != nullptr && *endPtr == '\0')
                        dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                    else
                        dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                }
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            /* After lift: [lon, lat, ts, distance, geometry] (geometry pushed last because lifted last in LIFO) */
            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto dist      = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdwithinTgeoGeoLogicalFunction(lon, lat, timestamp, geometry, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADWITHIN_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ADWITHIN_TGEO_TGEO */
        case AntlrSQLLexer::ADWITHIN_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("ADWITHIN_TGEO_TGEO requires exactly 7 arguments (lonA, latA, tsA, lonB, latB, tsB, distance), but got {}", argCount);

            /* Lift the distance constant */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdwithinTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADWITHIN_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: EDISJOINT_TGEO_GEO */
        case AntlrSQLLexer::EDISJOINT_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EDISJOINT_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdisjointTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDISJOINT_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ATOUCHES_TGEO_GEO */
        case AntlrSQLLexer::ATOUCHES_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ATOUCHES_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AtouchesTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ATOUCHES_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ECOVERS_TGEO_GEO */
        case AntlrSQLLexer::ECOVERS_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ECOVERS_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcoversTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECOVERS_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ACONTAINS_TGEO_GEO */
        case AntlrSQLLexer::ACONTAINS_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ACONTAINS_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcontainsTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACONTAINS_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ETOUCHES_TGEO_GEO */
        case AntlrSQLLexer::ETOUCHES_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ETOUCHES_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EtouchesTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ETOUCHES_TGEO_GEO */
        /* BEGIN CODEGEN PARSER GLUE: NAD_TFLOAT_FLOAT */
        case AntlrSQLLexer::NAD_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("NAD_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TINT_INT */
        case AntlrSQLLexer::NAD_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("NAD_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TFLOAT_TFLOAT */
        case AntlrSQLLexer::NAD_TFLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAD_TFLOAT_TFLOAT requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTfloatTfloatLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TFLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TINT_TINT */
        case AntlrSQLLexer::NAD_TINT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAD_TINT_TINT requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTintTintLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TINT_TINT */
        /* BEGIN CODEGEN PARSER GLUE: TGEO_AT_GEOM */
        case AntlrSQLLexer::TGEO_AT_GEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TGEO_AT_GEOM requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TgeoAtGeomLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGEO_AT_GEOM */

        /* BEGIN CODEGEN PARSER GLUE: TGEO_MINUS_GEOM */
        case AntlrSQLLexer::TGEO_MINUS_GEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TGEO_MINUS_GEOM requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TgeoMinusGeomLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGEO_MINUS_GEOM */
        /* BEGIN CODEGEN PARSER GLUE: ECONTAINS_TCBUFFER_GEO */
        case AntlrSQLLexer::ECONTAINS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ECONTAINS_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcontainsTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECONTAINS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ECOVERS_TCBUFFER_GEO */
        case AntlrSQLLexer::ECOVERS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ECOVERS_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcoversTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECOVERS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EDISJOINT_TCBUFFER_GEO */
        case AntlrSQLLexer::EDISJOINT_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EDISJOINT_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdisjointTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDISJOINT_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EINTERSECTS_TCBUFFER_GEO */
        case AntlrSQLLexer::EINTERSECTS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EINTERSECTS_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EintersectsTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EINTERSECTS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ETOUCHES_TCBUFFER_GEO */
        case AntlrSQLLexer::ETOUCHES_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ETOUCHES_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EtouchesTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ETOUCHES_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ACONTAINS_TCBUFFER_GEO */
        case AntlrSQLLexer::ACONTAINS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ACONTAINS_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcontainsTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACONTAINS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ACOVERS_TCBUFFER_GEO */
        case AntlrSQLLexer::ACOVERS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ACOVERS_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcoversTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACOVERS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ADISJOINT_TCBUFFER_GEO */
        case AntlrSQLLexer::ADISJOINT_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ADISJOINT_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdisjointTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADISJOINT_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: AINTERSECTS_TCBUFFER_GEO */
        case AntlrSQLLexer::AINTERSECTS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("AINTERSECTS_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AintersectsTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: AINTERSECTS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ATOUCHES_TCBUFFER_GEO */
        case AntlrSQLLexer::ATOUCHES_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ATOUCHES_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AtouchesTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ATOUCHES_TCBUFFER_GEO */
        /* BEGIN CODEGEN PARSER GLUE: ECONTAINS_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::ECONTAINS_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ECONTAINS_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcontainsTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECONTAINS_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ECOVERS_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::ECOVERS_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ECOVERS_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcoversTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECOVERS_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EDISJOINT_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::EDISJOINT_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EDISJOINT_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdisjointTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDISJOINT_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EINTERSECTS_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::EINTERSECTS_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EINTERSECTS_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EintersectsTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EINTERSECTS_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ETOUCHES_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::ETOUCHES_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ETOUCHES_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EtouchesTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ETOUCHES_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ACONTAINS_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::ACONTAINS_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ACONTAINS_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcontainsTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACONTAINS_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ACOVERS_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::ACOVERS_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ACOVERS_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AcoversTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACOVERS_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ADISJOINT_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::ADISJOINT_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ADISJOINT_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdisjointTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADISJOINT_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: AINTERSECTS_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::AINTERSECTS_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("AINTERSECTS_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AintersectsTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: AINTERSECTS_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ATOUCHES_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::ATOUCHES_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ATOUCHES_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AtouchesTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ATOUCHES_TCBUFFER_CBUFFER */
        /* BEGIN CODEGEN PARSER GLUE: ADISJOINT_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ADISJOINT_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ADISJOINT_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdisjointTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADISJOINT_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: AINTERSECTS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::AINTERSECTS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("AINTERSECTS_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AintersectsTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: AINTERSECTS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ATOUCHES_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ATOUCHES_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ATOUCHES_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AtouchesTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ATOUCHES_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ECOVERS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ECOVERS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ECOVERS_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EcoversTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECOVERS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EINTERSECTS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::EINTERSECTS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("EINTERSECTS_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EintersectsTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EINTERSECTS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ETOUCHES_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ETOUCHES_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ETOUCHES_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EtouchesTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ETOUCHES_TCBUFFER_TCBUFFER */
        /* BEGIN CODEGEN PARSER GLUE: EDWITHIN_TCBUFFER_GEO */
        case AntlrSQLLexer::EDWITHIN_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EDWITHIN_TCBUFFER_GEO requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdwithinTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDWITHIN_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ADWITHIN_TCBUFFER_GEO */
        case AntlrSQLLexer::ADWITHIN_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ADWITHIN_TCBUFFER_GEO requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdwithinTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADWITHIN_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EDWITHIN_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::EDWITHIN_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EDWITHIN_TCBUFFER_CBUFFER requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdwithinTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDWITHIN_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ADWITHIN_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::ADWITHIN_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ADWITHIN_TCBUFFER_CBUFFER requires exactly 6 arguments (lon, lat, radius, timestamp, blob, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();

                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto blobLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto distLast  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdwithinTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, blobLast, distLast));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADWITHIN_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EDWITHIN_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::EDWITHIN_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 9)
                throw InvalidQuerySyntax("EDWITHIN_TCBUFFER_TCBUFFER requires exactly 9 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EdwithinTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: EDWITHIN_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ADWITHIN_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ADWITHIN_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 9)
                throw InvalidQuerySyntax("ADWITHIN_TCBUFFER_TCBUFFER requires exactly 9 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, distance), but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::FLOAT64), std::move(v)));
            }

            auto dist    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdwithinTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB, dist));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADWITHIN_TCBUFFER_TCBUFFER */











































        /* BEGIN CODEGEN PARSER GLUE: NAD_TCBUFFER_GEO */
        case AntlrSQLLexer::NAD_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAD_TCBUFFER_GEO requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTcbufferGeoLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::NAD_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAD_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::NAD_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("NAD_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                NadTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TCBUFFER_TCBUFFER */
        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TFLOAT_FLOAT */
        case AntlrSQLLexer::ALWAYS_EQ_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_EQ_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TINT_INT */
        case AntlrSQLLexer::ALWAYS_EQ_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_EQ_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GE_TFLOAT_FLOAT */
        case AntlrSQLLexer::ALWAYS_GE_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GE_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGeTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GE_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GE_TINT_INT */
        case AntlrSQLLexer::ALWAYS_GE_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GE_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGeTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GE_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GT_TFLOAT_FLOAT */
        case AntlrSQLLexer::ALWAYS_GT_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GT_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGtTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GT_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GT_TINT_INT */
        case AntlrSQLLexer::ALWAYS_GT_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GT_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGtTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GT_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LE_TFLOAT_FLOAT */
        case AntlrSQLLexer::ALWAYS_LE_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LE_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLeTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LE_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LE_TINT_INT */
        case AntlrSQLLexer::ALWAYS_LE_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LE_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLeTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LE_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LT_TFLOAT_FLOAT */
        case AntlrSQLLexer::ALWAYS_LT_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LT_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLtTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LT_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LT_TINT_INT */
        case AntlrSQLLexer::ALWAYS_LT_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LT_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLtTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LT_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TFLOAT_FLOAT */
        case AntlrSQLLexer::ALWAYS_NE_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_NE_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TINT_INT */
        case AntlrSQLLexer::ALWAYS_NE_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_NE_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TFLOAT_FLOAT */
        case AntlrSQLLexer::EVER_EQ_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_EQ_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TINT_INT */
        case AntlrSQLLexer::EVER_EQ_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_EQ_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GE_TFLOAT_FLOAT */
        case AntlrSQLLexer::EVER_GE_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GE_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGeTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GE_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GE_TINT_INT */
        case AntlrSQLLexer::EVER_GE_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GE_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGeTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GE_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GT_TFLOAT_FLOAT */
        case AntlrSQLLexer::EVER_GT_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GT_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGtTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GT_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GT_TINT_INT */
        case AntlrSQLLexer::EVER_GT_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GT_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGtTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GT_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LE_TFLOAT_FLOAT */
        case AntlrSQLLexer::EVER_LE_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LE_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLeTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LE_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LE_TINT_INT */
        case AntlrSQLLexer::EVER_LE_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LE_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLeTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LE_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LT_TFLOAT_FLOAT */
        case AntlrSQLLexer::EVER_LT_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LT_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLtTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LT_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LT_TINT_INT */
        case AntlrSQLLexer::EVER_LT_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LT_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLtTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LT_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TFLOAT_FLOAT */
        case AntlrSQLLexer::EVER_NE_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_NE_TFLOAT_FLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTfloatFloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TINT_INT */
        case AntlrSQLLexer::EVER_NE_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_NE_TINT_INT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTintIntLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TINT_INT */
        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_FLOAT_TFLOAT */
        case AntlrSQLLexer::ALWAYS_EQ_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_EQ_FLOAT_TFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_INT_TINT */
        case AntlrSQLLexer::ALWAYS_EQ_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_EQ_INT_TINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::ALWAYS_EQ_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_EQ_TEMPORAL_TEMPORAL requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTemporalTemporalLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GE_FLOAT_TFLOAT */
        case AntlrSQLLexer::ALWAYS_GE_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GE_FLOAT_TFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGeFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GE_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GE_INT_TINT */
        case AntlrSQLLexer::ALWAYS_GE_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GE_INT_TINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGeIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GE_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GE_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::ALWAYS_GE_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_GE_TEMPORAL_TEMPORAL requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGeTemporalTemporalLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GE_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GT_FLOAT_TFLOAT */
        case AntlrSQLLexer::ALWAYS_GT_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GT_FLOAT_TFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGtFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GT_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GT_INT_TINT */
        case AntlrSQLLexer::ALWAYS_GT_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GT_INT_TINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGtIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GT_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GT_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::ALWAYS_GT_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_GT_TEMPORAL_TEMPORAL requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysGtTemporalTemporalLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GT_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LE_FLOAT_TFLOAT */
        case AntlrSQLLexer::ALWAYS_LE_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LE_FLOAT_TFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLeFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LE_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LE_INT_TINT */
        case AntlrSQLLexer::ALWAYS_LE_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LE_INT_TINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLeIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LE_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LE_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::ALWAYS_LE_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_LE_TEMPORAL_TEMPORAL requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLeTemporalTemporalLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LE_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LT_FLOAT_TFLOAT */
        case AntlrSQLLexer::ALWAYS_LT_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LT_FLOAT_TFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLtFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LT_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LT_INT_TINT */
        case AntlrSQLLexer::ALWAYS_LT_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LT_INT_TINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLtIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LT_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LT_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::ALWAYS_LT_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_LT_TEMPORAL_TEMPORAL requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysLtTemporalTemporalLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LT_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_FLOAT_TFLOAT */
        case AntlrSQLLexer::ALWAYS_NE_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_NE_FLOAT_TFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_INT_TINT */
        case AntlrSQLLexer::ALWAYS_NE_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_NE_INT_TINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::ALWAYS_NE_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_NE_TEMPORAL_TEMPORAL requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTemporalTemporalLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_FLOAT_TFLOAT */
        case AntlrSQLLexer::EVER_EQ_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_EQ_FLOAT_TFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_INT_TINT */
        case AntlrSQLLexer::EVER_EQ_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_EQ_INT_TINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::EVER_EQ_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_EQ_TEMPORAL_TEMPORAL requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTemporalTemporalLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GE_FLOAT_TFLOAT */
        case AntlrSQLLexer::EVER_GE_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GE_FLOAT_TFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGeFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GE_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GE_INT_TINT */
        case AntlrSQLLexer::EVER_GE_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GE_INT_TINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGeIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GE_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GE_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::EVER_GE_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_GE_TEMPORAL_TEMPORAL requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGeTemporalTemporalLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GE_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GT_FLOAT_TFLOAT */
        case AntlrSQLLexer::EVER_GT_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GT_FLOAT_TFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGtFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GT_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GT_INT_TINT */
        case AntlrSQLLexer::EVER_GT_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GT_INT_TINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGtIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GT_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GT_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::EVER_GT_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_GT_TEMPORAL_TEMPORAL requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverGtTemporalTemporalLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GT_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LE_FLOAT_TFLOAT */
        case AntlrSQLLexer::EVER_LE_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LE_FLOAT_TFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLeFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LE_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LE_INT_TINT */
        case AntlrSQLLexer::EVER_LE_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LE_INT_TINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLeIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LE_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LE_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::EVER_LE_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_LE_TEMPORAL_TEMPORAL requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLeTemporalTemporalLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LE_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LT_FLOAT_TFLOAT */
        case AntlrSQLLexer::EVER_LT_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LT_FLOAT_TFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLtFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LT_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LT_INT_TINT */
        case AntlrSQLLexer::EVER_LT_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LT_INT_TINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLtIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LT_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LT_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::EVER_LT_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_LT_TEMPORAL_TEMPORAL requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverLtTemporalTemporalLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LT_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_FLOAT_TFLOAT */
        case AntlrSQLLexer::EVER_NE_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_NE_FLOAT_TFLOAT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeFloatTfloatLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_INT_TINT */
        case AntlrSQLLexer::EVER_NE_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_NE_INT_TINT requires exactly 3 arguments (value, timestamp, scalar), but got {}", argCount);

            /* Lift the scalar constant — accept FLOAT64 (strtod-clean) and INT32 */
            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto scalar    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto value     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeIntTintLogicalFunction(value, timestamp, scalar));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::EVER_NE_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_NE_TEMPORAL_TEMPORAL requires exactly 4 arguments (valueA, tsA, valueB, tsB), but got {}", argCount);

            auto tsB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto valueA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTemporalTemporalLogicalFunction(valueA, tsA, valueB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TEMPORAL_TEMPORAL */
        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::ALWAYS_EQ_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYS_EQ_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ALWAYS_EQ_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ALWAYS_EQ_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TGEO_GEO */
        case AntlrSQLLexer::ALWAYS_EQ_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_EQ_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TGEO_TGEO */
        case AntlrSQLLexer::ALWAYS_EQ_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ALWAYS_EQ_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysEqTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::ALWAYS_NE_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYS_NE_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::ALWAYS_NE_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("ALWAYS_NE_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TGEO_GEO */
        case AntlrSQLLexer::ALWAYS_NE_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_NE_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TGEO_TGEO */
        case AntlrSQLLexer::ALWAYS_NE_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ALWAYS_NE_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AlwaysNeTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ATOUCHES_TPOINT_GEO */
        case AntlrSQLLexer::ATOUCHES_TPOINT_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ATOUCHES_TPOINT_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AtouchesTpointGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ATOUCHES_TPOINT_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ETOUCHES_TPOINT_GEO */
        case AntlrSQLLexer::ETOUCHES_TPOINT_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ETOUCHES_TPOINT_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EtouchesTpointGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: ETOUCHES_TPOINT_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::EVER_EQ_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVER_EQ_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::EVER_EQ_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("EVER_EQ_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TGEO_GEO */
        case AntlrSQLLexer::EVER_EQ_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_EQ_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TGEO_TGEO */
        case AntlrSQLLexer::EVER_EQ_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EVER_EQ_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverEqTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::EVER_NE_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVER_NE_TCBUFFER_CBUFFER requires exactly 5 arguments (lon, lat, radius, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radius    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTcbufferCbufferLogicalFunction(lon, lat, radius, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::EVER_NE_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("EVER_NE_TCBUFFER_TCBUFFER requires exactly 8 arguments (lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB), but got {}", argCount);

            auto tsB     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA     = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto radiusA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA    = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTcbufferTcbufferLogicalFunction(lonA, latA, radiusA, tsA, lonB, latB, radiusB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TGEO_GEO */
        case AntlrSQLLexer::EVER_NE_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_NE_TGEO_GEO requires exactly 4 arguments (lon, lat, timestamp, geometry), but got {}", argCount);

            /* Lift the WKT constant into the function builder */
            while (!helpers.top().constantBuilder.empty())
            {
                auto v = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(
                    ConstantValueLogicalFunction(
                        DataTypeProvider::provideDataType(DataType::Type::VARSIZED), std::move(v)));
            }

            auto geometry  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto timestamp = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lat       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lon       = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTgeoGeoLogicalFunction(lon, lat, timestamp, geometry));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TGEO_TGEO */
        case AntlrSQLLexer::EVER_NE_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("EVER_NE_TGEO_TGEO requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                EverNeTgeoTgeoLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TGEO_TGEO */
        /* BEGIN CODEGEN PARSER GLUE: ABOVE_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::ABOVE_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ABOVE_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AboveTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ABOVE_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::ADJACENT_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ADJACENT_TEMPORAL_TEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdjacentTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::ADJACENT_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("ADJACENT_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AdjacentTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::AFTER_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("AFTER_TEMPORAL_TEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AfterTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::AFTER_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("AFTER_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                AfterTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TBOOL_BOOL */
        case AntlrSQLLexer::ALWAYS_EQ_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_EQ_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TBOOL_BOOL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TBOOL_BOOL */
        case AntlrSQLLexer::ALWAYS_NE_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_NE_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TBOOL_BOOL */


        /* BEGIN CODEGEN PARSER GLUE: BEFORE_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::BEFORE_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("BEFORE_TEMPORAL_TEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BeforeTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::BEFORE_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("BEFORE_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BeforeTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: BELOW_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::BELOW_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("BELOW_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                BelowTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: BELOW_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::CONTAINED_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("CONTAINED_TEMPORAL_TEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainedTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::CONTAINED_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("CONTAINED_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainedTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::CONTAINS_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("CONTAINS_TEMPORAL_TEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainsTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::CONTAINS_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("CONTAINS_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                ContainsTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TBOOL_BOOL */
        case AntlrSQLLexer::EVER_EQ_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_EQ_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TBOOL_BOOL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TBOOL_BOOL */
        case AntlrSQLLexer::EVER_NE_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_NE_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TBOOL_BOOL */


        /* BEGIN CODEGEN PARSER GLUE: LEFT_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::LEFT_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("LEFT_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                LeftTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TNPOINT_GEO */
        case AntlrSQLLexer::NAD_TNPOINT_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAD_TNPOINT_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTnpointGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TNPOINT_GEO */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TPOSE_GEO */
        case AntlrSQLLexer::NAD_TPOSE_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAD_TPOSE_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTposeGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TPOSE_GEO */

        /* BEGIN CODEGEN PARSER GLUE: OVERABOVE_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::OVERABOVE_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERABOVE_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OveraboveTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERABOVE_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::OVERAFTER_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERAFTER_TEMPORAL_TEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverafterTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::OVERAFTER_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERAFTER_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverafterTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_TSPATIAL_TSPATIAL */


        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::OVERBEFORE_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERBEFORE_TEMPORAL_TEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbeforeTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::OVERBEFORE_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERBEFORE_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbeforeTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBELOW_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::OVERBELOW_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERBELOW_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverbelowTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBELOW_TSPATIAL_TSPATIAL */


        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::OVERLAPS_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERLAPS_TEMPORAL_TEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverlapsTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::OVERLAPS_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERLAPS_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverlapsTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::OVERLEFT_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERLEFT_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverleftTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::OVERRIGHT_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("OVERRIGHT_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                OverrightTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::RIGHT_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("RIGHT_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                RightTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: SAME_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::SAME_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("SAME_TEMPORAL_TEMPORAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                SameTemporalTemporalLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: SAME_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::SAME_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("SAME_TSPATIAL_TSPATIAL requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                SameTspatialTspatialLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: TBOOL_END_VALUE */
        case AntlrSQLLexer::TBOOL_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOOL_END_VALUE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboolEndValueLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOOL_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TBOOL_START_VALUE */
        case AntlrSQLLexer::TBOOL_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOOL_START_VALUE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboolStartValueLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOOL_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_CMP */
        case AntlrSQLLexer::TEMPORAL_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_CMP requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalCmpLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_CMP */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_DYNTIMEWARP_DISTANCE */
        case AntlrSQLLexer::TEMPORAL_DYNTIMEWARP_DISTANCE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_DYNTIMEWARP_DISTANCE requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalDyntimewarpDistanceLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_DYNTIMEWARP_DISTANCE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_EQ */
        case AntlrSQLLexer::TEMPORAL_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_EQ requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalEqLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_EQ */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_FRECHET_DISTANCE */
        case AntlrSQLLexer::TEMPORAL_FRECHET_DISTANCE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_FRECHET_DISTANCE requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalFrechetDistanceLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_FRECHET_DISTANCE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_GE */
        case AntlrSQLLexer::TEMPORAL_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_GE requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalGeLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_GE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_GT */
        case AntlrSQLLexer::TEMPORAL_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_GT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalGtLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_GT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_HAUSDORFF_DISTANCE */
        case AntlrSQLLexer::TEMPORAL_HAUSDORFF_DISTANCE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_HAUSDORFF_DISTANCE requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalHausdorffDistanceLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_HAUSDORFF_DISTANCE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_LE */
        case AntlrSQLLexer::TEMPORAL_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_LE requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalLeLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_LE */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_LT */
        case AntlrSQLLexer::TEMPORAL_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_LT requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalLtLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_LT */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_NE */
        case AntlrSQLLexer::TEMPORAL_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TEMPORAL_NE requires exactly 6 arguments (lonA, latA, tsA, lonB, latB, tsB), but got {}", argCount);

            auto tsB  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonB = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto tsA  = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto latA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto lonA = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(
                TemporalNeLogicalFunction(lonA, latA, tsA, lonB, latB, tsB));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_NE */

        /* BEGIN CODEGEN PARSER GLUE: TNPOINT_LENGTH */
        case AntlrSQLLexer::TNPOINT_LENGTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNPOINT_LENGTH requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnpointLengthLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNPOINT_LENGTH */
        /* BEGIN CODEGEN PARSER GLUE: TBOOL_TO_TINT */
        case AntlrSQLLexer::TBOOL_TO_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOOL_TO_TINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboolToTintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOOL_TO_TINT */

        /* BEGIN CODEGEN PARSER GLUE: TCBUFFER_TO_TFLOAT */
        case AntlrSQLLexer::TCBUFFER_TO_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TCBUFFER_TO_TFLOAT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcbufferToTfloatLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCBUFFER_TO_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_CEIL */
        case AntlrSQLLexer::TFLOAT_CEIL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_CEIL requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatCeilLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_CEIL */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_EXP */
        case AntlrSQLLexer::TFLOAT_EXP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_EXP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatExpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_EXP */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_FLOOR */
        case AntlrSQLLexer::TFLOAT_FLOOR:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_FLOOR requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatFloorLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_FLOOR */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_LN */
        case AntlrSQLLexer::TFLOAT_LN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_LN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatLnLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_LN */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_LOG10 */
        case AntlrSQLLexer::TFLOAT_LOG10:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_LOG10 requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatLog10LogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_LOG10 */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_RADIANS */
        case AntlrSQLLexer::TFLOAT_RADIANS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_RADIANS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatRadiansLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_RADIANS */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_TO_TINT */
        case AntlrSQLLexer::TFLOAT_TO_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_TO_TINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatToTintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_TO_TINT */

        /* BEGIN CODEGEN PARSER GLUE: TINT_TO_TFLOAT */
        case AntlrSQLLexer::TINT_TO_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TINT_TO_TFLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintToTfloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_TO_TFLOAT */
        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_TNUMBER_TBOX */
        case AntlrSQLLexer::ADJACENT_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADJACENT_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_TNUMBER_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_TNUMBER_TBOX */
        case AntlrSQLLexer::AFTER_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("AFTER_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_TNUMBER_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_TNUMBER_TBOX */
        case AntlrSQLLexer::BEFORE_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("BEFORE_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_TNUMBER_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TNUMBER_TBOX */
        case AntlrSQLLexer::CONTAINED_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINED_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TNUMBER_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_TNUMBER_TBOX */
        case AntlrSQLLexer::CONTAINS_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINS_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_TNUMBER_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_TNUMBER_TBOX */
        case AntlrSQLLexer::LEFT_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("LEFT_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_TNUMBER_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TCBUFFER_STBOX */
        case AntlrSQLLexer::NAD_TCBUFFER_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAD_TCBUFFER_STBOX requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTcbufferStboxLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TCBUFFER_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TFLOAT_TBOX */
        case AntlrSQLLexer::NAD_TFLOAT_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("NAD_TFLOAT_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTfloatTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TFLOAT_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TGEO_STBOX */
        case AntlrSQLLexer::NAD_TGEO_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAD_TGEO_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTgeoStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TGEO_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TINT_TBOX */
        case AntlrSQLLexer::NAD_TINT_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("NAD_TINT_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTintTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TINT_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TNPOINT_STBOX */
        case AntlrSQLLexer::NAD_TNPOINT_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAD_TNPOINT_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTnpointStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TNPOINT_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TPOSE_STBOX */
        case AntlrSQLLexer::NAD_TPOSE_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAD_TPOSE_STBOX requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTposeStboxLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TPOSE_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_TNUMBER_TBOX */
        case AntlrSQLLexer::OVERAFTER_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERAFTER_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_TNUMBER_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_TNUMBER_TBOX */
        case AntlrSQLLexer::OVERBEFORE_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERBEFORE_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_TNUMBER_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_TNUMBER_TBOX */
        case AntlrSQLLexer::OVERLAPS_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLAPS_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_TNUMBER_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_TNUMBER_TBOX */
        case AntlrSQLLexer::OVERLEFT_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLEFT_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_TNUMBER_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_TNUMBER_TBOX */
        case AntlrSQLLexer::OVERRIGHT_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERRIGHT_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_TNUMBER_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_TNUMBER_TBOX */
        case AntlrSQLLexer::RIGHT_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("RIGHT_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_TNUMBER_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: SAME_TNUMBER_TBOX */
        case AntlrSQLLexer::SAME_TNUMBER_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SAME_TNUMBER_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SameTnumberTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_TNUMBER_TBOX */
        /* BEGIN CODEGEN PARSER GLUE: ABOVE_STBOX_TSPATIAL */
        case AntlrSQLLexer::ABOVE_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ABOVE_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AboveStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ABOVE_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: ABOVE_TSPATIAL_STBOX */
        case AntlrSQLLexer::ABOVE_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ABOVE_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AboveTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ABOVE_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_STBOX_TSPATIAL */
        case AntlrSQLLexer::ADJACENT_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ADJACENT_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_TBOX_TNUMBER */
        case AntlrSQLLexer::ADJACENT_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADJACENT_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_TSPATIAL_STBOX */
        case AntlrSQLLexer::ADJACENT_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ADJACENT_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_STBOX_TSPATIAL */
        case AntlrSQLLexer::AFTER_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("AFTER_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_TBOX_TNUMBER */
        case AntlrSQLLexer::AFTER_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("AFTER_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_TSPATIAL_STBOX */
        case AntlrSQLLexer::AFTER_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("AFTER_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_TSPATIAL_STBOX */



        /* BEGIN CODEGEN PARSER GLUE: BEFORE_STBOX_TSPATIAL */
        case AntlrSQLLexer::BEFORE_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("BEFORE_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_TBOX_TNUMBER */
        case AntlrSQLLexer::BEFORE_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("BEFORE_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_TSPATIAL_STBOX */
        case AntlrSQLLexer::BEFORE_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("BEFORE_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: BELOW_STBOX_TSPATIAL */
        case AntlrSQLLexer::BELOW_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("BELOW_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BelowStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: BELOW_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: BELOW_TSPATIAL_STBOX */
        case AntlrSQLLexer::BELOW_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("BELOW_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BelowTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: BELOW_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_STBOX_TSPATIAL */
        case AntlrSQLLexer::CONTAINED_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("CONTAINED_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TBOX_TNUMBER */
        case AntlrSQLLexer::CONTAINED_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINED_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TSPATIAL_STBOX */
        case AntlrSQLLexer::CONTAINED_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("CONTAINED_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_STBOX_TSPATIAL */
        case AntlrSQLLexer::CONTAINS_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("CONTAINS_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_TBOX_TNUMBER */
        case AntlrSQLLexer::CONTAINS_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINS_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_TSPATIAL_STBOX */
        case AntlrSQLLexer::CONTAINS_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("CONTAINS_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_TSPATIAL_STBOX */



        /* BEGIN CODEGEN PARSER GLUE: LEFT_STBOX_TSPATIAL */
        case AntlrSQLLexer::LEFT_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("LEFT_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_TBOX_TNUMBER */
        case AntlrSQLLexer::LEFT_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("LEFT_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_TSPATIAL_STBOX */
        case AntlrSQLLexer::LEFT_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("LEFT_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERABOVE_STBOX_TSPATIAL */
        case AntlrSQLLexer::OVERABOVE_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERABOVE_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OveraboveStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERABOVE_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERABOVE_TSPATIAL_STBOX */
        case AntlrSQLLexer::OVERABOVE_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERABOVE_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OveraboveTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERABOVE_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_STBOX_TSPATIAL */
        case AntlrSQLLexer::OVERAFTER_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERAFTER_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_TBOX_TNUMBER */
        case AntlrSQLLexer::OVERAFTER_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERAFTER_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_TSPATIAL_STBOX */
        case AntlrSQLLexer::OVERAFTER_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERAFTER_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_TSPATIAL_STBOX */



        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_STBOX_TSPATIAL */
        case AntlrSQLLexer::OVERBEFORE_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERBEFORE_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_TBOX_TNUMBER */
        case AntlrSQLLexer::OVERBEFORE_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERBEFORE_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_TSPATIAL_STBOX */
        case AntlrSQLLexer::OVERBEFORE_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERBEFORE_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBELOW_STBOX_TSPATIAL */
        case AntlrSQLLexer::OVERBELOW_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERBELOW_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbelowStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBELOW_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBELOW_TSPATIAL_STBOX */
        case AntlrSQLLexer::OVERBELOW_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERBELOW_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbelowTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBELOW_TSPATIAL_STBOX */



        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_STBOX_TSPATIAL */
        case AntlrSQLLexer::OVERLAPS_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERLAPS_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_TBOX_TNUMBER */
        case AntlrSQLLexer::OVERLAPS_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLAPS_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_TSPATIAL_STBOX */
        case AntlrSQLLexer::OVERLAPS_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERLAPS_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_STBOX_TSPATIAL */
        case AntlrSQLLexer::OVERLEFT_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERLEFT_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_TBOX_TNUMBER */
        case AntlrSQLLexer::OVERLEFT_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLEFT_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_TSPATIAL_STBOX */
        case AntlrSQLLexer::OVERLEFT_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERLEFT_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_STBOX_TSPATIAL */
        case AntlrSQLLexer::OVERRIGHT_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERRIGHT_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_TBOX_TNUMBER */
        case AntlrSQLLexer::OVERRIGHT_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERRIGHT_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_TSPATIAL_STBOX */
        case AntlrSQLLexer::OVERRIGHT_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("OVERRIGHT_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_STBOX_TSPATIAL */
        case AntlrSQLLexer::RIGHT_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("RIGHT_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_TBOX_TNUMBER */
        case AntlrSQLLexer::RIGHT_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("RIGHT_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_TSPATIAL_STBOX */
        case AntlrSQLLexer::RIGHT_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("RIGHT_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: SAME_STBOX_TSPATIAL */
        case AntlrSQLLexer::SAME_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("SAME_STBOX_TSPATIAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SameStboxTspatialLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: SAME_TBOX_TNUMBER */
        case AntlrSQLLexer::SAME_TBOX_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SAME_TBOX_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SameTboxTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_TBOX_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: SAME_TSPATIAL_STBOX */
        case AntlrSQLLexer::SAME_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("SAME_TSPATIAL_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SameTspatialStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_TSPATIAL_STBOX */
        /* BEGIN CODEGEN PARSER GLUE: TPOINT_LENGTH */
        case AntlrSQLLexer::TPOINT_LENGTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TPOINT_LENGTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TpointLengthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOINT_LENGTH */
        /* BEGIN CODEGEN PARSER GLUE: ABOVE_STBOX_STBOX */
        case AntlrSQLLexer::ABOVE_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ABOVE_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AboveStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ABOVE_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_STBOX_STBOX */
        case AntlrSQLLexer::ADJACENT_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_STBOX_STBOX */
        case AntlrSQLLexer::AFTER_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: BACK_STBOX_STBOX */
        case AntlrSQLLexer::BACK_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BACK_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BackStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BACK_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_STBOX_STBOX */
        case AntlrSQLLexer::BEFORE_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: BELOW_STBOX_STBOX */
        case AntlrSQLLexer::BELOW_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BELOW_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BelowStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BELOW_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_STBOX_STBOX */
        case AntlrSQLLexer::CONTAINED_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_STBOX_STBOX */
        case AntlrSQLLexer::CONTAINS_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: FRONT_STBOX_STBOX */
        case AntlrSQLLexer::FRONT_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("FRONT_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FrontStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: FRONT_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_STBOX_STBOX */
        case AntlrSQLLexer::LEFT_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_STBOX_STBOX */
        case AntlrSQLLexer::NAD_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NAD_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERABOVE_STBOX_STBOX */
        case AntlrSQLLexer::OVERABOVE_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERABOVE_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OveraboveStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERABOVE_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_STBOX_STBOX */
        case AntlrSQLLexer::OVERAFTER_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBACK_STBOX_STBOX */
        case AntlrSQLLexer::OVERBACK_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBACK_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbackStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBACK_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_STBOX_STBOX */
        case AntlrSQLLexer::OVERBEFORE_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBELOW_STBOX_STBOX */
        case AntlrSQLLexer::OVERBELOW_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBELOW_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbelowStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBELOW_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERFRONT_STBOX_STBOX */
        case AntlrSQLLexer::OVERFRONT_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERFRONT_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverfrontStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERFRONT_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_STBOX_STBOX */
        case AntlrSQLLexer::OVERLAPS_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLAPS_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_STBOX_STBOX */
        case AntlrSQLLexer::OVERLEFT_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_STBOX_STBOX */
        case AntlrSQLLexer::OVERRIGHT_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_STBOX_STBOX */
        case AntlrSQLLexer::RIGHT_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: SAME_STBOX_STBOX */
        case AntlrSQLLexer::SAME_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SAME_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SameStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_CMP */
        case AntlrSQLLexer::STBOX_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOX_CMP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxCmpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_CMP */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_EQ */
        case AntlrSQLLexer::STBOX_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOX_EQ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxEqLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_EQ */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_GE */
        case AntlrSQLLexer::STBOX_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOX_GE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxGeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_GE */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_GT */
        case AntlrSQLLexer::STBOX_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOX_GT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxGtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_GT */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_LE */
        case AntlrSQLLexer::STBOX_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOX_LE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxLeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_LE */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_LT */
        case AntlrSQLLexer::STBOX_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOX_LT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxLtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_LT */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_NE */
        case AntlrSQLLexer::STBOX_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOX_NE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxNeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_NE */
        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_TNUMBER_TNUMBER */
        case AntlrSQLLexer::ADJACENT_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_TNUMBER_TNUMBER */
        case AntlrSQLLexer::AFTER_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_TNUMBER_TNUMBER */
        case AntlrSQLLexer::BEFORE_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TNUMBER_TNUMBER */
        case AntlrSQLLexer::CONTAINED_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_TNUMBER_TNUMBER */
        case AntlrSQLLexer::CONTAINS_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_TNUMBER_TNUMBER */
        case AntlrSQLLexer::LEFT_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_TNUMBER_TNUMBER */
        case AntlrSQLLexer::OVERAFTER_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_TNUMBER_TNUMBER */
        case AntlrSQLLexer::OVERBEFORE_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_TNUMBER_TNUMBER */
        case AntlrSQLLexer::OVERLAPS_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLAPS_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_TNUMBER_TNUMBER */
        case AntlrSQLLexer::OVERLEFT_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_TNUMBER_TNUMBER */
        case AntlrSQLLexer::OVERRIGHT_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_TNUMBER_TNUMBER */
        case AntlrSQLLexer::RIGHT_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: SAME_TNUMBER_TNUMBER */
        case AntlrSQLLexer::SAME_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SAME_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SameTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_TNUMBER_TNUMBER */
        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TNPOINT_TNPOINT */
        case AntlrSQLLexer::ALWAYS_EQ_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ALWAYS_EQ_TNPOINT_TNPOINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTnpointTnpointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TPOSE_TPOSE */
        case AntlrSQLLexer::ALWAYS_EQ_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ALWAYS_EQ_TPOSE_TPOSE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTposeTposeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TNPOINT_TNPOINT */
        case AntlrSQLLexer::ALWAYS_NE_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ALWAYS_NE_TNPOINT_TNPOINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTnpointTnpointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TPOSE_TPOSE */
        case AntlrSQLLexer::ALWAYS_NE_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ALWAYS_NE_TPOSE_TPOSE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTposeTposeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TNPOINT_TNPOINT */
        case AntlrSQLLexer::EVER_EQ_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("EVER_EQ_TNPOINT_TNPOINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTnpointTnpointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TPOSE_TPOSE */
        case AntlrSQLLexer::EVER_EQ_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("EVER_EQ_TPOSE_TPOSE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTposeTposeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TNPOINT_TNPOINT */
        case AntlrSQLLexer::EVER_NE_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("EVER_NE_TNPOINT_TNPOINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTnpointTnpointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TPOSE_TPOSE */
        case AntlrSQLLexer::EVER_NE_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("EVER_NE_TPOSE_TPOSE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTposeTposeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TNPOINT_TNPOINT */
        case AntlrSQLLexer::NAD_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NAD_TNPOINT_TNPOINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTnpointTnpointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TPOSE_TPOSE */
        case AntlrSQLLexer::NAD_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NAD_TPOSE_TPOSE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTposeTposeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TPOSE_TPOSE */
        /* BEGIN CODEGEN PARSER GLUE: ADD_TNUMBER_TNUMBER */
        case AntlrSQLLexer::ADD_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADD_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: DIV_TNUMBER_TNUMBER */
        case AntlrSQLLexer::DIV_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DIV_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DivTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DIV_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: MUL_TNUMBER_TNUMBER */
        case AntlrSQLLexer::MUL_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MUL_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MulTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MUL_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: SUB_TNUMBER_TNUMBER */
        case AntlrSQLLexer::SUB_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SUB_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SubTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SUB_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: TCONTAINS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::TCONTAINS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TCONTAINS_TCBUFFER_TCBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcontainsTcbufferTcbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCONTAINS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TCONTAINS_TGEO_TGEO */
        case AntlrSQLLexer::TCONTAINS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TCONTAINS_TGEO_TGEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcontainsTgeoTgeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCONTAINS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TCOVERS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::TCOVERS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TCOVERS_TCBUFFER_TCBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcoversTcbufferTcbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCOVERS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TCOVERS_TGEO_TGEO */
        case AntlrSQLLexer::TCOVERS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TCOVERS_TGEO_TGEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcoversTgeoTgeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCOVERS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::TDISTANCE_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TDISTANCE_TCBUFFER_TCBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTcbufferTcbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TGEO_TGEO */
        case AntlrSQLLexer::TDISTANCE_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TDISTANCE_TGEO_TGEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTgeoTgeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TNPOINT_TNPOINT */
        case AntlrSQLLexer::TDISTANCE_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TDISTANCE_TNPOINT_TNPOINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTnpointTnpointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TNUMBER_TNUMBER */
        case AntlrSQLLexer::TDISTANCE_TNUMBER_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TDISTANCE_TNUMBER_TNUMBER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTnumberTnumberLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TNUMBER_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TPOSE_TPOSE */
        case AntlrSQLLexer::TDISTANCE_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TDISTANCE_TPOSE_TPOSE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTposeTposeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TPOSE_TPOSE */
        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPAN_SPAN */
        case AntlrSQLLexer::CONTAINS_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_SPAN_SPAN */
        case AntlrSQLLexer::CONTAINED_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_SPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPAN_SPAN */
        case AntlrSQLLexer::ADJACENT_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SPAN_SPAN */
        case AntlrSQLLexer::LEFT_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SPAN_SPAN */
        case AntlrSQLLexer::RIGHT_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_SPAN_SPAN */
        case AntlrSQLLexer::OVERLAPS_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLAPS_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_SPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SPAN_SPAN */
        case AntlrSQLLexer::OVERLEFT_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SPAN_SPAN */
        case AntlrSQLLexer::OVERRIGHT_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SPAN_SPAN */
        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SET_SET */
        case AntlrSQLLexer::CONTAINS_SET_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SET_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSetSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SET_SET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_SET_SET */
        case AntlrSQLLexer::CONTAINED_SET_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_SET_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedSetSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_SET_SET */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SET_SET */
        case AntlrSQLLexer::LEFT_SET_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SET_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSetSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SET_SET */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SET_SET */
        case AntlrSQLLexer::RIGHT_SET_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SET_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSetSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SET_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_SET_SET */
        case AntlrSQLLexer::OVERLAPS_SET_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLAPS_SET_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsSetSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_SET_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SET_SET */
        case AntlrSQLLexer::OVERLEFT_SET_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SET_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSetSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SET_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SET_SET */
        case AntlrSQLLexer::OVERRIGHT_SET_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SET_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSetSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SET_SET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPANSET_SPAN */
        case AntlrSQLLexer::CONTAINS_SPANSET_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPANSET_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpansetSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPANSET_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_SPANSET_SPAN */
        case AntlrSQLLexer::CONTAINED_SPANSET_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_SPANSET_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedSpansetSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_SPANSET_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPANSET_SPAN */
        case AntlrSQLLexer::ADJACENT_SPANSET_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPANSET_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpansetSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPANSET_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SPANSET_SPAN */
        case AntlrSQLLexer::LEFT_SPANSET_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SPANSET_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSpansetSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SPANSET_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SPANSET_SPAN */
        case AntlrSQLLexer::RIGHT_SPANSET_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SPANSET_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSpansetSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SPANSET_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_SPANSET_SPAN */
        case AntlrSQLLexer::OVERLAPS_SPANSET_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLAPS_SPANSET_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsSpansetSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_SPANSET_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SPANSET_SPAN */
        case AntlrSQLLexer::OVERLEFT_SPANSET_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SPANSET_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSpansetSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SPANSET_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SPANSET_SPAN */
        case AntlrSQLLexer::OVERRIGHT_SPANSET_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SPANSET_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSpansetSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SPANSET_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPANSET_SPANSET */
        case AntlrSQLLexer::CONTAINS_SPANSET_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPANSET_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpansetSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPANSET_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_SPANSET_SPANSET */
        case AntlrSQLLexer::CONTAINED_SPANSET_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_SPANSET_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedSpansetSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_SPANSET_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPANSET_SPANSET */
        case AntlrSQLLexer::ADJACENT_SPANSET_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPANSET_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpansetSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPANSET_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SPANSET_SPANSET */
        case AntlrSQLLexer::LEFT_SPANSET_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SPANSET_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSpansetSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SPANSET_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SPANSET_SPANSET */
        case AntlrSQLLexer::RIGHT_SPANSET_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SPANSET_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSpansetSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SPANSET_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_SPANSET_SPANSET */
        case AntlrSQLLexer::OVERLAPS_SPANSET_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLAPS_SPANSET_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsSpansetSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_SPANSET_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SPANSET_SPANSET */
        case AntlrSQLLexer::OVERLEFT_SPANSET_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SPANSET_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSpansetSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SPANSET_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SPANSET_SPANSET */
        case AntlrSQLLexer::OVERRIGHT_SPANSET_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SPANSET_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSpansetSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SPANSET_SPANSET */
        /* BEGIN CODEGEN PARSER GLUE: UNION_SPAN_SPAN */
        case AntlrSQLLexer::UNION_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPAN_SPAN */
        case AntlrSQLLexer::INTERSECTION_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPAN_SPAN */
        case AntlrSQLLexer::MINUS_SPAN_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPAN_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpanSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPAN_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SET_SET */
        case AntlrSQLLexer::UNION_SET_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SET_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSetSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SET_SET */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SET_SET */
        case AntlrSQLLexer::INTERSECTION_SET_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SET_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSetSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SET_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SET_SET */
        case AntlrSQLLexer::MINUS_SET_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SET_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSetSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SET_SET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPANSET_SPANSET */
        case AntlrSQLLexer::UNION_SPANSET_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPANSET_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpansetSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPANSET_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPANSET_SPANSET */
        case AntlrSQLLexer::INTERSECTION_SPANSET_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPANSET_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpansetSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPANSET_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPANSET_SPANSET */
        case AntlrSQLLexer::MINUS_SPANSET_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPANSET_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpansetSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPANSET_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPAN_SPANSET */
        case AntlrSQLLexer::UNION_SPAN_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPAN_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpanSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPAN_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPAN_SPANSET */
        case AntlrSQLLexer::INTERSECTION_SPAN_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPAN_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpanSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPAN_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPAN_SPANSET */
        case AntlrSQLLexer::MINUS_SPAN_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPAN_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpanSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPAN_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPANSET_SPAN */
        case AntlrSQLLexer::UNION_SPANSET_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPANSET_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpansetSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPANSET_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPANSET_SPAN */
        case AntlrSQLLexer::INTERSECTION_SPANSET_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPANSET_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpansetSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPANSET_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPANSET_SPAN */
        case AntlrSQLLexer::MINUS_SPANSET_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPANSET_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpansetSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPANSET_SPAN */
        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPAN_FLOAT */
        case AntlrSQLLexer::ADJACENT_SPAN_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPAN_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpanFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPAN_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPAN_INT */
        case AntlrSQLLexer::ADJACENT_SPAN_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPAN_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpanIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPAN_INT */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPANSET_BIGINT */
        case AntlrSQLLexer::ADJACENT_SPANSET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPANSET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpansetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPANSET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPANSET_FLOAT */
        case AntlrSQLLexer::ADJACENT_SPANSET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPANSET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpansetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPANSET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPANSET_INT */
        case AntlrSQLLexer::ADJACENT_SPANSET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPANSET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpansetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPANSET_INT */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_TBOX_TBOX */
        case AntlrSQLLexer::BEFORE_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_BIGINT_SET */
        case AntlrSQLLexer::CONTAINED_BIGINT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_BIGINT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedBigintSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_BIGINT_SET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_BIGINT_SPAN */
        case AntlrSQLLexer::CONTAINED_BIGINT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_BIGINT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedBigintSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_BIGINT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_BIGINT_SPANSET */
        case AntlrSQLLexer::CONTAINED_BIGINT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_BIGINT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedBigintSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_BIGINT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_FLOAT_SET */
        case AntlrSQLLexer::CONTAINED_FLOAT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_FLOAT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedFloatSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_FLOAT_SET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_FLOAT_SPAN */
        case AntlrSQLLexer::CONTAINED_FLOAT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_FLOAT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedFloatSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_FLOAT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_FLOAT_SPANSET */
        case AntlrSQLLexer::CONTAINED_FLOAT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_FLOAT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedFloatSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_FLOAT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_INT_SET */
        case AntlrSQLLexer::CONTAINED_INT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_INT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedIntSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_INT_SET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_INT_SPAN */
        case AntlrSQLLexer::CONTAINED_INT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_INT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedIntSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_INT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_INT_SPANSET */
        case AntlrSQLLexer::CONTAINED_INT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_INT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedIntSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_INT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TBOX_TBOX */
        case AntlrSQLLexer::CONTAINED_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SET_BIGINT */
        case AntlrSQLLexer::CONTAINS_SET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SET_FLOAT */
        case AntlrSQLLexer::CONTAINS_SET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SET_INT */
        case AntlrSQLLexer::CONTAINS_SET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SET_INT */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPAN_BIGINT */
        case AntlrSQLLexer::CONTAINS_SPAN_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPAN_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpanBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPAN_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPAN_FLOAT */
        case AntlrSQLLexer::CONTAINS_SPAN_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPAN_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpanFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPAN_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPAN_INT */
        case AntlrSQLLexer::CONTAINS_SPAN_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPAN_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpanIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPAN_INT */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPANSET_BIGINT */
        case AntlrSQLLexer::CONTAINS_SPANSET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPANSET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpansetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPANSET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPANSET_FLOAT */
        case AntlrSQLLexer::CONTAINS_SPANSET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPANSET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpansetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPANSET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPANSET_INT */
        case AntlrSQLLexer::CONTAINS_SPANSET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPANSET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpansetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPANSET_INT */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_TBOX_TBOX */
        case AntlrSQLLexer::CONTAINS_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SET_INT */
        case AntlrSQLLexer::DISTANCE_SET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SET_INT */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SPAN_INT */
        case AntlrSQLLexer::DISTANCE_SPAN_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SPAN_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSpanIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SPAN_INT */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SPANSET_INT */
        case AntlrSQLLexer::DISTANCE_SPANSET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SPANSET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSpansetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SPANSET_INT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_FLOAT_SET */
        case AntlrSQLLexer::INTERSECTION_FLOAT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_FLOAT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionFloatSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_FLOAT_SET */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_INT_SET */
        case AntlrSQLLexer::INTERSECTION_INT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_INT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionIntSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_INT_SET */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SET_BIGINT */
        case AntlrSQLLexer::INTERSECTION_SET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SET_FLOAT */
        case AntlrSQLLexer::INTERSECTION_SET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SET_INT */
        case AntlrSQLLexer::INTERSECTION_SET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SET_INT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPAN_BIGINT */
        case AntlrSQLLexer::INTERSECTION_SPAN_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPAN_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpanBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPAN_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPAN_FLOAT */
        case AntlrSQLLexer::INTERSECTION_SPAN_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPAN_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpanFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPAN_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPAN_INT */
        case AntlrSQLLexer::INTERSECTION_SPAN_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPAN_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpanIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPAN_INT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPANSET_BIGINT */
        case AntlrSQLLexer::INTERSECTION_SPANSET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPANSET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpansetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPANSET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPANSET_FLOAT */
        case AntlrSQLLexer::INTERSECTION_SPANSET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPANSET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpansetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPANSET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPANSET_INT */
        case AntlrSQLLexer::INTERSECTION_SPANSET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPANSET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpansetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPANSET_INT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_STBOX_STBOX */
        case AntlrSQLLexer::INTERSECTION_STBOX_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_STBOX_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionStboxStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_STBOX_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_TBOX_TBOX */
        case AntlrSQLLexer::INTERSECTION_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_BIGINT_SET */
        case AntlrSQLLexer::LEFT_BIGINT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_BIGINT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftBigintSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_BIGINT_SET */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_BIGINT_SPAN */
        case AntlrSQLLexer::LEFT_BIGINT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_BIGINT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftBigintSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_BIGINT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_BIGINT_SPANSET */
        case AntlrSQLLexer::LEFT_BIGINT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_BIGINT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftBigintSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_BIGINT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_FLOAT_SET */
        case AntlrSQLLexer::LEFT_FLOAT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_FLOAT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftFloatSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_FLOAT_SET */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_FLOAT_SPAN */
        case AntlrSQLLexer::LEFT_FLOAT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_FLOAT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftFloatSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_FLOAT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_FLOAT_SPANSET */
        case AntlrSQLLexer::LEFT_FLOAT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_FLOAT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftFloatSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_FLOAT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_INT_SET */
        case AntlrSQLLexer::LEFT_INT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_INT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftIntSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_INT_SET */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_INT_SPAN */
        case AntlrSQLLexer::LEFT_INT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_INT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftIntSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_INT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_INT_SPANSET */
        case AntlrSQLLexer::LEFT_INT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_INT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftIntSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_INT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SET_BIGINT */
        case AntlrSQLLexer::LEFT_SET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SET_FLOAT */
        case AntlrSQLLexer::LEFT_SET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SET_INT */
        case AntlrSQLLexer::LEFT_SET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SET_INT */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SPAN_BIGINT */
        case AntlrSQLLexer::LEFT_SPAN_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SPAN_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSpanBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SPAN_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SPAN_FLOAT */
        case AntlrSQLLexer::LEFT_SPAN_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SPAN_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSpanFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SPAN_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SPAN_INT */
        case AntlrSQLLexer::LEFT_SPAN_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SPAN_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSpanIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SPAN_INT */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SPANSET_BIGINT */
        case AntlrSQLLexer::LEFT_SPANSET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SPANSET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSpansetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SPANSET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SPANSET_FLOAT */
        case AntlrSQLLexer::LEFT_SPANSET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SPANSET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSpansetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SPANSET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SPANSET_INT */
        case AntlrSQLLexer::LEFT_SPANSET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SPANSET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSpansetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SPANSET_INT */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_TBOX_TBOX */
        case AntlrSQLLexer::LEFT_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_BIGINT_SET */
        case AntlrSQLLexer::MINUS_BIGINT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_BIGINT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusBigintSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_BIGINT_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_BIGINT_SPAN */
        case AntlrSQLLexer::MINUS_BIGINT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_BIGINT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusBigintSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_BIGINT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_BIGINT_SPANSET */
        case AntlrSQLLexer::MINUS_BIGINT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_BIGINT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusBigintSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_BIGINT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_FLOAT_SET */
        case AntlrSQLLexer::MINUS_FLOAT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_FLOAT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusFloatSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_FLOAT_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_FLOAT_SPAN */
        case AntlrSQLLexer::MINUS_FLOAT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_FLOAT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusFloatSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_FLOAT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_FLOAT_SPANSET */
        case AntlrSQLLexer::MINUS_FLOAT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_FLOAT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusFloatSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_FLOAT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_INT_SET */
        case AntlrSQLLexer::MINUS_INT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_INT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusIntSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_INT_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_INT_SPAN */
        case AntlrSQLLexer::MINUS_INT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_INT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusIntSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_INT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_INT_SPANSET */
        case AntlrSQLLexer::MINUS_INT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_INT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusIntSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_INT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SET_BIGINT */
        case AntlrSQLLexer::MINUS_SET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SET_FLOAT */
        case AntlrSQLLexer::MINUS_SET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SET_INT */
        case AntlrSQLLexer::MINUS_SET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SET_INT */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPAN_BIGINT */
        case AntlrSQLLexer::MINUS_SPAN_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPAN_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpanBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPAN_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPAN_FLOAT */
        case AntlrSQLLexer::MINUS_SPAN_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPAN_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpanFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPAN_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPAN_INT */
        case AntlrSQLLexer::MINUS_SPAN_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPAN_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpanIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPAN_INT */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPANSET_BIGINT */
        case AntlrSQLLexer::MINUS_SPANSET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPANSET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpansetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPANSET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPANSET_FLOAT */
        case AntlrSQLLexer::MINUS_SPANSET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPANSET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpansetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPANSET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPANSET_INT */
        case AntlrSQLLexer::MINUS_SPANSET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPANSET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpansetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPANSET_INT */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TBOXINT_TBOXINT */
        case AntlrSQLLexer::NAD_TBOXINT_TBOXINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NAD_TBOXINT_TBOXINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTboxintTboxintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TBOXINT_TBOXINT */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_TBOX_TBOX */
        case AntlrSQLLexer::OVERAFTER_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_TBOX_TBOX */
        case AntlrSQLLexer::OVERBEFORE_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_TBOX_TBOX */
        case AntlrSQLLexer::OVERLAPS_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLAPS_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_BIGINT_SET */
        case AntlrSQLLexer::OVERLEFT_BIGINT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_BIGINT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftBigintSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_BIGINT_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_BIGINT_SPAN */
        case AntlrSQLLexer::OVERLEFT_BIGINT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_BIGINT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftBigintSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_BIGINT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_BIGINT_SPANSET */
        case AntlrSQLLexer::OVERLEFT_BIGINT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_BIGINT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftBigintSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_BIGINT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_FLOAT_SET */
        case AntlrSQLLexer::OVERLEFT_FLOAT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_FLOAT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftFloatSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_FLOAT_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_FLOAT_SPAN */
        case AntlrSQLLexer::OVERLEFT_FLOAT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_FLOAT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftFloatSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_FLOAT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_FLOAT_SPANSET */
        case AntlrSQLLexer::OVERLEFT_FLOAT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_FLOAT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftFloatSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_FLOAT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_INT_SET */
        case AntlrSQLLexer::OVERLEFT_INT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_INT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftIntSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_INT_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_INT_SPAN */
        case AntlrSQLLexer::OVERLEFT_INT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_INT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftIntSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_INT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_INT_SPANSET */
        case AntlrSQLLexer::OVERLEFT_INT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_INT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftIntSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_INT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SET_BIGINT */
        case AntlrSQLLexer::OVERLEFT_SET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SET_FLOAT */
        case AntlrSQLLexer::OVERLEFT_SET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SET_INT */
        case AntlrSQLLexer::OVERLEFT_SET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SET_INT */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SPAN_BIGINT */
        case AntlrSQLLexer::OVERLEFT_SPAN_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SPAN_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSpanBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SPAN_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SPAN_FLOAT */
        case AntlrSQLLexer::OVERLEFT_SPAN_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SPAN_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSpanFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SPAN_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SPAN_INT */
        case AntlrSQLLexer::OVERLEFT_SPAN_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SPAN_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSpanIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SPAN_INT */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SPANSET_BIGINT */
        case AntlrSQLLexer::OVERLEFT_SPANSET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SPANSET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSpansetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SPANSET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SPANSET_FLOAT */
        case AntlrSQLLexer::OVERLEFT_SPANSET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SPANSET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSpansetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SPANSET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SPANSET_INT */
        case AntlrSQLLexer::OVERLEFT_SPANSET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SPANSET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSpansetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SPANSET_INT */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_TBOX_TBOX */
        case AntlrSQLLexer::OVERLEFT_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_BIGINT_SET */
        case AntlrSQLLexer::OVERRIGHT_BIGINT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_BIGINT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightBigintSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_BIGINT_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_BIGINT_SPAN */
        case AntlrSQLLexer::OVERRIGHT_BIGINT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_BIGINT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightBigintSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_BIGINT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_BIGINT_SPANSET */
        case AntlrSQLLexer::OVERRIGHT_BIGINT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_BIGINT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightBigintSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_BIGINT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_FLOAT_SET */
        case AntlrSQLLexer::OVERRIGHT_FLOAT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_FLOAT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightFloatSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_FLOAT_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_FLOAT_SPAN */
        case AntlrSQLLexer::OVERRIGHT_FLOAT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_FLOAT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightFloatSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_FLOAT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_FLOAT_SPANSET */
        case AntlrSQLLexer::OVERRIGHT_FLOAT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_FLOAT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightFloatSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_FLOAT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_INT_SET */
        case AntlrSQLLexer::OVERRIGHT_INT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_INT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightIntSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_INT_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_INT_SPAN */
        case AntlrSQLLexer::OVERRIGHT_INT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_INT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightIntSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_INT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_INT_SPANSET */
        case AntlrSQLLexer::OVERRIGHT_INT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_INT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightIntSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_INT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SET_BIGINT */
        case AntlrSQLLexer::OVERRIGHT_SET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SET_FLOAT */
        case AntlrSQLLexer::OVERRIGHT_SET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SET_INT */
        case AntlrSQLLexer::OVERRIGHT_SET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SET_INT */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SPAN_BIGINT */
        case AntlrSQLLexer::OVERRIGHT_SPAN_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SPAN_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSpanBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SPAN_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SPAN_FLOAT */
        case AntlrSQLLexer::OVERRIGHT_SPAN_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SPAN_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSpanFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SPAN_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SPAN_INT */
        case AntlrSQLLexer::OVERRIGHT_SPAN_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SPAN_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSpanIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SPAN_INT */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SPANSET_BIGINT */
        case AntlrSQLLexer::OVERRIGHT_SPANSET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SPANSET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSpansetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SPANSET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SPANSET_FLOAT */
        case AntlrSQLLexer::OVERRIGHT_SPANSET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SPANSET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSpansetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SPANSET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SPANSET_INT */
        case AntlrSQLLexer::OVERRIGHT_SPANSET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SPANSET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSpansetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SPANSET_INT */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_TBOX_TBOX */
        case AntlrSQLLexer::OVERRIGHT_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_BIGINT_SET */
        case AntlrSQLLexer::RIGHT_BIGINT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_BIGINT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightBigintSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_BIGINT_SET */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_BIGINT_SPAN */
        case AntlrSQLLexer::RIGHT_BIGINT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_BIGINT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightBigintSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_BIGINT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_BIGINT_SPANSET */
        case AntlrSQLLexer::RIGHT_BIGINT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_BIGINT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightBigintSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_BIGINT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_FLOAT_SET */
        case AntlrSQLLexer::RIGHT_FLOAT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_FLOAT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightFloatSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_FLOAT_SET */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_FLOAT_SPAN */
        case AntlrSQLLexer::RIGHT_FLOAT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_FLOAT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightFloatSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_FLOAT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_FLOAT_SPANSET */
        case AntlrSQLLexer::RIGHT_FLOAT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_FLOAT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightFloatSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_FLOAT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_INT_SET */
        case AntlrSQLLexer::RIGHT_INT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_INT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightIntSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_INT_SET */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_INT_SPAN */
        case AntlrSQLLexer::RIGHT_INT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_INT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightIntSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_INT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_INT_SPANSET */
        case AntlrSQLLexer::RIGHT_INT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_INT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightIntSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_INT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SET_BIGINT */
        case AntlrSQLLexer::RIGHT_SET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SET_FLOAT */
        case AntlrSQLLexer::RIGHT_SET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SET_INT */
        case AntlrSQLLexer::RIGHT_SET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SET_INT */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SPAN_BIGINT */
        case AntlrSQLLexer::RIGHT_SPAN_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SPAN_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSpanBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SPAN_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SPAN_FLOAT */
        case AntlrSQLLexer::RIGHT_SPAN_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SPAN_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSpanFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SPAN_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SPAN_INT */
        case AntlrSQLLexer::RIGHT_SPAN_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SPAN_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSpanIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SPAN_INT */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SPANSET_BIGINT */
        case AntlrSQLLexer::RIGHT_SPANSET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SPANSET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSpansetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SPANSET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SPANSET_FLOAT */
        case AntlrSQLLexer::RIGHT_SPANSET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SPANSET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSpansetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SPANSET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SPANSET_INT */
        case AntlrSQLLexer::RIGHT_SPANSET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SPANSET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSpansetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SPANSET_INT */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_TBOX_TBOX */
        case AntlrSQLLexer::RIGHT_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: SAME_TBOX_TBOX */
        case AntlrSQLLexer::SAME_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SAME_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SameTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_EQ */
        case AntlrSQLLexer::TBOX_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOX_EQ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxEqLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_EQ */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_GE */
        case AntlrSQLLexer::TBOX_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOX_GE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxGeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_GE */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_GT */
        case AntlrSQLLexer::TBOX_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOX_GT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxGtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_GT */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_LE */
        case AntlrSQLLexer::TBOX_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOX_LE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxLeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_LE */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_LT */
        case AntlrSQLLexer::TBOX_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOX_LT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxLtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_LT */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_NE */
        case AntlrSQLLexer::TBOX_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOX_NE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxNeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_NE */

        /* BEGIN CODEGEN PARSER GLUE: UNION_BIGINT_SET */
        case AntlrSQLLexer::UNION_BIGINT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_BIGINT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionBigintSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_BIGINT_SET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_BIGINT_SPAN */
        case AntlrSQLLexer::UNION_BIGINT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_BIGINT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionBigintSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_BIGINT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: UNION_BIGINT_SPANSET */
        case AntlrSQLLexer::UNION_BIGINT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_BIGINT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionBigintSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_BIGINT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_FLOAT_SET */
        case AntlrSQLLexer::UNION_FLOAT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_FLOAT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionFloatSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_FLOAT_SET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_FLOAT_SPAN */
        case AntlrSQLLexer::UNION_FLOAT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_FLOAT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionFloatSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_FLOAT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: UNION_FLOAT_SPANSET */
        case AntlrSQLLexer::UNION_FLOAT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_FLOAT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionFloatSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_FLOAT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_INT_SET */
        case AntlrSQLLexer::UNION_INT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_INT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionIntSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_INT_SET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_INT_SPAN */
        case AntlrSQLLexer::UNION_INT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_INT_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionIntSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_INT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: UNION_INT_SPANSET */
        case AntlrSQLLexer::UNION_INT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_INT_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionIntSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_INT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SET_BIGINT */
        case AntlrSQLLexer::UNION_SET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SET_FLOAT */
        case AntlrSQLLexer::UNION_SET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SET_INT */
        case AntlrSQLLexer::UNION_SET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SET_INT */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPAN_BIGINT */
        case AntlrSQLLexer::UNION_SPAN_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPAN_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpanBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPAN_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPAN_FLOAT */
        case AntlrSQLLexer::UNION_SPAN_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPAN_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpanFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPAN_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPAN_INT */
        case AntlrSQLLexer::UNION_SPAN_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPAN_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpanIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPAN_INT */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPANSET_BIGINT */
        case AntlrSQLLexer::UNION_SPANSET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPANSET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpansetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPANSET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPANSET_FLOAT */
        case AntlrSQLLexer::UNION_SPANSET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPANSET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpansetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPANSET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPANSET_INT */
        case AntlrSQLLexer::UNION_SPANSET_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPANSET_INT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpansetIntLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPANSET_INT */
        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPAN_DATE */
        case AntlrSQLLexer::ADJACENT_SPAN_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPAN_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpanDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPAN_DATE */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPAN_SPANSET */
        case AntlrSQLLexer::ADJACENT_SPAN_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPAN_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpanSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPAN_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPAN_TIMESTAMPTZ */
        case AntlrSQLLexer::ADJACENT_SPAN_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPAN_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpanTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPAN_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPANSET_DATE */
        case AntlrSQLLexer::ADJACENT_SPANSET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPANSET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpansetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPANSET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPANSET_TIMESTAMPTZ */
        case AntlrSQLLexer::ADJACENT_SPANSET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPANSET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpansetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPANSET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_DATE_SPAN */
        case AntlrSQLLexer::AFTER_DATE_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_DATE_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterDateSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_DATE_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_DATE_SPANSET */
        case AntlrSQLLexer::AFTER_DATE_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_DATE_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterDateSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_DATE_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_SET_DATE */
        case AntlrSQLLexer::AFTER_SET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_SET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterSetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_SET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_SET_TIMESTAMPTZ */
        case AntlrSQLLexer::AFTER_SET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_SET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterSetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_SET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_SPAN_DATE */
        case AntlrSQLLexer::AFTER_SPAN_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_SPAN_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterSpanDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_SPAN_DATE */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_SPAN_TIMESTAMPTZ */
        case AntlrSQLLexer::AFTER_SPAN_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_SPAN_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterSpanTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_SPAN_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_SPANSET_DATE */
        case AntlrSQLLexer::AFTER_SPANSET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_SPANSET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterSpansetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_SPANSET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_SPANSET_TIMESTAMPTZ */
        case AntlrSQLLexer::AFTER_SPANSET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_SPANSET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterSpansetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_SPANSET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_TIMESTAMPTZ_SET */
        case AntlrSQLLexer::AFTER_TIMESTAMPTZ_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_TIMESTAMPTZ_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterTimestamptzSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_TIMESTAMPTZ_SET */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_TIMESTAMPTZ_SPAN */
        case AntlrSQLLexer::AFTER_TIMESTAMPTZ_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_TIMESTAMPTZ_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterTimestamptzSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_TIMESTAMPTZ_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_TIMESTAMPTZ_SPANSET */
        case AntlrSQLLexer::AFTER_TIMESTAMPTZ_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_TIMESTAMPTZ_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterTimestamptzSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_TIMESTAMPTZ_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_DATE_SET */
        case AntlrSQLLexer::BEFORE_DATE_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_DATE_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeDateSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_DATE_SET */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_DATE_SPAN */
        case AntlrSQLLexer::BEFORE_DATE_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_DATE_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeDateSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_DATE_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_DATE_SPANSET */
        case AntlrSQLLexer::BEFORE_DATE_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_DATE_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeDateSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_DATE_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_SET_DATE */
        case AntlrSQLLexer::BEFORE_SET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_SET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeSetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_SET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_SET_TIMESTAMPTZ */
        case AntlrSQLLexer::BEFORE_SET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_SET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeSetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_SET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_SPAN_DATE */
        case AntlrSQLLexer::BEFORE_SPAN_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_SPAN_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeSpanDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_SPAN_DATE */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_SPAN_TIMESTAMPTZ */
        case AntlrSQLLexer::BEFORE_SPAN_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_SPAN_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeSpanTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_SPAN_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_SPANSET_DATE */
        case AntlrSQLLexer::BEFORE_SPANSET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_SPANSET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeSpansetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_SPANSET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_SPANSET_TIMESTAMPTZ */
        case AntlrSQLLexer::BEFORE_SPANSET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_SPANSET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeSpansetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_SPANSET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_TIMESTAMPTZ_SET */
        case AntlrSQLLexer::BEFORE_TIMESTAMPTZ_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_TIMESTAMPTZ_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeTimestamptzSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_TIMESTAMPTZ_SET */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_TIMESTAMPTZ_SPAN */
        case AntlrSQLLexer::BEFORE_TIMESTAMPTZ_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_TIMESTAMPTZ_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeTimestamptzSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_TIMESTAMPTZ_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_TIMESTAMPTZ_SPANSET */
        case AntlrSQLLexer::BEFORE_TIMESTAMPTZ_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("BEFORE_TIMESTAMPTZ_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeTimestamptzSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_TIMESTAMPTZ_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_DATE_SET */
        case AntlrSQLLexer::CONTAINED_DATE_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_DATE_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedDateSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_DATE_SET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_DATE_SPAN */
        case AntlrSQLLexer::CONTAINED_DATE_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_DATE_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedDateSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_DATE_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_DATE_SPANSET */
        case AntlrSQLLexer::CONTAINED_DATE_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_DATE_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedDateSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_DATE_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_SPAN_SPANSET */
        case AntlrSQLLexer::CONTAINED_SPAN_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_SPAN_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedSpanSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_SPAN_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TIMESTAMPTZ_SET */
        case AntlrSQLLexer::CONTAINED_TIMESTAMPTZ_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_TIMESTAMPTZ_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedTimestamptzSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TIMESTAMPTZ_SET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TIMESTAMPTZ_SPAN */
        case AntlrSQLLexer::CONTAINED_TIMESTAMPTZ_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_TIMESTAMPTZ_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedTimestamptzSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TIMESTAMPTZ_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TIMESTAMPTZ_SPANSET */
        case AntlrSQLLexer::CONTAINED_TIMESTAMPTZ_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_TIMESTAMPTZ_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedTimestamptzSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TIMESTAMPTZ_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SET_DATE */
        case AntlrSQLLexer::CONTAINS_SET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SET_TIMESTAMPTZ */
        case AntlrSQLLexer::CONTAINS_SET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPAN_DATE */
        case AntlrSQLLexer::CONTAINS_SPAN_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPAN_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpanDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPAN_DATE */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPAN_SPANSET */
        case AntlrSQLLexer::CONTAINS_SPAN_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPAN_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpanSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPAN_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPAN_TIMESTAMPTZ */
        case AntlrSQLLexer::CONTAINS_SPAN_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPAN_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpanTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPAN_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPANSET_DATE */
        case AntlrSQLLexer::CONTAINS_SPANSET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPANSET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpansetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPANSET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SPANSET_TIMESTAMPTZ */
        case AntlrSQLLexer::CONTAINS_SPANSET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SPANSET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSpansetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SPANSET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SPAN_SPANSET */
        case AntlrSQLLexer::LEFT_SPAN_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SPAN_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSpanSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SPAN_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_DATE_SET */
        case AntlrSQLLexer::OVERAFTER_DATE_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_DATE_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterDateSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_DATE_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_DATE_SPAN */
        case AntlrSQLLexer::OVERAFTER_DATE_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_DATE_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterDateSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_DATE_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_DATE_SPANSET */
        case AntlrSQLLexer::OVERAFTER_DATE_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_DATE_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterDateSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_DATE_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_SET_DATE */
        case AntlrSQLLexer::OVERAFTER_SET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_SET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterSetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_SET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_SET_TIMESTAMPTZ */
        case AntlrSQLLexer::OVERAFTER_SET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_SET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterSetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_SET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_SPAN_DATE */
        case AntlrSQLLexer::OVERAFTER_SPAN_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_SPAN_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterSpanDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_SPAN_DATE */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_SPAN_TIMESTAMPTZ */
        case AntlrSQLLexer::OVERAFTER_SPAN_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_SPAN_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterSpanTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_SPAN_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_SPANSET_DATE */
        case AntlrSQLLexer::OVERAFTER_SPANSET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_SPANSET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterSpansetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_SPANSET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_SPANSET_TIMESTAMPTZ */
        case AntlrSQLLexer::OVERAFTER_SPANSET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_SPANSET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterSpansetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_SPANSET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_TIMESTAMPTZ_SET */
        case AntlrSQLLexer::OVERAFTER_TIMESTAMPTZ_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_TIMESTAMPTZ_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterTimestamptzSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_TIMESTAMPTZ_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_TIMESTAMPTZ_SPAN */
        case AntlrSQLLexer::OVERAFTER_TIMESTAMPTZ_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_TIMESTAMPTZ_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterTimestamptzSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_TIMESTAMPTZ_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_TIMESTAMPTZ_SPANSET */
        case AntlrSQLLexer::OVERAFTER_TIMESTAMPTZ_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERAFTER_TIMESTAMPTZ_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterTimestamptzSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_TIMESTAMPTZ_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_DATE_SET */
        case AntlrSQLLexer::OVERBEFORE_DATE_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_DATE_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeDateSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_DATE_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_DATE_SPAN */
        case AntlrSQLLexer::OVERBEFORE_DATE_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_DATE_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeDateSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_DATE_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_DATE_SPANSET */
        case AntlrSQLLexer::OVERBEFORE_DATE_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_DATE_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeDateSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_DATE_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_SET_DATE */
        case AntlrSQLLexer::OVERBEFORE_SET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_SET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeSetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_SET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_SET_TIMESTAMPTZ */
        case AntlrSQLLexer::OVERBEFORE_SET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_SET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeSetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_SET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_SPAN_DATE */
        case AntlrSQLLexer::OVERBEFORE_SPAN_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_SPAN_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeSpanDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_SPAN_DATE */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_SPAN_TIMESTAMPTZ */
        case AntlrSQLLexer::OVERBEFORE_SPAN_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_SPAN_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeSpanTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_SPAN_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_SPANSET_DATE */
        case AntlrSQLLexer::OVERBEFORE_SPANSET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_SPANSET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeSpansetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_SPANSET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_SPANSET_TIMESTAMPTZ */
        case AntlrSQLLexer::OVERBEFORE_SPANSET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_SPANSET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeSpansetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_SPANSET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_TIMESTAMPTZ_SET */
        case AntlrSQLLexer::OVERBEFORE_TIMESTAMPTZ_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_TIMESTAMPTZ_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeTimestamptzSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_TIMESTAMPTZ_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_TIMESTAMPTZ_SPAN */
        case AntlrSQLLexer::OVERBEFORE_TIMESTAMPTZ_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_TIMESTAMPTZ_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeTimestamptzSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_TIMESTAMPTZ_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_TIMESTAMPTZ_SPANSET */
        case AntlrSQLLexer::OVERBEFORE_TIMESTAMPTZ_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERBEFORE_TIMESTAMPTZ_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeTimestamptzSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_TIMESTAMPTZ_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_SPAN_SPANSET */
        case AntlrSQLLexer::OVERLAPS_SPAN_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLAPS_SPAN_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsSpanSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_SPAN_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SPAN_SPANSET */
        case AntlrSQLLexer::OVERLEFT_SPAN_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SPAN_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSpanSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SPAN_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SPAN_SPANSET */
        case AntlrSQLLexer::OVERRIGHT_SPAN_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SPAN_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSpanSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SPAN_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SPAN_SPANSET */
        case AntlrSQLLexer::RIGHT_SPAN_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SPAN_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSpanSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SPAN_SPANSET */
        /* BEGIN CODEGEN PARSER GLUE: INTSPAN_LOWER */
        case AntlrSQLLexer::INTSPAN_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPAN_LOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspanLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPAN_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: INTSPAN_UPPER */
        case AntlrSQLLexer::INTSPAN_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPAN_UPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspanUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPAN_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: INTSPAN_WIDTH */
        case AntlrSQLLexer::INTSPAN_WIDTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPAN_WIDTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspanWidthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPAN_WIDTH */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSPAN_LOWER */
        case AntlrSQLLexer::BIGINTSPAN_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINTSPAN_LOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintspanLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSPAN_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSPAN_UPPER */
        case AntlrSQLLexer::BIGINTSPAN_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINTSPAN_UPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintspanUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSPAN_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSPAN_WIDTH */
        case AntlrSQLLexer::BIGINTSPAN_WIDTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINTSPAN_WIDTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintspanWidthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSPAN_WIDTH */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPAN_LOWER */
        case AntlrSQLLexer::FLOATSPAN_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPAN_LOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPAN_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPAN_UPPER */
        case AntlrSQLLexer::FLOATSPAN_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPAN_UPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPAN_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPAN_WIDTH */
        case AntlrSQLLexer::FLOATSPAN_WIDTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPAN_WIDTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanWidthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPAN_WIDTH */

        /* BEGIN CODEGEN PARSER GLUE: DATESPAN_LOWER */
        case AntlrSQLLexer::DATESPAN_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESPAN_LOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespanLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPAN_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: DATESPAN_UPPER */
        case AntlrSQLLexer::DATESPAN_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESPAN_UPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespanUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPAN_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPAN_LOWER */
        case AntlrSQLLexer::TSTZSPAN_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPAN_LOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspanLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPAN_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPAN_UPPER */
        case AntlrSQLLexer::TSTZSPAN_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPAN_UPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspanUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPAN_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: INTSPANSET_LOWER */
        case AntlrSQLLexer::INTSPANSET_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPANSET_LOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspansetLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPANSET_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: INTSPANSET_UPPER */
        case AntlrSQLLexer::INTSPANSET_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPANSET_UPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspansetUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPANSET_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSPANSET_LOWER */
        case AntlrSQLLexer::BIGINTSPANSET_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINTSPANSET_LOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintspansetLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSPANSET_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSPANSET_UPPER */
        case AntlrSQLLexer::BIGINTSPANSET_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINTSPANSET_UPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintspansetUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSPANSET_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPANSET_LOWER */
        case AntlrSQLLexer::FLOATSPANSET_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPANSET_LOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspansetLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPANSET_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPANSET_UPPER */
        case AntlrSQLLexer::FLOATSPANSET_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPANSET_UPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspansetUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPANSET_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPANSET_LOWER */
        case AntlrSQLLexer::TSTZSPANSET_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPANSET_LOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspansetLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPANSET_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPANSET_UPPER */
        case AntlrSQLLexer::TSTZSPANSET_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPANSET_UPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspansetUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPANSET_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: INTSET_START_VALUE */
        case AntlrSQLLexer::INTSET_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSET_START_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntsetStartValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSET_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: INTSET_END_VALUE */
        case AntlrSQLLexer::INTSET_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSET_END_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntsetEndValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSET_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSET_START_VALUE */
        case AntlrSQLLexer::BIGINTSET_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINTSET_START_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintsetStartValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSET_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSET_END_VALUE */
        case AntlrSQLLexer::BIGINTSET_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINTSET_END_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintsetEndValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSET_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSET_START_VALUE */
        case AntlrSQLLexer::FLOATSET_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSET_START_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatsetStartValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSET_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSET_END_VALUE */
        case AntlrSQLLexer::FLOATSET_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSET_END_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatsetEndValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSET_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: DATESET_START_VALUE */
        case AntlrSQLLexer::DATESET_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESET_START_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatesetStartValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESET_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: DATESET_END_VALUE */
        case AntlrSQLLexer::DATESET_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESET_END_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatesetEndValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESET_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSET_START_VALUE */
        case AntlrSQLLexer::TSTZSET_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSET_START_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzsetStartValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSET_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSET_END_VALUE */
        case AntlrSQLLexer::TSTZSET_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSET_END_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzsetEndValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSET_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: SPAN_LOWER_INC */
        case AntlrSQLLexer::SPAN_LOWER_INC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPAN_LOWER_INC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpanLowerIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPAN_LOWER_INC */

        /* BEGIN CODEGEN PARSER GLUE: SPAN_UPPER_INC */
        case AntlrSQLLexer::SPAN_UPPER_INC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPAN_UPPER_INC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpanUpperIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPAN_UPPER_INC */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_LOWER_INC */
        case AntlrSQLLexer::SPANSET_LOWER_INC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPANSET_LOWER_INC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetLowerIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_LOWER_INC */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_UPPER_INC */
        case AntlrSQLLexer::SPANSET_UPPER_INC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPANSET_UPPER_INC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetUpperIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_UPPER_INC */

        /* BEGIN CODEGEN PARSER GLUE: SET_NUM_VALUES */
        case AntlrSQLLexer::SET_NUM_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SET_NUM_VALUES requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetNumValuesLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_NUM_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_NUM_SPANS */
        case AntlrSQLLexer::SPANSET_NUM_SPANS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPANSET_NUM_SPANS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetNumSpansLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_NUM_SPANS */
        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_CMP */
        case AntlrSQLLexer::CBUFFER_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CBUFFER_CMP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferCmpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_CMP */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_EQ */
        case AntlrSQLLexer::CBUFFER_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CBUFFER_EQ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferEqLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_EQ */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_GE */
        case AntlrSQLLexer::CBUFFER_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CBUFFER_GE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferGeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_GE */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_GT */
        case AntlrSQLLexer::CBUFFER_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CBUFFER_GT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferGtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_GT */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_LE */
        case AntlrSQLLexer::CBUFFER_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CBUFFER_LE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferLeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_LE */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_LT */
        case AntlrSQLLexer::CBUFFER_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CBUFFER_LT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferLtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_LT */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_NE */
        case AntlrSQLLexer::CBUFFER_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CBUFFER_NE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferNeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_NE */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_NSAME */
        case AntlrSQLLexer::CBUFFER_NSAME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CBUFFER_NSAME requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferNsameLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_NSAME */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_SAME */
        case AntlrSQLLexer::CBUFFER_SAME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CBUFFER_SAME requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferSameLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_SAME */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_CBUFFER_CBUFFER */
        case AntlrSQLLexer::CONTAINS_CBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_CBUFFER_CBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsCbufferCbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_CBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: COVERS_CBUFFER_CBUFFER */
        case AntlrSQLLexer::COVERS_CBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("COVERS_CBUFFER_CBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CoversCbufferCbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: COVERS_CBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: DISJOINT_CBUFFER_CBUFFER */
        case AntlrSQLLexer::DISJOINT_CBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISJOINT_CBUFFER_CBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DisjointCbufferCbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISJOINT_CBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTS_CBUFFER_CBUFFER */
        case AntlrSQLLexer::INTERSECTS_CBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTS_CBUFFER_CBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectsCbufferCbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTS_CBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_CMP */
        case AntlrSQLLexer::NPOINT_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NPOINT_CMP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointCmpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_CMP */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_EQ */
        case AntlrSQLLexer::NPOINT_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NPOINT_EQ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointEqLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_EQ */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_GE */
        case AntlrSQLLexer::NPOINT_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NPOINT_GE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointGeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_GE */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_GT */
        case AntlrSQLLexer::NPOINT_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NPOINT_GT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointGtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_GT */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_LE */
        case AntlrSQLLexer::NPOINT_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NPOINT_LE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointLeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_LE */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_LT */
        case AntlrSQLLexer::NPOINT_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NPOINT_LT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointLtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_LT */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_NE */
        case AntlrSQLLexer::NPOINT_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NPOINT_NE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointNeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_NE */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_SAME */
        case AntlrSQLLexer::NPOINT_SAME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NPOINT_SAME requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointSameLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_SAME */

        /* BEGIN CODEGEN PARSER GLUE: NSEGMENT_CMP */
        case AntlrSQLLexer::NSEGMENT_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NSEGMENT_CMP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NsegmentCmpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NSEGMENT_CMP */

        /* BEGIN CODEGEN PARSER GLUE: NSEGMENT_EQ */
        case AntlrSQLLexer::NSEGMENT_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NSEGMENT_EQ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NsegmentEqLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NSEGMENT_EQ */

        /* BEGIN CODEGEN PARSER GLUE: NSEGMENT_GE */
        case AntlrSQLLexer::NSEGMENT_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NSEGMENT_GE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NsegmentGeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NSEGMENT_GE */

        /* BEGIN CODEGEN PARSER GLUE: NSEGMENT_GT */
        case AntlrSQLLexer::NSEGMENT_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NSEGMENT_GT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NsegmentGtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NSEGMENT_GT */

        /* BEGIN CODEGEN PARSER GLUE: NSEGMENT_LE */
        case AntlrSQLLexer::NSEGMENT_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NSEGMENT_LE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NsegmentLeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NSEGMENT_LE */

        /* BEGIN CODEGEN PARSER GLUE: NSEGMENT_LT */
        case AntlrSQLLexer::NSEGMENT_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NSEGMENT_LT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NsegmentLtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NSEGMENT_LT */

        /* BEGIN CODEGEN PARSER GLUE: NSEGMENT_NE */
        case AntlrSQLLexer::NSEGMENT_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NSEGMENT_NE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NsegmentNeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NSEGMENT_NE */

        /* BEGIN CODEGEN PARSER GLUE: POSE_CMP */
        case AntlrSQLLexer::POSE_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("POSE_CMP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseCmpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_CMP */

        /* BEGIN CODEGEN PARSER GLUE: POSE_EQ */
        case AntlrSQLLexer::POSE_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("POSE_EQ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseEqLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_EQ */

        /* BEGIN CODEGEN PARSER GLUE: POSE_GE */
        case AntlrSQLLexer::POSE_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("POSE_GE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseGeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_GE */

        /* BEGIN CODEGEN PARSER GLUE: POSE_GT */
        case AntlrSQLLexer::POSE_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("POSE_GT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseGtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_GT */

        /* BEGIN CODEGEN PARSER GLUE: POSE_LE */
        case AntlrSQLLexer::POSE_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("POSE_LE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseLeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_LE */

        /* BEGIN CODEGEN PARSER GLUE: POSE_LT */
        case AntlrSQLLexer::POSE_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("POSE_LT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseLtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_LT */

        /* BEGIN CODEGEN PARSER GLUE: POSE_NE */
        case AntlrSQLLexer::POSE_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("POSE_NE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseNeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_NE */

        /* BEGIN CODEGEN PARSER GLUE: POSE_NSAME */
        case AntlrSQLLexer::POSE_NSAME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("POSE_NSAME requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseNsameLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_NSAME */

        /* BEGIN CODEGEN PARSER GLUE: POSE_SAME */
        case AntlrSQLLexer::POSE_SAME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("POSE_SAME requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseSameLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_SAME */

        /* BEGIN CODEGEN PARSER GLUE: TOUCHES_CBUFFER_CBUFFER */
        case AntlrSQLLexer::TOUCHES_CBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TOUCHES_CBUFFER_CBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TouchesCbufferCbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TOUCHES_CBUFFER_CBUFFER */
        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_SPAN_BIGINT */
        case AntlrSQLLexer::ADJACENT_SPAN_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_SPAN_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentSpanBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_SPAN_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_TBOX_TBOX */
        case AntlrSQLLexer::ADJACENT_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADJACENT_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_DATE_SET */
        case AntlrSQLLexer::AFTER_DATE_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_DATE_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterDateSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_DATE_SET */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_TBOX_TBOX */
        case AntlrSQLLexer::AFTER_TBOX_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("AFTER_TBOX_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterTboxTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_TBOX_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: DATESPANSET_END_DATE */
        case AntlrSQLLexer::DATESPANSET_END_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESPANSET_END_DATE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespansetEndDateLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPANSET_END_DATE */

        /* BEGIN CODEGEN PARSER GLUE: DATESPANSET_NUM_DATES */
        case AntlrSQLLexer::DATESPANSET_NUM_DATES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESPANSET_NUM_DATES requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespansetNumDatesLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPANSET_NUM_DATES */

        /* BEGIN CODEGEN PARSER GLUE: DATESPANSET_START_DATE */
        case AntlrSQLLexer::DATESPANSET_START_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESPANSET_START_DATE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespansetStartDateLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPANSET_START_DATE */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_BIGINTSET_BIGINTSET */
        case AntlrSQLLexer::DISTANCE_BIGINTSET_BIGINTSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_BIGINTSET_BIGINTSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceBigintsetBigintsetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_BIGINTSET_BIGINTSET */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_BIGINTSPAN_BIGINTSPAN */
        case AntlrSQLLexer::DISTANCE_BIGINTSPAN_BIGINTSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_BIGINTSPAN_BIGINTSPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceBigintspanBigintspanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_BIGINTSPAN_BIGINTSPAN */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_BIGINTSPANSET_BIGINTSPAN */
        case AntlrSQLLexer::DISTANCE_BIGINTSPANSET_BIGINTSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_BIGINTSPANSET_BIGINTSPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceBigintspansetBigintspanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_BIGINTSPANSET_BIGINTSPAN */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_BIGINTSPANSET_BIGINTSPANSET */
        case AntlrSQLLexer::DISTANCE_BIGINTSPANSET_BIGINTSPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_BIGINTSPANSET_BIGINTSPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceBigintspansetBigintspansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_BIGINTSPANSET_BIGINTSPANSET */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_CBUFFER_CBUFFER */
        case AntlrSQLLexer::DISTANCE_CBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_CBUFFER_CBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceCbufferCbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_CBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_DATESET_DATESET */
        case AntlrSQLLexer::DISTANCE_DATESET_DATESET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_DATESET_DATESET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceDatesetDatesetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_DATESET_DATESET */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_DATESPAN_DATESPAN */
        case AntlrSQLLexer::DISTANCE_DATESPAN_DATESPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_DATESPAN_DATESPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceDatespanDatespanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_DATESPAN_DATESPAN */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_DATESPANSET_DATESPAN */
        case AntlrSQLLexer::DISTANCE_DATESPANSET_DATESPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_DATESPANSET_DATESPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceDatespansetDatespanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_DATESPANSET_DATESPAN */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_DATESPANSET_DATESPANSET */
        case AntlrSQLLexer::DISTANCE_DATESPANSET_DATESPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_DATESPANSET_DATESPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceDatespansetDatespansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_DATESPANSET_DATESPANSET */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_FLOATSET_FLOATSET */
        case AntlrSQLLexer::DISTANCE_FLOATSET_FLOATSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_FLOATSET_FLOATSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceFloatsetFloatsetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_FLOATSET_FLOATSET */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_FLOATSPAN_FLOATSPAN */
        case AntlrSQLLexer::DISTANCE_FLOATSPAN_FLOATSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_FLOATSPAN_FLOATSPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceFloatspanFloatspanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_FLOATSPAN_FLOATSPAN */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_FLOATSPANSET_FLOATSPAN */
        case AntlrSQLLexer::DISTANCE_FLOATSPANSET_FLOATSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_FLOATSPANSET_FLOATSPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceFloatspansetFloatspanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_FLOATSPANSET_FLOATSPAN */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_FLOATSPANSET_FLOATSPANSET */
        case AntlrSQLLexer::DISTANCE_FLOATSPANSET_FLOATSPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_FLOATSPANSET_FLOATSPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceFloatspansetFloatspansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_FLOATSPANSET_FLOATSPANSET */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_INTSET_INTSET */
        case AntlrSQLLexer::DISTANCE_INTSET_INTSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_INTSET_INTSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceIntsetIntsetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_INTSET_INTSET */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_INTSPAN_INTSPAN */
        case AntlrSQLLexer::DISTANCE_INTSPAN_INTSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_INTSPAN_INTSPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceIntspanIntspanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_INTSPAN_INTSPAN */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_INTSPANSET_INTSPAN */
        case AntlrSQLLexer::DISTANCE_INTSPANSET_INTSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_INTSPANSET_INTSPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceIntspansetIntspanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_INTSPANSET_INTSPAN */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_INTSPANSET_INTSPANSET */
        case AntlrSQLLexer::DISTANCE_INTSPANSET_INTSPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_INTSPANSET_INTSPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceIntspansetIntspansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_INTSPANSET_INTSPANSET */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_POSE_POSE */
        case AntlrSQLLexer::DISTANCE_POSE_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_POSE_POSE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistancePosePoseLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_POSE_POSE */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SET_BIGINT */
        case AntlrSQLLexer::DISTANCE_SET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SET_DATE */
        case AntlrSQLLexer::DISTANCE_SET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SET_FLOAT */
        case AntlrSQLLexer::DISTANCE_SET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SET_TIMESTAMPTZ */
        case AntlrSQLLexer::DISTANCE_SET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SPAN_BIGINT */
        case AntlrSQLLexer::DISTANCE_SPAN_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SPAN_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSpanBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SPAN_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SPAN_DATE */
        case AntlrSQLLexer::DISTANCE_SPAN_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SPAN_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSpanDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SPAN_DATE */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SPAN_FLOAT */
        case AntlrSQLLexer::DISTANCE_SPAN_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SPAN_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSpanFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SPAN_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SPAN_TIMESTAMPTZ */
        case AntlrSQLLexer::DISTANCE_SPAN_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SPAN_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSpanTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SPAN_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SPANSET_BIGINT */
        case AntlrSQLLexer::DISTANCE_SPANSET_BIGINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SPANSET_BIGINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSpansetBigintLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SPANSET_BIGINT */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SPANSET_DATE */
        case AntlrSQLLexer::DISTANCE_SPANSET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SPANSET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSpansetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SPANSET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SPANSET_FLOAT */
        case AntlrSQLLexer::DISTANCE_SPANSET_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SPANSET_FLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSpansetFloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SPANSET_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_SPANSET_TIMESTAMPTZ */
        case AntlrSQLLexer::DISTANCE_SPANSET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_SPANSET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceSpansetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_SPANSET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_TSTZSET_TSTZSET */
        case AntlrSQLLexer::DISTANCE_TSTZSET_TSTZSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_TSTZSET_TSTZSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceTstzsetTstzsetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_TSTZSET_TSTZSET */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_TSTZSPAN_TSTZSPAN */
        case AntlrSQLLexer::DISTANCE_TSTZSPAN_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_TSTZSPAN_TSTZSPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceTstzspanTstzspanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_TSTZSPAN_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_TSTZSPANSET_TSTZSPAN */
        case AntlrSQLLexer::DISTANCE_TSTZSPANSET_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_TSTZSPANSET_TSTZSPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceTstzspansetTstzspanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_TSTZSPANSET_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_TSTZSPANSET_TSTZSPANSET */
        case AntlrSQLLexer::DISTANCE_TSTZSPANSET_TSTZSPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_TSTZSPANSET_TSTZSPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceTstzspansetTstzspansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_TSTZSPANSET_TSTZSPANSET */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TBOXFLOAT_TBOXFLOAT */
        case AntlrSQLLexer::NAD_TBOXFLOAT_TBOXFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NAD_TBOXFLOAT_TBOXFLOAT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTboxfloatTboxfloatLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TBOXFLOAT_TBOXFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: SET_CMP */
        case AntlrSQLLexer::SET_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SET_CMP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetCmpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_CMP */

        /* BEGIN CODEGEN PARSER GLUE: SET_EQ */
        case AntlrSQLLexer::SET_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SET_EQ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetEqLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_EQ */

        /* BEGIN CODEGEN PARSER GLUE: SET_GE */
        case AntlrSQLLexer::SET_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SET_GE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetGeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_GE */

        /* BEGIN CODEGEN PARSER GLUE: SET_GT */
        case AntlrSQLLexer::SET_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SET_GT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetGtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_GT */

        /* BEGIN CODEGEN PARSER GLUE: SET_LE */
        case AntlrSQLLexer::SET_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SET_LE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetLeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_LE */

        /* BEGIN CODEGEN PARSER GLUE: SET_LT */
        case AntlrSQLLexer::SET_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SET_LT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetLtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_LT */

        /* BEGIN CODEGEN PARSER GLUE: SET_NE */
        case AntlrSQLLexer::SET_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SET_NE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetNeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_NE */

        /* BEGIN CODEGEN PARSER GLUE: SPAN_CMP */
        case AntlrSQLLexer::SPAN_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPAN_CMP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpanCmpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPAN_CMP */

        /* BEGIN CODEGEN PARSER GLUE: SPAN_EQ */
        case AntlrSQLLexer::SPAN_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPAN_EQ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpanEqLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPAN_EQ */

        /* BEGIN CODEGEN PARSER GLUE: SPAN_GE */
        case AntlrSQLLexer::SPAN_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPAN_GE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpanGeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPAN_GE */

        /* BEGIN CODEGEN PARSER GLUE: SPAN_GT */
        case AntlrSQLLexer::SPAN_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPAN_GT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpanGtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPAN_GT */

        /* BEGIN CODEGEN PARSER GLUE: SPAN_LE */
        case AntlrSQLLexer::SPAN_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPAN_LE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpanLeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPAN_LE */

        /* BEGIN CODEGEN PARSER GLUE: SPAN_LT */
        case AntlrSQLLexer::SPAN_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPAN_LT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpanLtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPAN_LT */

        /* BEGIN CODEGEN PARSER GLUE: SPAN_NE */
        case AntlrSQLLexer::SPAN_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPAN_NE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpanNeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPAN_NE */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_CMP */
        case AntlrSQLLexer::SPANSET_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPANSET_CMP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetCmpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_CMP */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_EQ */
        case AntlrSQLLexer::SPANSET_EQ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPANSET_EQ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetEqLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_EQ */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_GE */
        case AntlrSQLLexer::SPANSET_GE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPANSET_GE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetGeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_GE */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_GT */
        case AntlrSQLLexer::SPANSET_GT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPANSET_GT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetGtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_GT */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_LE */
        case AntlrSQLLexer::SPANSET_LE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPANSET_LE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetLeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_LE */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_LT */
        case AntlrSQLLexer::SPANSET_LT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPANSET_LT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetLtLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_LT */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_NE */
        case AntlrSQLLexer::SPANSET_NE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPANSET_NE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetNeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_NE */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_CMP */
        case AntlrSQLLexer::TBOX_CMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOX_CMP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxCmpLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_CMP */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPANSET_END_TIMESTAMPTZ */
        case AntlrSQLLexer::TSTZSPANSET_END_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPANSET_END_TIMESTAMPTZ requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspansetEndTimestamptzLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPANSET_END_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPANSET_NUM_TIMESTAMPS */
        case AntlrSQLLexer::TSTZSPANSET_NUM_TIMESTAMPS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPANSET_NUM_TIMESTAMPS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspansetNumTimestampsLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPANSET_NUM_TIMESTAMPS */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPANSET_START_TIMESTAMPTZ */
        case AntlrSQLLexer::TSTZSPANSET_START_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPANSET_START_TIMESTAMPTZ requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspansetStartTimestamptzLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPANSET_START_TIMESTAMPTZ */
        /* BEGIN CODEGEN PARSER GLUE: ADD_FLOAT_TFLOAT */
        case AntlrSQLLexer::ADD_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADD_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ADD_INT_TINT */
        case AntlrSQLLexer::ADD_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADD_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: ADD_TFLOAT_FLOAT */
        case AntlrSQLLexer::ADD_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADD_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: ADD_TINT_INT */
        case AntlrSQLLexer::ADD_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADD_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: DIV_FLOAT_TFLOAT */
        case AntlrSQLLexer::DIV_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DIV_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DivFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DIV_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: DIV_INT_TINT */
        case AntlrSQLLexer::DIV_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DIV_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DivIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DIV_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: DIV_TFLOAT_FLOAT */
        case AntlrSQLLexer::DIV_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DIV_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DivTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DIV_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: DIV_TINT_INT */
        case AntlrSQLLexer::DIV_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DIV_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DivTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DIV_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: SUB_FLOAT_TFLOAT */
        case AntlrSQLLexer::SUB_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SUB_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SubFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SUB_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: SUB_INT_TINT */
        case AntlrSQLLexer::SUB_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SUB_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SubIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SUB_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: SUB_TFLOAT_FLOAT */
        case AntlrSQLLexer::SUB_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SUB_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SubTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SUB_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: SUB_TINT_INT */
        case AntlrSQLLexer::SUB_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SUB_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SubTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SUB_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TFLOAT_FLOAT */
        case AntlrSQLLexer::TDISTANCE_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TDISTANCE_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TINT_INT */
        case AntlrSQLLexer::TDISTANCE_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TDISTANCE_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_FLOAT_TFLOAT */
        case AntlrSQLLexer::TEQ_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEQ_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_INT_TINT */
        case AntlrSQLLexer::TEQ_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEQ_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TFLOAT_FLOAT */
        case AntlrSQLLexer::TEQ_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEQ_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TINT_INT */
        case AntlrSQLLexer::TEQ_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEQ_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_FROM_BASE_TEMP */
        case AntlrSQLLexer::TFLOAT_FROM_BASE_TEMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TFLOAT_FROM_BASE_TEMP requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatFromBaseTempLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_FROM_BASE_TEMP */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_MINUS_VALUE */
        case AntlrSQLLexer::TFLOAT_MINUS_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TFLOAT_MINUS_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatMinusValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_MINUS_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_SHIFT_VALUE */
        case AntlrSQLLexer::TFLOAT_SHIFT_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TFLOAT_SHIFT_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatShiftValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_SHIFT_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TGE_FLOAT_TFLOAT */
        case AntlrSQLLexer::TGE_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGE_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGE_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TGE_INT_TINT */
        case AntlrSQLLexer::TGE_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGE_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGE_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: TGE_TFLOAT_FLOAT */
        case AntlrSQLLexer::TGE_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGE_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGE_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TGE_TINT_INT */
        case AntlrSQLLexer::TGE_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGE_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGE_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: TGT_FLOAT_TFLOAT */
        case AntlrSQLLexer::TGT_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGT_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgtFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGT_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TGT_INT_TINT */
        case AntlrSQLLexer::TGT_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGT_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgtIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGT_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: TGT_TFLOAT_FLOAT */
        case AntlrSQLLexer::TGT_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGT_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgtTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGT_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TGT_TINT_INT */
        case AntlrSQLLexer::TGT_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGT_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgtTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGT_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: TINT_FROM_BASE_TEMP */
        case AntlrSQLLexer::TINT_FROM_BASE_TEMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TINT_FROM_BASE_TEMP requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintFromBaseTempLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_FROM_BASE_TEMP */

        /* BEGIN CODEGEN PARSER GLUE: TINT_MINUS_VALUE */
        case AntlrSQLLexer::TINT_MINUS_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TINT_MINUS_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintMinusValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_MINUS_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TINT_SHIFT_VALUE */
        case AntlrSQLLexer::TINT_SHIFT_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TINT_SHIFT_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintShiftValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_SHIFT_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TLE_FLOAT_TFLOAT */
        case AntlrSQLLexer::TLE_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLE_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TleFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLE_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TLE_INT_TINT */
        case AntlrSQLLexer::TLE_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLE_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TleIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLE_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: TLE_TFLOAT_FLOAT */
        case AntlrSQLLexer::TLE_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLE_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TleTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLE_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TLE_TINT_INT */
        case AntlrSQLLexer::TLE_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLE_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TleTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLE_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: TLT_FLOAT_TFLOAT */
        case AntlrSQLLexer::TLT_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLT_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TltFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLT_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TLT_INT_TINT */
        case AntlrSQLLexer::TLT_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLT_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TltIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLT_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: TLT_TFLOAT_FLOAT */
        case AntlrSQLLexer::TLT_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLT_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TltTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLT_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TLT_TINT_INT */
        case AntlrSQLLexer::TLT_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLT_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TltTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLT_TINT_INT */

        /* BEGIN CODEGEN PARSER GLUE: TNE_FLOAT_TFLOAT */
        case AntlrSQLLexer::TNE_FLOAT_TFLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNE_FLOAT_TFLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneFloatTfloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_FLOAT_TFLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TNE_INT_TINT */
        case AntlrSQLLexer::TNE_INT_TINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNE_INT_TINT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneIntTintLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_INT_TINT */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TFLOAT_FLOAT */
        case AntlrSQLLexer::TNE_TFLOAT_FLOAT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNE_TFLOAT_FLOAT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneTfloatFloatLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TFLOAT_FLOAT */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TINT_INT */
        case AntlrSQLLexer::TNE_TINT_INT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNE_TINT_INT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneTintIntLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TINT_INT */
        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_BOOL_TBOOL */
        case AntlrSQLLexer::ALWAYS_EQ_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_EQ_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::ALWAYS_EQ_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYS_EQ_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_NPOINT_TNPOINT */
        case AntlrSQLLexer::ALWAYS_EQ_NPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_EQ_NPOINT_TNPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqNpointTnpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_NPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_POSE_TPOSE */
        case AntlrSQLLexer::ALWAYS_EQ_POSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYS_EQ_POSE_TPOSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqPoseTposeLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_POSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TNPOINT_NPOINT */
        case AntlrSQLLexer::ALWAYS_EQ_TNPOINT_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_EQ_TNPOINT_NPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTnpointNpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TNPOINT_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TPOSE_POSE */
        case AntlrSQLLexer::ALWAYS_EQ_TPOSE_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYS_EQ_TPOSE_POSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTposePoseLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TPOSE_POSE */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_BOOL_TBOOL */
        case AntlrSQLLexer::ALWAYS_NE_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_NE_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::ALWAYS_NE_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYS_NE_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_NPOINT_TNPOINT */
        case AntlrSQLLexer::ALWAYS_NE_NPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_NE_NPOINT_TNPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeNpointTnpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_NPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_POSE_TPOSE */
        case AntlrSQLLexer::ALWAYS_NE_POSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYS_NE_POSE_TPOSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNePoseTposeLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_POSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TNPOINT_NPOINT */
        case AntlrSQLLexer::ALWAYS_NE_TNPOINT_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_NE_TNPOINT_NPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTnpointNpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TNPOINT_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TPOSE_POSE */
        case AntlrSQLLexer::ALWAYS_NE_TPOSE_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYS_NE_TPOSE_POSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTposePoseLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TPOSE_POSE */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_BOOL_TBOOL */
        case AntlrSQLLexer::EVER_EQ_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_EQ_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::EVER_EQ_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVER_EQ_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_NPOINT_TNPOINT */
        case AntlrSQLLexer::EVER_EQ_NPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_EQ_NPOINT_TNPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqNpointTnpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_NPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_POSE_TPOSE */
        case AntlrSQLLexer::EVER_EQ_POSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVER_EQ_POSE_TPOSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqPoseTposeLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_POSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TNPOINT_NPOINT */
        case AntlrSQLLexer::EVER_EQ_TNPOINT_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_EQ_TNPOINT_NPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTnpointNpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TNPOINT_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TPOSE_POSE */
        case AntlrSQLLexer::EVER_EQ_TPOSE_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVER_EQ_TPOSE_POSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTposePoseLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TPOSE_POSE */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_BOOL_TBOOL */
        case AntlrSQLLexer::EVER_NE_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_NE_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::EVER_NE_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVER_NE_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_NPOINT_TNPOINT */
        case AntlrSQLLexer::EVER_NE_NPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_NE_NPOINT_TNPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeNpointTnpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_NPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_POSE_TPOSE */
        case AntlrSQLLexer::EVER_NE_POSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVER_NE_POSE_TPOSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNePoseTposeLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_POSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TNPOINT_NPOINT */
        case AntlrSQLLexer::EVER_NE_TNPOINT_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_NE_TNPOINT_NPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTnpointNpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TNPOINT_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TPOSE_POSE */
        case AntlrSQLLexer::EVER_NE_TPOSE_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVER_NE_TPOSE_POSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTposePoseLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TPOSE_POSE */
        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TEXT_TTEXT */
        case AntlrSQLLexer::ALWAYS_EQ_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_EQ_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TTEXT_TEXT */
        case AntlrSQLLexer::ALWAYS_EQ_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_EQ_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GE_TEXT_TTEXT */
        case AntlrSQLLexer::ALWAYS_GE_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GE_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysGeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GE_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GE_TTEXT_TEXT */
        case AntlrSQLLexer::ALWAYS_GE_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GE_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysGeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GE_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GT_TEXT_TTEXT */
        case AntlrSQLLexer::ALWAYS_GT_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GT_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysGtTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GT_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_GT_TTEXT_TEXT */
        case AntlrSQLLexer::ALWAYS_GT_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_GT_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysGtTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_GT_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LE_TEXT_TTEXT */
        case AntlrSQLLexer::ALWAYS_LE_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LE_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysLeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LE_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LE_TTEXT_TEXT */
        case AntlrSQLLexer::ALWAYS_LE_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LE_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysLeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LE_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LT_TEXT_TTEXT */
        case AntlrSQLLexer::ALWAYS_LT_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LT_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysLtTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LT_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_LT_TTEXT_TEXT */
        case AntlrSQLLexer::ALWAYS_LT_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_LT_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysLtTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_LT_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TEXT_TTEXT */
        case AntlrSQLLexer::ALWAYS_NE_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_NE_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TTEXT_TEXT */
        case AntlrSQLLexer::ALWAYS_NE_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ALWAYS_NE_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TEXT_TTEXT */
        case AntlrSQLLexer::EVER_EQ_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_EQ_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TTEXT_TEXT */
        case AntlrSQLLexer::EVER_EQ_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_EQ_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GE_TEXT_TTEXT */
        case AntlrSQLLexer::EVER_GE_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GE_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverGeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GE_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GE_TTEXT_TEXT */
        case AntlrSQLLexer::EVER_GE_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GE_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverGeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GE_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GT_TEXT_TTEXT */
        case AntlrSQLLexer::EVER_GT_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GT_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverGtTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GT_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_GT_TTEXT_TEXT */
        case AntlrSQLLexer::EVER_GT_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_GT_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverGtTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_GT_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LE_TEXT_TTEXT */
        case AntlrSQLLexer::EVER_LE_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LE_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverLeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LE_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LE_TTEXT_TEXT */
        case AntlrSQLLexer::EVER_LE_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LE_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverLeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LE_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LT_TEXT_TTEXT */
        case AntlrSQLLexer::EVER_LT_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LT_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverLtTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LT_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_LT_TTEXT_TEXT */
        case AntlrSQLLexer::EVER_LT_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_LT_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverLtTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_LT_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TEXT_TTEXT */
        case AntlrSQLLexer::EVER_NE_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_NE_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TTEXT_TEXT */
        case AntlrSQLLexer::EVER_NE_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("EVER_NE_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TEXT_TTEXT */
        case AntlrSQLLexer::TEQ_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEQ_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TTEXT_TEXT */
        case AntlrSQLLexer::TEQ_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEQ_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TEXTCAT_TEXT_TTEXT */
        case AntlrSQLLexer::TEXTCAT_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEXTCAT_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextcatTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTCAT_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TEXTCAT_TTEXT_TEXT */
        case AntlrSQLLexer::TEXTCAT_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEXTCAT_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextcatTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTCAT_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TGE_TEXT_TTEXT */
        case AntlrSQLLexer::TGE_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGE_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGE_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TGE_TTEXT_TEXT */
        case AntlrSQLLexer::TGE_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGE_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGE_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TGT_TEXT_TTEXT */
        case AntlrSQLLexer::TGT_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGT_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgtTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGT_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TGT_TTEXT_TEXT */
        case AntlrSQLLexer::TGT_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGT_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgtTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGT_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TLE_TEXT_TTEXT */
        case AntlrSQLLexer::TLE_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLE_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TleTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLE_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TLE_TTEXT_TEXT */
        case AntlrSQLLexer::TLE_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLE_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TleTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLE_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TLT_TEXT_TTEXT */
        case AntlrSQLLexer::TLT_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLT_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TltTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLT_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TLT_TTEXT_TEXT */
        case AntlrSQLLexer::TLT_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TLT_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TltTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLT_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TEXT_TTEXT */
        case AntlrSQLLexer::TNE_TEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNE_TEXT_TTEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneTextTtextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TTEXT_TEXT */
        case AntlrSQLLexer::TNE_TTEXT_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNE_TTEXT_TEXT requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneTtextTextLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TTEXT_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_FROM_BASE_TEMP */
        case AntlrSQLLexer::TTEXT_FROM_BASE_TEMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TTEXT_FROM_BASE_TEMP requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextFromBaseTempLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_FROM_BASE_TEMP */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_MINUS_VALUE */
        case AntlrSQLLexer::TTEXT_MINUS_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TTEXT_MINUS_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextMinusValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_MINUS_VALUE */
        /* BEGIN CODEGEN PARSER GLUE: ACONTAINS_GEO_TGEO */
        case AntlrSQLLexer::ACONTAINS_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ACONTAINS_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AcontainsGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACONTAINS_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_GEO_TGEO */
        case AntlrSQLLexer::ALWAYS_EQ_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_EQ_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_GEO_TGEO */
        case AntlrSQLLexer::ALWAYS_NE_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ALWAYS_NE_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: ECOVERS_GEO_TGEO */
        case AntlrSQLLexer::ECOVERS_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("ECOVERS_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EcoversGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECOVERS_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_GEO_TGEO */
        case AntlrSQLLexer::EVER_EQ_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_EQ_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_GEO_TGEO */
        case AntlrSQLLexer::EVER_NE_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("EVER_NE_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TCONTAINS_GEO_TGEO */
        case AntlrSQLLexer::TCONTAINS_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TCONTAINS_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcontainsGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCONTAINS_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TCONTAINS_TGEO_GEO */
        case AntlrSQLLexer::TCONTAINS_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TCONTAINS_TGEO_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcontainsTgeoGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCONTAINS_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TCOVERS_GEO_TGEO */
        case AntlrSQLLexer::TCOVERS_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TCOVERS_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcoversGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCOVERS_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TCOVERS_TGEO_GEO */
        case AntlrSQLLexer::TCOVERS_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TCOVERS_TGEO_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcoversTgeoGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCOVERS_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TDISJOINT_GEO_TGEO */
        case AntlrSQLLexer::TDISJOINT_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TDISJOINT_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdisjointGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISJOINT_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TDISJOINT_TGEO_GEO */
        case AntlrSQLLexer::TDISJOINT_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TDISJOINT_TGEO_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdisjointTgeoGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISJOINT_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TGEO_GEO */
        case AntlrSQLLexer::TDISTANCE_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TDISTANCE_TGEO_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTgeoGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_GEO_TGEO */
        case AntlrSQLLexer::TEQ_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEQ_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TGEO_GEO */
        case AntlrSQLLexer::TEQ_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEQ_TGEO_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqTgeoGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TINTERSECTS_GEO_TGEO */
        case AntlrSQLLexer::TINTERSECTS_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TINTERSECTS_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintersectsGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINTERSECTS_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TINTERSECTS_TGEO_GEO */
        case AntlrSQLLexer::TINTERSECTS_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TINTERSECTS_TGEO_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintersectsTgeoGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINTERSECTS_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TNE_GEO_TGEO */
        case AntlrSQLLexer::TNE_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TNE_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TGEO_GEO */
        case AntlrSQLLexer::TNE_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TNE_TGEO_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneTgeoGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TTOUCHES_GEO_TGEO */
        case AntlrSQLLexer::TTOUCHES_GEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TTOUCHES_GEO_TGEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtouchesGeoTgeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTOUCHES_GEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TTOUCHES_TGEO_GEO */
        case AntlrSQLLexer::TTOUCHES_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TTOUCHES_TGEO_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtouchesTgeoGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTOUCHES_TGEO_GEO */
        /* BEGIN CODEGEN PARSER GLUE: ACONTAINS_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::ACONTAINS_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ACONTAINS_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AcontainsCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACONTAINS_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ACONTAINS_GEO_TCBUFFER */
        case AntlrSQLLexer::ACONTAINS_GEO_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ACONTAINS_GEO_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AcontainsGeoTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACONTAINS_GEO_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ACOVERS_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::ACOVERS_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ACOVERS_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AcoversCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACOVERS_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ACOVERS_GEO_TCBUFFER */
        case AntlrSQLLexer::ACOVERS_GEO_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ACOVERS_GEO_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AcoversGeoTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ACOVERS_GEO_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ECONTAINS_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::ECONTAINS_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ECONTAINS_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EcontainsCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECONTAINS_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: ECOVERS_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::ECOVERS_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ECOVERS_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EcoversCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ECOVERS_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TCONTAINS_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::TCONTAINS_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCONTAINS_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcontainsCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCONTAINS_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TCONTAINS_GEO_TCBUFFER */
        case AntlrSQLLexer::TCONTAINS_GEO_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCONTAINS_GEO_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcontainsGeoTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCONTAINS_GEO_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TCONTAINS_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::TCONTAINS_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCONTAINS_TCBUFFER_CBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcontainsTcbufferCbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCONTAINS_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TCONTAINS_TCBUFFER_GEO */
        case AntlrSQLLexer::TCONTAINS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCONTAINS_TCBUFFER_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcontainsTcbufferGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCONTAINS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TCOVERS_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::TCOVERS_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCOVERS_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcoversCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCOVERS_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TCOVERS_GEO_TCBUFFER */
        case AntlrSQLLexer::TCOVERS_GEO_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCOVERS_GEO_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcoversGeoTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCOVERS_GEO_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TCOVERS_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::TCOVERS_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCOVERS_TCBUFFER_CBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcoversTcbufferCbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCOVERS_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TCOVERS_TCBUFFER_GEO */
        case AntlrSQLLexer::TCOVERS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCOVERS_TCBUFFER_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcoversTcbufferGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCOVERS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TDISJOINT_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::TDISJOINT_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TDISJOINT_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdisjointCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISJOINT_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TDISJOINT_GEO_TCBUFFER */
        case AntlrSQLLexer::TDISJOINT_GEO_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TDISJOINT_GEO_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdisjointGeoTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISJOINT_GEO_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TDISJOINT_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::TDISJOINT_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TDISJOINT_TCBUFFER_CBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdisjointTcbufferCbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISJOINT_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TDISJOINT_TCBUFFER_GEO */
        case AntlrSQLLexer::TDISJOINT_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TDISJOINT_TCBUFFER_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdisjointTcbufferGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISJOINT_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::TDISTANCE_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TDISTANCE_TCBUFFER_CBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTcbufferCbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TCBUFFER_GEO */
        case AntlrSQLLexer::TDISTANCE_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TDISTANCE_TCBUFFER_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTcbufferGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TNPOINT_NPOINT */
        case AntlrSQLLexer::TDISTANCE_TNPOINT_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TDISTANCE_TNPOINT_NPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTnpointNpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TNPOINT_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TNPOINT_POINT */
        case AntlrSQLLexer::TDISTANCE_TNPOINT_POINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TDISTANCE_TNPOINT_POINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTnpointPointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TNPOINT_POINT */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TPOSE_POINT */
        case AntlrSQLLexer::TDISTANCE_TPOSE_POINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TDISTANCE_TPOSE_POINT requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTposePointLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TPOSE_POINT */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TPOSE_POSE */
        case AntlrSQLLexer::TDISTANCE_TPOSE_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TDISTANCE_TPOSE_POSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTposePoseLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TPOSE_POSE */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_BOOL_TBOOL */
        case AntlrSQLLexer::TEQ_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEQ_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::TEQ_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEQ_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_POSE_TPOSE */
        case AntlrSQLLexer::TEQ_POSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEQ_POSE_TPOSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqPoseTposeLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_POSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TBOOL_BOOL */
        case AntlrSQLLexer::TEQ_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEQ_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TBOOL_BOOL */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::TEQ_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEQ_TCBUFFER_CBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqTcbufferCbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TNPOINT_NPOINT */
        case AntlrSQLLexer::TEQ_TNPOINT_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEQ_TNPOINT_NPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqTnpointNpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TNPOINT_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TPOSE_POSE */
        case AntlrSQLLexer::TEQ_TPOSE_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEQ_TPOSE_POSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqTposePoseLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TPOSE_POSE */

        /* BEGIN CODEGEN PARSER GLUE: TINTERSECTS_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::TINTERSECTS_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TINTERSECTS_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintersectsCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINTERSECTS_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TINTERSECTS_GEO_TCBUFFER */
        case AntlrSQLLexer::TINTERSECTS_GEO_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TINTERSECTS_GEO_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintersectsGeoTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINTERSECTS_GEO_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TINTERSECTS_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::TINTERSECTS_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TINTERSECTS_TCBUFFER_CBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintersectsTcbufferCbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINTERSECTS_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TINTERSECTS_TCBUFFER_GEO */
        case AntlrSQLLexer::TINTERSECTS_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TINTERSECTS_TCBUFFER_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintersectsTcbufferGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINTERSECTS_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TNE_BOOL_TBOOL */
        case AntlrSQLLexer::TNE_BOOL_TBOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNE_BOOL_TBOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneBoolTboolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_BOOL_TBOOL */

        /* BEGIN CODEGEN PARSER GLUE: TNE_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::TNE_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TNE_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TNE_POSE_TPOSE */
        case AntlrSQLLexer::TNE_POSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TNE_POSE_TPOSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnePoseTposeLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_POSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TBOOL_BOOL */
        case AntlrSQLLexer::TNE_TBOOL_BOOL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNE_TBOOL_BOOL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneTboolBoolLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TBOOL_BOOL */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::TNE_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TNE_TCBUFFER_CBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneTcbufferCbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TNPOINT_NPOINT */
        case AntlrSQLLexer::TNE_TNPOINT_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TNE_TNPOINT_NPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneTnpointNpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TNPOINT_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TPOSE_POSE */
        case AntlrSQLLexer::TNE_TPOSE_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TNE_TPOSE_POSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneTposePoseLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TPOSE_POSE */

        /* BEGIN CODEGEN PARSER GLUE: TTOUCHES_CBUFFER_TCBUFFER */
        case AntlrSQLLexer::TTOUCHES_CBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TTOUCHES_CBUFFER_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtouchesCbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTOUCHES_CBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TTOUCHES_GEO_TCBUFFER */
        case AntlrSQLLexer::TTOUCHES_GEO_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TTOUCHES_GEO_TCBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtouchesGeoTcbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTOUCHES_GEO_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TTOUCHES_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::TTOUCHES_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TTOUCHES_TCBUFFER_CBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtouchesTcbufferCbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTOUCHES_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TTOUCHES_TCBUFFER_GEO */
        case AntlrSQLLexer::TTOUCHES_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TTOUCHES_TCBUFFER_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtouchesTcbufferGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTOUCHES_TCBUFFER_GEO */
        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_AT_TSTZSET */
        case AntlrSQLLexer::TEMPORAL_AT_TSTZSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_AT_TSTZSET requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalAtTstzsetLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_AT_TSTZSET */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_AT_TSTZSPAN */
        case AntlrSQLLexer::TEMPORAL_AT_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_AT_TSTZSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalAtTstzspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_AT_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_AT_TSTZSPANSET */
        case AntlrSQLLexer::TEMPORAL_AT_TSTZSPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_AT_TSTZSPANSET requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalAtTstzspansetLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_AT_TSTZSPANSET */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_AT_VALUES */
        case AntlrSQLLexer::TEMPORAL_AT_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_AT_VALUES requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalAtValuesLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_AT_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_MINUS_TSTZSET */
        case AntlrSQLLexer::TEMPORAL_MINUS_TSTZSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_MINUS_TSTZSET requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalMinusTstzsetLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_MINUS_TSTZSET */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_MINUS_TSTZSPAN */
        case AntlrSQLLexer::TEMPORAL_MINUS_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_MINUS_TSTZSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalMinusTstzspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_MINUS_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_MINUS_TSTZSPANSET */
        case AntlrSQLLexer::TEMPORAL_MINUS_TSTZSPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_MINUS_TSTZSPANSET requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalMinusTstzspansetLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_MINUS_TSTZSPANSET */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_MINUS_VALUES */
        case AntlrSQLLexer::TEMPORAL_MINUS_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_MINUS_VALUES requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalMinusValuesLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_MINUS_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: TNUMBER_AT_SPAN */
        case AntlrSQLLexer::TNUMBER_AT_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNUMBER_AT_SPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnumberAtSpanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNUMBER_AT_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: TNUMBER_AT_SPANSET */
        case AntlrSQLLexer::TNUMBER_AT_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNUMBER_AT_SPANSET requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnumberAtSpansetLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNUMBER_AT_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: TNUMBER_AT_TBOX */
        case AntlrSQLLexer::TNUMBER_AT_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNUMBER_AT_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnumberAtTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNUMBER_AT_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: TNUMBER_MINUS_SPAN */
        case AntlrSQLLexer::TNUMBER_MINUS_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNUMBER_MINUS_SPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnumberMinusSpanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNUMBER_MINUS_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: TNUMBER_MINUS_SPANSET */
        case AntlrSQLLexer::TNUMBER_MINUS_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNUMBER_MINUS_SPANSET requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnumberMinusSpansetLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNUMBER_MINUS_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: TNUMBER_MINUS_TBOX */
        case AntlrSQLLexer::TNUMBER_MINUS_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNUMBER_MINUS_TBOX requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnumberMinusTboxLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNUMBER_MINUS_TBOX */
        /* BEGIN CODEGEN PARSER GLUE: TEQ_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::TEQ_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEQ_TEMPORAL_TEMPORAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqTemporalTemporalLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: TGE_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::TGE_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TGE_TEMPORAL_TEMPORAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeTemporalTemporalLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGE_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: TGT_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::TGT_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TGT_TEMPORAL_TEMPORAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgtTemporalTemporalLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGT_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: TLE_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::TLE_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TLE_TEMPORAL_TEMPORAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TleTemporalTemporalLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLE_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: TLT_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::TLT_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TLT_TEMPORAL_TEMPORAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TltTemporalTemporalLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TLT_TEMPORAL_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TEMPORAL_TEMPORAL */
        case AntlrSQLLexer::TNE_TEMPORAL_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TNE_TEMPORAL_TEMPORAL requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneTemporalTemporalLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TEMPORAL_TEMPORAL */
        /* BEGIN CODEGEN PARSER GLUE: TDISJOINT_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::TDISJOINT_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TDISJOINT_TCBUFFER_TCBUFFER requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdisjointTcbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISJOINT_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TDISJOINT_TGEO_TGEO */
        case AntlrSQLLexer::TDISJOINT_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TDISJOINT_TGEO_TGEO requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdisjointTgeoTgeoLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISJOINT_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TEXTCAT_TTEXT_TTEXT */
        case AntlrSQLLexer::TEXTCAT_TTEXT_TTEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEXTCAT_TTEXT_TTEXT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextcatTtextTtextLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTCAT_TTEXT_TTEXT */

        /* BEGIN CODEGEN PARSER GLUE: TINTERSECTS_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::TINTERSECTS_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TINTERSECTS_TCBUFFER_TCBUFFER requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintersectsTcbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINTERSECTS_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TINTERSECTS_TGEO_TGEO */
        case AntlrSQLLexer::TINTERSECTS_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TINTERSECTS_TGEO_TGEO requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintersectsTgeoTgeoLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINTERSECTS_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: TTOUCHES_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::TTOUCHES_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("TTOUCHES_TCBUFFER_TCBUFFER requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtouchesTcbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTOUCHES_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TTOUCHES_TGEO_TGEO */
        case AntlrSQLLexer::TTOUCHES_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TTOUCHES_TGEO_TGEO requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtouchesTgeoTgeoLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTOUCHES_TGEO_TGEO */
        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_TEMPORAL_TSTZSPAN */
        case AntlrSQLLexer::ADJACENT_TEMPORAL_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADJACENT_TEMPORAL_TSTZSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentTemporalTstzspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_TEMPORAL_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_TSTZSPAN_TEMPORAL */
        case AntlrSQLLexer::ADJACENT_TSTZSPAN_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADJACENT_TSTZSPAN_TEMPORAL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentTstzspanTemporalLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_TSTZSPAN_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_TEMPORAL_TSTZSPAN */
        case AntlrSQLLexer::AFTER_TEMPORAL_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("AFTER_TEMPORAL_TSTZSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterTemporalTstzspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_TEMPORAL_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: AFTER_TSTZSPAN_TEMPORAL */
        case AntlrSQLLexer::AFTER_TSTZSPAN_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("AFTER_TSTZSPAN_TEMPORAL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AfterTstzspanTemporalLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: AFTER_TSTZSPAN_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_TEMPORAL_TSTZSPAN */
        case AntlrSQLLexer::BEFORE_TEMPORAL_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("BEFORE_TEMPORAL_TSTZSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeTemporalTstzspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_TEMPORAL_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: BEFORE_TSTZSPAN_TEMPORAL */
        case AntlrSQLLexer::BEFORE_TSTZSPAN_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("BEFORE_TSTZSPAN_TEMPORAL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BeforeTstzspanTemporalLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: BEFORE_TSTZSPAN_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_TEMPORAL_TSTZSPAN */
        case AntlrSQLLexer::OVERAFTER_TEMPORAL_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERAFTER_TEMPORAL_TSTZSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterTemporalTstzspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_TEMPORAL_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERAFTER_TSTZSPAN_TEMPORAL */
        case AntlrSQLLexer::OVERAFTER_TSTZSPAN_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERAFTER_TSTZSPAN_TEMPORAL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverafterTstzspanTemporalLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERAFTER_TSTZSPAN_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_TEMPORAL_TSTZSPAN */
        case AntlrSQLLexer::OVERBEFORE_TEMPORAL_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERBEFORE_TEMPORAL_TSTZSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeTemporalTstzspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_TEMPORAL_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERBEFORE_TSTZSPAN_TEMPORAL */
        case AntlrSQLLexer::OVERBEFORE_TSTZSPAN_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERBEFORE_TSTZSPAN_TEMPORAL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbeforeTstzspanTemporalLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBEFORE_TSTZSPAN_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_TNUMBER_NUMSPAN */
        case AntlrSQLLexer::ADJACENT_TNUMBER_NUMSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADJACENT_TNUMBER_NUMSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentTnumberNumspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_TNUMBER_NUMSPAN */

        /* BEGIN CODEGEN PARSER GLUE: ADJACENT_NUMSPAN_TNUMBER */
        case AntlrSQLLexer::ADJACENT_NUMSPAN_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("ADJACENT_NUMSPAN_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AdjacentNumspanTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADJACENT_NUMSPAN_TNUMBER */
        /* BEGIN CODEGEN PARSER GLUE: INTSPANSET_WIDTH */
        case AntlrSQLLexer::INTSPANSET_WIDTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPANSET_WIDTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspansetWidthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPANSET_WIDTH */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSPANSET_WIDTH */
        case AntlrSQLLexer::BIGINTSPANSET_WIDTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINTSPANSET_WIDTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintspansetWidthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSPANSET_WIDTH */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPANSET_WIDTH */
        case AntlrSQLLexer::FLOATSPANSET_WIDTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPANSET_WIDTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspansetWidthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPANSET_WIDTH */
        /* BEGIN CODEGEN PARSER GLUE: INTSPAN_SHIFT_SCALE */
        case AntlrSQLLexer::INTSPAN_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("INTSPAN_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspanShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPAN_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: INTSET_SHIFT_SCALE */
        case AntlrSQLLexer::INTSET_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("INTSET_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntsetShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSET_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: INTSPANSET_SHIFT_SCALE */
        case AntlrSQLLexer::INTSPANSET_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("INTSPANSET_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspansetShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPANSET_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSPAN_SHIFT_SCALE */
        case AntlrSQLLexer::BIGINTSPAN_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("BIGINTSPAN_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintspanShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSPAN_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSET_SHIFT_SCALE */
        case AntlrSQLLexer::BIGINTSET_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("BIGINTSET_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintsetShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSET_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSPANSET_SHIFT_SCALE */
        case AntlrSQLLexer::BIGINTSPANSET_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("BIGINTSPANSET_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintspansetShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSPANSET_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPAN_SHIFT_SCALE */
        case AntlrSQLLexer::FLOATSPAN_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("FLOATSPAN_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPAN_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSET_SHIFT_SCALE */
        case AntlrSQLLexer::FLOATSET_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("FLOATSET_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatsetShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSET_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPANSET_SHIFT_SCALE */
        case AntlrSQLLexer::FLOATSPANSET_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("FLOATSPANSET_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspansetShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPANSET_SHIFT_SCALE */
        /* BEGIN CODEGEN PARSER GLUE: INT_TO_SET */
        case AntlrSQLLexer::INT_TO_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INT_TO_SET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntToSetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INT_TO_SET */

        /* BEGIN CODEGEN PARSER GLUE: INT_TO_SPAN */
        case AntlrSQLLexer::INT_TO_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INT_TO_SPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntToSpanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INT_TO_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: INT_TO_SPANSET */
        case AntlrSQLLexer::INT_TO_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INT_TO_SPANSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntToSpansetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INT_TO_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: BIGINT_TO_SET */
        case AntlrSQLLexer::BIGINT_TO_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINT_TO_SET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintToSetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINT_TO_SET */

        /* BEGIN CODEGEN PARSER GLUE: BIGINT_TO_SPAN */
        case AntlrSQLLexer::BIGINT_TO_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINT_TO_SPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintToSpanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINT_TO_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: BIGINT_TO_SPANSET */
        case AntlrSQLLexer::BIGINT_TO_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINT_TO_SPANSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintToSpansetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINT_TO_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: FLOAT_TO_SET */
        case AntlrSQLLexer::FLOAT_TO_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOAT_TO_SET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatToSetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOAT_TO_SET */

        /* BEGIN CODEGEN PARSER GLUE: FLOAT_TO_SPAN */
        case AntlrSQLLexer::FLOAT_TO_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOAT_TO_SPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatToSpanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOAT_TO_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: FLOAT_TO_SPANSET */
        case AntlrSQLLexer::FLOAT_TO_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOAT_TO_SPANSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatToSpansetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOAT_TO_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: DATE_TO_SET */
        case AntlrSQLLexer::DATE_TO_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATE_TO_SET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DateToSetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATE_TO_SET */

        /* BEGIN CODEGEN PARSER GLUE: DATE_TO_SPAN */
        case AntlrSQLLexer::DATE_TO_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATE_TO_SPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DateToSpanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATE_TO_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: DATE_TO_SPANSET */
        case AntlrSQLLexer::DATE_TO_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATE_TO_SPANSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DateToSpansetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATE_TO_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: TIMESTAMPTZ_TO_SET */
        case AntlrSQLLexer::TIMESTAMPTZ_TO_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TIMESTAMPTZ_TO_SET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TimestamptzToSetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TIMESTAMPTZ_TO_SET */

        /* BEGIN CODEGEN PARSER GLUE: TIMESTAMPTZ_TO_SPAN */
        case AntlrSQLLexer::TIMESTAMPTZ_TO_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TIMESTAMPTZ_TO_SPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TimestamptzToSpanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TIMESTAMPTZ_TO_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: TIMESTAMPTZ_TO_SPANSET */
        case AntlrSQLLexer::TIMESTAMPTZ_TO_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TIMESTAMPTZ_TO_SPANSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TimestamptzToSpansetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TIMESTAMPTZ_TO_SPANSET */
        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_BIGINT_SET */
        case AntlrSQLLexer::INTERSECTION_BIGINT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_BIGINT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionBigintSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_BIGINT_SET */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_DATE_SET */
        case AntlrSQLLexer::INTERSECTION_DATE_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_DATE_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionDateSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_DATE_SET */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SET_DATE */
        case AntlrSQLLexer::INTERSECTION_SET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SET_TEXT */
        case AntlrSQLLexer::INTERSECTION_SET_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SET_TEXT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSetTextLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SET_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SET_TIMESTAMPTZ */
        case AntlrSQLLexer::INTERSECTION_SET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPAN_DATE */
        case AntlrSQLLexer::INTERSECTION_SPAN_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPAN_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpanDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPAN_DATE */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPAN_TIMESTAMPTZ */
        case AntlrSQLLexer::INTERSECTION_SPAN_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPAN_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpanTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPAN_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPANSET_DATE */
        case AntlrSQLLexer::INTERSECTION_SPANSET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPANSET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpansetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPANSET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SPANSET_TIMESTAMPTZ */
        case AntlrSQLLexer::INTERSECTION_SPANSET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SPANSET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSpansetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SPANSET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_TEXT_SET */
        case AntlrSQLLexer::INTERSECTION_TEXT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_TEXT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionTextSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_TEXT_SET */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_TIMESTAMPTZ_SET */
        case AntlrSQLLexer::INTERSECTION_TIMESTAMPTZ_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_TIMESTAMPTZ_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionTimestamptzSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_TIMESTAMPTZ_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_DATE_SET */
        case AntlrSQLLexer::MINUS_DATE_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_DATE_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusDateSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_DATE_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_DATE_SPAN */
        case AntlrSQLLexer::MINUS_DATE_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_DATE_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusDateSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_DATE_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_DATE_SPANSET */
        case AntlrSQLLexer::MINUS_DATE_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_DATE_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusDateSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_DATE_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SET_DATE */
        case AntlrSQLLexer::MINUS_SET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SET_TEXT */
        case AntlrSQLLexer::MINUS_SET_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SET_TEXT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSetTextLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SET_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SET_TIMESTAMPTZ */
        case AntlrSQLLexer::MINUS_SET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPAN_DATE */
        case AntlrSQLLexer::MINUS_SPAN_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPAN_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpanDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPAN_DATE */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPAN_TIMESTAMPTZ */
        case AntlrSQLLexer::MINUS_SPAN_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPAN_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpanTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPAN_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPANSET_DATE */
        case AntlrSQLLexer::MINUS_SPANSET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPANSET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpansetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPANSET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SPANSET_TIMESTAMPTZ */
        case AntlrSQLLexer::MINUS_SPANSET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SPANSET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSpansetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SPANSET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_TEXT_SET */
        case AntlrSQLLexer::MINUS_TEXT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_TEXT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusTextSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_TEXT_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_TIMESTAMPTZ_SET */
        case AntlrSQLLexer::MINUS_TIMESTAMPTZ_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_TIMESTAMPTZ_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusTimestamptzSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_TIMESTAMPTZ_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_TIMESTAMPTZ_SPAN */
        case AntlrSQLLexer::MINUS_TIMESTAMPTZ_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_TIMESTAMPTZ_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusTimestamptzSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_TIMESTAMPTZ_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_TIMESTAMPTZ_SPANSET */
        case AntlrSQLLexer::MINUS_TIMESTAMPTZ_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_TIMESTAMPTZ_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusTimestamptzSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_TIMESTAMPTZ_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_DATE_SET */
        case AntlrSQLLexer::UNION_DATE_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_DATE_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionDateSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_DATE_SET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_DATE_SPAN */
        case AntlrSQLLexer::UNION_DATE_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_DATE_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionDateSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_DATE_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: UNION_DATE_SPANSET */
        case AntlrSQLLexer::UNION_DATE_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_DATE_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionDateSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_DATE_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SET_DATE */
        case AntlrSQLLexer::UNION_SET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SET_TEXT */
        case AntlrSQLLexer::UNION_SET_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SET_TEXT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSetTextLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SET_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SET_TIMESTAMPTZ */
        case AntlrSQLLexer::UNION_SET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPAN_DATE */
        case AntlrSQLLexer::UNION_SPAN_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPAN_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpanDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPAN_DATE */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPAN_TIMESTAMPTZ */
        case AntlrSQLLexer::UNION_SPAN_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPAN_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpanTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPAN_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPANSET_DATE */
        case AntlrSQLLexer::UNION_SPANSET_DATE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPANSET_DATE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpansetDateLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPANSET_DATE */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SPANSET_TIMESTAMPTZ */
        case AntlrSQLLexer::UNION_SPANSET_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SPANSET_TIMESTAMPTZ requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSpansetTimestamptzLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SPANSET_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: UNION_TEXT_SET */
        case AntlrSQLLexer::UNION_TEXT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_TEXT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionTextSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_TEXT_SET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_TIMESTAMPTZ_SET */
        case AntlrSQLLexer::UNION_TIMESTAMPTZ_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_TIMESTAMPTZ_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionTimestamptzSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_TIMESTAMPTZ_SET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_TIMESTAMPTZ_SPAN */
        case AntlrSQLLexer::UNION_TIMESTAMPTZ_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_TIMESTAMPTZ_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionTimestamptzSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_TIMESTAMPTZ_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: UNION_TIMESTAMPTZ_SPANSET */
        case AntlrSQLLexer::UNION_TIMESTAMPTZ_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_TIMESTAMPTZ_SPANSET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionTimestamptzSpansetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_TIMESTAMPTZ_SPANSET */
        /* BEGIN CODEGEN PARSER GLUE: DATESET_SHIFT_SCALE */
        case AntlrSQLLexer::DATESET_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DATESET_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatesetShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESET_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: DATESPAN_SHIFT_SCALE */
        case AntlrSQLLexer::DATESPAN_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DATESPAN_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespanShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPAN_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: DATESPANSET_SHIFT_SCALE */
        case AntlrSQLLexer::DATESPANSET_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DATESPANSET_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespansetShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPANSET_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSET_SHIFT_SCALE */
        case AntlrSQLLexer::TSTZSET_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TSTZSET_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzsetShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSET_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPAN_SHIFT_SCALE */
        case AntlrSQLLexer::TSTZSPAN_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TSTZSPAN_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspanShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPAN_SHIFT_SCALE */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPANSET_SHIFT_SCALE */
        case AntlrSQLLexer::TSTZSPANSET_SHIFT_SCALE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TSTZSPANSET_SHIFT_SCALE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspansetShiftScaleLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPANSET_SHIFT_SCALE */
        /* BEGIN CODEGEN PARSER GLUE: TBOX_HASH */
        case AntlrSQLLexer::TBOX_HASH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_HASH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxHashLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_HASH */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_HAST */
        case AntlrSQLLexer::TBOX_HAST:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_HAST requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxHastLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_HAST */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_HASX */
        case AntlrSQLLexer::TBOX_HASX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_HASX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxHasxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_HASX */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_HASX */
        case AntlrSQLLexer::STBOX_HASX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_HASX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxHasxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_HASX */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_HASZ */
        case AntlrSQLLexer::STBOX_HASZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_HASZ requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxHaszLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_HASZ */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_HAST */
        case AntlrSQLLexer::STBOX_HAST:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_HAST requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxHastLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_HAST */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_ISGEODETIC */
        case AntlrSQLLexer::STBOX_ISGEODETIC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_ISGEODETIC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxIsgeodeticLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_ISGEODETIC */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_SRID */
        case AntlrSQLLexer::STBOX_SRID:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_SRID requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxSridLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_SRID */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_AREA */
        case AntlrSQLLexer::STBOX_AREA:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_AREA requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxAreaLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_AREA */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_PERIMETER */
        case AntlrSQLLexer::STBOX_PERIMETER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_PERIMETER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxPerimeterLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_PERIMETER */
        /* BEGIN CODEGEN PARSER GLUE: TBOX_TO_INTSPAN */
        case AntlrSQLLexer::TBOX_TO_INTSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_TO_INTSPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxToIntspanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_TO_INTSPAN */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_TO_FLOATSPAN */
        case AntlrSQLLexer::TBOX_TO_FLOATSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_TO_FLOATSPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxToFloatspanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_TO_FLOATSPAN */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_TO_TSTZSPAN */
        case AntlrSQLLexer::TBOX_TO_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_TO_TSTZSPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxToTstzspanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_TO_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: INT_TO_TBOX */
        case AntlrSQLLexer::INT_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INT_TO_TBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntToTboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INT_TO_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: FLOAT_TO_TBOX */
        case AntlrSQLLexer::FLOAT_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOAT_TO_TBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatToTboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOAT_TO_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: TIMESTAMPTZ_TO_TBOX */
        case AntlrSQLLexer::TIMESTAMPTZ_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TIMESTAMPTZ_TO_TBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TimestamptzToTboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TIMESTAMPTZ_TO_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: SPAN_TO_TBOX */
        case AntlrSQLLexer::SPAN_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPAN_TO_TBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpanToTboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPAN_TO_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: SET_TO_TBOX */
        case AntlrSQLLexer::SET_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SET_TO_TBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetToTboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_TO_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_TO_TBOX */
        case AntlrSQLLexer::SPANSET_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPANSET_TO_TBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetToTboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_TO_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: TNUMBER_TO_TBOX */
        case AntlrSQLLexer::TNUMBER_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TNUMBER_TO_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnumberToTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNUMBER_TO_TBOX */
        /* BEGIN CODEGEN PARSER GLUE: TBOX_XMIN */
        case AntlrSQLLexer::TBOX_XMIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_XMIN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxXminLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_XMIN */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_XMAX */
        case AntlrSQLLexer::TBOX_XMAX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_XMAX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxXmaxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_XMAX */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_TMIN */
        case AntlrSQLLexer::TBOX_TMIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_TMIN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxTminLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_TMIN */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_TMAX */
        case AntlrSQLLexer::TBOX_TMAX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_TMAX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxTmaxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_TMAX */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_XMIN_INC */
        case AntlrSQLLexer::TBOX_XMIN_INC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_XMIN_INC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxXminIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_XMIN_INC */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_XMAX_INC */
        case AntlrSQLLexer::TBOX_XMAX_INC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_XMAX_INC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxXmaxIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_XMAX_INC */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_TMIN_INC */
        case AntlrSQLLexer::TBOX_TMIN_INC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_TMIN_INC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxTminIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_TMIN_INC */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_TMAX_INC */
        case AntlrSQLLexer::TBOX_TMAX_INC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_TMAX_INC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxTmaxIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_TMAX_INC */

        /* BEGIN CODEGEN PARSER GLUE: TBOXFLOAT_XMIN */
        case AntlrSQLLexer::TBOXFLOAT_XMIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOXFLOAT_XMIN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxfloatXminLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOXFLOAT_XMIN */

        /* BEGIN CODEGEN PARSER GLUE: TBOXFLOAT_XMAX */
        case AntlrSQLLexer::TBOXFLOAT_XMAX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOXFLOAT_XMAX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxfloatXmaxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOXFLOAT_XMAX */

        /* BEGIN CODEGEN PARSER GLUE: TBOXINT_XMIN */
        case AntlrSQLLexer::TBOXINT_XMIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOXINT_XMIN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxintXminLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOXINT_XMIN */

        /* BEGIN CODEGEN PARSER GLUE: TBOXINT_XMAX */
        case AntlrSQLLexer::TBOXINT_XMAX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOXINT_XMAX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxintXmaxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOXINT_XMAX */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_TMIN */
        case AntlrSQLLexer::STBOX_TMIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_TMIN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxTminLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_TMIN */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_TMAX */
        case AntlrSQLLexer::STBOX_TMAX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_TMAX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxTmaxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_TMAX */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_TMIN_INC */
        case AntlrSQLLexer::STBOX_TMIN_INC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_TMIN_INC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxTminIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_TMIN_INC */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_TMAX_INC */
        case AntlrSQLLexer::STBOX_TMAX_INC:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_TMAX_INC requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxTmaxIncLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_TMAX_INC */
        /* BEGIN CODEGEN PARSER GLUE: TINT_VALUE_N */
        case AntlrSQLLexer::TINT_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TINT_VALUE_N requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintValueNLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_VALUE_N */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_VALUE_N */
        case AntlrSQLLexer::TFLOAT_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_VALUE_N requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatValueNLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_VALUE_N */

        /* BEGIN CODEGEN PARSER GLUE: TBOOL_VALUE_N */
        case AntlrSQLLexer::TBOOL_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOOL_VALUE_N requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboolValueNLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOOL_VALUE_N */

        /* BEGIN CODEGEN PARSER GLUE: INTSET_VALUE_N */
        case AntlrSQLLexer::INTSET_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSET_VALUE_N requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntsetValueNLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSET_VALUE_N */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSET_VALUE_N */
        case AntlrSQLLexer::BIGINTSET_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINTSET_VALUE_N requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintsetValueNLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSET_VALUE_N */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSET_VALUE_N */
        case AntlrSQLLexer::FLOATSET_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSET_VALUE_N requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatsetValueNLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSET_VALUE_N */

        /* BEGIN CODEGEN PARSER GLUE: DATESET_VALUE_N */
        case AntlrSQLLexer::DATESET_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESET_VALUE_N requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatesetValueNLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESET_VALUE_N */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSET_VALUE_N */
        case AntlrSQLLexer::TSTZSET_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSET_VALUE_N requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzsetValueNLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSET_VALUE_N */
        /* BEGIN CODEGEN PARSER GLUE: SET_TO_SPAN */
        case AntlrSQLLexer::SET_TO_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SET_TO_SPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetToSpanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_TO_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: SET_TO_SPANSET */
        case AntlrSQLLexer::SET_TO_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SET_TO_SPANSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetToSpansetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_TO_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: TNUMBER_TO_SPAN */
        case AntlrSQLLexer::TNUMBER_TO_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TNUMBER_TO_SPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnumberToSpanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNUMBER_TO_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_SPAN */
        case AntlrSQLLexer::SPANSET_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPANSET_SPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetSpanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_START_SPAN */
        case AntlrSQLLexer::SPANSET_START_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPANSET_START_SPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetStartSpanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_START_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_END_SPAN */
        case AntlrSQLLexer::SPANSET_END_SPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPANSET_END_SPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetEndSpanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_END_SPAN */

        /* BEGIN CODEGEN PARSER GLUE: SET_COPY */
        case AntlrSQLLexer::SET_COPY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SET_COPY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetCopyLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_COPY */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_COPY */
        case AntlrSQLLexer::SPANSET_COPY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPANSET_COPY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetCopyLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_COPY */
        /* BEGIN CODEGEN PARSER GLUE: INT_TIMESTAMPTZ_TO_TBOX */
        case AntlrSQLLexer::INT_TIMESTAMPTZ_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INT_TIMESTAMPTZ_TO_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntTimestamptzToTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INT_TIMESTAMPTZ_TO_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: FLOAT_TIMESTAMPTZ_TO_TBOX */
        case AntlrSQLLexer::FLOAT_TIMESTAMPTZ_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("FLOAT_TIMESTAMPTZ_TO_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatTimestamptzToTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOAT_TIMESTAMPTZ_TO_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: INT_TSTZSPAN_TO_TBOX */
        case AntlrSQLLexer::INT_TSTZSPAN_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INT_TSTZSPAN_TO_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntTstzspanToTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INT_TSTZSPAN_TO_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: FLOAT_TSTZSPAN_TO_TBOX */
        case AntlrSQLLexer::FLOAT_TSTZSPAN_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("FLOAT_TSTZSPAN_TO_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatTstzspanToTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOAT_TSTZSPAN_TO_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: NUMSPAN_TIMESTAMPTZ_TO_TBOX */
        case AntlrSQLLexer::NUMSPAN_TIMESTAMPTZ_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NUMSPAN_TIMESTAMPTZ_TO_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NumspanTimestamptzToTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NUMSPAN_TIMESTAMPTZ_TO_TBOX */

        /* BEGIN CODEGEN PARSER GLUE: NUMSPAN_TSTZSPAN_TO_TBOX */
        case AntlrSQLLexer::NUMSPAN_TSTZSPAN_TO_TBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NUMSPAN_TSTZSPAN_TO_TBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NumspanTstzspanToTboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NUMSPAN_TSTZSPAN_TO_TBOX */
        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_TO_STBOX */
        case AntlrSQLLexer::CBUFFER_TO_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("CBUFFER_TO_STBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferToStboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_TO_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: POSE_TO_STBOX */
        case AntlrSQLLexer::POSE_TO_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("POSE_TO_STBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseToStboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_TO_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_TO_STBOX */
        case AntlrSQLLexer::NPOINT_TO_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NPOINT_TO_STBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointToStboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_TO_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: NSEGMENT_TO_STBOX */
        case AntlrSQLLexer::NSEGMENT_TO_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NSEGMENT_TO_STBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NsegmentToStboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NSEGMENT_TO_STBOX */
        /* BEGIN CODEGEN PARSER GLUE: TTEXT_LOWER */
        case AntlrSQLLexer::TTEXT_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TTEXT_LOWER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextLowerLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_UPPER */
        case AntlrSQLLexer::TTEXT_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TTEXT_UPPER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextUpperLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_INITCAP */
        case AntlrSQLLexer::TTEXT_INITCAP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TTEXT_INITCAP requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextInitcapLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_INITCAP */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_START_VALUE */
        case AntlrSQLLexer::TTEXT_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TTEXT_START_VALUE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextStartValueLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_END_VALUE */
        case AntlrSQLLexer::TTEXT_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TTEXT_END_VALUE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextEndValueLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_MIN_VALUE */
        case AntlrSQLLexer::TTEXT_MIN_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TTEXT_MIN_VALUE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextMinValueLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_MIN_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_MAX_VALUE */
        case AntlrSQLLexer::TTEXT_MAX_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TTEXT_MAX_VALUE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextMaxValueLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_MAX_VALUE */
        /* BEGIN CODEGEN PARSER GLUE: TTEXT_VALUE_N */
        case AntlrSQLLexer::TTEXT_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TTEXT_VALUE_N requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextValueNLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_VALUE_N */

        /* BEGIN CODEGEN PARSER GLUE: TEXTSET_VALUE_N */
        case AntlrSQLLexer::TEXTSET_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TEXTSET_VALUE_N requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextsetValueNLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTSET_VALUE_N */
        /* BEGIN CODEGEN PARSER GLUE: TGEO_VALUE_N */
        case AntlrSQLLexer::TGEO_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGEO_VALUE_N requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeoValueNLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGEO_VALUE_N */

        /* BEGIN CODEGEN PARSER GLUE: TPOSE_VALUE_N */
        case AntlrSQLLexer::TPOSE_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TPOSE_VALUE_N requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TposeValueNLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOSE_VALUE_N */
        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_TO_TSTZSPAN */
        case AntlrSQLLexer::TEMPORAL_TO_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TEMPORAL_TO_TSTZSPAN requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalToTstzspanLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_TO_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_TIME */
        case AntlrSQLLexer::TEMPORAL_TIME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TEMPORAL_TIME requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalTimeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_TIME */

        /* BEGIN CODEGEN PARSER GLUE: TNUMBER_VALUESPANS */
        case AntlrSQLLexer::TNUMBER_VALUESPANS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TNUMBER_VALUESPANS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnumberValuespansLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNUMBER_VALUESPANS */
        /* BEGIN CODEGEN PARSER GLUE: SPAN_HASH */
        case AntlrSQLLexer::SPAN_HASH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPAN_HASH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpanHashLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPAN_HASH */

        /* BEGIN CODEGEN PARSER GLUE: SET_HASH */
        case AntlrSQLLexer::SET_HASH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SET_HASH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetHashLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_HASH */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_HASH */
        case AntlrSQLLexer::SPANSET_HASH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPANSET_HASH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetHashLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_HASH */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_HASH */
        case AntlrSQLLexer::STBOX_HASH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_HASH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxHashLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_HASH */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_HASH */
        case AntlrSQLLexer::CBUFFER_HASH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("CBUFFER_HASH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferHashLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_HASH */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_HASH */
        case AntlrSQLLexer::NPOINT_HASH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NPOINT_HASH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointHashLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_HASH */

        /* BEGIN CODEGEN PARSER GLUE: POSE_HASH */
        case AntlrSQLLexer::POSE_HASH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("POSE_HASH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseHashLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_HASH */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_HASH */
        case AntlrSQLLexer::TEMPORAL_HASH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TEMPORAL_HASH requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalHashLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_HASH */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_SRID */
        case AntlrSQLLexer::CBUFFER_SRID:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("CBUFFER_SRID requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferSridLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_SRID */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_SRID */
        case AntlrSQLLexer::NPOINT_SRID:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NPOINT_SRID requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointSridLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_SRID */

        /* BEGIN CODEGEN PARSER GLUE: NSEGMENT_SRID */
        case AntlrSQLLexer::NSEGMENT_SRID:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NSEGMENT_SRID requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NsegmentSridLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NSEGMENT_SRID */

        /* BEGIN CODEGEN PARSER GLUE: POSE_SRID */
        case AntlrSQLLexer::POSE_SRID:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("POSE_SRID requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseSridLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_SRID */
        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_ROUND */
        case AntlrSQLLexer::CBUFFER_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("CBUFFER_ROUND requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferRoundLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_ROUND */

        /* BEGIN CODEGEN PARSER GLUE: FLOAT_ROUND */
        case AntlrSQLLexer::FLOAT_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOAT_ROUND requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatRoundLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOAT_ROUND */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPAN_ROUND */
        case AntlrSQLLexer::FLOATSPAN_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPAN_ROUND requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanRoundLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPAN_ROUND */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPANSET_ROUND */
        case AntlrSQLLexer::FLOATSPANSET_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPANSET_ROUND requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspansetRoundLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPANSET_ROUND */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_ROUND */
        case AntlrSQLLexer::NPOINT_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NPOINT_ROUND requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointRoundLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_ROUND */

        /* BEGIN CODEGEN PARSER GLUE: POSE_ROUND */
        case AntlrSQLLexer::POSE_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("POSE_ROUND requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseRoundLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_ROUND */

        /* BEGIN CODEGEN PARSER GLUE: SET_ROUND */
        case AntlrSQLLexer::SET_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SET_ROUND requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetRoundLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_ROUND */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_ROUND */
        case AntlrSQLLexer::STBOX_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_ROUND requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxRoundLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_ROUND */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_ROUND */
        case AntlrSQLLexer::TBOX_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TBOX_ROUND requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxRoundLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_ROUND */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_ROUND */
        case AntlrSQLLexer::TEMPORAL_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TEMPORAL_ROUND requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalRoundLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_ROUND */
        /* BEGIN CODEGEN PARSER GLUE: DATESPAN_DURATION */
        case AntlrSQLLexer::DATESPAN_DURATION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESPAN_DURATION requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespanDurationLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPAN_DURATION */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPAN_DURATION */
        case AntlrSQLLexer::TSTZSPAN_DURATION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPAN_DURATION requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspanDurationLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPAN_DURATION */

        /* BEGIN CODEGEN PARSER GLUE: DATESPANSET_DURATION */
        case AntlrSQLLexer::DATESPANSET_DURATION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESPANSET_DURATION requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespansetDurationLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPANSET_DURATION */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPANSET_DURATION */
        case AntlrSQLLexer::TSTZSPANSET_DURATION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPANSET_DURATION requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspansetDurationLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPANSET_DURATION */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_DURATION */
        case AntlrSQLLexer::TEMPORAL_DURATION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TEMPORAL_DURATION requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalDurationLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_DURATION */
        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_RADIUS */
        case AntlrSQLLexer::CBUFFER_RADIUS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("CBUFFER_RADIUS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferRadiusLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_RADIUS */

        /* BEGIN CODEGEN PARSER GLUE: DATESPAN_TO_TSTZSPAN */
        case AntlrSQLLexer::DATESPAN_TO_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESPAN_TO_TSTZSPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespanToTstzspanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPAN_TO_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: DATESPANSET_TO_TSTZSPANSET */
        case AntlrSQLLexer::DATESPANSET_TO_TSTZSPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESPANSET_TO_TSTZSPANSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespansetToTstzspansetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPANSET_TO_TSTZSPANSET */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPAN_TO_INTSPAN */
        case AntlrSQLLexer::FLOATSPAN_TO_INTSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPAN_TO_INTSPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanToIntspanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPAN_TO_INTSPAN */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPANSET_TO_INTSPANSET */
        case AntlrSQLLexer::FLOATSPANSET_TO_INTSPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSPANSET_TO_INTSPANSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspansetToIntspansetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPANSET_TO_INTSPANSET */

        /* BEGIN CODEGEN PARSER GLUE: INTSPAN_TO_FLOATSPAN */
        case AntlrSQLLexer::INTSPAN_TO_FLOATSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPAN_TO_FLOATSPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspanToFloatspanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPAN_TO_FLOATSPAN */

        /* BEGIN CODEGEN PARSER GLUE: INTSPANSET_TO_FLOATSPANSET */
        case AntlrSQLLexer::INTSPANSET_TO_FLOATSPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSPANSET_TO_FLOATSPANSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspansetToFloatspansetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPANSET_TO_FLOATSPANSET */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_POSITION */
        case AntlrSQLLexer::NPOINT_POSITION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NPOINT_POSITION requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointPositionLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_POSITION */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_ROUTE */
        case AntlrSQLLexer::NPOINT_ROUTE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NPOINT_ROUTE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointRouteLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_ROUTE */

        /* BEGIN CODEGEN PARSER GLUE: NSEGMENT_ROUTE */
        case AntlrSQLLexer::NSEGMENT_ROUTE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NSEGMENT_ROUTE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NsegmentRouteLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NSEGMENT_ROUTE */

        /* BEGIN CODEGEN PARSER GLUE: POSE_ROTATION */
        case AntlrSQLLexer::POSE_ROTATION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("POSE_ROTATION requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseRotationLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_ROTATION */

        /* BEGIN CODEGEN PARSER GLUE: SPAN_TO_SPANSET */
        case AntlrSQLLexer::SPAN_TO_SPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("SPAN_TO_SPANSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpanToSpansetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPAN_TO_SPANSET */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_TO_TSTZSPAN */
        case AntlrSQLLexer::STBOX_TO_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_TO_TSTZSPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxToTstzspanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_TO_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_VOLUME */
        case AntlrSQLLexer::STBOX_VOLUME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_VOLUME requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxVolumeLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_VOLUME */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_XMAX */
        case AntlrSQLLexer::STBOX_XMAX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_XMAX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxXmaxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_XMAX */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_XMIN */
        case AntlrSQLLexer::STBOX_XMIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_XMIN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxXminLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_XMIN */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_YMAX */
        case AntlrSQLLexer::STBOX_YMAX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_YMAX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxYmaxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_YMAX */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_YMIN */
        case AntlrSQLLexer::STBOX_YMIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_YMIN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxYminLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_YMIN */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_ZMAX */
        case AntlrSQLLexer::STBOX_ZMAX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_ZMAX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxZmaxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_ZMAX */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_ZMIN */
        case AntlrSQLLexer::STBOX_ZMIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_ZMIN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxZminLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_ZMIN */

        /* BEGIN CODEGEN PARSER GLUE: TIMESTAMPTZ_TO_STBOX */
        case AntlrSQLLexer::TIMESTAMPTZ_TO_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TIMESTAMPTZ_TO_STBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TimestamptzToStboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TIMESTAMPTZ_TO_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSET_TO_STBOX */
        case AntlrSQLLexer::TSTZSET_TO_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSET_TO_STBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzsetToStboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSET_TO_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPAN_TO_DATESPAN */
        case AntlrSQLLexer::TSTZSPAN_TO_DATESPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPAN_TO_DATESPAN requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspanToDatespanLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPAN_TO_DATESPAN */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPAN_TO_STBOX */
        case AntlrSQLLexer::TSTZSPAN_TO_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPAN_TO_STBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspanToStboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPAN_TO_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPANSET_TO_DATESPANSET */
        case AntlrSQLLexer::TSTZSPANSET_TO_DATESPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPANSET_TO_DATESPANSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspansetToDatespansetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPANSET_TO_DATESPANSET */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPANSET_TO_STBOX */
        case AntlrSQLLexer::TSTZSPANSET_TO_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPANSET_TO_STBOX requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspansetToStboxLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPANSET_TO_STBOX */
        /* BEGIN CODEGEN PARSER GLUE: GEOSET_START_VALUE */
        case AntlrSQLLexer::GEOSET_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOSET_START_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeosetStartValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOSET_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: GEOSET_END_VALUE */
        case AntlrSQLLexer::GEOSET_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOSET_END_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeosetEndValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOSET_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: GEOSET_VALUE_N */
        case AntlrSQLLexer::GEOSET_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOSET_VALUE_N requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeosetValueNLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOSET_VALUE_N */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFERSET_START_VALUE */
        case AntlrSQLLexer::CBUFFERSET_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("CBUFFERSET_START_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbuffersetStartValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFERSET_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFERSET_END_VALUE */
        case AntlrSQLLexer::CBUFFERSET_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("CBUFFERSET_END_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbuffersetEndValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFERSET_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFERSET_VALUE_N */
        case AntlrSQLLexer::CBUFFERSET_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("CBUFFERSET_VALUE_N requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbuffersetValueNLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFERSET_VALUE_N */

        /* BEGIN CODEGEN PARSER GLUE: NPOINTSET_START_VALUE */
        case AntlrSQLLexer::NPOINTSET_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NPOINTSET_START_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointsetStartValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINTSET_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: NPOINTSET_END_VALUE */
        case AntlrSQLLexer::NPOINTSET_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NPOINTSET_END_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointsetEndValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINTSET_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: NPOINTSET_VALUE_N */
        case AntlrSQLLexer::NPOINTSET_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NPOINTSET_VALUE_N requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointsetValueNLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINTSET_VALUE_N */

        /* BEGIN CODEGEN PARSER GLUE: POSESET_START_VALUE */
        case AntlrSQLLexer::POSESET_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("POSESET_START_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PosesetStartValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSESET_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: POSESET_END_VALUE */
        case AntlrSQLLexer::POSESET_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("POSESET_END_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PosesetEndValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSESET_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: POSESET_VALUE_N */
        case AntlrSQLLexer::POSESET_VALUE_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("POSESET_VALUE_N requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PosesetValueNLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSESET_VALUE_N */
        /* BEGIN CODEGEN PARSER GLUE: GEO_COPY */
        case AntlrSQLLexer::GEO_COPY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEO_COPY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoCopyLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_COPY */

        /* BEGIN CODEGEN PARSER GLUE: GEO_IS_EMPTY */
        case AntlrSQLLexer::GEO_IS_EMPTY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEO_IS_EMPTY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoIsEmptyLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_IS_EMPTY */

        /* BEGIN CODEGEN PARSER GLUE: GEO_IS_UNITARY */
        case AntlrSQLLexer::GEO_IS_UNITARY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEO_IS_UNITARY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoIsUnitaryLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_IS_UNITARY */

        /* BEGIN CODEGEN PARSER GLUE: GEO_NUM_GEOS */
        case AntlrSQLLexer::GEO_NUM_GEOS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEO_NUM_GEOS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoNumGeosLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_NUM_GEOS */

        /* BEGIN CODEGEN PARSER GLUE: GEO_NUM_POINTS */
        case AntlrSQLLexer::GEO_NUM_POINTS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEO_NUM_POINTS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoNumPointsLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_NUM_POINTS */

        /* BEGIN CODEGEN PARSER GLUE: GEO_POINTS */
        case AntlrSQLLexer::GEO_POINTS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEO_POINTS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoPointsLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_POINTS */

        /* BEGIN CODEGEN PARSER GLUE: GEO_REVERSE */
        case AntlrSQLLexer::GEO_REVERSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEO_REVERSE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoReverseLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_REVERSE */

        /* BEGIN CODEGEN PARSER GLUE: GEO_ROUND */
        case AntlrSQLLexer::GEO_ROUND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEO_ROUND requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoRoundLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_ROUND */

        /* BEGIN CODEGEN PARSER GLUE: GEO_SRID */
        case AntlrSQLLexer::GEO_SRID:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEO_SRID requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoSridLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_SRID */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_BOUNDARY */
        case AntlrSQLLexer::GEOM_BOUNDARY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOM_BOUNDARY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomBoundaryLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_BOUNDARY */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_CENTROID */
        case AntlrSQLLexer::GEOM_CENTROID:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOM_CENTROID requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomCentroidLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_CENTROID */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_CONVEX_HULL */
        case AntlrSQLLexer::GEOM_CONVEX_HULL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOM_CONVEX_HULL requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomConvexHullLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_CONVEX_HULL */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_LENGTH */
        case AntlrSQLLexer::GEOM_LENGTH:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOM_LENGTH requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomLengthLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_LENGTH */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_PERIMETER */
        case AntlrSQLLexer::GEOM_PERIMETER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOM_PERIMETER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomPerimeterLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_PERIMETER */

        /* BEGIN CODEGEN PARSER GLUE: LINE_NUMPOINTS */
        case AntlrSQLLexer::LINE_NUMPOINTS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("LINE_NUMPOINTS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LineNumpointsLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: LINE_NUMPOINTS */
        /* BEGIN CODEGEN PARSER GLUE: TINT_AT_VALUE */
        case AntlrSQLLexer::TINT_AT_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TINT_AT_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintAtValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_AT_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_AT_VALUE */
        case AntlrSQLLexer::TFLOAT_AT_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TFLOAT_AT_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatAtValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_AT_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_AT_VALUE */
        case AntlrSQLLexer::TTEXT_AT_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TTEXT_AT_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextAtValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_AT_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TBOOL_AT_VALUE */
        case AntlrSQLLexer::TBOOL_AT_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TBOOL_AT_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboolAtValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOOL_AT_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TBOOL_MINUS_VALUE */
        case AntlrSQLLexer::TBOOL_MINUS_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TBOOL_MINUS_VALUE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboolMinusValueLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOOL_MINUS_VALUE */
        /* BEGIN CODEGEN PARSER GLUE: TSTZSPAN_EXPAND */
        case AntlrSQLLexer::TSTZSPAN_EXPAND:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TSTZSPAN_EXPAND requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspanExpandLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPAN_EXPAND */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_EXPAND_TIME */
        case AntlrSQLLexer::TBOX_EXPAND_TIME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOX_EXPAND_TIME requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxExpandTimeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_EXPAND_TIME */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_EXPAND_TIME */
        case AntlrSQLLexer::STBOX_EXPAND_TIME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("STBOX_EXPAND_TIME requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxExpandTimeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_EXPAND_TIME */

        /* BEGIN CODEGEN PARSER GLUE: ADD_TIMESTAMPTZ_INTERVAL */
        case AntlrSQLLexer::ADD_TIMESTAMPTZ_INTERVAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADD_TIMESTAMPTZ_INTERVAL requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddTimestamptzIntervalLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_TIMESTAMPTZ_INTERVAL */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_TIMESTAMPTZ_INTERVAL */
        case AntlrSQLLexer::MINUS_TIMESTAMPTZ_INTERVAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_TIMESTAMPTZ_INTERVAL requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusTimestamptzIntervalLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_TIMESTAMPTZ_INTERVAL */

        /* BEGIN CODEGEN PARSER GLUE: TIMESTAMPTZ_SHIFT */
        case AntlrSQLLexer::TIMESTAMPTZ_SHIFT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TIMESTAMPTZ_SHIFT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TimestamptzShiftLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TIMESTAMPTZ_SHIFT */

        /* BEGIN CODEGEN PARSER GLUE: ADD_INTERVAL_INTERVAL */
        case AntlrSQLLexer::ADD_INTERVAL_INTERVAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("ADD_INTERVAL_INTERVAL requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AddIntervalIntervalLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: ADD_INTERVAL_INTERVAL */

        /* BEGIN CODEGEN PARSER GLUE: MUL_INTERVAL_DOUBLE */
        case AntlrSQLLexer::MUL_INTERVAL_DOUBLE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MUL_INTERVAL_DOUBLE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MulIntervalDoubleLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MUL_INTERVAL_DOUBLE */
        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_CBUFFER_SET */
        case AntlrSQLLexer::CONTAINED_CBUFFER_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_CBUFFER_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedCbufferSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_CBUFFER_SET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_GEO_SET */
        case AntlrSQLLexer::CONTAINED_GEO_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_GEO_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedGeoSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_GEO_SET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_NPOINT_SET */
        case AntlrSQLLexer::CONTAINED_NPOINT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_NPOINT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedNpointSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_NPOINT_SET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_POSE_SET */
        case AntlrSQLLexer::CONTAINED_POSE_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_POSE_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedPoseSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_POSE_SET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TEXT_SET */
        case AntlrSQLLexer::CONTAINED_TEXT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINED_TEXT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedTextSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TEXT_SET */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SET_CBUFFER */
        case AntlrSQLLexer::CONTAINS_SET_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SET_CBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSetCbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SET_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SET_GEO */
        case AntlrSQLLexer::CONTAINS_SET_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SET_GEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSetGeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SET_GEO */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SET_NPOINT */
        case AntlrSQLLexer::CONTAINS_SET_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SET_NPOINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSetNpointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SET_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SET_POSE */
        case AntlrSQLLexer::CONTAINS_SET_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SET_POSE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSetPoseLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SET_POSE */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_SET_TEXT */
        case AntlrSQLLexer::CONTAINS_SET_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CONTAINS_SET_TEXT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsSetTextLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_SET_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_GEO_SET */
        case AntlrSQLLexer::INTERSECTION_GEO_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_GEO_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionGeoSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_GEO_SET */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SET_CBUFFER */
        case AntlrSQLLexer::INTERSECTION_SET_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SET_CBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSetCbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SET_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SET_GEO */
        case AntlrSQLLexer::INTERSECTION_SET_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SET_GEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSetGeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SET_GEO */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SET_NPOINT */
        case AntlrSQLLexer::INTERSECTION_SET_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SET_NPOINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSetNpointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SET_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: INTERSECTION_SET_POSE */
        case AntlrSQLLexer::INTERSECTION_SET_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("INTERSECTION_SET_POSE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntersectionSetPoseLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTERSECTION_SET_POSE */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_SET_TEXT */
        case AntlrSQLLexer::LEFT_SET_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_SET_TEXT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftSetTextLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_SET_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_TEXT_SET */
        case AntlrSQLLexer::LEFT_TEXT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("LEFT_TEXT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftTextSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_TEXT_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_CBUFFER_SET */
        case AntlrSQLLexer::MINUS_CBUFFER_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_CBUFFER_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusCbufferSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_CBUFFER_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_GEO_SET */
        case AntlrSQLLexer::MINUS_GEO_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_GEO_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusGeoSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_GEO_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_NPOINT_SET */
        case AntlrSQLLexer::MINUS_NPOINT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_NPOINT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusNpointSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_NPOINT_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_POSE_SET */
        case AntlrSQLLexer::MINUS_POSE_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_POSE_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusPoseSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_POSE_SET */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SET_CBUFFER */
        case AntlrSQLLexer::MINUS_SET_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SET_CBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSetCbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SET_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SET_GEO */
        case AntlrSQLLexer::MINUS_SET_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SET_GEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSetGeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SET_GEO */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SET_NPOINT */
        case AntlrSQLLexer::MINUS_SET_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SET_NPOINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSetNpointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SET_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: MINUS_SET_POSE */
        case AntlrSQLLexer::MINUS_SET_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("MINUS_SET_POSE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(MinusSetPoseLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: MINUS_SET_POSE */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_SET_TEXT */
        case AntlrSQLLexer::OVERLEFT_SET_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_SET_TEXT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftSetTextLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_SET_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_TEXT_SET */
        case AntlrSQLLexer::OVERLEFT_TEXT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERLEFT_TEXT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftTextSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_TEXT_SET */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_SET_TEXT */
        case AntlrSQLLexer::OVERRIGHT_SET_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_SET_TEXT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightSetTextLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_SET_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_TEXT_SET */
        case AntlrSQLLexer::OVERRIGHT_TEXT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("OVERRIGHT_TEXT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightTextSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_TEXT_SET */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_SET_TEXT */
        case AntlrSQLLexer::RIGHT_SET_TEXT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_SET_TEXT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightSetTextLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_SET_TEXT */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_TEXT_SET */
        case AntlrSQLLexer::RIGHT_TEXT_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("RIGHT_TEXT_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightTextSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_TEXT_SET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_GEO_SET */
        case AntlrSQLLexer::UNION_GEO_SET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_GEO_SET requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionGeoSetLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_GEO_SET */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SET_CBUFFER */
        case AntlrSQLLexer::UNION_SET_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SET_CBUFFER requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSetCbufferLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SET_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SET_GEO */
        case AntlrSQLLexer::UNION_SET_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SET_GEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSetGeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SET_GEO */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SET_NPOINT */
        case AntlrSQLLexer::UNION_SET_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SET_NPOINT requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSetNpointLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SET_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: UNION_SET_POSE */
        case AntlrSQLLexer::UNION_SET_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("UNION_SET_POSE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(UnionSetPoseLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: UNION_SET_POSE */
        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_NUMSPAN_TNUMBER */
        case AntlrSQLLexer::CONTAINED_NUMSPAN_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINED_NUMSPAN_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedNumspanTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_NUMSPAN_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TEMPORAL_TSTZSPAN */
        case AntlrSQLLexer::CONTAINED_TEMPORAL_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINED_TEMPORAL_TSTZSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedTemporalTstzspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TEMPORAL_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TNUMBER_NUMSPAN */
        case AntlrSQLLexer::CONTAINED_TNUMBER_NUMSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINED_TNUMBER_NUMSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedTnumberNumspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TNUMBER_NUMSPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINED_TSTZSPAN_TEMPORAL */
        case AntlrSQLLexer::CONTAINED_TSTZSPAN_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINED_TSTZSPAN_TEMPORAL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainedTstzspanTemporalLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINED_TSTZSPAN_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_NUMSPAN_TNUMBER */
        case AntlrSQLLexer::CONTAINS_NUMSPAN_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINS_NUMSPAN_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsNumspanTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_NUMSPAN_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_TEMPORAL_TSTZSPAN */
        case AntlrSQLLexer::CONTAINS_TEMPORAL_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINS_TEMPORAL_TSTZSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsTemporalTstzspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_TEMPORAL_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_TNUMBER_NUMSPAN */
        case AntlrSQLLexer::CONTAINS_TNUMBER_NUMSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINS_TNUMBER_NUMSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsTnumberNumspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_TNUMBER_NUMSPAN */

        /* BEGIN CODEGEN PARSER GLUE: CONTAINS_TSTZSPAN_TEMPORAL */
        case AntlrSQLLexer::CONTAINS_TSTZSPAN_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("CONTAINS_TSTZSPAN_TEMPORAL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ContainsTstzspanTemporalLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: CONTAINS_TSTZSPAN_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_NUMSPAN_TNUMBER */
        case AntlrSQLLexer::LEFT_NUMSPAN_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("LEFT_NUMSPAN_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftNumspanTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_NUMSPAN_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: LEFT_TNUMBER_NUMSPAN */
        case AntlrSQLLexer::LEFT_TNUMBER_NUMSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("LEFT_TNUMBER_NUMSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(LeftTnumberNumspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: LEFT_TNUMBER_NUMSPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_NUMSPAN_TNUMBER */
        case AntlrSQLLexer::OVERLAPS_NUMSPAN_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLAPS_NUMSPAN_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsNumspanTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_NUMSPAN_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_TEMPORAL_TSTZSPAN */
        case AntlrSQLLexer::OVERLAPS_TEMPORAL_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLAPS_TEMPORAL_TSTZSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsTemporalTstzspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_TEMPORAL_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_TNUMBER_NUMSPAN */
        case AntlrSQLLexer::OVERLAPS_TNUMBER_NUMSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLAPS_TNUMBER_NUMSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsTnumberNumspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_TNUMBER_NUMSPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERLAPS_TSTZSPAN_TEMPORAL */
        case AntlrSQLLexer::OVERLAPS_TSTZSPAN_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLAPS_TSTZSPAN_TEMPORAL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverlapsTstzspanTemporalLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLAPS_TSTZSPAN_TEMPORAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_NUMSPAN_TNUMBER */
        case AntlrSQLLexer::OVERLEFT_NUMSPAN_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLEFT_NUMSPAN_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftNumspanTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_NUMSPAN_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERLEFT_TNUMBER_NUMSPAN */
        case AntlrSQLLexer::OVERLEFT_TNUMBER_NUMSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERLEFT_TNUMBER_NUMSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverleftTnumberNumspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERLEFT_TNUMBER_NUMSPAN */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_NUMSPAN_TNUMBER */
        case AntlrSQLLexer::OVERRIGHT_NUMSPAN_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERRIGHT_NUMSPAN_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightNumspanTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_NUMSPAN_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: OVERRIGHT_TNUMBER_NUMSPAN */
        case AntlrSQLLexer::OVERRIGHT_TNUMBER_NUMSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("OVERRIGHT_TNUMBER_NUMSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverrightTnumberNumspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERRIGHT_TNUMBER_NUMSPAN */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_NUMSPAN_TNUMBER */
        case AntlrSQLLexer::RIGHT_NUMSPAN_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("RIGHT_NUMSPAN_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightNumspanTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_NUMSPAN_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: RIGHT_TNUMBER_NUMSPAN */
        case AntlrSQLLexer::RIGHT_TNUMBER_NUMSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("RIGHT_TNUMBER_NUMSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(RightTnumberNumspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: RIGHT_TNUMBER_NUMSPAN */

        /* BEGIN CODEGEN PARSER GLUE: SAME_NUMSPAN_TNUMBER */
        case AntlrSQLLexer::SAME_NUMSPAN_TNUMBER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SAME_NUMSPAN_TNUMBER requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SameNumspanTnumberLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_NUMSPAN_TNUMBER */

        /* BEGIN CODEGEN PARSER GLUE: SAME_TEMPORAL_TSTZSPAN */
        case AntlrSQLLexer::SAME_TEMPORAL_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SAME_TEMPORAL_TSTZSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SameTemporalTstzspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_TEMPORAL_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: SAME_TNUMBER_NUMSPAN */
        case AntlrSQLLexer::SAME_TNUMBER_NUMSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SAME_TNUMBER_NUMSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SameTnumberNumspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_TNUMBER_NUMSPAN */

        /* BEGIN CODEGEN PARSER GLUE: SAME_TSTZSPAN_TEMPORAL */
        case AntlrSQLLexer::SAME_TSTZSPAN_TEMPORAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("SAME_TSTZSPAN_TEMPORAL requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SameTstzspanTemporalLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: SAME_TSTZSPAN_TEMPORAL */
        /* BEGIN CODEGEN PARSER GLUE: NAD_TNPOINT_NPOINT */
        case AntlrSQLLexer::NAD_TNPOINT_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAD_TNPOINT_NPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTnpointNpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TNPOINT_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: NAD_TPOSE_POSE */
        case AntlrSQLLexer::NAD_TPOSE_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAD_TPOSE_POSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadTposePoseLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_TPOSE_POSE */

        /* BEGIN CODEGEN PARSER GLUE: NAI_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::NAI_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAI_TCBUFFER_CBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NaiTcbufferCbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAI_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: NAI_TCBUFFER_GEO */
        case AntlrSQLLexer::NAI_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAI_TCBUFFER_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NaiTcbufferGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAI_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: NAI_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::NAI_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("NAI_TCBUFFER_TCBUFFER requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NaiTcbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAI_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: NAI_TGEO_GEO */
        case AntlrSQLLexer::NAI_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAI_TGEO_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NaiTgeoGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAI_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: NAI_TGEO_TGEO */
        case AntlrSQLLexer::NAI_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("NAI_TGEO_TGEO requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NaiTgeoTgeoLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAI_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: NAI_TNPOINT_GEO */
        case AntlrSQLLexer::NAI_TNPOINT_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAI_TNPOINT_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NaiTnpointGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAI_TNPOINT_GEO */

        /* BEGIN CODEGEN PARSER GLUE: NAI_TNPOINT_NPOINT */
        case AntlrSQLLexer::NAI_TNPOINT_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("NAI_TNPOINT_NPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NaiTnpointNpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAI_TNPOINT_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: NAI_TNPOINT_TNPOINT */
        case AntlrSQLLexer::NAI_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("NAI_TNPOINT_TNPOINT requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NaiTnpointTnpointLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAI_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: NAI_TPOSE_GEO */
        case AntlrSQLLexer::NAI_TPOSE_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAI_TPOSE_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NaiTposeGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAI_TPOSE_GEO */

        /* BEGIN CODEGEN PARSER GLUE: NAI_TPOSE_POSE */
        case AntlrSQLLexer::NAI_TPOSE_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("NAI_TPOSE_POSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NaiTposePoseLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAI_TPOSE_POSE */

        /* BEGIN CODEGEN PARSER GLUE: NAI_TPOSE_TPOSE */
        case AntlrSQLLexer::NAI_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("NAI_TPOSE_TPOSE requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NaiTposeTposeLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAI_TPOSE_TPOSE */

        /* BEGIN CODEGEN PARSER GLUE: SHORTESTLINE_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::SHORTESTLINE_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("SHORTESTLINE_TCBUFFER_CBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ShortestlineTcbufferCbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: SHORTESTLINE_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: SHORTESTLINE_TCBUFFER_GEO */
        case AntlrSQLLexer::SHORTESTLINE_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("SHORTESTLINE_TCBUFFER_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ShortestlineTcbufferGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: SHORTESTLINE_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: SHORTESTLINE_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::SHORTESTLINE_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("SHORTESTLINE_TCBUFFER_TCBUFFER requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ShortestlineTcbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: SHORTESTLINE_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: SHORTESTLINE_TGEO_GEO */
        case AntlrSQLLexer::SHORTESTLINE_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("SHORTESTLINE_TGEO_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ShortestlineTgeoGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: SHORTESTLINE_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: SHORTESTLINE_TGEO_TGEO */
        case AntlrSQLLexer::SHORTESTLINE_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("SHORTESTLINE_TGEO_TGEO requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ShortestlineTgeoTgeoLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: SHORTESTLINE_TGEO_TGEO */

        /* BEGIN CODEGEN PARSER GLUE: SHORTESTLINE_TNPOINT_GEO */
        case AntlrSQLLexer::SHORTESTLINE_TNPOINT_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("SHORTESTLINE_TNPOINT_GEO requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ShortestlineTnpointGeoLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: SHORTESTLINE_TNPOINT_GEO */

        /* BEGIN CODEGEN PARSER GLUE: SHORTESTLINE_TNPOINT_NPOINT */
        case AntlrSQLLexer::SHORTESTLINE_TNPOINT_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("SHORTESTLINE_TNPOINT_NPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ShortestlineTnpointNpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: SHORTESTLINE_TNPOINT_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: SHORTESTLINE_TNPOINT_TNPOINT */
        case AntlrSQLLexer::SHORTESTLINE_TNPOINT_TNPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("SHORTESTLINE_TNPOINT_TNPOINT requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ShortestlineTnpointTnpointLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: SHORTESTLINE_TNPOINT_TNPOINT */

        /* BEGIN CODEGEN PARSER GLUE: SHORTESTLINE_TPOSE_GEO */
        case AntlrSQLLexer::SHORTESTLINE_TPOSE_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("SHORTESTLINE_TPOSE_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ShortestlineTposeGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: SHORTESTLINE_TPOSE_GEO */

        /* BEGIN CODEGEN PARSER GLUE: SHORTESTLINE_TPOSE_POSE */
        case AntlrSQLLexer::SHORTESTLINE_TPOSE_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("SHORTESTLINE_TPOSE_POSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ShortestlineTposePoseLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: SHORTESTLINE_TPOSE_POSE */

        /* BEGIN CODEGEN PARSER GLUE: SHORTESTLINE_TPOSE_TPOSE */
        case AntlrSQLLexer::SHORTESTLINE_TPOSE_TPOSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("SHORTESTLINE_TPOSE_TPOSE requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(ShortestlineTposeTposeLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: SHORTESTLINE_TPOSE_TPOSE */
        /* BEGIN CODEGEN PARSER GLUE: TNPOINT_ROUTE */
        case AntlrSQLLexer::TNPOINT_ROUTE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNPOINT_ROUTE requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnpointRouteLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNPOINT_ROUTE */

        /* BEGIN CODEGEN PARSER GLUE: TNPOINT_TRAJECTORY */
        case AntlrSQLLexer::TNPOINT_TRAJECTORY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNPOINT_TRAJECTORY requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnpointTrajectoryLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNPOINT_TRAJECTORY */

        /* BEGIN CODEGEN PARSER GLUE: TNPOINT_TWCENTROID */
        case AntlrSQLLexer::TNPOINT_TWCENTROID:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNPOINT_TWCENTROID requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnpointTwcentroidLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNPOINT_TWCENTROID */

        /* BEGIN CODEGEN PARSER GLUE: TPOSE_START_VALUE */
        case AntlrSQLLexer::TPOSE_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TPOSE_START_VALUE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TposeStartValueLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOSE_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TPOSE_END_VALUE */
        case AntlrSQLLexer::TPOSE_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TPOSE_END_VALUE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TposeEndValueLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOSE_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TPOSE_TRAJECTORY */
        case AntlrSQLLexer::TPOSE_TRAJECTORY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TPOSE_TRAJECTORY requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TposeTrajectoryLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOSE_TRAJECTORY */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_COPY */
        case AntlrSQLLexer::CBUFFER_COPY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("CBUFFER_COPY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferCopyLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_COPY */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_POINT */
        case AntlrSQLLexer::CBUFFER_POINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("CBUFFER_POINT requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferPointLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_POINT */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_TO_GEOM */
        case AntlrSQLLexer::CBUFFER_TO_GEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("CBUFFER_TO_GEOM requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferToGeomLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_TO_GEOM */

        /* BEGIN CODEGEN PARSER GLUE: POSE_COPY */
        case AntlrSQLLexer::POSE_COPY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("POSE_COPY requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseCopyLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_COPY */

        /* BEGIN CODEGEN PARSER GLUE: POSE_TO_POINT */
        case AntlrSQLLexer::POSE_TO_POINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("POSE_TO_POINT requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseToPointLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_TO_POINT */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_TO_GEOMPOINT */
        case AntlrSQLLexer::NPOINT_TO_GEOMPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NPOINT_TO_GEOMPOINT requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointToGeompointLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_TO_GEOMPOINT */

        /* BEGIN CODEGEN PARSER GLUE: NSEGMENT_TO_GEOM */
        case AntlrSQLLexer::NSEGMENT_TO_GEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NSEGMENT_TO_GEOM requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NsegmentToGeomLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NSEGMENT_TO_GEOM */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_TO_GEO */
        case AntlrSQLLexer::STBOX_TO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("STBOX_TO_GEO requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxToGeoLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_TO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TEXTSET_START_VALUE */
        case AntlrSQLLexer::TEXTSET_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TEXTSET_START_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextsetStartValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTSET_START_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TEXTSET_END_VALUE */
        case AntlrSQLLexer::TEXTSET_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TEXTSET_END_VALUE requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextsetEndValueLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTSET_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TBOOL_FROM_BASE_TEMP */
        case AntlrSQLLexer::TBOOL_FROM_BASE_TEMP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TBOOL_FROM_BASE_TEMP requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboolFromBaseTempLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOOL_FROM_BASE_TEMP */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_DEGREES */
        case AntlrSQLLexer::TFLOAT_DEGREES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TFLOAT_DEGREES requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatDegreesLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_DEGREES */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_MAKE */
        case AntlrSQLLexer::TBOX_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOX_MAKE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxMakeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_MAKE */
        /* BEGIN CODEGEN PARSER GLUE: TCBUFFER_AT_CBUFFER */
        case AntlrSQLLexer::TCBUFFER_AT_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCBUFFER_AT_CBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcbufferAtCbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCBUFFER_AT_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TCBUFFER_AT_GEOM */
        case AntlrSQLLexer::TCBUFFER_AT_GEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCBUFFER_AT_GEOM requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcbufferAtGeomLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCBUFFER_AT_GEOM */

        /* BEGIN CODEGEN PARSER GLUE: TCBUFFER_MINUS_CBUFFER */
        case AntlrSQLLexer::TCBUFFER_MINUS_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCBUFFER_MINUS_CBUFFER requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcbufferMinusCbufferLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCBUFFER_MINUS_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TCBUFFER_MINUS_GEOM */
        case AntlrSQLLexer::TCBUFFER_MINUS_GEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCBUFFER_MINUS_GEOM requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcbufferMinusGeomLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCBUFFER_MINUS_GEOM */

        /* BEGIN CODEGEN PARSER GLUE: TGEO_AT_VALUE */
        case AntlrSQLLexer::TGEO_AT_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TGEO_AT_VALUE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeoAtValueLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGEO_AT_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TGEO_MINUS_VALUE */
        case AntlrSQLLexer::TGEO_MINUS_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TGEO_MINUS_VALUE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeoMinusValueLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGEO_MINUS_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TNPOINT_AT_NPOINT */
        case AntlrSQLLexer::TNPOINT_AT_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TNPOINT_AT_NPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnpointAtNpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNPOINT_AT_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TNPOINT_MINUS_NPOINT */
        case AntlrSQLLexer::TNPOINT_MINUS_NPOINT:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TNPOINT_MINUS_NPOINT requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnpointMinusNpointLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNPOINT_MINUS_NPOINT */

        /* BEGIN CODEGEN PARSER GLUE: TPOINT_AT_GEOM */
        case AntlrSQLLexer::TPOINT_AT_GEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TPOINT_AT_GEOM requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TpointAtGeomLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOINT_AT_GEOM */

        /* BEGIN CODEGEN PARSER GLUE: TPOINT_AT_VALUE */
        case AntlrSQLLexer::TPOINT_AT_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TPOINT_AT_VALUE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TpointAtValueLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOINT_AT_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TPOINT_MINUS_GEOM */
        case AntlrSQLLexer::TPOINT_MINUS_GEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TPOINT_MINUS_GEOM requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TpointMinusGeomLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOINT_MINUS_GEOM */

        /* BEGIN CODEGEN PARSER GLUE: TPOINT_MINUS_VALUE */
        case AntlrSQLLexer::TPOINT_MINUS_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TPOINT_MINUS_VALUE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TpointMinusValueLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOINT_MINUS_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TPOSE_AT_GEOM */
        case AntlrSQLLexer::TPOSE_AT_GEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TPOSE_AT_GEOM requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TposeAtGeomLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOSE_AT_GEOM */

        /* BEGIN CODEGEN PARSER GLUE: TPOSE_AT_POSE */
        case AntlrSQLLexer::TPOSE_AT_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TPOSE_AT_POSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TposeAtPoseLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOSE_AT_POSE */

        /* BEGIN CODEGEN PARSER GLUE: TPOSE_MINUS_GEOM */
        case AntlrSQLLexer::TPOSE_MINUS_GEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TPOSE_MINUS_GEOM requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TposeMinusGeomLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOSE_MINUS_GEOM */

        /* BEGIN CODEGEN PARSER GLUE: TPOSE_MINUS_POSE */
        case AntlrSQLLexer::TPOSE_MINUS_POSE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TPOSE_MINUS_POSE requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TposeMinusPoseLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOSE_MINUS_POSE */
        /* BEGIN CODEGEN PARSER GLUE: TCBUFFER_AT_STBOX */
        case AntlrSQLLexer::TCBUFFER_AT_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCBUFFER_AT_STBOX requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcbufferAtStboxLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCBUFFER_AT_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: TCBUFFER_MINUS_STBOX */
        case AntlrSQLLexer::TCBUFFER_MINUS_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TCBUFFER_MINUS_STBOX requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TcbufferMinusStboxLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TCBUFFER_MINUS_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: TGEO_MINUS_STBOX */
        case AntlrSQLLexer::TGEO_MINUS_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TGEO_MINUS_STBOX requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeoMinusStboxLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGEO_MINUS_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: TPOSE_AT_STBOX */
        case AntlrSQLLexer::TPOSE_AT_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TPOSE_AT_STBOX requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TposeAtStboxLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOSE_AT_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: TPOSE_MINUS_STBOX */
        case AntlrSQLLexer::TPOSE_MINUS_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TPOSE_MINUS_STBOX requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TposeMinusStboxLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOSE_MINUS_STBOX */
        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_CBUFFER_GEO */
        case AntlrSQLLexer::DISTANCE_CBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_CBUFFER_GEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceCbufferGeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_CBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_CBUFFER_STBOX */
        case AntlrSQLLexer::DISTANCE_CBUFFER_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_CBUFFER_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistanceCbufferStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_CBUFFER_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_POSE_GEO */
        case AntlrSQLLexer::DISTANCE_POSE_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_POSE_GEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistancePoseGeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_POSE_GEO */

        /* BEGIN CODEGEN PARSER GLUE: DISTANCE_POSE_STBOX */
        case AntlrSQLLexer::DISTANCE_POSE_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("DISTANCE_POSE_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DistancePoseStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: DISTANCE_POSE_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_CBUFFER_STBOX */
        case AntlrSQLLexer::NAD_CBUFFER_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NAD_CBUFFER_STBOX requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadCbufferStboxLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_CBUFFER_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: NAD_STBOX_GEO */
        case AntlrSQLLexer::NAD_STBOX_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NAD_STBOX_GEO requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NadStboxGeoLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NAD_STBOX_GEO */
        /* BEGIN CODEGEN PARSER GLUE: INTSPAN_MAKE */
        case AntlrSQLLexer::INTSPAN_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("INTSPAN_MAKE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspanMakeLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPAN_MAKE */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSPAN_MAKE */
        case AntlrSQLLexer::BIGINTSPAN_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("BIGINTSPAN_MAKE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintspanMakeLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSPAN_MAKE */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPAN_MAKE */
        case AntlrSQLLexer::FLOATSPAN_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("FLOATSPAN_MAKE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanMakeLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPAN_MAKE */

        /* BEGIN CODEGEN PARSER GLUE: DATESPAN_MAKE */
        case AntlrSQLLexer::DATESPAN_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("DATESPAN_MAKE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespanMakeLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPAN_MAKE */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPAN_MAKE */
        case AntlrSQLLexer::TSTZSPAN_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TSTZSPAN_MAKE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspanMakeLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPAN_MAKE */
        /* BEGIN CODEGEN PARSER GLUE: TFLOATINST_MAKE */
        case AntlrSQLLexer::TFLOATINST_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOATINST_MAKE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatinstMakeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOATINST_MAKE */

        /* BEGIN CODEGEN PARSER GLUE: TINTINST_MAKE */
        case AntlrSQLLexer::TINTINST_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TINTINST_MAKE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintinstMakeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINTINST_MAKE */

        /* BEGIN CODEGEN PARSER GLUE: TPOINTINST_MAKE */
        case AntlrSQLLexer::TPOINTINST_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TPOINTINST_MAKE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TpointinstMakeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOINTINST_MAKE */

        /* BEGIN CODEGEN PARSER GLUE: TGEOINST_MAKE */
        case AntlrSQLLexer::TGEOINST_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TGEOINST_MAKE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeoinstMakeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGEOINST_MAKE */

        /* BEGIN CODEGEN PARSER GLUE: TNPOINTINST_MAKE */
        case AntlrSQLLexer::TNPOINTINST_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TNPOINTINST_MAKE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnpointinstMakeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNPOINTINST_MAKE */

        /* BEGIN CODEGEN PARSER GLUE: NPOINT_MAKE */
        case AntlrSQLLexer::NPOINT_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("NPOINT_MAKE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointMakeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINT_MAKE */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFER_MAKE */
        case AntlrSQLLexer::CBUFFER_MAKE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("CBUFFER_MAKE requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbufferMakeLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFER_MAKE */

        /* BEGIN CODEGEN PARSER GLUE: POSE_MAKE_2D */
        case AntlrSQLLexer::POSE_MAKE_2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("POSE_MAKE_2D requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseMake2dLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_MAKE_2D */

        /* BEGIN CODEGEN PARSER GLUE: POSE_MAKE_POINT2D */
        case AntlrSQLLexer::POSE_MAKE_POINT2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("POSE_MAKE_POINT2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PoseMakePoint2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSE_MAKE_POINT2D */
        /* BEGIN CODEGEN PARSER GLUE: GEO_EQUALS */
        case AntlrSQLLexer::GEO_EQUALS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEO_EQUALS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoEqualsLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_EQUALS */

        /* BEGIN CODEGEN PARSER GLUE: GEO_GEO_N */
        case AntlrSQLLexer::GEO_GEO_N:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEO_GEO_N requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoGeoNLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_GEO_N */

        /* BEGIN CODEGEN PARSER GLUE: GEO_SAME */
        case AntlrSQLLexer::GEO_SAME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEO_SAME requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoSameLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_SAME */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_CONTAINS */
        case AntlrSQLLexer::GEOM_CONTAINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_CONTAINS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomContainsLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_CONTAINS */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_COVERS */
        case AntlrSQLLexer::GEOM_COVERS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_COVERS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomCoversLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_COVERS */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_DIFFERENCE2D */
        case AntlrSQLLexer::GEOM_DIFFERENCE2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_DIFFERENCE2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomDifference2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_DIFFERENCE2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_DISJOINT2D */
        case AntlrSQLLexer::GEOM_DISJOINT2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_DISJOINT2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomDisjoint2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_DISJOINT2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_DISTANCE2D */
        case AntlrSQLLexer::GEOM_DISTANCE2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_DISTANCE2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomDistance2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_DISTANCE2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_DISTANCE3D */
        case AntlrSQLLexer::GEOM_DISTANCE3D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_DISTANCE3D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomDistance3dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_DISTANCE3D */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_INTERSECTION2D */
        case AntlrSQLLexer::GEOM_INTERSECTION2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_INTERSECTION2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomIntersection2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_INTERSECTION2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_INTERSECTION2D_COLL */
        case AntlrSQLLexer::GEOM_INTERSECTION2D_COLL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_INTERSECTION2D_COLL requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomIntersection2dCollLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_INTERSECTION2D_COLL */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_INTERSECTS2D */
        case AntlrSQLLexer::GEOM_INTERSECTS2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_INTERSECTS2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomIntersects2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_INTERSECTS2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_INTERSECTS3D */
        case AntlrSQLLexer::GEOM_INTERSECTS3D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_INTERSECTS3D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomIntersects3dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_INTERSECTS3D */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_SHORTESTLINE2D */
        case AntlrSQLLexer::GEOM_SHORTESTLINE2D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_SHORTESTLINE2D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomShortestline2dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_SHORTESTLINE2D */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_SHORTESTLINE3D */
        case AntlrSQLLexer::GEOM_SHORTESTLINE3D:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_SHORTESTLINE3D requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomShortestline3dLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_SHORTESTLINE3D */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_TO_CBUFFER */
        case AntlrSQLLexer::GEOM_TO_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOM_TO_CBUFFER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomToCbufferLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_TO_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_TOUCHES */
        case AntlrSQLLexer::GEOM_TOUCHES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_TOUCHES requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomTouchesLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_TOUCHES */

        /* BEGIN CODEGEN PARSER GLUE: GEOM_UNARY_UNION */
        case AntlrSQLLexer::GEOM_UNARY_UNION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEOM_UNARY_UNION requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomUnaryUnionLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_UNARY_UNION */
        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_VALUES */
        case AntlrSQLLexer::TFLOAT_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TFLOAT_VALUES requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatValuesLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: TINT_VALUES */
        case AntlrSQLLexer::TINT_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TINT_VALUES requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintValuesLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: TBOOL_VALUES */
        case AntlrSQLLexer::TBOOL_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TBOOL_VALUES requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboolValuesLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOOL_VALUES */
        /* BEGIN CODEGEN PARSER GLUE: TGEO_VALUES */
        case AntlrSQLLexer::TGEO_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TGEO_VALUES requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeoValuesLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGEO_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: TPOSE_VALUES */
        case AntlrSQLLexer::TPOSE_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TPOSE_VALUES requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TposeValuesLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TPOSE_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: TTEXT_VALUES */
        case AntlrSQLLexer::TTEXT_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TTEXT_VALUES requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TtextValuesLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TTEXT_VALUES */
        /* BEGIN CODEGEN PARSER GLUE: INTSET_VALUES */
        case AntlrSQLLexer::INTSET_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSET_VALUES requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntsetValuesLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSET_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: DATESET_VALUES */
        case AntlrSQLLexer::DATESET_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESET_VALUES requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatesetValuesLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESET_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: GEOSET_VALUES */
        case AntlrSQLLexer::GEOSET_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOSET_VALUES requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeosetValuesLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOSET_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: CBUFFERSET_VALUES */
        case AntlrSQLLexer::CBUFFERSET_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("CBUFFERSET_VALUES requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(CbuffersetValuesLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: CBUFFERSET_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: NPOINTSET_VALUES */
        case AntlrSQLLexer::NPOINTSET_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("NPOINTSET_VALUES requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(NpointsetValuesLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: NPOINTSET_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: POSESET_VALUES */
        case AntlrSQLLexer::POSESET_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("POSESET_VALUES requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(PosesetValuesLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: POSESET_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: TEXTSET_VALUES */
        case AntlrSQLLexer::TEXTSET_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TEXTSET_VALUES requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextsetValuesLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTSET_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_TIMESTAMPS */
        case AntlrSQLLexer::TEMPORAL_TIMESTAMPS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TEMPORAL_TIMESTAMPS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalTimestampsLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_TIMESTAMPS */
        /* BEGIN CODEGEN PARSER GLUE: FLOATSET_VALUES */
        case AntlrSQLLexer::FLOATSET_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSET_VALUES requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatsetValuesLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSET_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSET_VALUES */
        case AntlrSQLLexer::BIGINTSET_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("BIGINTSET_VALUES requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintsetValuesLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSET_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSET_VALUES */
        case AntlrSQLLexer::TSTZSET_VALUES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSET_VALUES requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzsetValuesLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSET_VALUES */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_SPANS */
        case AntlrSQLLexer::TEMPORAL_SPANS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TEMPORAL_SPANS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalSpansLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_SPANS */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_INSTANTS */
        case AntlrSQLLexer::TEMPORAL_INSTANTS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TEMPORAL_INSTANTS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalInstantsLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_INSTANTS */
        /* BEGIN CODEGEN PARSER GLUE: TDWITHIN_TCBUFFER_CBUFFER */
        case AntlrSQLLexer::TDWITHIN_TCBUFFER_CBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TDWITHIN_TCBUFFER_CBUFFER requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdwithinTcbufferCbufferLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDWITHIN_TCBUFFER_CBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TDWITHIN_TCBUFFER_GEO */
        case AntlrSQLLexer::TDWITHIN_TCBUFFER_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 6)
                throw InvalidQuerySyntax("TDWITHIN_TCBUFFER_GEO requires exactly 6 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdwithinTcbufferGeoLogicalFunction(a0, a1, a2, a3, a4, a5));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDWITHIN_TCBUFFER_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TDWITHIN_TCBUFFER_TCBUFFER */
        case AntlrSQLLexer::TDWITHIN_TCBUFFER_TCBUFFER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 9)
                throw InvalidQuerySyntax("TDWITHIN_TCBUFFER_TCBUFFER requires exactly 9 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a8 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdwithinTcbufferTcbufferLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7, a8));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDWITHIN_TCBUFFER_TCBUFFER */

        /* BEGIN CODEGEN PARSER GLUE: TDWITHIN_TGEO_GEO */
        case AntlrSQLLexer::TDWITHIN_TGEO_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TDWITHIN_TGEO_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdwithinTgeoGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDWITHIN_TGEO_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TDWITHIN_TGEO_TGEO */
        case AntlrSQLLexer::TDWITHIN_TGEO_TGEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("TDWITHIN_TGEO_TGEO requires exactly 7 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdwithinTgeoTgeoLogicalFunction(a0, a1, a2, a3, a4, a5, a6));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDWITHIN_TGEO_TGEO */
        /* BEGIN CODEGEN PARSER GLUE: INT_GET_BIN */
        case AntlrSQLLexer::INT_GET_BIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("INT_GET_BIN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntGetBinLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: INT_GET_BIN */

        /* BEGIN CODEGEN PARSER GLUE: BIGINT_GET_BIN */
        case AntlrSQLLexer::BIGINT_GET_BIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("BIGINT_GET_BIN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintGetBinLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINT_GET_BIN */

        /* BEGIN CODEGEN PARSER GLUE: FLOAT_GET_BIN */
        case AntlrSQLLexer::FLOAT_GET_BIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("FLOAT_GET_BIN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatGetBinLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOAT_GET_BIN */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_SHIFT_SCALE_TIME */
        case AntlrSQLLexer::STBOX_SHIFT_SCALE_TIME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("STBOX_SHIFT_SCALE_TIME requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxShiftScaleTimeLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_SHIFT_SCALE_TIME */

        /* BEGIN CODEGEN PARSER GLUE: TBOX_SHIFT_SCALE_TIME */
        case AntlrSQLLexer::TBOX_SHIFT_SCALE_TIME:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TBOX_SHIFT_SCALE_TIME requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TboxShiftScaleTimeLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TBOX_SHIFT_SCALE_TIME */
        /* BEGIN CODEGEN PARSER GLUE: TIMESTAMPTZ_GET_BIN */
        case AntlrSQLLexer::TIMESTAMPTZ_GET_BIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TIMESTAMPTZ_GET_BIN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TimestamptzGetBinLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TIMESTAMPTZ_GET_BIN */

        /* BEGIN CODEGEN PARSER GLUE: DATE_GET_BIN */
        case AntlrSQLLexer::DATE_GET_BIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DATE_GET_BIN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DateGetBinLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATE_GET_BIN */

        /* BEGIN CODEGEN PARSER GLUE: TIMESTAMPTZ_TPRECISION */
        case AntlrSQLLexer::TIMESTAMPTZ_TPRECISION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TIMESTAMPTZ_TPRECISION requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TimestamptzTprecisionLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TIMESTAMPTZ_TPRECISION */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPAN_TPRECISION */
        case AntlrSQLLexer::TSTZSPAN_TPRECISION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TSTZSPAN_TPRECISION requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspanTprecisionLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPAN_TPRECISION */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSET_TPRECISION */
        case AntlrSQLLexer::TSTZSET_TPRECISION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TSTZSET_TPRECISION requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzsetTprecisionLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSET_TPRECISION */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPANSET_TPRECISION */
        case AntlrSQLLexer::TSTZSPANSET_TPRECISION:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TSTZSPANSET_TPRECISION requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspansetTprecisionLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPANSET_TPRECISION */
        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_AFTER_TIMESTAMPTZ */
        case AntlrSQLLexer::TEMPORAL_AFTER_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_AFTER_TIMESTAMPTZ requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalAfterTimestamptzLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_AFTER_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_BEFORE_TIMESTAMPTZ */
        case AntlrSQLLexer::TEMPORAL_BEFORE_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_BEFORE_TIMESTAMPTZ requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalBeforeTimestamptzLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_BEFORE_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_DELETE_TIMESTAMPTZ */
        case AntlrSQLLexer::TEMPORAL_DELETE_TIMESTAMPTZ:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_DELETE_TIMESTAMPTZ requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalDeleteTimestamptzLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_DELETE_TIMESTAMPTZ */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_DELETE_TSTZSET */
        case AntlrSQLLexer::TEMPORAL_DELETE_TSTZSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_DELETE_TSTZSET requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalDeleteTstzsetLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_DELETE_TSTZSET */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_DELETE_TSTZSPAN */
        case AntlrSQLLexer::TEMPORAL_DELETE_TSTZSPAN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_DELETE_TSTZSPAN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalDeleteTstzspanLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_DELETE_TSTZSPAN */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_DELETE_TSTZSPANSET */
        case AntlrSQLLexer::TEMPORAL_DELETE_TSTZSPANSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TEMPORAL_DELETE_TSTZSPANSET requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalDeleteTstzspansetLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_DELETE_TSTZSPANSET */
        /* BEGIN CODEGEN PARSER GLUE: SET_SPLIT_N_SPANS */
        case AntlrSQLLexer::SET_SPLIT_N_SPANS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SET_SPLIT_N_SPANS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetSplitNSpansLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_SPLIT_N_SPANS */

        /* BEGIN CODEGEN PARSER GLUE: SET_SPLIT_EACH_N_SPANS */
        case AntlrSQLLexer::SET_SPLIT_EACH_N_SPANS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SET_SPLIT_EACH_N_SPANS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SetSplitEachNSpansLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SET_SPLIT_EACH_N_SPANS */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_SPLIT_N_SPANS */
        case AntlrSQLLexer::SPANSET_SPLIT_N_SPANS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPANSET_SPLIT_N_SPANS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetSplitNSpansLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_SPLIT_N_SPANS */

        /* BEGIN CODEGEN PARSER GLUE: SPANSET_SPLIT_EACH_N_SPANS */
        case AntlrSQLLexer::SPANSET_SPLIT_EACH_N_SPANS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("SPANSET_SPLIT_EACH_N_SPANS requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(SpansetSplitEachNSpansLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: SPANSET_SPLIT_EACH_N_SPANS */

        /* BEGIN CODEGEN PARSER GLUE: TGEO_SPLIT_N_STBOXES */
        case AntlrSQLLexer::TGEO_SPLIT_N_STBOXES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TGEO_SPLIT_N_STBOXES requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeoSplitNStboxesLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGEO_SPLIT_N_STBOXES */

        /* BEGIN CODEGEN PARSER GLUE: TGEO_SPLIT_EACH_N_STBOXES */
        case AntlrSQLLexer::TGEO_SPLIT_EACH_N_STBOXES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TGEO_SPLIT_EACH_N_STBOXES requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeoSplitEachNStboxesLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGEO_SPLIT_EACH_N_STBOXES */

        /* BEGIN CODEGEN PARSER GLUE: TNUMBER_SPLIT_N_TBOXES */
        case AntlrSQLLexer::TNUMBER_SPLIT_N_TBOXES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNUMBER_SPLIT_N_TBOXES requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnumberSplitNTboxesLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNUMBER_SPLIT_N_TBOXES */

        /* BEGIN CODEGEN PARSER GLUE: TNUMBER_SPLIT_EACH_N_TBOXES */
        case AntlrSQLLexer::TNUMBER_SPLIT_EACH_N_TBOXES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("TNUMBER_SPLIT_EACH_N_TBOXES requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TnumberSplitEachNTboxesLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNUMBER_SPLIT_EACH_N_TBOXES */

        /* BEGIN CODEGEN PARSER GLUE: GEO_SPLIT_N_STBOXES */
        case AntlrSQLLexer::GEO_SPLIT_N_STBOXES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEO_SPLIT_N_STBOXES requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoSplitNStboxesLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_SPLIT_N_STBOXES */

        /* BEGIN CODEGEN PARSER GLUE: GEO_SPLIT_EACH_N_STBOXES */
        case AntlrSQLLexer::GEO_SPLIT_EACH_N_STBOXES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("GEO_SPLIT_EACH_N_STBOXES requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeoSplitEachNStboxesLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEO_SPLIT_EACH_N_STBOXES */
        /* BEGIN CODEGEN PARSER GLUE: INTSPAN_BINS */
        case AntlrSQLLexer::INTSPAN_BINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("INTSPAN_BINS requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspanBinsLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPAN_BINS */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPAN_BINS */
        case AntlrSQLLexer::FLOATSPAN_BINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("FLOATSPAN_BINS requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspanBinsLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPAN_BINS */

        /* BEGIN CODEGEN PARSER GLUE: INTSPANSET_BINS */
        case AntlrSQLLexer::INTSPANSET_BINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("INTSPANSET_BINS requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntspansetBinsLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSPANSET_BINS */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSPANSET_BINS */
        case AntlrSQLLexer::FLOATSPANSET_BINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("FLOATSPANSET_BINS requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatspansetBinsLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSPANSET_BINS */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_VALUE_BINS */
        case AntlrSQLLexer::TFLOAT_VALUE_BINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TFLOAT_VALUE_BINS requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatValueBinsLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_VALUE_BINS */

        /* BEGIN CODEGEN PARSER GLUE: TINT_VALUE_BINS */
        case AntlrSQLLexer::TINT_VALUE_BINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TINT_VALUE_BINS requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintValueBinsLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_VALUE_BINS */
        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_VALUE_BOXES */
        case AntlrSQLLexer::TFLOAT_VALUE_BOXES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TFLOAT_VALUE_BOXES requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatValueBoxesLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_VALUE_BOXES */

        /* BEGIN CODEGEN PARSER GLUE: TINT_VALUE_BOXES */
        case AntlrSQLLexer::TINT_VALUE_BOXES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TINT_VALUE_BOXES requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintValueBoxesLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_VALUE_BOXES */

        /* BEGIN CODEGEN PARSER GLUE: TFLOAT_TIME_BOXES */
        case AntlrSQLLexer::TFLOAT_TIME_BOXES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TFLOAT_TIME_BOXES requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TfloatTimeBoxesLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TFLOAT_TIME_BOXES */

        /* BEGIN CODEGEN PARSER GLUE: TINT_TIME_BOXES */
        case AntlrSQLLexer::TINT_TIME_BOXES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TINT_TIME_BOXES requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TintTimeBoxesLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TINT_TIME_BOXES */

        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_TIME_BINS */
        case AntlrSQLLexer::TEMPORAL_TIME_BINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TEMPORAL_TIME_BINS requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalTimeBinsLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_TIME_BINS */

        /* BEGIN CODEGEN PARSER GLUE: DATESPAN_BINS */
        case AntlrSQLLexer::DATESPAN_BINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("DATESPAN_BINS requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatespanBinsLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESPAN_BINS */
        /* BEGIN CODEGEN PARSER GLUE: BIGINTSPAN_BINS */
        case AntlrSQLLexer::BIGINTSPAN_BINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("BIGINTSPAN_BINS requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintspanBinsLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSPAN_BINS */

        /* BEGIN CODEGEN PARSER GLUE: BIGINTSPANSET_BINS */
        case AntlrSQLLexer::BIGINTSPANSET_BINS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("BIGINTSPANSET_BINS requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BigintspansetBinsLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: BIGINTSPANSET_BINS */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_SPACE_TILES */
        case AntlrSQLLexer::STBOX_SPACE_TILES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("STBOX_SPACE_TILES requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxSpaceTilesLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_SPACE_TILES */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_TIME_TILES */
        case AntlrSQLLexer::STBOX_TIME_TILES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("STBOX_TIME_TILES requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxTimeTilesLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_TIME_TILES */

        /* BEGIN CODEGEN PARSER GLUE: STBOX_SPACE_TIME_TILES */
        case AntlrSQLLexer::STBOX_SPACE_TIME_TILES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("STBOX_SPACE_TIME_TILES requires exactly 7 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(StboxSpaceTimeTilesLogicalFunction(a0, a1, a2, a3, a4, a5, a6));
        }
        break;
        /* END CODEGEN PARSER GLUE: STBOX_SPACE_TIME_TILES */

        /* BEGIN CODEGEN PARSER GLUE: TGEO_SPACE_BOXES */
        case AntlrSQLLexer::TGEO_SPACE_BOXES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 7)
                throw InvalidQuerySyntax("TGEO_SPACE_BOXES requires exactly 7 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TgeoSpaceBoxesLogicalFunction(a0, a1, a2, a3, a4, a5, a6));
        }
        break;
        /* END CODEGEN PARSER GLUE: TGEO_SPACE_BOXES */
        /* BEGIN CODEGEN PARSER GLUE: TEMPORAL_SEQUENCES */
        case AntlrSQLLexer::TEMPORAL_SEQUENCES:
        {
            const auto argCount = context->expression().size();
            if (argCount != 2)
                throw InvalidQuerySyntax("TEMPORAL_SEQUENCES requires exactly 2 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TemporalSequencesLogicalFunction(a0, a1));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEMPORAL_SEQUENCES */
        /* BEGIN CODEGEN PARSER GLUE: GEOM_TO_GEOG */
        case AntlrSQLLexer::GEOM_TO_GEOG:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOM_TO_GEOG requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeomToGeogLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOM_TO_GEOG */

        /* BEGIN CODEGEN PARSER GLUE: GEOG_DWITHIN */
        case AntlrSQLLexer::GEOG_DWITHIN:
        {
            const auto argCount = context->expression().size();
            if (argCount != 3)
                throw InvalidQuerySyntax("GEOG_DWITHIN requires exactly 3 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeogDwithinLogicalFunction(a0, a1, a2));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOG_DWITHIN */
        /* BEGIN CODEGEN PARSER GLUE: BACK_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::BACK_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("BACK_TSPATIAL_TSPATIAL requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BackTspatialTspatialLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: BACK_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: FRONT_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::FRONT_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("FRONT_TSPATIAL_TSPATIAL requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FrontTspatialTspatialLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: FRONT_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBACK_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::OVERBACK_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("OVERBACK_TSPATIAL_TSPATIAL requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbackTspatialTspatialLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBACK_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERFRONT_TSPATIAL_TSPATIAL */
        case AntlrSQLLexer::OVERFRONT_TSPATIAL_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 8)
                throw InvalidQuerySyntax("OVERFRONT_TSPATIAL_TSPATIAL requires exactly 8 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a7 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a6 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a5 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverfrontTspatialTspatialLogicalFunction(a0, a1, a2, a3, a4, a5, a6, a7));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERFRONT_TSPATIAL_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: BACK_TSPATIAL_STBOX */
        case AntlrSQLLexer::BACK_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("BACK_TSPATIAL_STBOX requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BackTspatialStboxLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: BACK_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: FRONT_TSPATIAL_STBOX */
        case AntlrSQLLexer::FRONT_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("FRONT_TSPATIAL_STBOX requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FrontTspatialStboxLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: FRONT_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERBACK_TSPATIAL_STBOX */
        case AntlrSQLLexer::OVERBACK_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("OVERBACK_TSPATIAL_STBOX requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbackTspatialStboxLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBACK_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: OVERFRONT_TSPATIAL_STBOX */
        case AntlrSQLLexer::OVERFRONT_TSPATIAL_STBOX:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("OVERFRONT_TSPATIAL_STBOX requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverfrontTspatialStboxLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERFRONT_TSPATIAL_STBOX */

        /* BEGIN CODEGEN PARSER GLUE: BACK_STBOX_TSPATIAL */
        case AntlrSQLLexer::BACK_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("BACK_STBOX_TSPATIAL requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(BackStboxTspatialLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: BACK_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: FRONT_STBOX_TSPATIAL */
        case AntlrSQLLexer::FRONT_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("FRONT_STBOX_TSPATIAL requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FrontStboxTspatialLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: FRONT_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERBACK_STBOX_TSPATIAL */
        case AntlrSQLLexer::OVERBACK_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("OVERBACK_STBOX_TSPATIAL requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverbackStboxTspatialLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERBACK_STBOX_TSPATIAL */

        /* BEGIN CODEGEN PARSER GLUE: OVERFRONT_STBOX_TSPATIAL */
        case AntlrSQLLexer::OVERFRONT_STBOX_TSPATIAL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("OVERFRONT_STBOX_TSPATIAL requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(OverfrontStboxTspatialLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: OVERFRONT_STBOX_TSPATIAL */
        /* BEGIN CODEGEN PARSER GLUE: FLOATSET_CEIL */
        case AntlrSQLLexer::FLOATSET_CEIL:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSET_CEIL requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatsetCeilLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSET_CEIL */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSET_FLOOR */
        case AntlrSQLLexer::FLOATSET_FLOOR:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSET_FLOOR requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatsetFloorLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSET_FLOOR */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSET_RADIANS */
        case AntlrSQLLexer::FLOATSET_RADIANS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSET_RADIANS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatsetRadiansLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSET_RADIANS */


        /* BEGIN CODEGEN PARSER GLUE: TEXTSET_LOWER */
        case AntlrSQLLexer::TEXTSET_LOWER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TEXTSET_LOWER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextsetLowerLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTSET_LOWER */

        /* BEGIN CODEGEN PARSER GLUE: TEXTSET_UPPER */
        case AntlrSQLLexer::TEXTSET_UPPER:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TEXTSET_UPPER requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextsetUpperLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTSET_UPPER */

        /* BEGIN CODEGEN PARSER GLUE: TEXTSET_INITCAP */
        case AntlrSQLLexer::TEXTSET_INITCAP:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TEXTSET_INITCAP requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TextsetInitcapLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEXTSET_INITCAP */

        /* BEGIN CODEGEN PARSER GLUE: INTSET_TO_FLOATSET */
        case AntlrSQLLexer::INTSET_TO_FLOATSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("INTSET_TO_FLOATSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(IntsetToFloatsetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: INTSET_TO_FLOATSET */

        /* BEGIN CODEGEN PARSER GLUE: FLOATSET_TO_INTSET */
        case AntlrSQLLexer::FLOATSET_TO_INTSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("FLOATSET_TO_INTSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(FloatsetToIntsetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: FLOATSET_TO_INTSET */

        /* BEGIN CODEGEN PARSER GLUE: DATESET_TO_TSTZSET */
        case AntlrSQLLexer::DATESET_TO_TSTZSET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("DATESET_TO_TSTZSET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(DatesetToTstzsetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: DATESET_TO_TSTZSET */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSET_TO_DATESET */
        case AntlrSQLLexer::TSTZSET_TO_DATESET:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSET_TO_DATESET requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzsetToDatesetLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSET_TO_DATESET */

        /* BEGIN CODEGEN PARSER GLUE: TSTZSPANSET_TIMESTAMPS */
        case AntlrSQLLexer::TSTZSPANSET_TIMESTAMPS:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("TSTZSPANSET_TIMESTAMPS requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TstzspansetTimestampsLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: TSTZSPANSET_TIMESTAMPS */

        /* BEGIN CODEGEN PARSER GLUE: GEOG_TO_GEOM */
        case AntlrSQLLexer::GEOG_TO_GEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 1)
                throw InvalidQuerySyntax("GEOG_TO_GEOM requires exactly 1 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(GeogToGeomLogicalFunction(a0));
        }
        break;
        /* END CODEGEN PARSER GLUE: GEOG_TO_GEOM */
        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_GEO_TRGEOMETRY */
        case AntlrSQLLexer::ALWAYS_EQ_GEO_TRGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYS_EQ_GEO_TRGEOMETRY requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqGeoTrgeometryLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_GEO_TRGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_EQ_TRGEOMETRY_GEO */
        case AntlrSQLLexer::ALWAYS_EQ_TRGEOMETRY_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYS_EQ_TRGEOMETRY_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysEqTrgeometryGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_EQ_TRGEOMETRY_GEO */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_GEO_TRGEOMETRY */
        case AntlrSQLLexer::ALWAYS_NE_GEO_TRGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYS_NE_GEO_TRGEOMETRY requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeGeoTrgeometryLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_GEO_TRGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: ALWAYS_NE_TRGEOMETRY_GEO */
        case AntlrSQLLexer::ALWAYS_NE_TRGEOMETRY_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("ALWAYS_NE_TRGEOMETRY_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(AlwaysNeTrgeometryGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: ALWAYS_NE_TRGEOMETRY_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_GEO_TRGEOMETRY */
        case AntlrSQLLexer::EVER_EQ_GEO_TRGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVER_EQ_GEO_TRGEOMETRY requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqGeoTrgeometryLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_GEO_TRGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: EVER_EQ_TRGEOMETRY_GEO */
        case AntlrSQLLexer::EVER_EQ_TRGEOMETRY_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVER_EQ_TRGEOMETRY_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverEqTrgeometryGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_EQ_TRGEOMETRY_GEO */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_GEO_TRGEOMETRY */
        case AntlrSQLLexer::EVER_NE_GEO_TRGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVER_NE_GEO_TRGEOMETRY requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeGeoTrgeometryLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_GEO_TRGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: EVER_NE_TRGEOMETRY_GEO */
        case AntlrSQLLexer::EVER_NE_TRGEOMETRY_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("EVER_NE_TRGEOMETRY_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(EverNeTrgeometryGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: EVER_NE_TRGEOMETRY_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TDISTANCE_TRGEOMETRY_GEO */
        case AntlrSQLLexer::TDISTANCE_TRGEOMETRY_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TDISTANCE_TRGEOMETRY_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TdistanceTrgeometryGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TDISTANCE_TRGEOMETRY_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_GEO_TRGEOMETRY */
        case AntlrSQLLexer::TEQ_GEO_TRGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEQ_GEO_TRGEOMETRY requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqGeoTrgeometryLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_GEO_TRGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TEQ_TRGEOMETRY_GEO */
        case AntlrSQLLexer::TEQ_TRGEOMETRY_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TEQ_TRGEOMETRY_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TeqTrgeometryGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TEQ_TRGEOMETRY_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TNE_GEO_TRGEOMETRY */
        case AntlrSQLLexer::TNE_GEO_TRGEOMETRY:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TNE_GEO_TRGEOMETRY requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneGeoTrgeometryLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_GEO_TRGEOMETRY */

        /* BEGIN CODEGEN PARSER GLUE: TNE_TRGEOMETRY_GEO */
        case AntlrSQLLexer::TNE_TRGEOMETRY_GEO:
        {
            const auto argCount = context->expression().size();
            if (argCount != 5)
                throw InvalidQuerySyntax("TNE_TRGEOMETRY_GEO requires exactly 5 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a4 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TneTrgeometryGeoLogicalFunction(a0, a1, a2, a3, a4));
        }
        break;
        /* END CODEGEN PARSER GLUE: TNE_TRGEOMETRY_GEO */

        /* BEGIN CODEGEN PARSER GLUE: TRGEOMETRY_END_VALUE */
        case AntlrSQLLexer::TRGEOMETRY_END_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TRGEOMETRY_END_VALUE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TrgeometryEndValueLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TRGEOMETRY_END_VALUE */

        /* BEGIN CODEGEN PARSER GLUE: TRGEOMETRY_GEOM */
        case AntlrSQLLexer::TRGEOMETRY_GEOM:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TRGEOMETRY_GEOM requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TrgeometryGeomLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TRGEOMETRY_GEOM */

        /* BEGIN CODEGEN PARSER GLUE: TRGEOMETRY_START_VALUE */
        case AntlrSQLLexer::TRGEOMETRY_START_VALUE:
        {
            const auto argCount = context->expression().size();
            if (argCount != 4)
                throw InvalidQuerySyntax("TRGEOMETRY_START_VALUE requires exactly 4 arguments, but got {}", argCount);

            while (!helpers.top().constantBuilder.empty())
            {
                auto constantValue = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                DataType dataType;
                char* endPtr = nullptr;
                std::strtod(constantValue.c_str(), &endPtr);
                if (endPtr != nullptr && *endPtr == '\0')
                    dataType = DataTypeProvider::provideDataType(DataType::Type::FLOAT64);
                else
                    dataType = DataTypeProvider::provideDataType(DataType::Type::VARSIZED);
                helpers.top().functionBuilder.emplace_back(ConstantValueLogicalFunction(dataType, std::move(constantValue)));
            }

            auto a3 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a2 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a1 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();
            auto a0 = helpers.top().functionBuilder.back(); helpers.top().functionBuilder.pop_back();

            helpers.top().functionBuilder.emplace_back(TrgeometryStartValueLogicalFunction(a0, a1, a2, a3));
        }
        break;
        /* END CODEGEN PARSER GLUE: TRGEOMETRY_START_VALUE */


























































































        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_NUM_INSTANTS (case-switch) */
        case AntlrSQLLexer::TEMPORAL_NUM_INSTANTS:
            // Per-(window, group) count of instants in the assembled tgeo trajectory.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TEMPORAL_NUM_INSTANTS requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_NUM_INSTANTS arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalNumInstantsAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_NUM_INSTANTS (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_NUM_SEQUENCES (case-switch) */
        case AntlrSQLLexer::TEMPORAL_NUM_SEQUENCES:
            // Per-(window, group) count of sub-sequences in the assembled tgeo trajectory.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TEMPORAL_NUM_SEQUENCES requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_NUM_SEQUENCES arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalNumSequencesAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_NUM_SEQUENCES (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_NUM_TIMESTAMPS (case-switch) */
        case AntlrSQLLexer::TEMPORAL_NUM_TIMESTAMPS:
            // Per-(window, group) count of distinct timestamps in the assembled tgeo trajectory.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TEMPORAL_NUM_TIMESTAMPS requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_NUM_TIMESTAMPS arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalNumTimestampsAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_NUM_TIMESTAMPS (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TFLOAT_START_VALUE (case-switch) */
        case AntlrSQLLexer::TFLOAT_START_VALUE:
            // Value at the first instant of the per-(window, group) tfloat sequence.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TFLOAT_START_VALUE requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TFLOAT_START_VALUE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TfloatStartValueAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TFLOAT_START_VALUE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TFLOAT_END_VALUE (case-switch) */
        case AntlrSQLLexer::TFLOAT_END_VALUE:
            // Value at the last instant of the per-(window, group) tfloat sequence.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TFLOAT_END_VALUE requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TFLOAT_END_VALUE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TfloatEndValueAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TFLOAT_END_VALUE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TFLOAT_MIN_VALUE (case-switch) */
        case AntlrSQLLexer::TFLOAT_MIN_VALUE:
            // Minimum value across instants of the per-(window, group) tfloat sequence.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TFLOAT_MIN_VALUE requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TFLOAT_MIN_VALUE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TfloatMinValueAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TFLOAT_MIN_VALUE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TFLOAT_MAX_VALUE (case-switch) */
        case AntlrSQLLexer::TFLOAT_MAX_VALUE:
            // Maximum value across instants of the per-(window, group) tfloat sequence.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TFLOAT_MAX_VALUE requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TFLOAT_MAX_VALUE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TfloatMaxValueAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TFLOAT_MAX_VALUE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_INTEGRAL (case-switch) */
        case AntlrSQLLexer::TNUMBER_INTEGRAL:
            // Time-weighted integral (area under the value-vs-time curve) of the per-(window, group) tfloat sequence.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TNUMBER_INTEGRAL requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TNUMBER_INTEGRAL arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TnumberIntegralAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TNUMBER_INTEGRAL (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TINT_START_VALUE (case-switch) */
        case AntlrSQLLexer::TINT_START_VALUE:
            // Value at the first instant of the per-(window, group) tint sequence.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TINT_START_VALUE requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TINT_START_VALUE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TintStartValueAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TINT_START_VALUE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TINT_END_VALUE (case-switch) */
        case AntlrSQLLexer::TINT_END_VALUE:
            // Value at the last instant of the per-(window, group) tint sequence.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TINT_END_VALUE requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TINT_END_VALUE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TintEndValueAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TINT_END_VALUE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TINT_MIN_VALUE (case-switch) */
        case AntlrSQLLexer::TINT_MIN_VALUE:
            // Minimum value across instants of the per-(window, group) tint sequence.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TINT_MIN_VALUE requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TINT_MIN_VALUE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TintMinValueAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TINT_MIN_VALUE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TINT_MAX_VALUE (case-switch) */
        case AntlrSQLLexer::TINT_MAX_VALUE:
            // Maximum value across instants of the per-(window, group) tint sequence.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TINT_MAX_VALUE requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TINT_MAX_VALUE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TintMaxValueAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TINT_MAX_VALUE (case-switch) */
        /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_AVG_VALUE (case-switch) */
        case AntlrSQLLexer::TNUMBER_AVG_VALUE:
            // Arithmetic mean of all instant values in the per-(window, group) tfloat sequence.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TNUMBER_AVG_VALUE requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TNUMBER_AVG_VALUE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TnumberAvgValueAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TNUMBER_AVG_VALUE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_TWAVG (case-switch) */
        case AntlrSQLLexer::TNUMBER_TWAVG:
            // Time-weighted average of values across the per-(window, group) tfloat sequence.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TNUMBER_TWAVG requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TNUMBER_TWAVG arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TnumberTwavgAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TNUMBER_TWAVG (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_START_TIMESTAMPTZ (case-switch) */
        case AntlrSQLLexer::TEMPORAL_START_TIMESTAMPTZ:
            // TimestampTz (MEOS μs-since-2000) of the first instant in the per-(window, group) tgeo trajectory.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TEMPORAL_START_TIMESTAMPTZ requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_START_TIMESTAMPTZ arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalStartTimestamptzAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_START_TIMESTAMPTZ (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_END_TIMESTAMPTZ (case-switch) */
        case AntlrSQLLexer::TEMPORAL_END_TIMESTAMPTZ:
            // TimestampTz (MEOS μs-since-2000) of the last instant in the per-(window, group) tgeo trajectory.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TEMPORAL_END_TIMESTAMPTZ requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_END_TIMESTAMPTZ arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalEndTimestamptzAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_END_TIMESTAMPTZ (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_LOWER_INC (case-switch) */
        case AntlrSQLLexer::TEMPORAL_LOWER_INC:
            // True if the per-(window, group) tgeo trajectory's lower period bound is inclusive.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TEMPORAL_LOWER_INC requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_LOWER_INC arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalLowerIncAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_LOWER_INC (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_UPPER_INC (case-switch) */
        case AntlrSQLLexer::TEMPORAL_UPPER_INC:
            // True if the per-(window, group) tgeo trajectory's upper period bound is inclusive.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TEMPORAL_UPPER_INC requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_UPPER_INC arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalUpperIncAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_UPPER_INC (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_IS_SIMPLE (case-switch) */
        case AntlrSQLLexer::TPOINT_IS_SIMPLE:
            // True if the per-(window, group) tgeo trajectory does not self-intersect.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TPOINT_IS_SIMPLE requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TPOINT_IS_SIMPLE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TpointIsSimpleAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TPOINT_IS_SIMPLE (case-switch) */
        /* BEGIN CODEGEN AGGREGATION GLUE: TSPATIAL_EXTENT_TRANSFN (case-switch) */
        case AntlrSQLLexer::TSPATIAL_EXTENT_TRANSFN:
            // Windowed spatiotemporal extent (STBox) over a tgeo trajectory via tspatial_extent_transfn.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TSPATIAL_EXTENT_TRANSFN requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TSPATIAL_EXTENT_TRANSFN arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TspatialExtentTransfnAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TSPATIAL_EXTENT_TRANSFN (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_EXTENT_TRANSFN (case-switch) */
        case AntlrSQLLexer::TNUMBER_EXTENT_TRANSFN:
            // Windowed numeric-temporal extent (TBox) over a tnumber sequence via tnumber_extent_transfn.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TNUMBER_EXTENT_TRANSFN requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TNUMBER_EXTENT_TRANSFN arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TnumberExtentTransfnAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TNUMBER_EXTENT_TRANSFN (case-switch) */
        /* BEGIN CODEGEN AGGREGATION GLUE: FLOAT_EXTENT_TRANSFN (case-switch) */
        case AntlrSQLLexer::FLOAT_EXTENT_TRANSFN:
            // Windowed value extent (FLOATSPAN) over a tfloat stream via float_extent_transfn.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("FLOAT_EXTENT_TRANSFN requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("FLOAT_EXTENT_TRANSFN arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    FloatExtentTransfnAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: FLOAT_EXTENT_TRANSFN (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: INT_EXTENT_TRANSFN (case-switch) */
        case AntlrSQLLexer::INT_EXTENT_TRANSFN:
            // Windowed value extent (INTSPAN) over a tint stream via int_extent_transfn.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("INT_EXTENT_TRANSFN requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("INT_EXTENT_TRANSFN arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    IntExtentTransfnAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: INT_EXTENT_TRANSFN (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: BIGINT_EXTENT_TRANSFN (case-switch) */
        case AntlrSQLLexer::BIGINT_EXTENT_TRANSFN:
            // Windowed value extent (BIGINTSPAN) over a tbigint stream via bigint_extent_transfn.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("BIGINT_EXTENT_TRANSFN requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("BIGINT_EXTENT_TRANSFN arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    BigintExtentTransfnAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: BIGINT_EXTENT_TRANSFN (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TIMESTAMPTZ_EXTENT_TRANSFN (case-switch) */
        case AntlrSQLLexer::TIMESTAMPTZ_EXTENT_TRANSFN:
            // Windowed time extent (TSTZSPAN) over an event-time field via timestamptz_extent_transfn.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TIMESTAMPTZ_EXTENT_TRANSFN requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TIMESTAMPTZ_EXTENT_TRANSFN arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TimestamptzExtentTransfnAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TIMESTAMPTZ_EXTENT_TRANSFN (case-switch) */
        /* BEGIN CODEGEN AGGREGATION GLUE: FLOAT_UNION_TRANSFN (case-switch) */
        case AntlrSQLLexer::FLOAT_UNION_TRANSFN:
            // Windowed value union (FLOATSET) over a tfloat stream via float_union_transfn + set_union_finalfn.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("FLOAT_UNION_TRANSFN requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("FLOAT_UNION_TRANSFN arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    FloatUnionTransfnAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: FLOAT_UNION_TRANSFN (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: INT_UNION_TRANSFN (case-switch) */
        case AntlrSQLLexer::INT_UNION_TRANSFN:
            // Windowed value union (INTSET) over a tint stream via int_union_transfn + set_union_finalfn.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("INT_UNION_TRANSFN requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("INT_UNION_TRANSFN arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    IntUnionTransfnAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: INT_UNION_TRANSFN (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: BIGINT_UNION_TRANSFN (case-switch) */
        case AntlrSQLLexer::BIGINT_UNION_TRANSFN:
            // Windowed value union (BIGINTSET) over a tbigint stream via bigint_union_transfn + set_union_finalfn.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("BIGINT_UNION_TRANSFN requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("BIGINT_UNION_TRANSFN arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    BigintUnionTransfnAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: BIGINT_UNION_TRANSFN (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TIMESTAMPTZ_UNION_TRANSFN (case-switch) */
        case AntlrSQLLexer::TIMESTAMPTZ_UNION_TRANSFN:
            // Windowed time union (TSTZSET) over an event-time field via timestamptz_union_transfn + set_union_finalfn.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TIMESTAMPTZ_UNION_TRANSFN requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TIMESTAMPTZ_UNION_TRANSFN arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TimestamptzUnionTransfnAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TIMESTAMPTZ_UNION_TRANSFN (case-switch) */
        /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_TRAJECTORY (case-switch) */
        case AntlrSQLLexer::TPOINT_TRAJECTORY:
            // Windowed mini-trip trajectory materialized as hex-WKB — the value the MEOS function library composes over.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TPOINT_TRAJECTORY requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TPOINT_TRAJECTORY arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TpointTrajectoryAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TPOINT_TRAJECTORY (case-switch) */
        /* BEGIN CODEGEN AGGREGATION GLUE: TGEO_CENTROID (case-switch) */
        case AntlrSQLLexer::TGEO_CENTROID:
            // Windowed centroid trajectory via tgeo_centroid over the expandable mini-trip, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TGEO_CENTROID requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TGEO_CENTROID arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TgeoCentroidAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TGEO_CENTROID (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_AZIMUTH (case-switch) */
        case AntlrSQLLexer::TPOINT_AZIMUTH:
            // Windowed azimuth (tfloat) via tpoint_azimuth over the expandable mini-trip, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TPOINT_AZIMUTH requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TPOINT_AZIMUTH arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TpointAzimuthAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TPOINT_AZIMUTH (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_ANGULAR_DIFFERENCE (case-switch) */
        case AntlrSQLLexer::TPOINT_ANGULAR_DIFFERENCE:
            // Windowed angular difference via tpoint_angular_difference over the expandable mini-trip, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TPOINT_ANGULAR_DIFFERENCE requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TPOINT_ANGULAR_DIFFERENCE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TpointAngularDifferenceAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TPOINT_ANGULAR_DIFFERENCE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TGEOMPOINT_TO_TGEOMETRY (case-switch) */
        case AntlrSQLLexer::TGEOMPOINT_TO_TGEOMETRY:
            // Windowed tgeompoint->tgeometry conversion over the expandable mini-trip, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TGEOMPOINT_TO_TGEOMETRY requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TGEOMPOINT_TO_TGEOMETRY arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TgeompointToTgeometryAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TGEOMPOINT_TO_TGEOMETRY (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_COPY (case-switch) */
        case AntlrSQLLexer::TEMPORAL_COPY:
            // Windowed temporal_copy over the expandable mini-trip, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TEMPORAL_COPY requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_COPY arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalCopyAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_COPY (case-switch) */
        /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_ABS (case-switch) */
        case AntlrSQLLexer::TNUMBER_ABS:
            // Windowed tnumber_abs over the expandable tfloat mini-series, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TNUMBER_ABS requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TNUMBER_ABS arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TnumberAbsAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TNUMBER_ABS (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_DELTA_VALUE (case-switch) */
        case AntlrSQLLexer::TNUMBER_DELTA_VALUE:
            // Windowed tnumber_delta_value over the expandable tfloat mini-series, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TNUMBER_DELTA_VALUE requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TNUMBER_DELTA_VALUE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TnumberDeltaValueAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TNUMBER_DELTA_VALUE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_ANGULAR_DIFFERENCE (case-switch) */
        case AntlrSQLLexer::TNUMBER_ANGULAR_DIFFERENCE:
            // Windowed tnumber_angular_difference over the expandable tfloat mini-series, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TNUMBER_ANGULAR_DIFFERENCE requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TNUMBER_ANGULAR_DIFFERENCE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TnumberAngularDifferenceAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TNUMBER_ANGULAR_DIFFERENCE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_DERIVATIVE (case-switch) */
        case AntlrSQLLexer::TEMPORAL_DERIVATIVE:
            // Windowed temporal_derivative over the expandable tfloat mini-series, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TEMPORAL_DERIVATIVE requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_DERIVATIVE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalDerivativeAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_DERIVATIVE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_AT_MAX (case-switch) */
        case AntlrSQLLexer::TEMPORAL_AT_MAX:
            // Windowed temporal_at_max over the expandable tfloat mini-series, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TEMPORAL_AT_MAX requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_AT_MAX arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalAtMaxAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_AT_MAX (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_AT_MIN (case-switch) */
        case AntlrSQLLexer::TEMPORAL_AT_MIN:
            // Windowed temporal_at_min over the expandable tfloat mini-series, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TEMPORAL_AT_MIN requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_AT_MIN arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalAtMinAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_AT_MIN (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_MINUS_MAX (case-switch) */
        case AntlrSQLLexer::TEMPORAL_MINUS_MAX:
            // Windowed temporal_minus_max over the expandable tfloat mini-series, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TEMPORAL_MINUS_MAX requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_MINUS_MAX arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalMinusMaxAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_MINUS_MAX (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_MINUS_MIN (case-switch) */
        case AntlrSQLLexer::TEMPORAL_MINUS_MIN:
            // Windowed temporal_minus_min over the expandable tfloat mini-series, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TEMPORAL_MINUS_MIN requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TEMPORAL_MINUS_MIN arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TemporalMinusMinAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TEMPORAL_MINUS_MIN (case-switch) */
        /* BEGIN CODEGEN AGGREGATION GLUE: TNPOINT_CUMULATIVE_LENGTH (case-switch) */
        case AntlrSQLLexer::TNPOINT_CUMULATIVE_LENGTH:
            // Windowed tnpoint_cumulative_length over the expandable tnpoint mini-series, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TNPOINT_CUMULATIVE_LENGTH requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TNPOINT_CUMULATIVE_LENGTH arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TnpointCumulativeLengthAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TNPOINT_CUMULATIVE_LENGTH (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TNPOINT_SPEED (case-switch) */
        case AntlrSQLLexer::TNPOINT_SPEED:
            // Windowed tnpoint_speed over the expandable tnpoint mini-series, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TNPOINT_SPEED requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TNPOINT_SPEED arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TnpointSpeedAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TNPOINT_SPEED (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TNPOINT_TO_TGEOMPOINT (case-switch) */
        case AntlrSQLLexer::TNPOINT_TO_TGEOMPOINT:
            // Windowed tnpoint_to_tgeompoint over the expandable tnpoint mini-series, emitted as hex-WKB.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TNPOINT_TO_TGEOMPOINT requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TNPOINT_TO_TGEOMPOINT arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TnpointToTgeompointAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TNPOINT_TO_TGEOMPOINT (case-switch) */
        /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_CUMULATIVE_LENGTH (case-switch) */
        case AntlrSQLLexer::TPOINT_CUMULATIVE_LENGTH:
            // Windowed tgeompoint mini-trip grown by appendInstant; tpoint_cumulative_length emits the running planar length as a tfloat (hex-WKB).
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TPOINT_CUMULATIVE_LENGTH requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TPOINT_CUMULATIVE_LENGTH arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TpointCumulativeLengthAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TPOINT_CUMULATIVE_LENGTH (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_SPEED (case-switch) */
        case AntlrSQLLexer::TPOINT_SPEED:
            // Windowed tgeompoint mini-trip; tpoint_speed differentiates it into a speed tfloat (hex-WKB).
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TPOINT_SPEED requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TPOINT_SPEED arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TpointSpeedAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TPOINT_SPEED (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_GET_X (case-switch) */
        case AntlrSQLLexer::TPOINT_GET_X:
            // Windowed tgeompoint mini-trip; tpoint_get_x projects the X coordinate as a tfloat (hex-WKB).
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TPOINT_GET_X requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TPOINT_GET_X arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TpointGetXAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TPOINT_GET_X (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_GET_Y (case-switch) */
        case AntlrSQLLexer::TPOINT_GET_Y:
            // Windowed tgeompoint mini-trip; tpoint_get_y projects the Y coordinate as a tfloat (hex-WKB).
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TPOINT_GET_Y requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TPOINT_GET_Y arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TpointGetYAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TPOINT_GET_Y (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_TREND (case-switch) */
        case AntlrSQLLexer::TNUMBER_TREND:
            // Windowed tnumber mini-series; tnumber_trend emits the rising/falling trend as a tint (hex-WKB).
            if (helpers.top().functionBuilder.size() != 2) {
                throw InvalidQuerySyntax("TNUMBER_TREND requires exactly two arguments (value, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto valueFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!valueFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TNUMBER_TREND arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TnumberTrendAggregationLogicalFunction::create(valueFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(valueFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TNUMBER_TREND (case-switch) */
        /* BEGIN CODEGEN AGGREGATION GLUE: TGEO_START_VALUE (case-switch) */
        case AntlrSQLLexer::TGEO_START_VALUE:
            // Windowed tgeompoint mini-trip; tgeo_start_value emits the first point geometry as hex-EWKB.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TGEO_START_VALUE requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TGEO_START_VALUE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TgeoStartValueAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TGEO_START_VALUE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TGEO_END_VALUE (case-switch) */
        case AntlrSQLLexer::TGEO_END_VALUE:
            // Windowed tgeompoint mini-trip; tgeo_end_value emits the last point geometry as hex-EWKB.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TGEO_END_VALUE requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TGEO_END_VALUE arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TgeoEndValueAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TGEO_END_VALUE (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TGEO_CONVEX_HULL (case-switch) */
        case AntlrSQLLexer::TGEO_CONVEX_HULL:
            // Windowed tgeompoint mini-trip; tgeo_convex_hull emits the trajectory convex hull as hex-EWKB.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TGEO_CONVEX_HULL requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TGEO_CONVEX_HULL arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TgeoConvexHullAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TGEO_CONVEX_HULL (case-switch) */

        /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_TWCENTROID (case-switch) */
        case AntlrSQLLexer::TPOINT_TWCENTROID:
            // Windowed tgeompoint mini-trip; tpoint_twcentroid emits the time-weighted centroid point as hex-EWKB.
            if (helpers.top().functionBuilder.size() != 3) {
                throw InvalidQuerySyntax("TPOINT_TWCENTROID requires exactly three arguments (longitude, latitude, timestamp), but got {}", helpers.top().functionBuilder.size());
            }
            {
                const auto timestampFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto latitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto longitudeFunction = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();

                if (!longitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !latitudeFunction.tryGet<FieldAccessLogicalFunction>() ||
                    !timestampFunction.tryGet<FieldAccessLogicalFunction>()) {
                    throw InvalidQuerySyntax("TPOINT_TWCENTROID arguments must be field references");
                }

                helpers.top().windowAggs.push_back(
                    TpointTwcentroidAggregationLogicalFunction::create(longitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    latitudeFunction.get<FieldAccessLogicalFunction>(),
                                                                    timestampFunction.get<FieldAccessLogicalFunction>()));
                helpers.top().functionBuilder.push_back(longitudeFunction);
            }
            break;
        /* END CODEGEN AGGREGATION GLUE: TPOINT_TWCENTROID (case-switch) */

















        default:
            /// Check if the function is a constructor for a datatype
            if (const auto dataType = DataTypeProvider::tryProvideDataType(funcName); dataType.has_value())
            {
                if (helpers.top().constantBuilder.empty())
                {
                    throw InvalidQuerySyntax("Expected constant, got nothing at {}", context->getText());
                }
                helpers.top().hasUnnamedAggregation = false;
                auto value = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                auto constFunctionItem = ConstantValueLogicalFunction(*dataType, std::move(value));
                helpers.top().functionBuilder.emplace_back(constFunctionItem);
            }
            else if (funcName == "VAR")
            {
                if (helpers.top().functionBuilder.empty())
                {
                    throw InvalidQuerySyntax("Aggregation requires argument at {}", context->getText());
                }
                const auto& lastArg = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().windowAggs.push_back(std::make_shared<VarAggregationLogicalFunction>(lastArg));
            }
            else if (funcName == "TEMPORAL_SEQUENCE")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TEMPORAL_SEQUENCE requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalSequenceAggregationLogicalFunctionV2::create(lon, lat, ts));
            }
            else if (funcName == "TEMPORAL_LENGTH")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TEMPORAL_LENGTH requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalLengthAggregationLogicalFunction::create(lon, lat, ts));
            }
            else if (funcName == "PAIR_MEETING")
            {
                // Five-arg shape: 4 FieldAccess + 1 numeric constant (dMeet metres).
                if (helpers.top().constantBuilder.empty())
                {
                    throw InvalidQuerySyntax(
                        "PAIR_MEETING requires a numeric constant fifth argument (dMeet metres) at {}",
                        context->getText());
                }
                auto dMeetString = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                double dMeetMetres;
                try { dMeetMetres = std::stod(dMeetString); }
                catch (const std::exception&) {
                    throw InvalidQuerySyntax(
                        "PAIR_MEETING fifth argument must be a numeric constant (dMeet metres), got `{}` at {}",
                        dMeetString, context->getText());
                }
                if (helpers.top().functionBuilder.size() < 4)
                {
                    throw InvalidQuerySyntax(
                        "PAIR_MEETING requires four field args + 1 constant at {}", context->getText());
                }
                const auto vid = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(PairMeetingAggregationLogicalFunction::create(lon, lat, ts, vid, dMeetMetres));
            }
            else if (funcName == "CROSS_DISTANCE")
            {
                // Six-arg shape: 4 FieldAccess + 2 numeric constants (vidA, vidB).
                if (helpers.top().constantBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax(
                        "CROSS_DISTANCE requires two numeric constant arguments (vidA, vidB) at {}",
                        context->getText());
                }
            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_NUM_INSTANTS (funcName chain) */
            else if (funcName == "TEMPORAL_NUM_INSTANTS")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TEMPORAL_NUM_INSTANTS requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalNumInstantsAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_NUM_INSTANTS (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_NUM_SEQUENCES (funcName chain) */
            else if (funcName == "TEMPORAL_NUM_SEQUENCES")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TEMPORAL_NUM_SEQUENCES requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalNumSequencesAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_NUM_SEQUENCES (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_NUM_TIMESTAMPS (funcName chain) */
            else if (funcName == "TEMPORAL_NUM_TIMESTAMPS")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TEMPORAL_NUM_TIMESTAMPS requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalNumTimestampsAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_NUM_TIMESTAMPS (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TFLOAT_START_VALUE (funcName chain) */
            else if (funcName == "TFLOAT_START_VALUE")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TFLOAT_START_VALUE requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TfloatStartValueAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TFLOAT_START_VALUE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TFLOAT_END_VALUE (funcName chain) */
            else if (funcName == "TFLOAT_END_VALUE")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TFLOAT_END_VALUE requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TfloatEndValueAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TFLOAT_END_VALUE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TFLOAT_MIN_VALUE (funcName chain) */
            else if (funcName == "TFLOAT_MIN_VALUE")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TFLOAT_MIN_VALUE requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TfloatMinValueAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TFLOAT_MIN_VALUE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TFLOAT_MAX_VALUE (funcName chain) */
            else if (funcName == "TFLOAT_MAX_VALUE")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TFLOAT_MAX_VALUE requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TfloatMaxValueAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TFLOAT_MAX_VALUE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_INTEGRAL (funcName chain) */
            else if (funcName == "TNUMBER_INTEGRAL")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TNUMBER_INTEGRAL requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TnumberIntegralAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TNUMBER_INTEGRAL (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TINT_START_VALUE (funcName chain) */
            else if (funcName == "TINT_START_VALUE")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TINT_START_VALUE requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TintStartValueAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TINT_START_VALUE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TINT_END_VALUE (funcName chain) */
            else if (funcName == "TINT_END_VALUE")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TINT_END_VALUE requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TintEndValueAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TINT_END_VALUE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TINT_MIN_VALUE (funcName chain) */
            else if (funcName == "TINT_MIN_VALUE")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TINT_MIN_VALUE requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TintMinValueAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TINT_MIN_VALUE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TINT_MAX_VALUE (funcName chain) */
            else if (funcName == "TINT_MAX_VALUE")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TINT_MAX_VALUE requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TintMaxValueAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TINT_MAX_VALUE (funcName chain) */
            /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_AVG_VALUE (funcName chain) */
            else if (funcName == "TNUMBER_AVG_VALUE")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TNUMBER_AVG_VALUE requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TnumberAvgValueAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TNUMBER_AVG_VALUE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_TWAVG (funcName chain) */
            else if (funcName == "TNUMBER_TWAVG")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TNUMBER_TWAVG requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TnumberTwavgAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TNUMBER_TWAVG (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_START_TIMESTAMPTZ (funcName chain) */
            else if (funcName == "TEMPORAL_START_TIMESTAMPTZ")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TEMPORAL_START_TIMESTAMPTZ requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalStartTimestamptzAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_START_TIMESTAMPTZ (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_END_TIMESTAMPTZ (funcName chain) */
            else if (funcName == "TEMPORAL_END_TIMESTAMPTZ")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TEMPORAL_END_TIMESTAMPTZ requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalEndTimestamptzAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_END_TIMESTAMPTZ (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_LOWER_INC (funcName chain) */
            else if (funcName == "TEMPORAL_LOWER_INC")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TEMPORAL_LOWER_INC requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalLowerIncAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_LOWER_INC (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_UPPER_INC (funcName chain) */
            else if (funcName == "TEMPORAL_UPPER_INC")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TEMPORAL_UPPER_INC requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalUpperIncAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_UPPER_INC (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_IS_SIMPLE (funcName chain) */
            else if (funcName == "TPOINT_IS_SIMPLE")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TPOINT_IS_SIMPLE requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TpointIsSimpleAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TPOINT_IS_SIMPLE (funcName chain) */
            /* BEGIN CODEGEN AGGREGATION GLUE: TSPATIAL_EXTENT_TRANSFN (funcName chain) */
            else if (funcName == "TSPATIAL_EXTENT_TRANSFN")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TSPATIAL_EXTENT_TRANSFN requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TspatialExtentTransfnAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TSPATIAL_EXTENT_TRANSFN (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_EXTENT_TRANSFN (funcName chain) */
            else if (funcName == "TNUMBER_EXTENT_TRANSFN")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TNUMBER_EXTENT_TRANSFN requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TnumberExtentTransfnAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TNUMBER_EXTENT_TRANSFN (funcName chain) */
            /* BEGIN CODEGEN AGGREGATION GLUE: FLOAT_EXTENT_TRANSFN (funcName chain) */
            else if (funcName == "FLOAT_EXTENT_TRANSFN")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("FLOAT_EXTENT_TRANSFN requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(FloatExtentTransfnAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: FLOAT_EXTENT_TRANSFN (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: INT_EXTENT_TRANSFN (funcName chain) */
            else if (funcName == "INT_EXTENT_TRANSFN")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("INT_EXTENT_TRANSFN requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(IntExtentTransfnAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: INT_EXTENT_TRANSFN (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: BIGINT_EXTENT_TRANSFN (funcName chain) */
            else if (funcName == "BIGINT_EXTENT_TRANSFN")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("BIGINT_EXTENT_TRANSFN requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(BigintExtentTransfnAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: BIGINT_EXTENT_TRANSFN (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TIMESTAMPTZ_EXTENT_TRANSFN (funcName chain) */
            else if (funcName == "TIMESTAMPTZ_EXTENT_TRANSFN")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TIMESTAMPTZ_EXTENT_TRANSFN requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TimestamptzExtentTransfnAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TIMESTAMPTZ_EXTENT_TRANSFN (funcName chain) */
            /* BEGIN CODEGEN AGGREGATION GLUE: FLOAT_UNION_TRANSFN (funcName chain) */
            else if (funcName == "FLOAT_UNION_TRANSFN")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("FLOAT_UNION_TRANSFN requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(FloatUnionTransfnAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: FLOAT_UNION_TRANSFN (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: INT_UNION_TRANSFN (funcName chain) */
            else if (funcName == "INT_UNION_TRANSFN")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("INT_UNION_TRANSFN requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(IntUnionTransfnAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: INT_UNION_TRANSFN (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: BIGINT_UNION_TRANSFN (funcName chain) */
            else if (funcName == "BIGINT_UNION_TRANSFN")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("BIGINT_UNION_TRANSFN requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(BigintUnionTransfnAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: BIGINT_UNION_TRANSFN (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TIMESTAMPTZ_UNION_TRANSFN (funcName chain) */
            else if (funcName == "TIMESTAMPTZ_UNION_TRANSFN")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TIMESTAMPTZ_UNION_TRANSFN requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TimestamptzUnionTransfnAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TIMESTAMPTZ_UNION_TRANSFN (funcName chain) */
            /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_TRAJECTORY (funcName chain) */
            else if (funcName == "TPOINT_TRAJECTORY")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TPOINT_TRAJECTORY requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TpointTrajectoryAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TPOINT_TRAJECTORY (funcName chain) */
            /* BEGIN CODEGEN AGGREGATION GLUE: TGEO_CENTROID (funcName chain) */
            else if (funcName == "TGEO_CENTROID")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TGEO_CENTROID requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TgeoCentroidAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TGEO_CENTROID (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_AZIMUTH (funcName chain) */
            else if (funcName == "TPOINT_AZIMUTH")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TPOINT_AZIMUTH requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TpointAzimuthAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TPOINT_AZIMUTH (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_ANGULAR_DIFFERENCE (funcName chain) */
            else if (funcName == "TPOINT_ANGULAR_DIFFERENCE")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TPOINT_ANGULAR_DIFFERENCE requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TpointAngularDifferenceAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TPOINT_ANGULAR_DIFFERENCE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TGEOMPOINT_TO_TGEOMETRY (funcName chain) */
            else if (funcName == "TGEOMPOINT_TO_TGEOMETRY")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TGEOMPOINT_TO_TGEOMETRY requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TgeompointToTgeometryAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TGEOMPOINT_TO_TGEOMETRY (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_COPY (funcName chain) */
            else if (funcName == "TEMPORAL_COPY")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TEMPORAL_COPY requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalCopyAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_COPY (funcName chain) */
            /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_ABS (funcName chain) */
            else if (funcName == "TNUMBER_ABS")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TNUMBER_ABS requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TnumberAbsAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TNUMBER_ABS (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_DELTA_VALUE (funcName chain) */
            else if (funcName == "TNUMBER_DELTA_VALUE")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TNUMBER_DELTA_VALUE requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TnumberDeltaValueAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TNUMBER_DELTA_VALUE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_ANGULAR_DIFFERENCE (funcName chain) */
            else if (funcName == "TNUMBER_ANGULAR_DIFFERENCE")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TNUMBER_ANGULAR_DIFFERENCE requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TnumberAngularDifferenceAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TNUMBER_ANGULAR_DIFFERENCE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_DERIVATIVE (funcName chain) */
            else if (funcName == "TEMPORAL_DERIVATIVE")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TEMPORAL_DERIVATIVE requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalDerivativeAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_DERIVATIVE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_AT_MAX (funcName chain) */
            else if (funcName == "TEMPORAL_AT_MAX")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TEMPORAL_AT_MAX requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalAtMaxAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_AT_MAX (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_AT_MIN (funcName chain) */
            else if (funcName == "TEMPORAL_AT_MIN")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TEMPORAL_AT_MIN requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalAtMinAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_AT_MIN (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_MINUS_MAX (funcName chain) */
            else if (funcName == "TEMPORAL_MINUS_MAX")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TEMPORAL_MINUS_MAX requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalMinusMaxAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_MINUS_MAX (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TEMPORAL_MINUS_MIN (funcName chain) */
            else if (funcName == "TEMPORAL_MINUS_MIN")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TEMPORAL_MINUS_MIN requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TemporalMinusMinAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TEMPORAL_MINUS_MIN (funcName chain) */
            /* BEGIN CODEGEN AGGREGATION GLUE: TNPOINT_CUMULATIVE_LENGTH (funcName chain) */
            else if (funcName == "TNPOINT_CUMULATIVE_LENGTH")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TNPOINT_CUMULATIVE_LENGTH requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TnpointCumulativeLengthAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TNPOINT_CUMULATIVE_LENGTH (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TNPOINT_SPEED (funcName chain) */
            else if (funcName == "TNPOINT_SPEED")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TNPOINT_SPEED requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TnpointSpeedAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TNPOINT_SPEED (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TNPOINT_TO_TGEOMPOINT (funcName chain) */
            else if (funcName == "TNPOINT_TO_TGEOMPOINT")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TNPOINT_TO_TGEOMPOINT requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TnpointToTgeompointAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TNPOINT_TO_TGEOMPOINT (funcName chain) */
            /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_CUMULATIVE_LENGTH (funcName chain) */
            else if (funcName == "TPOINT_CUMULATIVE_LENGTH")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TPOINT_CUMULATIVE_LENGTH requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TpointCumulativeLengthAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TPOINT_CUMULATIVE_LENGTH (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_SPEED (funcName chain) */
            else if (funcName == "TPOINT_SPEED")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TPOINT_SPEED requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TpointSpeedAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TPOINT_SPEED (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_GET_X (funcName chain) */
            else if (funcName == "TPOINT_GET_X")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TPOINT_GET_X requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TpointGetXAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TPOINT_GET_X (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_GET_Y (funcName chain) */
            else if (funcName == "TPOINT_GET_Y")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TPOINT_GET_Y requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TpointGetYAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TPOINT_GET_Y (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TNUMBER_TREND (funcName chain) */
            else if (funcName == "TNUMBER_TREND")
            {
                if (helpers.top().functionBuilder.size() < 2)
                {
                    throw InvalidQuerySyntax("TNUMBER_TREND requires two arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto value = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TnumberTrendAggregationLogicalFunction::create(value, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TNUMBER_TREND (funcName chain) */
            /* BEGIN CODEGEN AGGREGATION GLUE: TGEO_START_VALUE (funcName chain) */
            else if (funcName == "TGEO_START_VALUE")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TGEO_START_VALUE requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TgeoStartValueAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TGEO_START_VALUE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TGEO_END_VALUE (funcName chain) */
            else if (funcName == "TGEO_END_VALUE")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TGEO_END_VALUE requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TgeoEndValueAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TGEO_END_VALUE (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TGEO_CONVEX_HULL (funcName chain) */
            else if (funcName == "TGEO_CONVEX_HULL")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TGEO_CONVEX_HULL requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TgeoConvexHullAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TGEO_CONVEX_HULL (funcName chain) */

            /* BEGIN CODEGEN AGGREGATION GLUE: TPOINT_TWCENTROID (funcName chain) */
            else if (funcName == "TPOINT_TWCENTROID")
            {
                if (helpers.top().functionBuilder.size() < 3)
                {
                    throw InvalidQuerySyntax("TPOINT_TWCENTROID requires three arguments at {}", context->getText());
                }
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(TpointTwcentroidAggregationLogicalFunction::create(lon, lat, ts));
            }
            /* END CODEGEN AGGREGATION GLUE: TPOINT_TWCENTROID (funcName chain) */












                auto vidBString = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                auto vidAString = std::move(helpers.top().constantBuilder.back());
                helpers.top().constantBuilder.pop_back();
                uint64_t vidA, vidB;
                try {
                    vidA = std::stoull(vidAString);
                    vidB = std::stoull(vidBString);
                } catch (const std::exception&) {
                    throw InvalidQuerySyntax(
                        "CROSS_DISTANCE constant arguments must be unsigned integers (vidA, vidB), got `{}` and `{}` at {}",
                        vidAString, vidBString, context->getText());
                }
                if (helpers.top().functionBuilder.size() < 4)
                {
                    throw InvalidQuerySyntax(
                        "CROSS_DISTANCE requires four field args + 2 constants at {}", context->getText());
                }
                const auto vid = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto ts = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back().get<FieldAccessLogicalFunction>();
                helpers.top().functionBuilder.pop_back();
                helpers.top().windowAggs.push_back(CrossDistanceAggregationLogicalFunction::create(lon, lat, ts, vid, vidA, vidB));
            }
            else if (auto logicalFunction = LogicalFunctionProvider::tryProvide(funcName, helpers.top().functionBuilder))
            {
                /// Remove exactly the functions used to create the 'logicalFunction' from the back of the function builder
                helpers.top().functionBuilder.resize(helpers.top().functionBuilder.size() - logicalFunction.value().getChildren().size());
                helpers.top().functionBuilder.push_back(*logicalFunction);
            }
            else if (funcName == "TEMPORAL_INTERSECTS")
            {
                if (helpers.top().functionBuilder.size() != 3) {
                    throw InvalidQuerySyntax("TEMPORAL_INTERSECTS requires exactly three arguments (lon, lat, timestamp), but got {}", helpers.top().functionBuilder.size());
                }
                const auto ts = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto lat = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                const auto lon = helpers.top().functionBuilder.back();
                helpers.top().functionBuilder.pop_back();
                helpers.top().functionBuilder.emplace_back(TemporalIntersectsFunction(lon, lat, ts));
            }
            else
            {
                throw InvalidQuerySyntax("Unknown (aggregation) function: {}, resolved to token type: {}", funcName, tokenType);
            }
    }
}

void AntlrSQLQueryPlanCreator::exitThresholdMinSizeParameter(AntlrSQLParser::ThresholdMinSizeParameterContext* context)
{
    helpers.top().minimumCount = std::stoi(context->getText());
}

void AntlrSQLQueryPlanCreator::enterInlineSource(AntlrSQLParser::InlineSourceContext* context)
{
    const auto type = bindIdentifier(context->type);

    const auto parameters = bindConfigOptions(context->parameters->namedConfigExpression());

    helpers.top().setInlineSource(type, parameters);
}

void AntlrSQLQueryPlanCreator::enterSetOperation(AntlrSQLParser::SetOperationContext*)
{
    AntlrSQLHelper helper;
    helper.isSetOperation = true;
    helpers.push(helper);
}

void AntlrSQLQueryPlanCreator::exitSetOperation(AntlrSQLParser::SetOperationContext* context)
{
    INVARIANT(!helpers.empty(), "the set operation helper should not disappear before this function call");

    auto& helperPlans = helpers.top().queryPlans;
    if (helperPlans.size() < 2)
    {
        throw InvalidQuerySyntax("Union does not have sufficient amount of QueryPlans for unifying.");
    }

    auto rightQuery = std::move(helperPlans.back());
    helperPlans.pop_back();
    auto leftQuery = std::move(helperPlans.back());
    helperPlans.pop_back();
    helpers.pop();

    auto queryPlan = LogicalPlanBuilder::addUnion(std::move(leftQuery), std::move(rightQuery));
    if (!helpers.empty())
    {
        /// we are in a subquery
        helpers.top().queryPlans.push_back(std::move(queryPlan));
    }
    else
    {
        queryPlans.push(std::move(queryPlan));
    }
    AntlrSQLBaseListener::exitSetOperation(context);
}

void AntlrSQLQueryPlanCreator::enterGroupByClause(AntlrSQLParser::GroupByClauseContext* context)
{
    helpers.top().isGroupBy = true;
    AntlrSQLBaseListener::enterGroupByClause(context);
}

void AntlrSQLQueryPlanCreator::exitGroupByClause(AntlrSQLParser::GroupByClauseContext* context)
{
    helpers.top().isGroupBy = false;
    AntlrSQLBaseListener::exitGroupByClause(context);
}
}
