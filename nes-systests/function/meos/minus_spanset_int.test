# name: MEOS_MINUS_SPANSET_INT
# description: minus_spanset_int — autogenerated algebra wave; result recorded.
# groups: [Function, MEOS, Algebra, Codegen]
CREATE LOGICAL SOURCE minusspansetints(id UINT64, a VARSIZED, b INT32);
CREATE PHYSICAL SOURCE FOR minusspansetints TYPE File SET('|' AS PARSER.FIELD_DELIMITER);
ATTACH INLINE
1|'{[1, 5), [10, 15)}'|3
2|'{[3, 7)}'|7

CREATE SINK minusspansetints_out(minusspansetints.id UINT64, r VARSIZED) TYPE File;
SELECT id, MINUS_SPANSET_INT(a, b) AS r FROM minusspansetints INTO minusspansetints_out;
----
1,{[1,3),[4,5),[10,15)}
2,{[3,7)}
