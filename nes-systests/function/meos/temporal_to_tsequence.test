# name: MEOS_TEMPORAL_TO_TSEQUENCE
# description: temporal_to_tsequence — autogenerated algebra wave; result recorded.
# groups: [Function, MEOS, Algebra, Codegen]
CREATE LOGICAL SOURCE temporaltotsequencs(id UINT64, value FLOAT64, ts UINT64);
CREATE PHYSICAL SOURCE FOR temporaltotsequencs TYPE File SET('|' AS PARSER.FIELD_DELIMITER);
ATTACH INLINE
1|5.5|1609459200
2|8.5|1609545600

CREATE SINK temporaltotsequencs_out(temporaltotsequencs.id UINT64, r VARSIZED) TYPE File;
SELECT id, TEMPORAL_TO_TSEQUENCE(value, ts) AS r FROM temporaltotsequencs INTO temporaltotsequencs_out;
----
1,Interp=Step;[5.5@2021-01-01,00:00:00+00]
2,Interp=Step;[8.5@2021-01-02,00:00:00+00]
