# name: MEOS_ADJACENT_SPAN_SPANSET
# description: adjacent_span_spanset — autogenerated algebra wave; result recorded.
# groups: [Function, MEOS, Algebra, Codegen]
CREATE LOGICAL SOURCE adjacentspanspanses(id UINT64, a VARSIZED, b VARSIZED);
CREATE PHYSICAL SOURCE FOR adjacentspanspanses TYPE File SET('|' AS PARSER.FIELD_DELIMITER);
ATTACH INLINE
1|'[1, 5)'|'{[1, 5), [10, 15)}'
2|'[3, 9)'|'{[3, 7)}'

CREATE SINK adjacentspanspanses_out(adjacentspanspanses.id UINT64, r INT32) TYPE File;
SELECT id, ADJACENT_SPAN_SPANSET(a, b) AS r FROM adjacentspanspanses INTO adjacentspanspanses_out;
----
1,0
2,0
