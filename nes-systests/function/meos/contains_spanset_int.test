# name: MEOS_CONTAINS_SPANSET_INT
# description: contains_spanset_int — autogenerated algebra wave; result recorded.
# groups: [Function, MEOS, Algebra, Codegen]
CREATE LOGICAL SOURCE containsspansetints(id UINT64, a VARSIZED, b INT32);
CREATE PHYSICAL SOURCE FOR containsspansetints TYPE File SET('|' AS PARSER.FIELD_DELIMITER);
ATTACH INLINE
1|'{[1, 5), [10, 15)}'|3
2|'{[3, 7)}'|7

CREATE SINK containsspansetints_out(containsspansetints.id UINT64, r INT32) TYPE File;
SELECT id, CONTAINS_SPANSET_INT(a, b) AS r FROM containsspansetints INTO containsspansetints_out;
----
1,1
2,0
