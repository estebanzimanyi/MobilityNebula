# name: MEOS_UNION_SPAN_BIGINT
# description: union_span_bigint — autogenerated algebra wave; result recorded.
# groups: [Function, MEOS, Algebra, Codegen]
CREATE LOGICAL SOURCE unionspanbigints(id UINT64, a VARSIZED, b INT64);
CREATE PHYSICAL SOURCE FOR unionspanbigints TYPE File SET('|' AS PARSER.FIELD_DELIMITER);
ATTACH INLINE
1|'[1, 5)'|3
2|'[3, 9)'|7

CREATE SINK unionspanbigints_out(unionspanbigints.id UINT64, r VARSIZED) TYPE File;
SELECT id, UNION_SPAN_BIGINT(a, b) AS r FROM unionspanbigints INTO unionspanbigints_out;
----
1,{[1,5)}
2,{[3,9)}
