# name: MEOS_DISTANCE_BIGINTSPANSET_BIGINTSPANSET
# description: distance_bigintspanset_bigintspanset — autogenerated algebra wave; result recorded.
# groups: [Function, MEOS, Algebra, Codegen]
CREATE LOGICAL SOURCE distancebigintspans(id UINT64, a VARSIZED, b VARSIZED);
CREATE PHYSICAL SOURCE FOR distancebigintspans TYPE File SET('|' AS PARSER.FIELD_DELIMITER);
ATTACH INLINE
1|'{[1, 5), [10, 15)}'|'{[1, 5), [10, 15)}'
2|'{[3, 7)}'|'{[3, 7)}'

CREATE SINK distancebigintspans_out(distancebigintspans.id UINT64, r INT64) TYPE File;
SELECT id, DISTANCE_BIGINTSPANSET_BIGINTSPANSET(a, b) AS r FROM distancebigintspans INTO distancebigintspans_out;
----
1,0
2,0
