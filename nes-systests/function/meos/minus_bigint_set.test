# name: MEOS_MINUS_BIGINT_SET
# description: minus_bigint_set — autogenerated algebra wave; result recorded.
# groups: [Function, MEOS, Algebra, Codegen]
CREATE LOGICAL SOURCE minusbigintsets(id UINT64, a VARSIZED, b INT64);
CREATE PHYSICAL SOURCE FOR minusbigintsets TYPE File SET('|' AS PARSER.FIELD_DELIMITER);
ATTACH INLINE
1|'{1, 3, 5}'|7
2|'{2, 4, 6}'|9

CREATE SINK minusbigintsets_out(minusbigintsets.id UINT64, r VARSIZED) TYPE File;
SELECT id, MINUS_BIGINT_SET(a, b) AS r FROM minusbigintsets INTO minusbigintsets_out;
----
1,{7}
2,{9}
