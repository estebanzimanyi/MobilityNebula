# name: MEOS_TEMPORAL_MAX_INSTANT
# description: temporal_max_instant — autogenerated algebra wave; result recorded.
# groups: [Function, MEOS, Algebra, Codegen]
CREATE LOGICAL SOURCE temporalmaxinstants(id UINT64, value FLOAT64, ts UINT64);
CREATE PHYSICAL SOURCE FOR temporalmaxinstants TYPE File SET('|' AS PARSER.FIELD_DELIMITER);
ATTACH INLINE
1|5.5|1609459200
2|8.5|1609545600

CREATE SINK temporalmaxinstants_out(temporalmaxinstants.id UINT64, r VARSIZED) TYPE File;
SELECT id, TEMPORAL_MAX_INSTANT(value, ts) AS r FROM temporalmaxinstants INTO temporalmaxinstants_out;
----
1,5.5@2021-01-01,00:00:00+00
2,8.5@2021-01-02,00:00:00+00
