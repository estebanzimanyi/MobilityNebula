# name: MEOS_BIGINTSPANSET_LOWER
# description: bigintspanset_lower — autogenerated algebra wave; result recorded.
# groups: [Function, MEOS, Algebra, Codegen]
CREATE LOGICAL SOURCE bigintspansetlowers(id UINT64, a VARSIZED);
CREATE PHYSICAL SOURCE FOR bigintspansetlowers TYPE File SET('|' AS PARSER.FIELD_DELIMITER);
ATTACH INLINE
1|'{[1, 5), [10, 15)}'
2|'{[3, 7)}'

CREATE SINK bigintspansetlowers_out(bigintspansetlowers.id UINT64, r INT64) TYPE File;
SELECT id, BIGINTSPANSET_LOWER(a) AS r FROM bigintspansetlowers INTO bigintspansetlowers_out;
----
1,1
2,3
