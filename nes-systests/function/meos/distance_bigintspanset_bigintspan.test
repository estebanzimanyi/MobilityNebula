# name: MEOS_DISTANCE_BIGINTSPANSET_BIGINTSPAN
# description: distance_bigintspanset_bigintspan — autogenerated algebra wave; result recorded.
# groups: [Function, MEOS, Algebra, Codegen]
CREATE LOGICAL SOURCE distancebigintspans(id UINT64, a VARSIZED, b VARSIZED);
CREATE PHYSICAL SOURCE FOR distancebigintspans TYPE File SET('|' AS PARSER.FIELD_DELIMITER);
ATTACH INLINE
1|'{[1, 5), [10, 15)}'|'[1, 5)'
2|'{[3, 7)}'|'[3, 9)'

CREATE SINK distancebigintspans_out(distancebigintspans.id UINT64, r INT64) TYPE File;
SELECT id, DISTANCE_BIGINTSPANSET_BIGINTSPAN(a, b) AS r FROM distancebigintspans INTO distancebigintspans_out;
----
1,0
2,0
