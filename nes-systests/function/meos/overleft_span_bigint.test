# name: MEOS_OVERLEFT_SPAN_BIGINT
# description: overleft_span_bigint — autogenerated algebra wave; result recorded.
# groups: [Function, MEOS, Algebra, Codegen]
CREATE LOGICAL SOURCE overleftspanbigints(id UINT64, a VARSIZED, b INT64);
CREATE PHYSICAL SOURCE FOR overleftspanbigints TYPE File SET('|' AS PARSER.FIELD_DELIMITER);
ATTACH INLINE
1|'[1, 5)'|3
2|'[3, 9)'|7

CREATE SINK overleftspanbigints_out(overleftspanbigints.id UINT64, r INT32) TYPE File;
SELECT id, OVERLEFT_SPAN_BIGINT(a, b) AS r FROM overleftspanbigints INTO overleftspanbigints_out;
----
1,0
2,0
