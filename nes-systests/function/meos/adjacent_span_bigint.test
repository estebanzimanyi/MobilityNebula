# name: MEOS_ADJACENT_SPAN_BIGINT
# description: adjacent_span_bigint — autogenerated algebra wave; result recorded.
# groups: [Function, MEOS, Algebra, Codegen]
CREATE LOGICAL SOURCE adjacentspanbigints(id UINT64, a VARSIZED, b INT64);
CREATE PHYSICAL SOURCE FOR adjacentspanbigints TYPE File SET('|' AS PARSER.FIELD_DELIMITER);
ATTACH INLINE
1|'[1, 5)'|3
2|'[3, 9)'|7

CREATE SINK adjacentspanbigints_out(adjacentspanbigints.id UINT64, r INT32) TYPE File;
SELECT id, ADJACENT_SPAN_BIGINT(a, b) AS r FROM adjacentspanbigints INTO adjacentspanbigints_out;
----
1,0
2,0
